# Three-source conformance coverage

Auto-checked by `tests/conformance.rs`. Each row is one
algorithm; columns are fixture counts per source plus the Rust pass rate.

> Phase-0 invariant: every `done` AWU must contribute ≥ 1 fixture from
> each of the three sources. CI fails otherwise.

| Algorithm | C | py | R | Rust | Notes |
|-----------|---|----|---|------|-------|
| bfs       | 2 | 1  | 1 | 4/4  | Caught the `igraph_ring(circular=0)` = path bug on first run; see git log of `scripts/test_extract/from_c.py`. |
| dfs       | 1 | 1  | 1 | 3/3  | DFS revealed (a) `neighbors()` was unsorted within from-bucket — fixed `rebuild_indexes` to sort by `(from, to)` lexicographically; (b) `tests/conformance.rs::build_graph` was discarding `directed=true` from R's `make_star(3)` fixture — fixed to honour `payload.directed`. |
| connected_components | 2 | 1  | 1 | 4/4  | First AWU with non-Vec<u32> result — refactored `tests/conformance.rs::run_conformance` to JSON-typed return so a `{membership, count}` shape can be compared structurally. |
| strongly_connected_components | 2 | 1 | 1 | 4/4 | First directed-graph fixture suite. SCC oracle initially failed because `tests/common/mod.rs::GraphPayload::from_graph` hardcoded `directed=false` and dropped reverse edges; fixed by branching on `g.is_directed()` and emitting out-neighbours directly for the directed case. Membership labels match python-igraph exactly because both follow Kosaraju's natural grandfather-pop order (no reindex). |
| distances | 1 | 1 | 1 | 3/3 | Single-source unweighted BFS distances. First AWU returning `Vec<Option<u32>>` — `None` corresponds to upstream's `IGRAPH_INFINITY`. Oracle/wire format encodes `None` as JSON `null` and `Some(d)` as integer. |
| is_eulerian | 3 | 0 | 2 | 5/5 | **Per-source skip**: python-igraph 0.11.x exposes no Eulerian API at all (no `g.is_eulerian` / `has_eulerian_path` etc.) — verified by `[m for m in dir(g) if 'euler' in m.lower()]` returning empty. The conformance harness gained `run_conformance_with_skip(algo, &["py"], …)` for this and similar future cases. Fixtures are pulled from upstream igraph C `tests/unit/igraph_is_eulerian.c` (3) and rigraph `tests/testthat/test-eulerian.R` (2). Re-add `py` once a future python-igraph release exposes the API. |
| articulation_points | 1 | 1 | 1 | 3/3 | DFS-discovery order is implementation-dependent across the three reference impls; the conformance runner sorts the AP vector before comparing. Fixtures: igraph C `igraph_biconnected_components.c` test graph (10v with isolated/disconnected components, expected APs [2,5]); python-igraph `Graph.Tree(5,2)` (APs [0,1]); R `path_graph_impl(n=3)` from `test-aaa-auto.R` (AP [1]). |
| bridges | 2 | 1 | 1 | 4/4 | Edge-id outputs sorted before comparison (DFS discovery order varies). Conformance runs against the fixture's edge-list ordering, so edge ids are stable across the test runner; oracle tests use a `(min,max)` endpoint-pair canonicalisation because `GraphPayload::from_graph` rebuilds python's edge list in a different order. Fixtures: igraph C `igraph_bridges.c` 7v two-triangles + multigraph cases; python-igraph 4-path; R `make_graph("krackhardt_kite")` from `test-components.R`. |
| is_biconnected | 2 | 1 | 1 | 4/4 | Boolean output, no canonicalisation needed. Fixtures: igraph C `igraph_is_biconnected.c` (4-cycle + 3-cycle sharing vertex 2 → false; ring(10) → true); python-igraph K4 → true; R `path_graph(n=3)` from `test-aaa-auto.R` → false. |
| girth | 2 | 1 | 1 | 4/4 | `Option<u32>` encoded as JSON `null` (no cycle) / integer. First fixture suite with a `null`-valued expected — the `tests/common::run_ok` helper was generalised to return `Value::Null` when the oracle's `result` field is null (previously panicked). Fixtures: igraph C `examples/simple/igraph_girth.c` ring(100)+chord and null-graph; python-igraph 5-cycle; R `test-structural-properties.R:'girth() works'` make_ring(100). |
| eccentricity | 1 | 1 | 1 | 3/3 | `Vec<u32>` (per-vertex). Fixtures: 5-path, 4-vertex star, 3-path. All BFS-from-each-vertex consuming SP-006. |
| radius | 1 | 1 | 1 | 3/3 | `Option<u32>` (None for null graph). Fixtures: 5-path → 2, 4-vertex star → 1, 3-path → 1. |
| diameter | 1 | 1 | 1 | 3/3 | `Option<u32>`. Fixtures: directed ring(10) → 9 (igraph_diameter.c); 4-cycle → 2; R disjoint trees (make_tree(7,2) ∪ make_tree(4,3), unconnected=TRUE) → 4. |
| count_triangles | 1 | 1 | 1 | 3/3 | `u64`. python-igraph 0.11 has no direct `count_triangles`; oracle uses `len(g.list_triangles())`. Fixtures: igraph C K4 → 4, python-igraph 5-cycle → 0, R path(3) → 0. |
| transitivity_undirected | 2 | 1 | 1 | 4/4 | `Option<f64>` encoded as JSON null (no triples) or float. The 3*triangles/triples ratio is exactly representable as f64 for integer operands at any practical scale, so direct `==` comparison holds. Fixtures: igraph C `global_transitivity.c` Famous("Zachary") → 0.255681818..., K4 → 1.0; python-igraph K4-minus-edge → 0.75; R path(3) → 0.0. |
| transitivity_local_undirected | 1 | 1 | 1 | 3/3 | Per-vertex `Vec<Option<f64>>`; `None` when degree<2 (matches NaN in upstream's `IGRAPH_TRANSITIVITY_NAN` mode). Karate oracle uses 1e-12 tolerance to absorb f64 round-trip through JSON. Fixtures with rationals that fit exactly use `==`. |
| density | 1 | 1 | 1 | 3/3 | `Option<f64>`. **Triggered the conformance runner's float-tolerance overhaul**: the same f64 `0x1.1cbfa862911ccp-3` round-trips as 17-digit "0.13903743315508021" via Rust's serde_json but as the 16-digit "0.1390374331550802" via Python's `json.dumps`, which serde_json then re-parses as the ULP-different f64. Solution: `tests/conformance::json_approx_eq` does relative-1e-12 comparison on numbers, exact on everything else. |
| mean_distance | 1 | 1 | 1 | 3/3 | `Option<f64>` — None for n<2 or no connected pairs. Same float-tolerance comparison as density. |
| eulerian_path | 1 | 0 | 1 | 2/2 | py-skipped (python-igraph 0.11.x has no Eulerian API). Multiple valid walks exist for a given graph; conformance fixtures compare `len(walk)` only — proptest `eulerian_path_visits_every_edge_once_when_it_exists` enforces the actual structural correctness. |
| count_reachable | 1 | 1 | 1 | 3/3 | `Vec<u32>`. python-igraph 0.11 lacks `count_reachable` directly; oracle uses `len(g.subcomponent(v, mode='out'))` per vertex. |
| assortativity_degree | 1 | 1 | 1 | 3/3 | `Option<f64>`; `None` for regular graphs (variance denominator zero — matches upstream NaN). Fixtures: igraph C Famous("Zachary") → -0.4756, python-igraph K4-minus-edge → -2/3, R path(3) → -1.0 (perfectly disassortative). Phase-1 minimal: undirected, unweighted only. |
| reachability_matrix | 1 | 1 | 1 | 3/3 | `Vec<Vec<bool>>` dense matrix. Fixtures: directed 3-cycle → all true; undirected path-3 → all true (single component); 2 disjoint edges → block-diagonal. python-igraph 0.11 lacks the API directly; oracle uses per-vertex `g.subcomponent(v, mode='out')`. |
| transitive_closure | 1 | 1 | 1 | 3/3 | New `Graph` with all reachability edges (no self-loops; undirected dedups symmetric pairs). Wire format `{vcount, directed, edges}`; runner sorts edges before comparison. python-igraph 0.11 has no API; oracle emulates via per-vertex `subcomponent`. |
| closeness | 1 | 1 | 1 | 3/3 | `Vec<Option<f64>>` per-vertex closeness (normalized: `reach / sum_dist`). `None` for isolated. Karate oracle uses 1e-12 tolerance. Fixtures: 5-path → end 0.4, centre 4/6; 4-star → centre 1.0, leaves 0.6; triangle → all 1.0. |
| harmonic_centrality | 1 | 1 | 1 | 3/3 | `Vec<f64>` per-vertex (unreachable contributes 0; 0.0 for isolated, no NaN). Defined on disconnected graphs. Fixtures: 5-path symmetric profile, 4-star (centre 1.0, leaves 2/3), triangle 1.0. |
| betweenness | 1 | 1 | 1 | 3/3 | `Vec<f64>` per-vertex Brandes betweenness (unweighted, raw counts). Karate oracle uses 1e-10 tolerance because Zachary has betweenness ≈ 231 (larger magnitudes amplify f64 round-trip noise). Fixtures: 5-path classic [0,3,4,3,0], 4-star centre 3.0, 4-cycle uniform 0.5. |
| edge_betweenness | 1 | 1 | 1 | 3/3 | `Vec<f64>` per-edge Brandes (deposits dependency on edges instead of vertices). Karate oracle compares via `(min,max)` endpoint-pair canonicalisation since edge ids change across the wire format. Fixtures: 4-path [3,4,3], 4-star uniform 3.0, triangle uniform 1.0. |
| pagerank | 1 | 1 | 1 | 3/3 | `Vec<f64>` PageRank via power iteration (damping=0.85, eps=1e-10, max_iter=1000). Karate oracle uses 1e-6 tolerance because python-igraph defaults to ARPACK (eigensolver), which converges to the same fixed point but with different fp drift. Fixtures: directed 4-cycle uniform 0.25, triangle uniform 1/3, K4 uniform 0.25. |
| biconnected_components | 1 | 1 | 1 | 3/3 | Multi-output: `{count, components, articulation_points}`. Components canonicalised (each sorted, list sorted) for stable comparison. Fixtures: igraph C upstream `igraph_biconnected_components.c` 10v graph (4 components, APs [2,5]); python-igraph 4-star (3 components, AP [0]); R triangle (1 component, no APs). |
| avg_nearest_neighbor_degree_weighted | 1 | 1 | 1 | 3/3 | Barrat formula `k_nn(v) = (1/s_v) Σ w_{uv} k_u` with strength `s_v` (sum of incident edge weights, LOOPS-counted). Bespoke fixture-walking runner (needs `case.graph.weights`). Crucial implementation detail: `neighbors()` and `incident()` are NOT positionally aligned for undirected graphs — neighbors merges out/in lists ascending, incident concatenates — so the weighted code path iterates `incident()` and resolves the neighbour via `edge_other()`. Fixtures: C weighted triangle (1,2,4) → all knn=2.0 (constant-degree neighbours); py 3-path with weights (1,3) → [2,1,2] hand-checked; R K4 unit weights collapses to unweighted [3,3,3,3]. |
| knnk | 1 | 1 | 1 | 3/3 | Per-degree aggregate `k_nn(k)` (unweighted). Output indexed by degree: `result[k-1]` is mean of `knn[v]` over vertices with `deg(v)=k`; `None` for unused degrees. Fixtures: C 4-star → [3.0, None, 1.0] (deg-1 leaves see deg-3 centre, deg-3 centre sees deg-1 leaves); py K4 → [None, None, 3.0]; R 5-path → [2.0, 5/3]. |
| knnk_weighted | 1 | 1 | 1 | 3/3 | Per-degree weighted aggregate. Pools `Σ_{deg(v)=k} sum_v / Σ_{deg(v)=k} strength_v` per upstream (deghist accumulates strength, NOT count). Bespoke fixture-walking runner. Fixtures: C weighted triangle (1,2,4) all-deg-2 → [None, 2.0]; py weighted 3-path (1,3) → [2.0, 1.0] hand-checked pooled (8/4 and 4/4); R K4 unit weights → [None, None, 3.0]. |
| biconnected_component_edges | 1 | 1 | 1 | 3/3 | CC-012 explicit `component_edges` output (companion of CC-011). Re-scans each component's vertex incidences and picks edges with both endpoints in the component, using `nei < vert` as canonicalisation guard (matches upstream). Self-loops are dropped by that guard. Conformance encodes per-component edge sets as sorted lists of `(min, max)` endpoint pairs (outer list sorted). Fixtures: C `igraph_biconnected_components.c` 10v graph partitioned into 4 components (4-cycle 4 edges + triangle 3 edges + 2 single-edge bridges); py triangle + pendant 0-3 (one 3-edge cycle + one 1-edge bridge); R K4 (single component with all 6 edges). |
| eigenvector_centrality | 1 | 1 | 1 | 3/3 | `Vec<f64>` via shifted power iteration `(A+I)·x` (breaks ±λ symmetry on bipartite graphs); normalized so max=1. Tighter eps (1e-14) than other centrality AWUs to match python-igraph's ARPACK precision. Karate oracle uses 1e-6 tolerance. Fixtures: triangle uniform 1.0; 4-star centre 1, leaves 1/√3; K4 uniform 1.0. |
| simplify  | 1 | 1 | 1 | 3/3  | Multi-output `Graph` (returns a copy; upstream igraph mutates in place). Wire format `{vcount, directed, edges}`; runner sorts edges before comparison and the oracle test canonicalises endpoints to handle python-igraph's reordering of (a,b) ⇄ (b,a) for undirected graphs. Fixtures exercise all three flag combos: igraph C `igraph_simplify.c` 5x parallel undirected with `(remove_multiple=true, remove_loops=true)`; python-igraph `test_operators.py:432 simplify(loops=False)` (multi only); rigraph-style directed `(remove_multiple=false, remove_loops=true)` to verify directed (a,b) and (b,a) survive when only loops are stripped. |
| disjoint_union | 1 | 1 | 1 | 3/3 | Two-graph operator. Wire format threads the right graph through `params.right_graph` as the standard `{n, edges, directed, weights}` payload; the conformance runner re-builds it via `build_graph` before invoking the Rust function. Output is the standard `{vcount, directed, edges}` graph payload, sorted before comparison. Fixtures: C two K3 triangles; py 3-path + 2-path with vertex shift; rigraph-style directed two paths. |
| complementer | 1 | 1 | 1 | 3/3 | Single-graph operator with a `loops` flag in params. Output is the `{vcount, directed, edges}` payload, sorted before comparison; undirected oracle helper canonicalises endpoint pairs because python-igraph's `g.complementer()` returns edges in vertex-ascending order. Fixtures cover all three combinations of directedness × loops: C 3-path no-loops, py 3 isolated with loops (K3 + self-loops), R directed single-edge no-loops. |
| dijkstra_distances | 1 | 1 | 1 | 3/3 | First weighted-algorithm fixture suite. Required two infrastructure extensions: (a) the test_extract emit functions now read an optional `graph_weights` manifest key and stash it on the wire payload; (b) the conformance harness's `GraphPayload.weights` is now consumed (not just `#[allow(dead_code)]`). The dijkstra conformance test fn is bespoke — it walks the fixture dir directly to grab `case.graph.weights` (the standard `run_conformance` signature only passes `(graph, params)`). Fixtures: C triangle (1,4,2) with 0→1→2 shortcut, py directed chain with `(0→3, 5.0)` vs `0→1→2→3` (1+1+1), R disconnected pair with unreachable. |
| dijkstra_paths | 1 | 1 | 1 | 3/3 | SP-001b: SPT distances + parents + inbound edges. The conformance runner only checks `distances` (parents/inbound depend on tie-breaking which differs across igraph C / py / R / Rust). The three implementations were verified separately to share the same shortest-path tree distances. Fixtures: C triangle (1,4,2) with shortcut, py directed chain with shortcut, R two-edge undirected with unreachable target. |
| dijkstra_path_to | 1 | 1 | 1 | 3/3 | SP-001b single source-to-target convenience. Wire format `{vertices, edges}` or `null` (unreachable). Vertex list canonicalised at construction (oracle uses python-igraph's `get_shortest_paths(...output='vpath')`). Fixtures: C directed P4 with heavy shortcut 0→3 weight 5 (best path 0→1→2→3, edges [0,1,2]); py undirected triangle (1,4,2) routing 0→1→2 via the shortcut; R two disjoint edges with target=2 unreachable from source=0 ⇒ `null`. |
| dijkstra_distances_cutoff | 1 | 1 | 1 | 3/3 | SP-001b cutoff variant. Vertices whose dist > cutoff masked to None. Cutoff threaded through `params.cutoff` as `f64`. Fixtures: C P5 unit weights with cutoff 2.5 → [0,1,2,None,None]; py directed chain with cutoff 2.0 → [0,1,2,None]; R undirected P4 unit weights with cutoff 1.5 → [0,1,None,None]. |
| dijkstra_distances_with_mode | 1 | 1 | 1 | 3/3 | SP-001c mode-aware distances. `params.mode` ∈ {"out","in","all"}. Mode threaded through inner BFS via `incident_for_mode` helper; on undirected graphs every mode collapses identically. Fixtures: C directed P3 IN-mode from sink → [3.0, 2.0, 0.0]; py directed P3 ALL-mode → [0.0, 1.0, 3.0]; R undirected P4 ALL-mode → [0.0, 1.0, 3.0, 6.0]. |
| dijkstra_all_shortest_paths | 1 | 1 | 1 | 3/3 | SP-001c all-paths variant: every shortest source→v path with multi-parent SPT DAG and `nrgeo` count. Conformance compares `distances` (re-derived from `dijkstra_distances_with_mode`) and `nrgeo` only — path enumeration order varies across impls. Tie detection uses `cmp_eps` mirroring `igraph_cmp_epsilon` (1e-10 epsilon, scale-relative); equal-cost alt paths recorded only when connecting edge has positive weight (matches upstream's zero-weight loop guard). Fixtures: C diamond (two geodesics to vertex 3) → distances [0,1,1,2] nrgeo [1,1,1,2]; py triangle with shortcut (unique chain) → distances [0,1,3] nrgeo [1,1,1]; R disconnected pair (vertex 2 unreachable) → nrgeo[2..4] = [0,0]. |
| eccentricity_weighted_with_mode | 1 | 1 | 1 | 3/3 | SP-021..023 weighted: counterpart of `igraph_eccentricity(_, weights, _, vss_all(), mode)`. Implementation runs `dijkstra_distances_with_mode` from each vertex and folds the max-of-finite (unconn=true ignores unreachable pairs; isolated vertex → 0.0). Bespoke fixture-walking runner (needs case.graph.weights). Fixtures: C undirected P3 weights (1, 2.5) ALL-mode → [3.5, 2.5, 3.5]; py directed P3 weights (1, 2) OUT-mode → [3, 2, 0]; R two disjoint weighted edges ALL-mode → [1, 1, 4, 4]. |
| radius_weighted_with_mode | 1 | 1 | 1 | 3/3 | SP-021..023 weighted radius = min over `eccentricity_weighted_with_mode`. python-igraph oracle uses `Graph.radius(weights="weight", mode=mode)`. Fixtures: C P3 (1, 2.5) → 2.5; py undirected triangle (1, 2, 4) ALL → 2.0 (vertex 1 has min ecc); R disconnected pair → 1.0 (smaller component min). |
| diameter_weighted_with_mode | 1 | 1 | 1 | 3/3 | SP-021..023 weighted diameter = max over `eccentricity_weighted_with_mode`. python-igraph oracle: `Graph.diameter(weights="weight", directed=...)` toggles OUT/ALL via `directed` flag; IN-mode is emulated by edge-reversal (same convention as the unweighted SP-021abc oracle). Fixtures: C P3 (1, 2.5) ALL → 3.5; py directed P3 (1, 2) OUT → 3.0; R disconnected pair ALL → 4.0 (larger component diameter). |
| closeness_weighted | 1 | 1 | 1 | 3/3 | Built on SP-001's wire-format and conformance scaffolding (also bespoke fixture-walking runner since `run_conformance` doesn't forward weights). Fixtures: C 4-star non-uniform weights `(1,2,3)` with hand-checked centre 0.5, py directed weighted path producing 4/9 closeness at root, R 4-vertex undirected weighted path with mid-vertex symmetry. |
| harmonic_centrality_weighted | 1 | 1 | 1 | 3/3 | Same wire-format and bespoke runner as closeness_weighted/dijkstra_distances. Defined on disconnected graphs because `1/inf = 0`. Fixtures: C 3-path weighted (1, 2) with hand-checked centre 0.75, py directed chain with shortcut, R 4-vertex with isolated vertex 3 producing 0.0 harmonic. |
| betweenness_weighted | 1 | 1 | 1 | 3/3 | Brandes-Dijkstra. Same bespoke fixture-walking runner. Fixtures: C triangle with shortcut weights (1,1,5) routes through vertex 1, py 5-path unit weights collapses to PR-008's [0,3,4,3,0], R directed chain with shortcut 0→3@5 making intermediates 1, 2 each carry 2.0. |
| edge_betweenness_weighted | 1 | 1 | 1 | 3/3 | Per-edge Brandes-Dijkstra. Bespoke runner: parses the parallel `(edges, values)` payload and canonicalises endpoint pairs (`(min, max)` for undirected) to absorb edge-id reordering across the wire. Fixtures: C 4-path unit weights = [3,4,3], py triangle with heavy chord (5) so chord betweenness = 0, R directed chain + heavy shortcut (0→3 weight 5) so chain edges carry [3,4,3] and the shortcut carries 0. |
| pagerank_weighted | 1 | 1 | 1 | 3/3 | Power iteration with weighted in-flow `Σ w(u→v) PR[u] / out_strength(u)` and dangling-vertex redistribution. 1e-6 tolerance (vs python-igraph's ARPACK default — same drift as PR-011's pagerank conformance test). Fixtures: C directed 4-cycle unit weights → uniform 0.25, py heavy-edge concentration (0→1 weight 100 vs 0→2 weight 0.01), R undirected triangle uniform 1/3. |
| assortativity_degree_weighted | 1 | 1 | 1 | 3/3 | python-igraph 0.11 has no Python-level weighted assortativity (`Graph.assortativity` lacks a `weights` kwarg), so non-unit-weight fixtures use **hand-computed reference values** from the upstream Pearson formula (Σ w·(s_u·s_v) etc.) — derivation lives in the manifest comment. Unit-weight cases use python-igraph's unweighted `assortativity_degree` as oracle (the formula collapses identically). Fixtures: C 3-path weights (1, 4) → -0.64/1.36, py 4-path unit weights → -0.5, R diamond non-uniform → -0.3521... |
| assortativity_degree_weighted | 1 | 1 | 1 | 3/3 | python-igraph 0.11 has no Python-level weighted assortativity (`Graph.assortativity` lacks a `weights` kwarg), so non-unit-weight fixtures use **hand-computed reference values** from the upstream Pearson formula (Σ w·(s_u·s_v) etc.) — derivation lives in the manifest comment. Unit-weight cases use python-igraph's unweighted `assortativity_degree` as oracle (the formula collapses identically). Fixtures: C 3-path weights (1, 4) → -0.64/1.36, py 4-path unit weights → -0.5, R diamond non-uniform → -0.3521... |
| modularity_directed | 1 | 1 | 1 | 3/3 | `Option<f64>` Newman-Girvan modularity for directed graphs (Leicht-Newman 2008). Formula: `Q = (1/m) Σ (A_ij − γ k_out_i k_in_j / m) δ(c_i, c_j)`. Each directed edge contributes 1 to one cell of the adjacency (no symmetry). Per-partition `k_out` and `k_in` differ — that's the key directed adaptation. Undirected graphs route to the canonical [`modularity`] (matches python-igraph). Fixtures: C two directed triangles + bridge with hand-checked Q = 18/49; py directed 3-cycle, single community → 0.0; R directed 4-chain split into 2 blocks → Q = 2/9. |
| assortativity_degree_directed | 1 | 1 | 1 | 3/3 | `Option<f64>` directed Pearson correlation: per directed edge (u → v), correlate `out_deg(u)` against `in_deg(v)`. Different formula from the symmetric undirected version: `r = (num1 − num2*num3/m) / (sqrt(den1 − num2²/m) * sqrt(den2 − num3²/m))`. Returns `None` for graphs with no edges or where either variance term collapses (e.g. directed 3-cycle has constant out- and in-degrees → both vanish). Undirected graphs route to the canonical undirected formula. Fixtures: C 0→1, 1→2, 0→2 → −0.5 (variance well-defined on both sides); py 4-vertex hub-in (0/1/2/4 → 3) → −0.7071...; R directed 3-cycle → None. |
| coreness_with_mode | 1 | 1 | 1 | 3/3 | `Vec<u32>` per-vertex coreness with explicit `CorenessMode { All, In, Out }`. Undirected graphs always behave as `All` (modes equivalent). Directed graphs in `Out` mode initialise `cores[v]` to the out-degree and peel via in-neighbours; `In` mode mirrors that with the opposite direction. Self-loops contribute 1 to each of in/out-degree (and 2 to total). Fixtures: C directed K3 (mutual on all pairs) → out-cores [2,2,2]; py directed 3-cycle in-mode → [1,1,1]; R directed 3-star out-mode → [0,0,0,0] (peeling leaves drains the centre). |
| disjoint_union_many | 1 | 1 | 1 | 3/3 | Variadic `disjoint_union` over a slice of graphs. Each graph's vertices shift by the cumulative `vcount` of all preceding inputs. Empty slice → null graph; single-input case clones the input. Bespoke conformance runner walks `params.extra_graphs` (list of `{n, edges, directed, weights}` payloads) since the standard `run_conformance` only forwards a single graph. Fixtures: C three K3 triangles → 9-vertex 9-edge stack; py 2-path + 4-path + 1-edge → 8-vertex 5-edge mixed-size; R two directed paths → 5-vertex 3-edge directed. |
| is_simple_with_mode | 1 | 1 | 1 | 3/3 | `bool` with `SimpleMode { DirectedAsDirected, DirectedAsUndirected }`. Undirected graphs return the same answer for both modes. Directed graphs in undirected view canonicalise each edge to `(min, max)` and check for self-loops + duplicates (mutual `{a→b, b→a}` becomes a multi-edge). Fixtures: C directed mutual pair → false (collapses to doubled undirected edge); py directed 3-cycle → true (three distinct unordered pairs); R directed self-loop → false (loops always disqualify). |
| modularity_weighted | 1 | 1 | 1 | 3/3 | `Option<f64>` Newman-Girvan modularity with edge weights. Strength `s_v = Σ w_e` substitutes for degree; W (sum of weights) substitutes for m. Bespoke conformance runner because the standard `run_conformance` doesn't forward `case.graph.weights`. Fixtures: C balanced-heavy K3∪K3+bridge (10×, bridge 0.1× → ≈0.498); py unit weights collapsing to unweighted `modularity` (6/7-0.5); R two disjoint edges with unit weights → 0.5. Note: pumping weight asymmetrically into ONE community drags Q down because the s² penalty grows quadratically — documented in [`weighted_asymmetric_heavy_one_side_drags_q_down`] unit test. |
| reciprocity_with_mode | 1 | 1 | 1 | 3/3 | `Option<f64>` with explicit `mode` (`Default` / `Ratio`) and `ignore_loops` parameters. The two formulas only agree on graphs where every directed edge has its reverse (rec == m, nonrec == 0); otherwise Ratio (`rec/(rec+nonrec)`) penalises one-way edges twice (once at source, once at destination) while Default (`rec/m`) counts each edge once. Fixtures: C 0↔1 + 0→2 → ratio 0.5 vs default 0.667; py self-loop + mutual pair with `ignore_loops=true` → 1.0 (loop dropped from both numerator and denom); R directed 3-cycle ratio 0.0. |
| coreness | 1 | 1 | 1 | 3/3 | `Vec<u32>` per-vertex coreness via Batagelj-Zaversnik bin-sort O(V+E). Phase-1 minimal: undirected only (returns `Unsupported` for directed; PR-015b ships IN/OUT modes). The C-style triangle+pendant fixture exercises the canonical "peel the leaf, triangle survives" case (expected `[2,2,2,1]`); py K4-minus-edge fixture validates that asymmetric removal still degrades all four vertices to 2-core; R 5-path fixture confirms unanimous 1-core on a tree. Karate oracle test confirms the algorithm is faithful on a 34-vertex real graph. |
| floyd_warshall_distances | 1 | 1 | 1 | 3/3 | All-pairs `Vec<Vec<Option<f64>>>` matrix. Bespoke runner in `tests/conformance.rs` (encodes each row separately as a JSON array with `null` for unreachable, then defers to `json_approx_eq` for f64-roundtrip tolerance). Fixtures: C directed chain 0→1→2→3 + shortcut 0→3@5 (chain wins 3 < 5); py undirected 4-path unit weights with classic taxicab matrix; R undirected weighted triangle (1, 4, 2) with shortcut routing 0→2 via vertex 1. python-igraph's `g.distances()` is the oracle for both py and R fixtures (rigraph's R-side `distances()` ultimately delegates to the same C kernel), so the weighted-undirected path also serves as a cross-check that our undirected mode matches igraph's IGRAPH_ALL semantics. |
| assortativity_degree_weighted | 1 | 1 | 1 | 3/3 | python-igraph 0.11 has no Python-level weighted assortativity (`Graph.assortativity` lacks a `weights` kwarg), so non-unit-weight fixtures use **hand-computed reference values** from the upstream Pearson formula (Σ w·(s_u·s_v) etc.) — derivation lives in the manifest comment. Unit-weight cases use python-igraph's unweighted `assortativity_degree` as oracle (the formula collapses identically). Fixtures: C 3-path weights (1, 4) → -0.64/1.36, py 4-path unit weights → -0.5, R diamond non-uniform → -0.3521... |
| assortativity_degree_directed_weighted | 1 | 1 | 1 | 3/3 | PR-006d: directed Pearson on (out_strength_source, in_strength_target) weighted by edge weight. Same hand-computed convention as the undirected variant since python-igraph still has no Python-level weighted assortativity API. Fixtures: C chain-with-branch (0→1, 1→2, 0→2) unit weights → -0.5 (formula collapse to unweighted directed); py DAG diamond unit weights → -1.0 (hand-computed: out_str=[2,1,1,0], in_str=[0,1,1,2] yields perfect anti-correlation); R directed 3-cycle weights (1,2,4) → 1.0 (hand-computed: weighted Pearson gives perfect alignment between out- and in-strength). |
| modularity | 1 | 1 | 1 | 3/3 | `Option<f64>` (None for ecount==0). Phase-1 minimal: undirected, unweighted; resolution γ configurable. Fixtures: C K3 ∪ K3 + bridge with hand-checked Q = 6/7 − 1/2 (exact rational, 1e-12 tol); python-igraph `test_structural.py:127 testModularity` K5 ∪ K5 + bridge → 0.45238095238095233; rigraph-style `path_graph_impl(n=3)` with membership [1,2,1] → Q = -0.5 (also exact). |
| is_simple | 1 | 1 | 1 | 3/3 | `bool`. Phase-1 directed-as-directed slice: `(a,b)` and `(b,a)` are distinct in directed graphs (mutual pairs stay simple). Fixtures: C 4-path → true; py self-loop → false; rigraph-style two parallel undirected → false. |
| has_loop / has_multiple | 2 | 2 | 2 | 6/6 | Two predicates × three sources. `has_loop` is the trivial O(\|E\|) self-loop scan; `has_multiple` collapses canonicalised `(from, to)` pairs and looks for adjacent duplicates (so two self-loops at the same vertex correctly flag as multi). Fixtures cover positive + negative cases for both. |
| is_loop / is_multiple | 2 | 2 | 2 | 6/6 | Per-edge `Vec<bool>`. Edge ids reorder over the wire (Python's `make_graph` rebuilds in vertex-ascending order), so the conformance runner sorts the bool vector before comparing — this turns the comparison into a multiset check that is robust to edge-id permutations. `is_multiple` follows upstream's "second-or-more" contract: the canonical/lowest-id edge in each parallel group stays `false`, the rest flip to `true`. |
| is_loop / is_multiple | 2 | 2 | 2 | 6/6 | Per-edge `Vec<bool>`. Edge ids reorder over the wire (Python's `make_graph` rebuilds in vertex-ascending order), so the conformance runner sorts the bool vector before comparing — this turns the comparison into a multiset check that is robust to edge-id permutations. `is_multiple` follows upstream's "second-or-more" contract: the canonical/lowest-id edge in each parallel group stays `false`, the rest flip to `true`. |
| decompose | 1 | 1 | 1 | 3/3 | `Vec<Graph>` weak decomposition (Phase-1 minimal slice; strong follow-up). Each component subgraph has vertices renumbered to `0..k` in BFS-from-`actstart` discovery order. Conformance fixtures choose graphs where the BFS visit order is deterministic across our impl and python-igraph (sorted-neighbour identity-mapping cases) so exact edge-set comparison holds. The karate oracle test asserts only `vcount` + `ecount` because BFS-discovery vertex remapping diverges between python-igraph's C kernel and our Rust impl when neighbour iteration order matters. Fixtures: C triangle ∪ edge → `[K3, K2]`; py `K3 ∪ P3 ∪ {6}` → 3 components incl. an isolated vertex; R K4 → single 4-clique component. Edges canonicalised via `(min, max)` and sorted. |
| transitivity_barrat | 1 | 1 | 1 | 3/3 | `Vec<Option<f64>>` Barrat weighted local clustering (PNAS 101 3747 eqn 5). Bespoke conformance runner because the weights ride on `case.graph.weights`. Phase-1 minimal: simple undirected only (rejects multi-edges, loops, directed); python-igraph's `g.transitivity_local_undirected(weights=...)` auto-routes to the C `igraph_transitivity_barrat()` so the same Python entry point doubles as oracle. Critical impl detail: must iterate `incident()` directly (not `neighbors()`) and resolve the neighbour with `edge_other()` — the two are NOT positionally aligned (neighbours merges sorted ascending; incident concatenates out\|in). Fixtures: C weighted triangle (1,2,4) all-vertex 1.0 (hand-checked); py K4-minus-edge unit → [1, 2/3, 2/3, 1] (collapses to unweighted local clustering); R K4 unit → [1,1,1,1]. |
| union | 1 | 1 | 1 | 3/3 | Two-graph max-multiplicity union (ALGO-OP-004). vcount = max(left, right); per canonicalised endpoint pair the result has `max(count_left, count_right)` edges. Uses BTreeMap-based merge to keep output deterministic — output edges are sorted by `(src, tgt)`. Wire format threads the right graph through `params.right_graph`; the conformance runner re-builds it via `build_graph` and serialises the result as `{vcount, directed, edges}` after edge sort. Fixtures: C upstream `igraph_union.c` BINARY VERSION (directed, with self-loop and disjoint endpoints) → 5v 5e; py K3 ∪ P4 sharing vertices → 4v 4e; R directed opposite paths (0→1→2 vs 2→1→0) → 3v 4e (orientations preserved separately). |
| intersection | 1 | 1 | 1 | 3/3 | Two-graph min-multiplicity intersection (ALGO-OP-005). Mirror of OP-004 with `min(count_left, count_right)` per pair, so pairs unique to one side drop out entirely. vcount still uses `max(left, right)` (matches upstream's "common edges, larger vertex set" contract — isolated vertices on the larger side survive). Iterates the smaller-keyed BTreeMap and looks up matches in the other for O((E1+E2) log(E1+E2)); output sorted by `(src, tgt)` for determinism. Same wire-format and conformance-runner shape as OP-004. Fixtures: C reuses the `igraph_union.c` BINARY VERSION inputs but asks for intersection → 5v 3e (common ordered pairs (0,1)/(1,2)/(2,2); (2,3) and (2,4) drop); py K3 ∩ P4 → 4v 2e (only (0,1) and (1,2) shared); R K4 ∩ K3-on-{0,1,2} → 4v 3e (triangle survives, isolated vertex 3 stays in vcount). |
| difference | 1 | 1 | 1 | 3/3 | Two-graph clamped multiset subtract `orig \ sub` (ALGO-OP-006). Per canonicalised pair the result keeps `max(0, count_orig − count_sub)` copies; pairs unique to `sub` drop out entirely (difference cannot synthesise edges). **vcount = orig.vcount() only — asymmetric, unlike union/intersection.** Same BTreeMap template as OP-004/OP-005 (walk `count_orig`, look up `count_sub`, subtract clamped). python-igraph exposes `g.difference(h)` as a Graph method (not module-level), so the oracle handler calls it that way. Same wire-format (`params.right_graph` for `sub`) and conformance-runner shape as OP-005. Fixtures: C reuses `igraph_union.c` BINARY VERSION inputs but asks for difference → 4v 1e (only (2,3) survives in orig; sub (2,4) is ignored because vcount stays at orig.vcount()=4); py K3 \ P4 → 3v 1e (only (0,2) survives — (0,1)/(1,2) cancel, (2,3) is sub-only); R K4 \ K3-on-{0,1,2} → 4v 3e (the star at vertex 3: (0,3)/(1,3)/(2,3)). |
| eccentricity_with_mode | 1 | 1 | 1 | 3/3 | Mode-aware eccentricity (ALGO-SP-021abc). `Vec<u32>` per-vertex BFS following `mode` direction (`Out`/`In`/`All`); for undirected graphs every mode reduces to `All`. Inlined BFS that picks per-vertex neighbour iter via `out_neighbors_vec`/`in_neighbors_vec`/concat — no allocation reuse beyond the distance vector. python-igraph's `g.eccentricity(mode=...)` accepts the same lowercase strings. Fixtures: C directed P4 (0→1→2→3) under IN-mode → ecc=[0,1,2,3] (each vertex's reverse-BFS depth from sink to source); py directed 3-cycle under ALL-mode → ecc=[1,1,1] (collapses to undirected K3); R directed out-star (centre 0 → 3 leaves) under OUT-mode → ecc=[1,0,0,0] (only the centre has out-edges). |
| radius_with_mode | 1 | 1 | 1 | 3/3 | Mode-aware radius (ALGO-SP-021abc). `Option<u32>` = min over `eccentricity_with_mode`. Fixtures share graphs with `eccentricity_with_mode`: C P4-IN min ecc 0 (vertex 0 has no in-edges); py directed 3-cycle ALL → 1; R directed out-star OUT → 0 (any leaf has zero out-eccentricity). |
| diameter_with_mode | 1 | 1 | 1 | 3/3 | Mode-aware diameter (ALGO-SP-021abc). `Option<u32>` = max over `eccentricity_with_mode`. python-igraph's `Graph.diameter` lacks an explicit IN mode, so the oracle reverses edges for `mode="in"` directed graphs and queries with `directed=True`; OUT/ALL map to `directed=True`/`directed=False`. Fixtures: C P4-IN max ecc 3 (vertex 3's reverse-reach to 0); py directed 3-cycle ALL → 1; R directed out-star OUT → 1 (centre's reach). |

## How to add a row

`/awu-conformance ALGO-XXX-NNN` (or follow
[`.claude/skills/awu-conformance/SKILL.md`](../../.claude/skills/awu-conformance/SKILL.md))
extracts fixtures into `tests/conformance/{c,py,r}/<algo>/`. After CI is
green, append a row here with the new counts.

## Phase totals

| Phase | Algorithms in conformance | Total fixtures | C / py / R |
|-------|---------------------------|----------------|------------|
| 0     | 1 (bfs)                   | 4              | 2 / 1 / 1  |
| 1     | 75 (+ ecc/radius/diameter_with_mode + ecc/radius/diameter_weighted_with_mode + dijkstra_paths/path_to/cutoff/with_mode/all_shortest_paths + assortativity_degree_directed_weighted) | 232 | 83 / 73 / 76 |
