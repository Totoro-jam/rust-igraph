# rust-igraph Algorithm Work Unit (AWU) tracker

Single source of truth for per-algorithm progress. **Update on every PR.**
See [docs/plans/MASTER_PLAN.md](../../docs/plans/MASTER_PLAN.md) §4 for the
9-step SOP and §5.2 for the full Phase 1-10 algorithm catalog.

## Status legend

- `todo` — not started
- `wip` — implementation in progress (not on `main`)
- `review` — PR open, waiting for review
- `done` — merged to `main`, all 9 steps green
- `verified` — passed nightly full-conformance for ≥7 days
- `blocked` — waiting on a prerequisite AWU or external decision
- `perf-todo` — functionally `done` but performance > python-igraph × 3
- `native` — semantically satisfied by Rust standard-library types or
  the `core/` modules; no per-method translation needed. We **borrow
  from** igraph C / python-igraph / R-igraph as references and build
  an idiomatic Rust API, rather than mirroring every C-level data
  structure. See ARCHITECTURE.md "Why no DS-* per-method translation"
  for rationale.

## Complexity legend

- `copy` — 1:1 translation from igraph C, ~30 LOC/h with AI
- `adapt` — needs Rust ownership/lifetimes adaptation, ~20 LOC/h
- `rewrite` — algorithmic re-design (e.g. C++ class hierarchies), ~12 LOC/h
- `novel` — no C reference, design from scratch, ~8 LOC/h

## Phase 0 — Walking skeleton + infrastructure (37 BOOT tasks)

| ID | Task | Status | Commit |
|----|------|--------|--------|
| BOOT-01 | git init + LICENSE + README skeleton | done | 2ce55aa |
| BOOT-02 | Cargo workspace + 3 crate skeleton | done | 2ce55aa |
| BOOT-03 | IgraphError + IgraphResult | done | 2ce55aa |
| BOOT-04 | Minimal Graph<u32> | done | 2ce55aa |
| BOOT-05 | EdgeList reader | done | 2ce55aa |
| BOOT-06 | Minimal BFS | done | 2ce55aa |
| BOOT-07 | example bfs_karate.rs | done | 2ce55aa |
| BOOT-08 | karate.edges fixture | done | 2ce55aa |
| BOOT-09 | scripts/oracle.py + Python venv + requirements.txt | done | 5779a31 |
| BOOT-10 | tests/oracle/ harness (Rust subprocess wrapper) | done | 5779a31 |
| BOOT-11 | First oracle test: BFS on karate | done | 5779a31 |
| BOOT-12 | proptest skeleton + BFS invariant | done | 5779a31 |
| BOOT-13 | criterion bench skeleton + BFS baseline | done | 5779a31 |
| BOOT-14..18 | GitHub Actions CI matrix | done | 8d688d8, a6b1cf8 |
| BOOT-14b | Codecov coverage upload (cargo-llvm-cov + codecov.yml + README badge) | done | (next) |
| BOOT-19..22 | templates/ (algo, test, oracle, bench) | done | (next) |
| BOOT-23 | ALGORITHMS.md (this file) | done | 8d688d8 |
| BOOT-24 | ARCHITECTURE.md | done | (next) |
| BOOT-25 | DEVELOPMENT.md (maintainer setup + AWU SOP) + CONTRIBUTING.md (alpha external policy) | done | (next) |
| BOOT-26 | mdBook docs scaffold | done | (next) |
| BOOT-27 | scripts/bench_compare.py (perf baseline) | done | (next) |
| BOOT-28 | RESUME.md (part-time resumability) | done | (next) |
| BOOT-29 | scripts/test_extract/from_c.py | done | 5779a31 |
| BOOT-30 | scripts/test_extract/from_py.py | done | 5779a31 |
| BOOT-31 | scripts/test_extract/from_r.{R,py} | done | 5779a31 |
| BOOT-32 | tests/conformance/ + three-source BFS test | done | 5779a31 |
| BOOT-33 | CLAUDE.md | done | 8d688d8 |
| BOOT-34 | .claude/agents/ (7 agents) | done | 8d688d8 |
| BOOT-35 | .claude/skills/ (9 skills) | done | 8d688d8 |
| BOOT-36 | .claude/hooks/ + settings.json | done | 8d688d8 |
| BOOT-37 | AI_PROMPTS.md | done | 8d688d8 |

## Phase 1 — Data structures (~80 AWU)

| ID | Task | C source | Lines | Cx | Deps | Status | Commit | Bench | Conformance |
|----|------|----------|-------|----|------|--------|--------|-------|-------------|
| ALGO-CORE-001a | Graph struct + new/with_vertices + add_{vertices,edges,edge} + vcount/ecount/is_directed + neighbors/degree + Clone | type_indexededgelist.c (lines 99-413, 829-1265) | ~700 | adapt | - | done | (next) | - | - |
| ALGO-CORE-001b | incident + edge-id helpers (edge / edge_source / edge_target / edge_other) | type_indexededgelist.c (lines 1775-1912) | ~250 | adapt | CORE-001a | done | (next) | - | - |
| ALGO-CORE-001c | delete_edges + delete_vertices + delete_vertices_map | type_indexededgelist.c (lines 500-825) | ~400 | adapt | CORE-001a | done | (next) | O(V+E) per call (rebuild_indexes) | no fixtures (structural mutation; 16 unit + 2 proptest cover) |
| ALGO-CORE-001d | edge query helpers (`get_eid`, `find_eid`, `get_all_eids_between`) | type_indexededgelist.c:1522-1773 | ~150 | adapt | CORE-001a | done | (next) | O(deg(from)) linear scan | - |
| ALGO-CORE-001e | `is_same_graph` (structural equality on labelled vertex/edge sets) | type_indexededgelist.c (lines 1947-2003) | 57 | adapt | CORE-001a..d | done | (next) | O(E log E) lex-sort | C:2 / py:2 / R:2 |
| ALGO-CORE-001f | Property cache subsystem (`is_dag`, `is_forest`, `has_loop`, `has_multi`, `has_mutual` caching) | graph/caching.{c,h} | ~220 | adapt | CORE-001a..d | done | (next) | O(1) cache hits; compute path unchanged | unit×11 + integration×12 |
| ALGO-CORE-010 | basic queries (vcount, ecount, degree, ...) | basic_query.c | 406 | copy | CORE-001a | done | 2ce55aa | - | satisfied by Graph methods (vcount, ecount, degree, neighbors, is_directed, etc.) |
| ALGO-DS-V-001..030 | Vector / VectorInt / VectorBool | vector.c | ~2500 | adapt | - | native | - | - | satisfied by `Vec<T>` + `core/types` |
| ALGO-DS-M-001..020 | Matrix / MatrixInt | matrix.c | ~1500 | adapt | - | native | - | - | satisfied by `ndarray`-style `Vec<Vec<T>>` + `core/matrix` |
| ALGO-DS-S-001..010 | SparseMatrix CSR/CSC | sparsemat.c | 3251 | rewrite | - | native | - | - | satisfied by `core/sparse` (CSR/COO) when needed per-AWU |
| ALGO-DS-SEL-001..010 | VertexSelector / EdgeSelector | iterators.c | 2048 | adapt | - | native | - | - | satisfied by Rust iterator protocol (`Iterator<Item = VertexId>`) |
| ALGO-DS-ADJ-001..005 | Adjacency lists | adjlist.c | 1328 | adapt | - | native | - | - | satisfied by `Graph::neighbors` + `incident_edges` accessors |

## Phase 2 — Traversal + Shortest Paths + Connectivity (~45 AWU)

| ID | Task | C source | Lines | Cx | Deps | Status | Commit | Bench | Conformance |
|----|------|----------|-------|----|------|--------|--------|-------|-------------|
| ALGO-TR-001 | BFS multi-output (`bfs_tree`: order + distances + parents) | bfs.c | 300 | adapt | CORE-001 | done | (next) | (BFS + bookkeeping) | C:2 / py:1 / R:1 (Phase 0 BFS fixtures cover) |
| ALGO-TR-002 | DFS (single-root pre-order) | visitors.c:479 | 200 | adapt | CORE-001a/b | done | (next) | 1.84 µs/karate | C:1 / py:1 / R:1 |
| ALGO-TR-003 | Random walk | random_walk.c | 340 | adapt | CORE-001 | done | (next) | O(steps · max_deg) per call | no fixtures (RNG-dependent; proptest covers structural invariants) |
| ALGO-TR-004 | Node2Vec second-order biased random walk | random_walk.c (node2vec extension) | 310 | self-roll | TR-003 | done | (next) | O(steps · max_deg²) per call | no fixtures (RNG-dependent; 12 unit tests + proptest-style bias validation) |
| ALGO-TR-005 | Batch random walks (random_walks / random_walks_node2vec / random_walks_from) | — (graph-ML corpus gen) | 170 | self-roll | TR-003, TR-004 | done | (next) | O(walks × steps × max_deg) | no fixtures (RNG-dependent; 14 unit tests + 3 doctests) |
| ALGO-TR-006 | K-hop neighbor sampling (`neighbor_sample`, `neighbor_sample_weighted`) — layer-wise GraphSAGE-style mini-batch sampling with uniform or weighted edge selection; deterministic via SplitMix64; returns layers + inter-layer edges for GNN computation graph construction | — (novel, no C upstream) | ~200 | novel | core/Graph, core/rng | done | (next) | O(hops × frontier × fan_out) | no three-source (novel); 15 unit tests + 2 doctests |
| ALGO-TR-007 | Negative edge sampling (`sample_negative_edges`, `sample_negative_edges_excluding`, `sample_negative_edges_degree_biased`) — rejection-based non-edge sampling for link prediction training; uniform, exclusion-aware, and degree-biased variants; deterministic via SplitMix64 | — (novel, no C upstream) | ~260 | novel | core/Graph, core/rng | done | (next) | O(count × density⁻¹) rejection | no three-source (novel); 12 unit tests + 3 doctests |
| ALGO-TR-009 | Edge sampling and train/test split (`sample_edges`, `split_edges`, `split_edges_connected`) — uniform edge sampling without replacement, random train/test edge partitioning, and connectivity-preserving split for link prediction evaluation | — (novel, no C upstream) | ~220 | novel | core/Graph, core/rng | done | (next) | O(E) Fisher-Yates shuffle | no three-source (novel); 15 unit tests + 3 doctests |
| ALGO-TR-010 | Random Walk Positional Encoding (`rwpe`, `rwpe_vertices`) — diagonal entries of powers of transition matrix P=D⁻¹A; used in Graph Transformers (GraphGPS, SAN, SignNet) as structural node features | — (novel, no C upstream) | ~180 | novel | core/Graph | done | (next) | O(k·V·E) probability propagation | no three-source (novel); 11 unit tests + 2 doctests |
| ALGO-TR-011 | Graph signal diffusion (`heat_kernel_diffuse`, `ppr_diffuse`, `symmetric_diffuse`) — core graph signal processing primitives: heat kernel via Poisson-weighted Taylor expansion, PPR via power iteration (APPNP-style), and GCN-style symmetric normalized propagation D^{-1/2}AD^{-1/2}; used in GCN, APPNP, GPR-GNN, graph semi-supervised learning | — (novel, no C upstream) | ~230 | novel | core/Graph | done | (next) | O(terms·(V+E)) per diffusion | no three-source (novel); 22 unit tests + 3 doctests |
| ALGO-TR-012 | Label spreading for semi-supervised classification (`label_spread`, `label_propagate_predict`) — iterative label propagation via symmetric normalized adjacency S=D^{-1/2}AD^{-1/2} with clamping (Zhou et al. 2004); plus one-hop majority voting baseline; returns predicted labels + confidence matrix | — (novel, no C upstream) | ~210 | novel | core/Graph | done | (next) | O(iter·classes·(V+E)) | no three-source (novel); 15 unit tests + 2 doctests |
| ALGO-TR-013 | Link prediction heuristic scores (`link_pred_common_neighbors`, `link_pred_adamic_adar`, `link_pred_resource_allocation`, `link_pred_preferential_attachment`, `link_pred_jaccard`) — classical topology-based link prediction features scoring vertex pairs by structural proximity; used as GNN baselines and edge features | — (novel, no C upstream) | ~260 | novel | core/Graph | done | (next) | O(pairs·max_degree²) | no three-source (novel); 19 unit tests + 5 doctests |
| ALGO-TR-014 | Discrete graph curvature (`forman_ricci_curvature`, `augmented_forman_ricci_curvature`, `ollivier_ricci_curvature`, `mean_forman_ricci`) — edge-level Forman-Ricci (combinatorial), augmented Forman-Ricci (+quadrangles), and Ollivier-Ricci (Lin-Lu-Yau closed-form) curvature; used in CurvGN, Ricci-flow rewiring, and geometric graph analysis | — (novel, no C upstream) | ~280 | novel | core/Graph | done | (next) | O(E·max_degree²) | no three-source (novel); 18 unit tests + 4 doctests |
| ALGO-TR-015 | Graph homophily metrics (`edge_homophily`, `node_homophily`, `class_homophily`, `edge_heterophily`) — measures tendency of edges to connect same-label vertices; edge ratio (Zhu 2020), node-average ratio (Pei 2020), class-balanced adjusted ratio (Lim 2021); central to GNN expressiveness analysis | — (novel, no C upstream) | ~210 | novel | core/Graph | done | (next) | O(V+E) | no three-source (novel); 17 unit tests + 4 doctests |
| ALGO-TR-016 | Graph structural entropy (`degree_entropy`, `edge_entropy`, `von_neumann_entropy`, `degree_structural_info`) — information-theoretic graph descriptors: Shannon entropy of degree distribution, edge-degree distribution entropy, approximate Von Neumann entropy via trace formula, and degree-partition structural information content; used in graph classification and complexity analysis | — (novel, no C upstream) | ~260 | novel | core/Graph | done | (next) | O(V+E) | no three-source (novel); 17 unit tests + 4 doctests |
| ALGO-TR-017 | Structural feature vectors (`structural_feature_vectors`, `degree_profile`) — per-vertex multi-dimensional descriptors (degree, log_degree, clustering, avg/min/max neighbor degree, triangles, square_clustering) + lightweight degree profile; suitable for GNN input or classical ML on graphs | — (novel, no C upstream) | ~370 | novel | core/Graph | done | (next) | O(V·deg²) | no three-source (novel); 9 unit tests + 2 doctests |
| ALGO-TR-018 | Graph cut quality metrics (`cut_size`, `normalized_cut`, `ratio_cut`, `conductance`, `expansion`) — standard spectral clustering evaluation metrics: cut size, Shi-Malik NCut, Hagen-Kahng ratio cut, conductance, and expansion; supports optional edge weights and k-way partitions | — (novel, no C upstream) | ~400 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 21 unit tests + 5 doctests |
| ALGO-TR-019 | Graph signal smoothness (`dirichlet_energy`, `total_variation`, `normalized_dirichlet_energy`, `smoothness_ratio`) — Dirichlet energy (L2 smoothness), total variation (L1), normalized Rayleigh quotient, and smoothness ratio; central to GSP, GNN over-smoothing analysis, and semi-supervised learning | — (novel, no C upstream) | ~300 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 23 unit tests + 4 doctests |
| ALGO-TR-020 | Neighborhood aggregation operators (`neighbor_aggregate`, `attention_aggregate`, `AggMode`) — Mean/Sum/Max/Min pooling and softmax-attention-weighted aggregation over vertex neighborhoods; core primitives for message-passing / GNN-style computation | — (novel, no C upstream) | ~430 | novel | core/Graph | done | (next) | O(V+E) | no three-source (novel); 19 unit tests + 2 doctests |
| ALGO-TR-021 | Spectral graph metrics (`estrada_index`, `graph_energy`, `spectral_radius`, `spectral_gap`, `natural_connectivity`, `subgraph_centrality`, `communicability_matrix`) — eigenvalue-based invariants via Jacobi dense eigensolver; Estrada index, graph energy, spectral radius/gap, natural connectivity, subgraph centrality, and communicability matrix | — (novel, no C upstream) | ~590 | novel | core/Graph + eigen | done | (next) | O(n³) Jacobi | no three-source (novel); 34 unit tests + 7 doctests |
| ALGO-TR-022 | Effective resistance & Kirchhoff index (`effective_resistance`, `effective_resistance_matrix`, `kirchhoff_index`, `resistance_centrality`) — Laplacian pseudoinverse-based resistance distance, Kirchhoff index (total resistance), and resistance centrality; uses Jacobi dense eigensolver | — (novel, no C upstream) | ~560 | novel | core/Graph | done | (next) | O(n³) Jacobi | no three-source (novel); 23 unit tests + 4 doctests |
| ALGO-TR-023 | Algebraic connectivity & Fiedler vector (`algebraic_connectivity`, `fiedler_vector`, `spectral_bisection`, `laplacian_spectrum`, `spanning_tree_count`) — second-smallest Laplacian eigenvalue, spectral bisection, full Laplacian spectrum, Kirchhoff spanning tree count | — (novel, no C upstream) | ~600 | novel | core/Graph | done | (next) | O(n³) Jacobi | no three-source (novel); 28 unit tests + 5 doctests |
| ALGO-TR-024 | Normalized Laplacian metrics (`normalized_laplacian_spectrum`, `normalized_algebraic_connectivity`, `cheeger_bounds`, `spectral_gap_ratio`, `bipartiteness_ratio`) — degree-normalized spectral analysis, Cheeger inequality bounds, expansion ratio, bipartiteness measure | — (novel, no C upstream) | ~530 | novel | core/Graph | done | (next) | O(n³) Jacobi | no three-source (novel); 31 unit tests + 5 doctests |
| ALGO-TR-025 | Distance spectrum & Wiener index (`distance_spectrum`, `distance_spectral_radius`, `distance_energy`, `distance_estrada_index`, `wiener_index`) — eigenvalues of the shortest-path distance matrix, distance energy, distance Estrada index, Wiener index (sum of all pairwise distances) | — (novel, no C upstream) | ~530 | novel | core/Graph | done | (next) | O(n²·BFS + n³·Jacobi) | no three-source (novel); 25 unit tests + 5 doctests |
| ALGO-TR-026 | Signless Laplacian metrics (`signless_laplacian_spectrum`, `signless_laplacian_smallest`, `signless_laplacian_spectral_radius`, `signless_laplacian_energy`) — Q = D+A spectrum, bipartiteness detection via q_1=0, spectral radius, signless Laplacian energy | — (novel, no C upstream) | ~460 | novel | core/Graph | done | (next) | O(n³) Jacobi | no three-source (novel); 22 unit tests + 4 doctests |
| ALGO-TR-027 | Graph robustness (`vertex_resilience`, `edge_resilience`, `graph_toughness`, `graph_integrity`) — resilience fractions via flow-based connectivity, brute-force toughness and integrity for small graphs | — (novel, no C upstream) | ~500 | novel | core/Graph, flow | done | (next) | O(2^n) brute-force for toughness/integrity | no three-source (novel); 24 unit tests + 4 doctests |
| ALGO-TR-028 | Graph coloring (`greedy_coloring`, `greedy_coloring_with_order`, `greedy_coloring_largest_first`, `chromatic_number_greedy`, `is_proper_coloring`, `greedy_clique_number`) — greedy vertex coloring with natural/custom/LF ordering, chromatic bound, proper coloring validator, greedy clique number | — (novel, no C upstream) | ~500 | novel | core/Graph | done | (next) | O(nΔ) greedy, O(n³) clique | no three-source (novel); 29 unit tests + 6 doctests |
| ALGO-TR-029 | Independent set heuristics (`greedy_independent_set`, `independence_ratio`) — greedy maximal independent set (min-degree heuristic), independence ratio α/n | — (novel, no C upstream) | ~250 | novel | core/Graph, cliques | done | (next) | O(n²) greedy | no three-source (novel); 18 unit tests + 2 doctests |
| ALGO-TR-030 | Graph matching (`greedy_matching`, `maximum_matching`, `matching_number`, `is_valid_matching`, `is_perfect_matching`) — greedy maximal matching, brute-force maximum matching, matching number ν(G), validity and perfectness checks | — (novel, no C upstream) | ~490 | novel | core/Graph | done | (next) | O(2^m) brute-force maximum | no three-source (novel); 28 unit tests + 5 doctests |
| ALGO-TR-031 | Hamiltonian path/cycle detection (`is_hamiltonian_path`, `is_hamiltonian_cycle`, `hamiltonian_path`, `hamiltonian_cycle`, `has_hamiltonian_path`, `has_hamiltonian_cycle`) — backtracking with reachability pruning | — (novel, no C upstream) | ~580 | novel | core/Graph | done | (next) | O(n!) backtracking | no three-source (novel); 34 unit tests + 6 doctests |
| ALGO-TR-032 | Clique cover (`is_clique_cover`, `greedy_clique_cover`, `clique_cover_number`) — validation, greedy heuristic, brute-force minimum via Bron-Kerbosch + set cover | — (novel, no C upstream) | ~530 | novel | core/Graph | done | (next) | Bron-Kerbosch + set cover | no three-source (novel); 28 unit tests + 3 doctests |
| ALGO-TR-033 | Graph bandwidth (`bandwidth_of_labeling`, `bandwidth_lower_bound`, `bandwidth`) — labeling bandwidth, degree-based lower bound, brute-force optimal via permutation search | — (novel, no C upstream) | ~380 | novel | core/Graph | done | (next) | O(n!) brute-force | no three-source (novel); 25 unit tests + 3 doctests |
| ALGO-TR-034 | Graph treewidth (`treewidth_upper_bound`, `treewidth_min_fill`, `elimination_ordering`) — min-degree and min-fill elimination heuristics for treewidth upper bounds | — (novel, no C upstream) | ~440 | novel | core/Graph | done | (next) | O(n³) elimination | no three-source (novel); 24 unit tests + 3 doctests |
| ALGO-TR-035 | Wiener polarity index (`wiener_polarity_index`, `count_pairs_at_distance`) — vertex pairs at distance 3 (polarity) or arbitrary k via BFS | — (novel, no C upstream) | ~360 | novel | core/Graph | done | (next) | O(V(V+E)) BFS | no three-source (novel); 23 unit tests + 2 doctests |
| ALGO-TR-036 | Hosoya index (`hosoya_index`, `matching_count_sequence`) — total number of matchings (Z-index) via backtracking enumeration | — (novel, no C upstream) | ~350 | novel | core/Graph | done | (next) | O(m·2^m) backtracking | no three-source (novel); 23 unit tests + 2 doctests |
| ALGO-TR-037 | Merrifield–Simmons index (`merrifield_simmons_index`, `independent_set_count_sequence`) — total independent set count (σ-index) via backtracking enumeration | — (novel, no C upstream) | ~340 | novel | core/Graph | done | (next) | O(2^n) backtracking | no three-source (novel); 26 unit tests + 2 doctests |
| ALGO-TR-038 | Graph periphery (`graph_periphery`, `eccentricity_classes`) — periphery vertices (max eccentricity) and combined center/periphery/radius/diameter classification | — (novel, no C upstream) | ~280 | novel | core/Graph | done | (next) | O(V(V+E)) BFS | no three-source (novel); 17 unit tests + 2 doctests |
| ALGO-TR-039 | Gromov δ-hyperbolicity (`hyperbolicity_twice`, `hyperbolicity`) — four-point condition δ-hyperbolicity measuring tree-likeness of graph metric | — (novel, no C upstream) | ~310 | novel | core/Graph | done | (next) | O(n⁴) brute-force | no three-source (novel); 17 unit tests + 2 doctests |
| ALGO-TR-040 | Chemical topological indices (`first_zagreb_index`, `second_zagreb_index`, `randic_index`, `abc_index`, `harmonic_graph_index`) — degree-based molecular descriptors | — (novel, no C upstream) | ~170 | novel | core/Graph | done | (next) | O(V+E) | no three-source (novel); 30 unit tests + 5 doctests |
| ALGO-TR-041 | Distance-based topological indices (`szeged_index`, `revised_szeged_index`, `pi_index`) — Szeged, revised Szeged, Padmakar-Ivan indices | — (novel, no C upstream) | ~220 | novel | core/Graph | done | (next) | O(V²+VE) all-pairs BFS | no three-source (novel); 25 unit tests + 3 doctests |
| ALGO-TR-042 | Mostar index, degree-distance, Gutman index (`mostar_index`, `degree_distance`, `gutman_index`) — peripherality and Schultz-type distance-degree indices | — (novel, no C upstream) | ~200 | novel | core/Graph | done | (next) | O(V²+VE) all-pairs BFS | no three-source (novel); 23 unit tests + 3 doctests |
| ALGO-TR-043 | Balaban J index (`balaban_j_index`) — distance-based topological index J(G) = m/(μ+1) · Σ 1/√(σ_u·σ_v) | — (novel, no C upstream) | ~140 | novel | core/Graph | done | (next) | O(V²+VE) all-pairs BFS | no three-source (novel); 24 unit tests + 1 doctest |
| ALGO-TR-044 | Hyper-Wiener index and Harary index (`hyper_wiener_index`, `harary_index`) — distance-squared and reciprocal-distance graph invariants | — (novel, no C upstream) | ~140 | novel | core/Graph | done | (next) | O(V²+VE) all-pairs BFS | no three-source (novel); 37 unit tests + 2 doctests |
| ALGO-TR-045 | Eccentric connectivity index (`eccentric_connectivity_index`, `total_eccentricity`, `connective_eccentricity_index`) — eccentricity-based molecular descriptors | — (novel, no C upstream) | ~170 | novel | core/Graph | done | (next) | O(V(V+E)) per-source BFS | no three-source (novel); 43 unit tests + 3 doctests |
| ALGO-TR-046 | Augmented Zagreb / atom-bond sum-connectivity / geometric-arithmetic (`augmented_zagreb_index`, `atom_bond_sum_connectivity`, `geometric_arithmetic_index`) — degree-based molecular descriptors | — (novel, no C upstream) | ~150 | novel | core/Graph | done | (next) | O(E) single pass | no three-source (novel); 34 unit tests + 3 doctests |
| ALGO-TR-047 | Sum-connectivity / inverse sum indeg / symmetric division deg (`sum_connectivity_index`, `inverse_sum_indeg_index`, `symmetric_division_deg_index`) — degree-based molecular descriptors | — (novel, no C upstream) | ~150 | novel | core/Graph | done | (next) | O(E) single pass | no three-source (novel); 44 unit tests + 3 doctests |
| ALGO-TR-048 | Irregularity indices (`albertson_index`, `sigma_index`, `total_irregularity`, `degree_variance`) — measures of deviation from regularity | — (novel, no C upstream) | ~170 | novel | core/Graph | done | (next) | O(E) edge / O(V²) pairs | no three-source (novel); 57 unit tests + 4 doctests |
| ALGO-TR-049 | Forgotten index / reduced second Zagreb / modified first Zagreb (`forgotten_index`, `reduced_second_zagreb`, `modified_first_zagreb`) — degree-based molecular descriptors | — (novel, no C upstream) | ~130 | novel | core/Graph | done | (next) | O(E) / O(V) single pass | no three-source (novel); 46 unit tests + 3 doctests |
| ALGO-TR-050 | General Randić / general sum-connectivity / reciprocal Randić (`general_randic_index`, `general_sum_connectivity_index`, `reciprocal_randic_index`) — parameterised degree-based indices | — (novel, no C upstream) | ~145 | novel | core/Graph | done | (next) | O(E) single pass | no three-source (novel); 32 unit tests + 3 doctests |
| ALGO-TR-051 | Zagreb connection indices (`first_zagreb_connection`, `second_zagreb_connection`, `modified_first_zagreb_connection`) — second-neighbour based topological indices | — (novel, no C upstream) | ~120 | novel | core/Graph | done | (next) | O(V·(V+E)) 2-hop BFS | no three-source (novel); 46 unit tests + 3 doctests |
| ALGO-TR-052 | Narumi-Katayama / multiplicative Zagreb indices (`narumi_katayama_index`, `first_multiplicative_zagreb`, `second_multiplicative_zagreb`) — product-based degree descriptors | — (novel, no C upstream) | ~110 | novel | core/Graph | done | (next) | O(V) / O(E) single pass | no three-source (novel); 42 unit tests + 3 doctests |
| ALGO-TR-053 | Schultz index (`schultz_index`) — degree-distance molecular topological index | — (novel, no C upstream) | ~90 | novel | core/Graph | done | (next) | O(V²) all-pairs BFS | no three-source (novel); 22 unit tests + 1 doctest |
| ALGO-TR-054 | Reformulated Zagreb / third Zagreb (`first_reformulated_zagreb`, `second_reformulated_zagreb`, `third_zagreb_index`) — edge-degree based indices | — (novel, no C upstream) | ~130 | novel | core/Graph | done | (next) | O(E) / O(V·d²) | no three-source (novel); 37 unit tests + 3 doctests |
| ALGO-TR-055 | ABC₄ / GA₅ / degree-sum index (`fourth_abc_index`, `fifth_ga_index`, `degree_sum_index`) — eccentricity/degree based variants | — (novel, no C upstream) | ~130 | novel | core/Graph | done | (next) | O(V²) BFS / O(E) | no three-source (novel); 38 unit tests + 3 doctests |
| ALGO-TR-056 | Inverse degree / Zagreb coindices (`inverse_degree_index`, `first_zagreb_coindex`, `second_zagreb_coindex`) — reciprocal degree sum + non-edge degree indices | — (novel, no C upstream) | ~130 | novel | core/Graph | done | (next) | O(V) / O(E) | no three-source (novel); 37 unit tests + 3 doctests |
| ALGO-TR-057 | Sombor index variants (`sombor_index`, `reduced_sombor_index`, `average_sombor_index`) — geometric degree-based indices (Gutman 2021) | — (novel, no C upstream) | ~100 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 33 unit tests + 3 doctests |
| ALGO-TR-058 | Degree-eccentricity indices (`lanzhou_index`, `degree_eccentricity_index`, `eccentric_distance_sum`) — degree×eccentricity vertex sums | — (novel, no C upstream) | ~160 | novel | core/Graph | done | (next) | O(V²) BFS | no three-source (novel); 40 unit tests + 3 doctests |
| ALGO-TR-059 | Edge-Szeged / Graovac-Ghorbani (`edge_szeged_index`, `edge_pi_index`, `graovac_ghorbani_index`) — edge proximity + log-ratio indices | — (novel, no C upstream) | ~230 | novel | core/Graph | done | (next) | O(V²+E²) | no three-source (novel); 30 unit tests + 3 doctests |
| ALGO-TR-060 | Hyper-Zagreb / Redefined Zagreb (`first_hyper_zagreb`, `second_hyper_zagreb`, `first_redefined_zagreb`) — squared degree-sum/product + reciprocal indices | — (novel, no C upstream) | ~110 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 35 unit tests + 3 doctests |
| ALGO-TR-061 | Edge-degree indices (`platt_index`, `gordon_scantlebury_index`, `bertz_complexity_index`) — Platt/GS/Bertz edge complexity | — (novel, no C upstream) | ~110 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 42 unit tests + 3 doctests |
| ALGO-TR-062 | Neighborhood Zagreb indices (`first_neighborhood_zagreb`, `second_neighborhood_zagreb`, `neighborhood_forgotten_index`) — S(v)-based indices | — (novel, no C upstream) | ~120 | novel | core/Graph | done | (next) | O(V+E) | no three-source (novel); 44 unit tests + 3 doctests |
| ALGO-TR-063 | Gourava indices (`first_gourava_index`, `second_gourava_index`, `first_hyper_gourava_index`) — sum+product degree edge indices | — (novel, no C upstream) | ~110 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 42 unit tests + 3 doctests |
| ALGO-TR-064 | Nirmala indices (`nirmala_index`, `first_inverse_nirmala`, `second_inverse_nirmala`) — √(d(u)+d(v)) and reciprocal variants | — (novel, no C upstream) | ~110 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 38 unit tests + 3 doctests |
| ALGO-TR-065 | Transmission Zagreb indices (`first_transmission_zagreb`, `second_transmission_zagreb`, `reciprocal_transmission_index`) — σ(v)-based indices | — (novel, no C upstream) | ~150 | novel | core/Graph | done | (next) | O(V²) BFS | no three-source (novel); 40 unit tests + 3 doctests |
| ALGO-TR-066 | Leap Zagreb indices (`first_leap_zagreb`, `second_leap_zagreb`, `third_leap_zagreb`) — d₂(v) second-degree-based indices | — (novel, no C upstream) | ~120 | novel | core/Graph | done | (next) | O(V(V+E)) BFS-2 | no three-source (novel); 42 unit tests + 3 doctests |
| ALGO-TR-067 | Reciprocal distance-degree indices (`reciprocal_degree_distance`, `multiplicatively_weighted_harary`, `terminal_wiener_index`) — degree×1/dist hybrids | — (novel, no C upstream) | ~120 | novel | core/Graph | done | (next) | O(V²) BFS | no three-source (novel); 38 unit tests + 3 doctests |
| ALGO-TR-068 | VE-degree Zagreb indices (`first_ve_degree_zagreb_alpha`, `first_ve_degree_zagreb_beta`, `second_ve_degree_zagreb`) — vertex-edge degree indices | — (novel, no C upstream) | ~100 | novel | core/Graph | done | (next) | O(V+E) | no three-source (novel); 40 unit tests + 3 doctests |
| ALGO-TR-069 | EV-degree indices (`first_ev_degree_zagreb`, `second_ev_degree_zagreb`, `ev_degree_randic`) — edge-vertex degree indices | — (novel, no C upstream) | ~100 | novel | core/Graph | done | (next) | O(E·Δ) | no three-source (novel); 38 unit tests + 3 doctests |
| ALGO-TR-070 | Sombor variants (`elliptic_sombor_index`, `modified_sombor_index`, `sombor_coindex`) — Gutman Sombor family extensions | — (novel, no C upstream) | ~100 | novel | core/Graph | done | (next) | O(V²) coindex | no three-source (novel); 35 unit tests + 3 doctests |
| ALGO-TR-071 | Forgotten/hyper-Zagreb coindices (`forgotten_coindex`, `first_hyper_zagreb_coindex`, `second_hyper_zagreb_coindex`) — complement-edge coindices | — (novel, no C upstream) | ~100 | novel | core/Graph | done | (next) | O(V²) | no three-source (novel); 37 unit tests + 3 doctests |
| ALGO-TR-072 | Degree-sum variants (`arithmetic_geometric_index`, `sigma_coindex`, `albertson_coindex`) — AG index + complement irregularity | — (novel, no C upstream) | ~100 | novel | core/Graph | done | (next) | O(V²) | no three-source (novel); 36 unit tests + 3 doctests |
| ALGO-TR-073 | Exponential degree indices (`exponential_augmented_zagreb`, `exponential_randic`, `exponential_abc`, `exponential_ga`) — exp-weighted bond-additive | — (novel, no C upstream) | ~160 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 44 unit tests + 4 doctests |
| ALGO-TR-074 | Multiplicative connectivity (`multiplicative_sum_connectivity`, `multiplicative_randic`, `multiplicative_abc`, `multiplicative_ga`) — product-based bond-additive | — (novel, no C upstream) | ~190 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 40 unit tests + 4 doctests |
| ALGO-TR-075 | Reduced degree indices (`reduced_reciprocal_randic`, `reduced_sum_connectivity`, `reduced_first_zagreb`, `reduced_forgotten_index`) — (d-1)-based variants | — (novel, no C upstream) | ~150 | novel | core/Graph | done | (next) | O(V+E) | no three-source (novel); 41 unit tests + 4 doctests |
| ALGO-TR-076 | Index entropy (`first_zagreb_entropy`, `second_zagreb_entropy`, `randic_entropy`, `abc_entropy`) — Shannon entropy of edge-weight distributions | — (novel, no C upstream) | ~170 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 37 unit tests + 4 doctests |
| ALGO-TR-077 | Degree-power indices (`general_zeroth_order_randic`, `variable_sum_exdeg`, `inverse_degree_power`, `variable_first_zagreb`) — parameterized vertex/edge degree sums | — (novel, no C upstream) | ~160 | novel | core/Graph | done | (next) | O(V) or O(E) | no three-source (novel); 32 unit tests + 4 doctests |
| ALGO-TR-078 | Extended irregularity (`bell_index`, `collatz_sinogowitz`, `irl_irregularity`, `irlu_irregularity`, `degree_cv`) — Bell, Collatz–Sinogowitz, log-irregularity, CV | — (novel, no C upstream) | ~210 | novel | core/Graph | done | (next) | O(V+E) | no three-source (novel); 32 unit tests + 5 doctests |
| ALGO-TR-079 | Edge irregularity indices (`ird_index`, `ira_index`, `irb_index`, `irga_index`) — square-diff, power-diff, root-diff, GA-irregularity | — (novel, no C upstream) | ~170 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 31 unit tests + 4 doctests |
| ALGO-TR-080 | Exponential vertex indices (`exponential_first_zagreb`, `exponential_forgotten`, `exponential_inverse_degree`, `exponential_sum_connectivity`) | — (novel, no C upstream) | ~140 | novel | core/Graph | done | (next) | O(V) or O(E) | no three-source (novel); 28 unit tests + 4 doctests |
| ALGO-TR-081 | Degree-ratio indices (`symmetric_degree_ratio`, `minmax_degree_ratio`, `degree_harmonic_mean_index`, `degree_diff_connectivity`) — SR, min-max, DHM, DDC | — (novel, no C upstream) | ~160 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 35 unit tests + 4 doctests |
| ALGO-TR-082 | Degree distribution moments (`degree_skewness`, `degree_kurtosis`, `degree_gini`, `degree_max_deviation`) — skewness, excess kurtosis, Gini coefficient, max deviation | — (novel, no C upstream) | ~160 | novel | core/Graph | done | (next) | O(V) or O(V²) for Gini | no three-source (novel); 32 unit tests + 4 doctests |
| ALGO-TR-083 | Degree spread measures (`degree_range`, `degree_span_ratio`, `degree_median`, `degree_iqr`) — range, span ratio, median, interquartile range | — (novel, no C upstream) | ~150 | novel | core/Graph | done | (next) | O(V log V) for sorting | no three-source (novel); 34 unit tests + 4 doctests |
| ALGO-TR-084 | Degree deviation measures (`degree_mad`, `degree_median_ad`, `degree_entropy_ln`, `degree_entropy_normalized`) — MAD, MedAD, natural-log entropy, normalized entropy | — (novel, no C upstream) | ~160 | novel | core/Graph | done | (next) | O(V) | no three-source (novel); 32 unit tests + 4 doctests |
| ALGO-TR-085 | Degree distribution shape (`degree_mode`, `degree_concentration`, `degree_diversity`, `hub_dominance`) — mode, concentration, distinct count, hub share | — (novel, no C upstream) | ~150 | novel | core/Graph | done | (next) | O(V) | no three-source (novel); 32 unit tests + 4 doctests |
| ALGO-TR-086 | Edge degree-pair aggregates (`edge_degree_min_sum`, `edge_degree_max_sum`, `edge_degree_log_product`, `edge_degree_mean_sum`) — min/max/log-product/mean over endpoint degrees | — (novel, no C upstream) | ~150 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 30 unit tests + 4 doctests |
| ALGO-TR-087 | Edge degree mean-type indices (`edge_degree_harmonic_sum`, `edge_degree_geometric_sum`, `edge_degree_ratio_sum`, `edge_degree_rms`) — harmonic/geometric/ratio/RMS of endpoint degrees | — (novel, no C upstream) | ~160 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 32 unit tests + 4 doctests |
| ALGO-TR-088 | Degree vertex class ratios (`degree_leaf_ratio`, `degree_isolated_ratio`, `degree_core_ratio`, `degree_tail_ratio`) — leaf/isolated/core/tail vertex fractions | — (novel, no C upstream) | ~155 | novel | core/Graph | done | (next) | O(V) | no three-source (novel); 30 unit tests + 4 doctests |
| ALGO-TR-089 | Vertex neighbor degree statistics (`degree_neighbor_max_sum`, `degree_neighbor_min_sum`, `degree_neighbor_range_sum`, `degree_neighbor_variance_sum`) — per-vertex neighbor degree aggregates | — (novel, no C upstream) | ~180 | novel | core/Graph | done | (next) | O(V+E) | no three-source (novel); 32 unit tests + 4 doctests |
| ALGO-TR-090 | Edge degree normalized indices (`edge_inverse_degree_sum`, `edge_degree_diff_ratio`, `edge_degree_sorensen`, `edge_degree_product_ratio`) — inverse-sum, normalized difference, Sørensen, product/sum² per edge | — (novel, no C upstream) | ~160 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 30 unit tests + 4 doctests |
| ALGO-TR-091 | Degree inequality indices (`degree_herfindahl`, `degree_theil`, `degree_palma`, `degree_hoover`) — Herfindahl–Hirschman concentration, Theil generalized entropy, Palma ratio, Hoover/Robin Hood index | — (novel, no C upstream) | ~200 | novel | core/Graph | done | (next) | O(V) (Palma: O(V log V) sort) | no three-source (novel); 33 unit tests + 4 doctests |
| ALGO-TR-092 | Edge neighborhood overlap aggregates (`edge_common_neighbor_sum`, `edge_jaccard_sum`, `edge_overlap_sum`, `edge_adamic_adar_sum`) — common-neighbor count, Jaccard, overlap coefficient, Adamic–Adar summed over edges | — (novel, no C upstream) | ~210 | novel | core/Graph | done | (next) | O(E·Δ·log Δ) sorted-merge intersection | no three-source (novel); 36 unit tests + 4 doctests |
| ALGO-TR-093 | Edge degree correlation indices (`edge_degree_covariance`, `edge_degree_pearson`, `edge_degree_cosine`, `edge_degree_discrepancy`) — covariance, Pearson r, cosine similarity, normalized squared diff of endpoint degrees | — (novel, no C upstream) | ~170 | novel | core/Graph | done | (next) | O(E) | no three-source (novel); 29 unit tests + 4 doctests |
| ALGO-TR-094 | Graph density profile (`triangle_density`, `square_density`, `edge_connectivity_ratio`, `degree_density`) — triangle density 6T/n(n-1)(n-2), chordless 4-cycle density, edge ratio 2m/n(n-1), normalized degree second moment | — (novel, no C upstream) | ~200 | novel | core/Graph | done | (next) | O(V²·Δ) triangle/square; O(1) ratio; O(V) degree | no three-source (novel); 37 unit tests + 4 doctests |
| ALGO-TR-095 | Walk diversity indices (`walk_entropy`, `walk_regularity`, `degree_laplacian_energy`, `avg_neighbor_connectivity`) — Shannon entropy of degree distribution, coefficient of variation regularity, normalized mean absolute deviation, average neighbor degree ratio | — (novel, no C upstream) | ~200 | novel | core/Graph | done | (next) | O(V+E) | no three-source (novel); 42 unit tests + 4 doctests |
| ALGO-TR-096 | Graph connectivity ratios (`circuit_rank_ratio`, `meshedness_coefficient`, `edge_surplus_ratio`, `connectivity_index`) — cyclomatic number / edges, circuit rank / max planar, surplus edges / max surplus, avg degree / max degree | — (novel, no C upstream) | ~210 | novel | core/Graph | done | (next) | O(V+E) BFS components | no three-source (novel); 44 unit tests + 4 doctests |
| ALGO-TR-097 | Subgraph ratios (`pendant_edge_ratio`, `bridge_ratio`, `triangle_participation`, `isolated_vertex_ratio`) — fraction of pendant edges, bridges (Tarjan), vertices in triangles, isolated vertices | — (novel, no C upstream) | ~220 | novel | core/Graph | done | (next) | O(V+E) Tarjan bridges | no three-source (novel); 40 unit tests + 4 doctests |
| ALGO-TR-098 | Edge density ratios (`self_loop_ratio`, `multi_edge_ratio`, `reciprocity_ratio`, `avg_local_clustering`) — fraction of self-loops, multi-edges, reciprocal directed edges, mean local clustering coefficient | — (novel, no C upstream) | ~190 | novel | core/Graph | done | (next) | O(V·deg²) clustering | no three-source (novel); 38 unit tests + 4 doctests |
| ALGO-TR-099 | Spectral ratios (`degree_spectral_gap_estimate`, `degree_variance_ratio`, `edge_vertex_ratio`, `cyclomatic_density`) — degree-based spectral gap proxy, normalized degree variance, edges per vertex, circuit rank per vertex | — (novel, no C upstream) | ~200 | novel | core/Graph | done | (next) | O(V+E) BFS components | no three-source (novel); 44 unit tests + 4 doctests |
| ALGO-TR-100 | Neighborhood density (`avg_neighbor_degree_ratio`, `hub_ratio`, `leaf_to_hub_ratio`, `freeman_degree_centralization`) — mean neighbor-degree/own-degree ratio, fraction of above-average vertices, leaf/hub count ratio, Freeman's degree centralization | — (novel, no C upstream) | ~200 | novel | core/Graph | done | (next) | O(V·deg) neighbor scan | no three-source (novel); 38 unit tests + 4 doctests |
| ALGO-TR-101 | Path ratios (`diameter_radius_ratio`, `avg_path_fraction`, `efficiency_ratio`, `graph_compactness`) — diameter/radius ratio, avg-dist/diameter fraction, global efficiency, 1−avg_dist/diameter compactness | — (novel, no C upstream) | ~240 | novel | core/Graph | done | (next) | O(V²+VE) all-pairs BFS | no three-source (novel); 37 unit tests + 4 doctests |
| ALGO-TR-102 | Connectivity ratios (`component_ratio`, `largest_component_fraction`, `giant_component_gap`, `vertex_connectivity_ratio`) — components/vertices, largest component share, gap between top-2 components, min-degree/(n−1) connectivity proxy | — (novel, no C upstream) | ~180 | novel | core/Graph | done | (next) | O(V+E) BFS components | no three-source (novel); 38 unit tests + 4 doctests |
| ALGO-TR-103 | Degree-distance ratios (`degree_distance_correlation`, `local_efficiency_ratio`, `transmission_ratio`, `degree_closeness_correlation`) — Pearson correlation of degree vs eccentricity, mean-local/global efficiency, mean/max transmission, degree-closeness Pearson | — (novel, no C upstream) | ~290 | novel | core/Graph | done | (next) | O(V²+VE) all-pairs BFS | no three-source (novel); 36 unit tests + 4 doctests |
| ALGO-TR-104 | Clustering ratios (`clustering_degree_correlation`, `transitivity_gap`, `closed_triplet_ratio`, `square_clustering_ratio`) — Pearson r(degree, local clustering), global-avg transitivity gap, global transitivity, square clustering average | — (novel, no C upstream) | ~280 | novel | core/Graph | done | (next) | O(V·d²) triangle counting | no three-source (novel); 34 unit tests + 4 doctests |
| ALGO-TR-105 | Centrality ratios (`degree_centralization`, `betweenness_centralization`, `closeness_centralization`, `centrality_correlation`) — Freeman degree/betweenness/closeness centralization, Pearson r(betweenness, closeness) | — (novel, no C upstream) | ~300 | novel | core/Graph | done | (next) | O(V²+VE) BFS + Brandes betweenness | no three-source (novel); 42 unit tests + 4 doctests |
| ALGO-TR-106 | Resilience ratios (`vertex_conn_ratio`, `edge_conn_ratio`, `diameter_vulnerability`, `neighbor_degree_disparity`) — min_degree/(n-1) connectivity proxies, max diameter increase on vertex removal, mean knn/degree disparity | — (novel, no C upstream) | ~310 | novel | core/Graph | done | (next) | O(V³) diameter vulnerability via BFS | no three-source (novel); 36 unit tests + 4 doctests |
| ALGO-TR-107 | Mixing ratios (`degree_assortativity_proxy`, `rich_club_density`, `degree_mixing_entropy`, `hub_dominance_ratio`) — edge-endpoint Pearson r, top-25% density, Shannon entropy of degree pairs, max_degree/m | — (novel, no C upstream) | ~280 | novel | core/Graph | done | (next) | O(V+E) edge iteration | no three-source (novel); 34 unit tests + 4 doctests |
| ALGO-TR-108 | Small-world ratios (`smallworld_sigma`, `smallworld_omega`, `clustering_path_ratio`, `navigability_ratio`) — sigma/omega indices, C*n/L, ln(n)/L | — (novel, no C upstream) | ~260 | novel | core/Graph | done | (next) | O(V(V+E)) BFS all-pairs | no three-source (novel); 28 unit tests + 4 doctests |
| ALGO-TR-109 | Bipartivity ratios (`bipartivity_index`, `frustration_ratio`, `odd_cycle_density`, `even_odd_walk_ratio`) — BFS 2-coloring partition fraction, frustrated edge ratio, triangle density, Σd²/2m | — (novel, no C upstream) | ~260 | novel | core/Graph | done | (next) | O(V+E) BFS + neighbor scan | no three-source (novel); 37 unit tests + 4 doctests |
| ALGO-TR-110 | Core-periphery ratios (`core_ratio`, `core_density`, `periphery_fraction`, `core_periphery_gradient`) — max k-core fraction/density, coreness-1 fraction, normalized core depth | — (novel, no C upstream) | ~290 | novel | core/Graph | done | (next) | O(V+E) Batagelj-Zaversnik | no three-source (novel); 41 unit tests + 4 doctests |
| ALGO-TR-111 | Bridge ratios (`bridge_edge_ratio`, `articulation_ratio`, `biconnected_ratio`, `leaf_ratio`) — Tarjan bridge/cut-vertex fractions, largest block edge fraction, pendant vertex ratio | — (novel, no C upstream) | ~370 | novel | core/Graph | done | (next) | O(V+E) Tarjan DFS | no three-source (novel); 47 unit tests + 4 doctests |
| ALGO-TR-112 | Distance distribution ratios (`distance_skewness`, `distance_kurtosis`, `diameter_ratio`, `mean_eccentricity_ratio`) — shape moments of all-pairs distance distribution, normalized diameter, mean ecc/diam | — (novel, no C upstream) | ~270 | novel | core/Graph | done | (next) | O(V(V+E)) all-pairs BFS | no three-source (novel); 38 unit tests + 4 doctests |
| ALGO-TR-113 | Local structure ratios (`local_density_ratio`, `neighbor_connectivity_ratio`, `degree_neighbor_correlation`) — ego-network density, neighbor degree homogeneity, degree-neighbor Pearson correlation | — (novel, no C upstream) | ~210 | novel | core/Graph | done | (next) | O(V·d²) neighborhood scan | no three-source (novel); 26 unit tests + 3 doctests |
| ALGO-TR-114 | Spectral gap ratios (`adjacency_spectral_gap_ratio`, `spectral_radius_ratio`, `energy_ratio`) — adjacency eigenvalue gap, normalized spectral radius, graph energy ratio vs random baseline | — (novel, no C upstream) | ~440 | novel | core/Graph | done | (next) | O(V²) power iteration + QR | no three-source (novel); 19 unit tests + 3 doctests |
| ALGO-TR-115 | Information ratios (`degree_entropy_ratio`, `edge_distribution_entropy`, `structural_information_content`) — Shannon entropy of degree dist., edge-type entropy, log2(distinct degrees)/log2(n) | — (novel, no C upstream) | ~200 | novel | core/Graph | done | (next) | O(V+E) histogram | no three-source (novel); 27 unit tests + 3 doctests |
| ALGO-TR-116 | Robustness ratios (`min_degree_connectivity_ratio`, `degree_range_ratio`, `average_path_resilience`) — min-deg/avg-deg, min-deg/max-deg, diameter resilience under hub removal | — (novel, no C upstream) | ~220 | novel | core/Graph | done | (next) | O(V(V+E)) BFS | no three-source (novel); 25 unit tests + 3 doctests |
| ALGO-TR-117 | Modularity ratios (`modularity_upper_bound_ratio`, `community_size_balance`, `inter_community_edge_ratio`) — greedy modularity / Q_max, community size entropy, inter-community edge fraction | — (novel, no C upstream) | ~260 | novel | core/Graph | done | (next) | O(V·E) label propagation | no three-source (novel); 17 unit tests + 3 doctests |
| ALGO-TR-118 | Flow ratios (`max_flow_efficiency`, `bottleneck_ratio`, `flow_hierarchy_ratio`) — avg min-degree proxy / global min-degree, min/max edge betweenness, (n-1)/m tree ratio | — (novel, no C upstream) | ~240 | novel | core/Graph | done | (next) | O(V²(V+E)) edge betweenness | no three-source (novel); 24 unit tests + 3 doctests |
| ALGO-TR-119 | Resistance ratios (`kirchhoff_index_ratio`, `resistance_regularity`, `spanning_tree_ratio`) — Wiener/diameter proxy, min/max edge-resistance, log(τ)/((n-1)log(n)) via Kirchhoff matrix-tree | — (novel, no C upstream) | ~300 | novel | core/Graph | done | (next) | O(V²(V+E)) BFS + O(V³) Jacobi | no three-source (novel); 28 unit tests + 3 doctests |
| ALGO-TR-120 | Hierarchy ratios (`degree_hierarchy`, `layer_ratio`, `dominance_ratio`) — Gini coefficient of degrees, BFS depth from hub / max depth, neighborhood inclusion dominance fraction | — (novel, no C upstream) | ~200 | novel | core/Graph | done | (next) | O(V²) dominance check | no three-source (novel); 23 unit tests + 3 doctests |
| ALGO-TR-121 | Centrality diversity indices (`centrality_entropy`, `centrality_divergence`, `centrality_rank_correlation`) — Shannon entropy of degree distribution, Jensen-Shannon divergence between degree and betweenness distributions, Spearman rank correlation of degree vs betweenness | — (novel, no C upstream) | ~240 | novel | TR-120 | done | (next) | O(V³) betweenness | no three-source (novel); 12 unit tests |
| ALGO-TR-122 | Core profile indices (`core_persistence`, `shell_diversity`, `degeneracy_gap`) — average coreness / degeneracy ratio, Shannon entropy of k-shell size distribution, normalised gap between degeneracy and average coreness | — (novel, no C upstream) | ~120 | novel | TR-121 | done | (next) | O(E) coreness | no three-source (novel); 16 unit tests + 3 doctests |
| ALGO-TR-123 | Edge distribution entropy indices (`edge_degree_entropy`, `edge_weight_balance`, `degree_pair_concentration`) — Shannon entropy of edge degree-pair distribution, normalised entropy (balance), fraction of edges in most common degree-pair class | — (novel, no C upstream) | ~140 | novel | TR-122 | done | (next) | O(E) edge scan | no three-source (novel); 14 unit tests + 3 doctests |
| ALGO-TR-124 | Clustering profile indices (`clustering_variance`, `clustering_entropy`, `clustering_bimodality`) — variance of local CC distribution, normalised Shannon entropy of binned CC histogram, Sarle's bimodality coefficient | — (novel, no C upstream) | ~130 | novel | TR-123 | done | (next) | O(V·deg²) triangles | no three-source (novel); 10 unit tests + 3 doctests |
| ALGO-TR-125 | Distance profile indices (`hop_entropy`, `distance_gini`, `reach_decay`) — normalised Shannon entropy of hop-distance histogram, Gini coefficient of pairwise distances, average fraction reachable within half diameter | — (novel, no C upstream) | ~160 | novel | TR-124 | done | (next) | O(V²) BFS all-pairs | no three-source (novel); 10 unit tests + 3 doctests |
| ALGO-SP-001 | Dijkstra single-source distances (`dijkstra_distances`, OUT mode) | paths/dijkstra.c:322-331 | ~250 | adapt | TR-001 | done | (next) | O(E log V + V) heap | C:1 / py:1 / R:1 |
| ALGO-SP-001b | Dijkstra paths/parents + multi-source + cutoff | paths/dijkstra.c | ~400 | adapt | SP-001 | done | (next) | O(E log V + V) heap | C:3 / py:3 / R:3 (paths + path_to + cutoff) |
| ALGO-SP-001c | Dijkstra IN/ALL mode + all-shortest-paths | paths/dijkstra.c | ~250 | adapt | SP-001b | done | (next) | O(E log V + V) heap | C:2 / py:2 / R:2 (dist_with_mode + all_shortest_paths) |
| ALGO-SP-002 | Bellman-Ford single-source distances + mode-aware variant | distances_bellman_ford.c (lines 1-295) | 591 | adapt | SP-001 | done | (next) | O(V·E) SPFA worst case | C:3 / py:3 / R:3 |
| ALGO-SP-003 | Johnson's all-pairs shortest distances | distances_johnson.c (lines 83-254) | 254 | adapt | SP-001,002 | done | (next) | O(V·E + V·(V+E)·log V) — V Dijkstras + 1 BF | C:2 / py:2 / R:2 |
| ALGO-SP-004 | Floyd-Warshall (`floyd_warshall_distances`, original variant) | paths/floyd_warshall.c:270-365 | 365 | adapt | - | done | (next) | O(V³) triple-loop | C:1 / py:1 / R:1 |
| ALGO-SP-005 | A* | astar.c | 273 | adapt | TR-001 | done | (next) | O((V+E)log V), better w/ admissible heuristic | C:1 / py:1 / R:1 |
| ALGO-SP-006 | BFS distances (single-source, unweighted, OUT mode) | unweighted.c:273-325 | 240 | adapt | TR-001 | done | (next) | 2.5 µs/karate | C:1 / py:1 / R:1 |
| ALGO-SP-007 | Voronoi cells (multi-source BFS / Dijkstra + tiebreaker) | paths/voronoi.c:30-418 | ~420 | adapt | SP-001c, SP-006 | done | (next) | 4.63 µs / karate-3gens (10.06×) | C:4 / py:3 / R:3 |
| ALGO-SP-010 | Single-source widest-path widths (Dijkstra-style) + mode-aware variant | widest_paths.c (lines 596-741) | 145 | adapt | SP-001 | done | (next) | O((V+E) log V) | C:2 / py:2 / R:2 |
| ALGO-SP-011 | Single-source single-target widest path (returns path) + mode-aware variant | widest_paths.c (lines 102-405) | 305 | adapt | SP-010 | done | (next) | O((V+E) log V) | C:2 / py:2 / R:2 |
| ALGO-SP-012 | All-pairs widest widths via Floyd-Warshall + mode-aware variant | widest_paths.c (lines 451-555) | 105 | adapt | SP-010 | done | (next) | O(V³) | C:2 / py:2 / R:2 |
| ALGO-SP-013 | Multi-target widest paths (single source) + mode-aware variant | widest_paths.c (lines 102-322) | 220 | adapt | SP-011 | done | (next) | O((V+E) log V) | C:2 / py:2 / R:2 |
| ALGO-SP-014 | WidestPaths struct: widths + parents + inbound_edges SPT outputs + mode-aware variant | widest_paths.c (lines 102-322) | ~80 | adapt | SP-013 | done | (next) | O((V+E) log V) | C:2 / py:2 / R:2 |
| ALGO-SP-020 | Eccentricity / radius / diameter (unweighted) | distances.c:257-363, shortest_paths.c:1259 | ~250 | adapt | SP-006 | done | (next) | ecc 92 µs / rad 88 µs / karate | C:3 / py:3 / R:3 (3 algos × 3 sources) |
| ALGO-SP-021abc | Mode-aware (`*_with_mode` accepting OUT/IN/ALL) eccentricity/radius/diameter | distances.c, shortest_paths.c | ~150 | adapt | SP-020 | done | (next) | BFS reuse, O(V·(V+E)) | C:1 / py:1 / R:1 (× 3 algos = 9 fixtures) |
| ALGO-SP-021..023 | Weighted (Dijkstra-based) eccentricity/radius/diameter | distances.c, shortest_paths.c | ~150 | adapt | SP-001, SP-021abc | done | (next) | O(V·(V+E)logV) Dijkstra-from-each | C:1 / py:1 / R:1 (× 3 algos = 9 fixtures) |
| ALGO-SP-030 | `path_length_hist` (all-pairs shortest-path length histogram via BFS from every vertex; returns `PathLengthHistResult { hist: Vec<f64>, unconnected: f64 }` where `hist[d]` = count of vertex pairs at distance `d+1`; `directed` parameter controls whether a directed graph uses OUT-mode traversal (ordered pairs) or ALL-mode (unordered pairs, halved); self-loops/multi-edges traversed by BFS but do not create extra pair counts; empty/single-vertex graphs return empty hist and 0 unconnected) | paths/histogram.c (141 lines) | ~120 | adapt | core/Graph | done | 2026-05-27 | O(V·(V+E)) all-pairs BFS | C:3 / py:2 / R:2 |
| ALGO-SP-031 | `all_simple_paths` — enumerate all simple (vertex-disjoint) paths from source to targets via DFS backtracking; supports mode (Out/In/All), min/max length, max results; deduplicates multi-edges and ignores self-loops | paths/simple_paths.c (189 lines) | ~170 | adapt | core/Graph | done | 2026-05-27 | O(n!) worst-case DFS backtracking | C:2 / py:2 / R:2 |
| ALGO-SP-032 | `k_shortest_paths` — Yen's algorithm for k shortest loopless paths between two vertices; returns up to k paths in order of increasing weight; uses Dijkstra as inner shortest-path finder with edge-removal via sentinel weights | paths/shortest_paths.c:1402 | ~280 | adapt | SP-001c | done | 2026-05-28 | O(kV(V+E)logV) | unit tests only |
| ALGO-SP-033 | `bellman_ford_path_to` + `bellman_ford_path_to_with_mode` — single-source single-target shortest path via Bellman-Ford with parent tracking; supports negative weights; returns vertex+edge path or None if unreachable | paths/bellman_ford.c | ~100 | adapt | SP-002 | done | 2026-05-28 | O(V·E) | unit tests only |
| ALGO-SP-034 | `distances_cutoff` + `distances_cutoff_with_mode` + `distances_cutoff_multi` — unweighted BFS shortest-path distances with early termination at caller-specified cutoff; single-source and multi-source variants; direction-aware (Out/In/All) | paths/unweighted.c:102-219 | ~340 | adapt | SP-006 | done | 2026-05-29 | O(V+E) per source | unit tests + doctests |
| ALGO-SP-035 | `get_shortest_path` — single-pair shortest path returning both the vertex sequence and the edge sequence; convenience wrapper over `get_shortest_paths` for one target; backend selected by `weights` (None → unweighted BFS faithful to upstream incident-order tie-breaking; non-negative → Dijkstra; negative → Bellman-Ford); direction-aware (Out/In/All); empty path when unreachable, `[from]` when `from == to` | paths/unweighted.c:662-703 | ~280 | adapt | SP-001b, SP-033, SP-006 | done | 2026-05-29 | O(V+E) unweighted / O(E log V) Dijkstra / O(V·E) Bellman-Ford | unit tests + proptest + doctests |
| ALGO-SP-036 | `get_shortest_path_astar` — A* single-pair shortest path with admissible heuristic callback; returns vertex+edge path (`ShortestPath`); null heuristic degenerates to Dijkstra; non-negative weights only; direction-aware (Out/In/All); lazy-deletion binary heap | paths/astar.c | ~230 | adapt | SP-035 | done | 2026-05-31 | O(E log V) worst case | C:2 / py:3 / R:2 |
| ALGO-SP-037 | `get_all_shortest_paths_dijkstra` — all shortest paths from single source via Dijkstra with multi-parent tracking; returns vertex paths, edge paths, and nrgeo (path counts); non-negative weights; direction-aware (Out/In/All); epsilon-based distance comparison | paths/dijkstra.c:797-1228 | ~500 | adapt | SP-001b | done | 2026-05-31 | O(V·E + V·P) where P = total paths | C:2 / py:3 / R:2 |
| ALGO-SP-038 | `get_shortest_paths_dijkstra` — weighted shortest paths from single source to all vertices via Dijkstra parent-tree reconstruction; returns vertex and edge path sequences; non-negative weights; direction-aware (Out/In/All) | paths/dijkstra.c:336-635 | ~140 | wrap | SP-001b | done | 2026-05-31 | O(V log V + E) Dijkstra + O(V·L) path reconstruction | C:2 / py:3 / R:2 |
| ALGO-LO-001 | `layout_random`, `layout_random_3d`, `layout_circle`, `layout_star`, `layout_grid`, `layout_grid_3d`, `layout_sphere` — simple deterministic/random graph layouts (2D/3D); circle uses Saff-Kuijlaars spiral for sphere | layout/*.c | ~320 | adapt | core/Graph | done | 2026-05-27 | O(V) all layouts | C:2 / py:2 / R:2 |
| ALGO-LO-002 | `layout_fruchterman_reingold` + `layout_fruchterman_reingold_3d` — force-directed layout (FR'91); attractive f_a(d)=-w·d², repulsive f_r(d)=1/d; linear cooling; grid acceleration for V>1000; per-vertex bounds; disconnected-graph global attraction; weighted edges | layout/fruchterman_reingold.c (676 lines) | ~680 | adapt | core/Graph, CC-001 | done | 2026-05-27 | O(V²·niter) naive, O(V·niter) grid | 10 unit + 2 doctest |
| ALGO-LO-003 | `layout_kamada_kawai` + `layout_kamada_kawai_3d` — spring-embedder layout (KK'89); all-pairs shortest paths → spring model; iterative energy minimization via Newton-Raphson on vertex with max gradient; 2×2/3×3 Hessian solved by Cramer's rule; weighted edges; per-vertex bounds | layout/kamada_kawai.c (702 lines) | ~500 | adapt | core/Graph, SP-004 (Floyd-Warshall) | done | 2026-05-27 | O(V²logV) init + O(V·maxiter) | 12 unit + 2 doctest |
| ALGO-LO-004 | `layout_reingold_tilford` + `layout_reingold_tilford_circular` — layered tree layout (RT'81); BFS spanning tree → postorder contour-merging with threading → coordinate accumulation; circular variant maps to polar coords; RtMode (Out/In/All) | layout/layout.c (reingold_tilford, ~600 lines) | ~580 | adapt | core/Graph | done | 2026-05-27 | O(V²) worst BFS+contour | 11 unit + 2 doctest |
| ALGO-LO-005 | `layout_sugiyama` — Sugiyama layered layout for DAGs/general graphs; 4-phase: DFS feedback arc set cycle removal, longest-path layer assignment, barycenter crossing minimization, simplified Brandes-Köpf coordinate placement; supports user-supplied layers; dummy nodes for long edges; returns extended graph with edge mapping | layout/sugiyama.c (~1200 lines) | ~1080 | adapt | core/Graph | done | 2026-05-27 | O(V·E + L·iter·max_layer_width²) | 13 unit + 1 doctest |
| ALGO-LO-006 | `layout_bipartite` — bipartite layout via Sugiyama; two-row placement with crossing minimization | layout/bipartite.c | ~90 | adapt | core/Graph, LO-005 | done | 2026-05-27 | O(V·E·iter) | 8 unit + 1 doctest |
| ALGO-LO-007 | `layout_gem` — GEM force-directed layout (Frick-Ludwig-Mehldau 1994); per-vertex adaptive temperature, oscillation/rotation detection, gravitational barycenter pull | layout/gem.c (~280 lines) | ~310 | adapt | core/Graph | done | 2026-05-27 | O(V²·maxiter) | 11 unit + 1 doctest |
| ALGO-LO-008 | `layout_davidson_harel` — Davidson-Harel simulated annealing layout (1996); 5-component energy function (node-node repulsion, border, edge lengths, crossings, node-edge distance); SA with fine-tuning phase | layout/davidson_harel.c (~500 lines) | ~540 | adapt | core/Graph | done | 2026-05-27 | O(V²·E·rounds·tries) | 11 unit + 1 doctest |
| ALGO-LO-009 | `layout_graphopt` — GraphOpt force-directed layout (Schmuhl); Coulomb repulsion + Hooke spring attraction; configurable charge/mass/spring/iterations | layout/graphopt.c | ~265 | adapt | core/Graph | done | 2026-05-27 | O(V²·niter) | 11 unit + 1 doctest |
| ALGO-LO-010 | `layout_mds` — classical MDS layout via double centering + eigendecomposition; power iteration for top-2 eigenvectors; disconnected graph handling | layout/mds.c | ~320 | adapt | core/Graph, SP-004 | done | 2026-05-27 | O(V³) Floyd-Warshall + O(V²·k·iter) eigen | 11 unit + 1 doctest |
| ALGO-LO-011 | `layout_lgl` — Large Graph Layout; BFS layering from root + FR-style force-directed with grid-accelerated repulsion; configurable cooling/area/charge | layout/large_graph.c (~389 lines) | ~430 | adapt | core/Graph | done | 2026-05-27 | O(V²·maxit) brute / O(V·maxit) grid | 12 unit + 1 doctest |
| ALGO-LO-012 | `layout_drl` — DrL (Distributed Recursive Layout); multi-phase simulated annealing (liquid/expansion/cooldown/crunch/simmer) with 400×400 density-grid repulsion; 5 preset templates; weighted edges; seed positions | layout/drl/ (~4200 lines C++) | ~640 | adapt | core/Graph | done | 2026-05-27 | O(V·E·stages·iters) | 12 unit + 1 doctest |
| ALGO-LO-013 | `layout_umap` + `layout_umap_3d` — UMAP (Uniform Manifold Approximation and Projection); distance→weight conversion with sigma/rho, Gauss-Newton a/b fitting, SGD with negative-sampling repulsion; 2D/3D; seed support | layout/umap.c (~1285 lines) | ~640 | adapt | core/Graph | done | 2026-05-27 | O(E·epochs·neg_rate) | 16 unit + 1 doctest |
| ALGO-LO-014 | `layout_merge_dla` — merge multiple 2D component layouts into a single combined layout via DLA (Diffusion Limited Aggregation); bounding circle coverage + DLA random walk placement with 200×200 MergeGrid collision detection; deterministic SplitMix64 seed | layout/merge_dla.c + merge_grid.c | ~480 | adapt | — | done | 2026-05-27 | O(n_components² · grid_size) | 8 unit + 1 doctest |
| ALGO-LO-015 | `layout_drl_3d` — 3D extension of DrL force-directed layout; 100³ density grid (vs 400² for 2D); same multi-phase simulated annealing with 3D centroid/random energy minimization; shares `DrlOptions`/`DrlTemplate` with 2D variant | layout/drl/ (~1250 lines C++) | ~400 | adapt | ALGO-LO-012 | done | 2026-06-01 | O(V·E·stages·iters) | 12 unit + 1 doctest |
| ALGO-LO-016 | `layout_align` — nematic tensor alignment; centers layout on origin, computes nematic tensor Q from edge vectors (or vertex positions if edgeless), eigendecomposes Q via Jacobi rotation, rotates layout to align principal axes with coordinate axes, reorders axes by extent (widest first); supports arbitrary dimensionality; symmetry-breaking retry when Q ≈ 0 | layout/align.c (~320 lines) | ~250 | adapt | — | done | 2026-06-01 | O(V·dim² + E·dim²) | 16 unit + 1 doctest |
| ALGO-CC-001 | Weakly connected components | components.c:82-180 | 100 | adapt | TR-001 | done | (next) | 4.1 µs/karate | C:2 / py:1 / R:1 |
| ALGO-CC-002 | Strongly connected components (Kosaraju) | components.c:203-386 | 184 | adapt | CC-001 | done | (next) | 4.49 µs/karate-dir | C:2 / py:1 / R:1 |
| ALGO-CC-003 | Decompose graph by components (`decompose`, weak only) | components.c:566-732 | 350 | adapt | CC-001,002 | done | (next) | O(V+E) BFS + edge sweep | C:1 / py:1 / R:1 |
| ALGO-CC-010 | Articulation points (`articulation_points`) | components.c:969-972 (driver at 1085-1209) | ~250 | adapt | TR-002 | done | (next) | 3.2 µs/karate | C:1 / py:1 / R:1 |
| ALGO-CC-014 | Bridges (`bridges`) | components.c:1400-1504 | ~200 | adapt | TR-002 | done | (next) | 3.8 µs/karate | C:2 / py:1 / R:1 |
| ALGO-CC-013 | `is_biconnected` (delegate to CC-001 + CC-010) | components.c:1254-1379 | ~80 | copy | CC-001, CC-010 | done | (next) | (delegate; ≈7.3 µs/karate) | C:2 / py:1 / R:1 |
| ALGO-CC-011 | Biconnected components multi-output (`biconnected_components`) | components.c:1032-1227 | ~250 | adapt | CC-010 | done | (next) | O(V+E) DFS | C:1 / py:1 / R:1 |
| ALGO-CC-012 | Biconnected components: explicit `component_edges` output | components.c:1176-1195 | ~80 | adapt | CC-011 | done | (next) | (CC-011 + O(Σ_v deg(v) per comp)) | C:1 / py:1 / R:1 |
| ALGO-CC-020 | Reachability counts (`count_reachable`) | reachability.c:179 | ~80 | adapt | SP-006 | done | (next) | (BFS-from-each, ≈ vcount * SP-006) | C:1 / py:1 / R:1 |
| ALGO-CC-021 | Reachability matrix (`reachability_matrix`) | reachability.c:72-148 | ~80 | adapt | SP-006 | done | (next) | O(V*(V+E)) BFS-from-each | C:1 / py:1 / R:1 |
| ALGO-CC-022 | Transitive closure (`transitive_closure`) | reachability.c:225-257 | ~80 | adapt | CC-021 | done | (next) | (CC-021 + closure ctor) | C:1 / py:1 / R:1 |
| ALGO-CC-023 | SCC-based reachability (`reachability`) | reachability.c:72-149 | ~130 | adapt | CC-001, CC-002 | done | (next) | O(\|C\|·\|V\|/w + \|V\| + \|E\|) SCC condensation + bitset propagation | C:1 / py:0 / R:0 |
| ALGO-CC-030 | Edge-list percolation (union-find on vertex-pair sequence) | percolation.c (lines 105-180) | 76 | adapt | - | done | (next) | O(\|E\| α(\|E\|)) | C:2 / py:2 / R:2 |
| ALGO-CC-031 | Bond percolation (Graph + edge id order → thin wrapper over edgelist) | percolation.c (lines 214-265) | 51 | adapt | CC-030 | done | (next) | O(\|E\| α(\|E\|)) | C:2 / py:2 / R:2 |
| ALGO-CC-032 | Site percolation (Graph + vertex order — incremental site activation) | percolation.c (lines 328-410) | 80 | adapt | CC-030 | done | (next) | O(\|V\| + \|E\| α(\|E\|)) | C:2 / py:2 / R:2 |
| ALGO-CC-040 | Eulerian existence (`is_eulerian`) | eulerian.c:333 (incl. directed/undirected helpers) | ~280 | adapt | CC-001 | done | (next) | 4.7 µs/karate | C:3 / py:0 / R:2 (py skipped — see CONFORMANCE.md) |
| ALGO-CC-041 | Eulerian path/cycle construction, undirected (Hierholzer) | eulerian.c:345-450 | ~200 | adapt | CC-040 | done | (next) | (CC-040 + O(V+E)) | C:1 / py:0 / R:1 (py skipped) |
| ALGO-CC-042 | Eulerian path/cycle construction, directed (Hierholzer) | eulerian.c:453-560 | (folded into CC-041 module) | adapt | CC-041 | done | (next) | (CC-040 + O(V+E)) | C:1 / py:0 / R:0 (py skipped) |
| ALGO-PR-001 | Girth (shortest cycle length) | properties/girth.c:73 | ~200 | adapt | CC-001 | done | (next) | 2.6 µs/karate | C:2 / py:1 / R:1 |
| ALGO-PR-002 | Triangle count + global transitivity | properties/triangles.c:405-630 | ~250 | adapt | - | done | (next) | 2.7 µs/karate | C:3 / py:2 / R:2 (2 algos × 3 sources) |
| ALGO-PR-002b | Local transitivity per-vertex (`transitivity_local_undirected`) | properties/triangles.c:330+185-280 | ~150 | adapt | PR-002 | done | (next) | (shares PR-002 baseline) | C:1 / py:1 / R:1 |
| ALGO-PR-002c | Barrat weighted transitivity (`transitivity_barrat`) | properties/triangles.c:632 | ~150 | adapt | PR-002b | done | (next) | O(V·d²) (shares PR-002 baseline) | C:1 / py:1 / R:1 |
| ALGO-PR-002d | Per-vertex adjacent triangle count (`count_adjacent_triangles`) | properties/triangles.c:522 | ~30 | adapt | PR-002b | done | (next) | (shares PR-002 baseline) | C:2 / py:2 / R:2 |
| ALGO-PR-003 | Density + mean shortest-path length (unweighted) | basic_properties.c:71, shortest_paths.c:329 | ~150 | adapt | SP-006 | done | (next) | density O(1); mean_distance ≈ ecc | C:2 / py:2 / R:2 |
| ALGO-PR-004 | Reciprocity (default mode) | basic_properties.c:325 | ~80 | adapt | - | done | (next) | O(V+E) | C:1 / py:1 / R:1 |
| ALGO-PR-004b | Reciprocity ratio mode + ignore_loops (`reciprocity_with_mode`) | basic_properties.c:325 | ~80 | adapt | PR-004 | done | (next) | O(V+E) | C:1 / py:1 / R:1 |
| ALGO-PR-005 | Average nearest-neighbour degree (`avg_nearest_neighbor_degree`) | properties/degrees.c:263 | ~80 | adapt | - | done | (next) | O(V+E) | C:1 / py:1 / R:1 |
| ALGO-PR-005b | knn weighted + per-degree aggregate (`knnk`) | properties/degrees.c:263 | ~120 | adapt | PR-005 | done | (next) | O(V+E) per call | C:3 / py:3 / R:3 (3 algos × 3 sources) |
| ALGO-PR-006 | Degree assortativity (`assortativity_degree`, undirected) | misc/mixing.c:443 + 273 | ~150 | adapt | - | done | (next) | O(V+E) | C:1 / py:1 / R:1 |
| ALGO-PR-006b | Weighted assortativity (`assortativity_degree_weighted`, undirected) | misc/mixing.c | ~120 | adapt | PR-006 | done | (next) | O(V + E) | C:1 / py:1 / R:1 |
| ALGO-PR-006c | Directed assortativity (`assortativity_degree_directed`, unweighted) | misc/mixing.c:351-405 | ~120 | adapt | PR-006b | done | (next) | O(V+E) | C:1 / py:1 / R:1 |
| ALGO-PR-006d | Directed weighted assortativity | misc/mixing.c:351-405 | ~80 | adapt | PR-006c | done | (next) | O(V+E) | C:1 / py:1 / R:1 |
| ALGO-PR-067 | Generic value-based assortativity (`assortativity`, arbitrary per-vertex numeric values; optional `values_in` for directed targets, optional per-edge `weights`, `directed`, `normalized`; Pearson-correlates endpoint values over the edge list — `assortativity_degree` is the values=degree special case; returns `Option<f64>`, `None` = upstream `IGRAPH_NAN`) | misc/mixing.c (`igraph_assortativity`) | ~230 | adapt | PR-006 | done | 2026-05-31 | O(V+E) single edge pass | C:2 / py:3 / R:2 |
| ALGO-PR-007 | Closeness centrality (`closeness`, unweighted, IGRAPH_OUT/ALL) | centrality/closeness.c:33+ | ~120 | adapt | SP-006 | done | (next) | O(V*(V+E)) BFS | C:1 / py:1 / R:1 |
| ALGO-PR-007b | Weighted closeness (`closeness_weighted`, Dijkstra) | centrality/closeness.c | ~150 | adapt | PR-007, SP-001 | done | (next) | O(V*(V+E)logV) Dijkstra-from-each | C:1 / py:1 / R:1 |
| ALGO-PR-009 | Harmonic centrality (`harmonic_centrality`, unweighted) | centrality/closeness.c:740-805 | ~80 | adapt | SP-006 | done | (next) | O(V*(V+E)) BFS | C:1 / py:1 / R:1 |
| ALGO-PR-009b | Weighted harmonic centrality (`harmonic_centrality_weighted`) | centrality/closeness.c | ~80 | adapt | PR-009, SP-001 | done | (next) | O(V*(V+E)logV) Dijkstra-from-each | C:1 / py:1 / R:1 |
| ALGO-PR-008 | Betweenness centrality (`betweenness`, Brandes unweighted) | centrality/betweenness.c:504+ | ~120 | adapt | SP-006 | done | (next) | O(V*(V+E)) Brandes | C:1 / py:1 / R:1 |
| ALGO-PR-008b | Weighted betweenness (`betweenness_weighted`, Brandes-Dijkstra) | centrality/betweenness.c | ~150 | adapt | PR-008, SP-001 | done | (next) | O(V*(V+E)logV) | C:1 / py:1 / R:1 |
| ALGO-PR-010 | Edge betweenness (`edge_betweenness`, Brandes unweighted) | centrality/betweenness.c:766+ | ~120 | adapt | PR-008 | done | (next) | O(V*(V+E)) Brandes | C:1 / py:1 / R:1 |
| ALGO-PR-010b | Weighted edge betweenness (`edge_betweenness_weighted`, Brandes-Dijkstra) | centrality/betweenness.c | ~150 | adapt | PR-010, SP-001 | done | (next) | O(V*(V+E)logV) | C:1 / py:1 / R:1 |
| ALGO-PR-011 | PageRank (`pagerank`, power iteration, damping=0.85) | centrality/pagerank.c | ~180 | adapt | - | done | (next) | O(iter*(V+E)) power | C:1 / py:1 / R:1 |
| ALGO-PR-011b | Weighted PageRank (`pagerank_weighted`, power iteration) | centrality/pagerank.c | ~180 | adapt | PR-011 | done | (next) | O(iter*(V+E)) power | C:1 / py:1 / R:1 |
| ALGO-PR-011c | `pagerank_linsys` GMRES linear-system backend (cast PageRank as `(I − αP)x = (1−α)/n · 1`, solve via hand-rolled restarted GMRES with modified Gram-Schmidt Arnoldi + Givens rotations on the Hessenberg system; converges in O(spectral gap) Arnoldi steps so it wins on low-α graphs; arithmetic parity to PR-011 at 1e-9 on the shared pagerank fixtures (limited by PR-011's `eps = 1e-10` power-iter stopping rule; GMRES itself reaches the true fixed point to ≤ machine precision); flat row-major Krylov basis + Hessenberg buffers + pre-computed `inv_out_deg`, no per-iteration allocations; no new deps) | centrality/pagerank.c (Google-matrix shape only; the GMRES side is self-rolled, not ported) | ~400 | rewrite | PR-011 | done | 2026-05-26 | O(restart · arnoldi · (\|V\| + \|E\|)) | C: reuses PR-011 / py: reuses PR-011 / R: reuses PR-011 |
| ALGO-PR-012 | Eigenvector centrality (`eigenvector_centrality`, undirected) | centrality/eigenvector.c | ~120 | adapt | - | done | (next) | shifted power | C:1 / py:1 / R:1 |
| ALGO-PR-012b | Directed eigenvector + weighted (`eigenvector_centrality_weighted`, `eigenvector_centrality_directed`, `eigenvector_centrality_directed_weighted`, `eigenvector_centrality_full`; self-rolled shifted power-iter on (M+σI), Perron-Frobenius guarantee, DAG sentinel, Rayleigh-quotient λ) | centrality/eigenvector.c | ~600 | rewrite | PR-012 | done | (next) | shifted power on (M+σI) | weighted: C:1/py:1/R:1; directed: C:1/py:1/R:1 |
| ALGO-OP-001 | Simplify graph (`simplify`: remove loops + multi-edges) | operators/simplify.c | ~200 | adapt | CORE-001a/d | done | (next) | O(V + E log E) | C:1 / py:1 / R:1 |
| ALGO-OP-002 | Disjoint union (`disjoint_union`, two-graph variant) | operators/disjoint_union.c | ~80 | adapt | CORE-001a | done | (next) | O(V + E) | C:1 / py:1 / R:1 |
| ALGO-OP-002b | Multi-arg `disjoint_union_many` | operators/disjoint_union.c | ~80 | adapt | OP-002 | done | (next) | O(ΣV + ΣE) | C:1 / py:1 / R:1 |
| ALGO-OP-003 | Complementer (`complementer`, configurable loops) | operators/complementer.c | ~100 | adapt | CORE-001a/d | done | (next) | O(V² log E) | C:1 / py:1 / R:1 |
| ALGO-OP-004 | Union of two graphs (`union`, max-multiplicity) | operators/union.c:69 | ~150 | adapt | CORE-001a | done | (next) | O((E1+E2) log(E1+E2)) BTreeMap merge | C:1 / py:1 / R:1 |
| ALGO-OP-005 | Intersection of two graphs (`intersection`, min-multiplicity) | operators/intersection.c:71 | ~150 | adapt | CORE-001a | done | (next) | O((E1+E2) log(E1+E2)) BTreeMap lookup | C:1 / py:1 / R:1 |
| ALGO-OP-006 | Difference of two graphs (`difference`, clamped multiset subtract) | operators/difference.c:54 | ~120 | adapt | CORE-001a | done | (next) | O((E1+E2) log(E1+E2)) BTreeMap lookup | C:1 / py:1 / R:1 |
| ALGO-OP-007 | Induced subgraph (`induced_subgraph`, vertex subset) | operators/subgraph.c | ~120 | adapt | CORE-001a | done | (next) | O(V' + E) | C:1 / py:1 / R:1 |
| ALGO-OP-007b | `is_same_graph` (structural equality predicate) | operators/misc.c | ~40 | adapt | CORE-001a | done | (next) | O(E) | C:1 / py:1 / R:1 |
| ALGO-OP-008 | Reverse (`reverse`, flip directed edges) | operators/reverse.c | ~40 | adapt | CORE-001a | done | (next) | O(E) | C:1 / py:1 / R:1 |
| ALGO-OP-009 | Permute vertices (`permute_vertices`, bijective remapping) | operators/permute_vertices.c | ~80 | adapt | CORE-001a | done | (next) | O(V + E) | C:1 / py:1 / R:1 |
| ALGO-OP-010 | `modular_product` + `graph_product` dispatcher (`GraphProductType` enum) — modular product of two simple graphs = tensor(g1,g2) ∪ tensor(complement(g1),complement(g2)); unified `graph_product` dispatcher for all 5 non-rooted product types (Cartesian/Lexicographic/Strong/Tensor/Modular) matching C `igraph_product` | operators/products.c (lines 336-386, 388-540) | ~200 | adapt | OP-003 (complementer) + OP-004 (union) + PR-013 (is_simple) | done | (now) | O(V1·V2 + E1·E2) | - |
| ALGO-CO-001 | Modularity (`modularity`, undirected, unweighted, configurable γ) | community/modularity.c | ~150 | adapt | CORE-001a/d | done | (next) | O(V + E) | C:1 / py:1 / R:1 |
| ALGO-PR-013 | `is_simple` predicate (no loops, no multi-edges) | properties/multiplicity.c | ~80 | adapt | CORE-001a | done | (next) | O(V + E) | C:1 / py:1 / R:1 |
| ALGO-PR-013b | `is_simple_with_mode` (directed-as-undirected) | properties/multiplicity.c | ~80 | adapt | PR-013 | done | (next) | O(V+E) / O(E log E) | C:1 / py:1 / R:1 |
| ALGO-PR-014 | `has_loop` + `has_multiple` predicates | properties/loops.c + multiplicity.c | ~100 | adapt | CORE-001a/d | done | (next) | O(E) / O(E log E) | C:2 / py:2 / R:2 |
| ALGO-PR-014b | per-edge `is_loop` + `is_multiple` | properties/loops.c + multiplicity.c | ~80 | adapt | PR-014 | done | (next) | O(E) / O(E log E) | C:2 / py:2 / R:2 |
| ALGO-PR-014c | `count_loops` (scalar) + `count_multiple` (per-edge multiplicity) | properties/loops.c + multiplicity.c | ~80 | adapt | PR-014b | done | (next) | O(E) / O(E log E) | C:4 / py:4 / R:4 |
| ALGO-CO-001b | Directed modularity Leicht-Newman (`modularity_directed`) | community/modularity.c | ~120 | adapt | CO-001 | done | (next) | O(V+E) | C:1 / py:1 / R:1 |
| ALGO-CO-001c | Weighted modularity (`modularity_weighted`, undirected) | community/modularity.c | ~150 | adapt | CO-001 | done | (next) | O(V+E) | C:1 / py:1 / R:1 |
| ALGO-CO-002 | Louvain multilevel community detection (`louvain`, `louvain_weighted`, `louvain_with_options`) | community/community_multilevel.c | ~700 | adapt | CO-001 + CORE-001a/d | done | (next) | O(passes·(V+E)) | C:2 / py:2 / R:2 |
| ALGO-CO-003 | Leiden community detection — fastmove + refinement + aggregation (`leiden`, `leiden_weighted`, `leiden_with_options`; Modularity/CPM/ER objectives) | community/leiden.c | ~1300 | adapt | CO-001 + CORE-001a/d | done | (next) | O(iters·(V+E)) | C:2 / py:2 / R:2 |
| ALGO-CO-004 | Label propagation community detection — three variants (Fast/Dominance/Retention) + weights + initial/fixed masks (`label_propagation`, `label_propagation_weighted`, `label_propagation_with_options`; `LpaVariant`, `LpaOptions`, `LpaResult`) | community/label_propagation.c | ~620 | adapt | CO-001 + CORE-001a/d | done | (next) | O(iters·(V+E)) | C:2 / py:2 / R:2 |
| ALGO-CO-005 | Fluid Communities (Parés et al. 2017) — density-weighted label propagation with user-pinned community count `k` (`fluid_communities`, `fluid_communities_with_options`; `FluidOptions`, `FluidResult`) | community/fluid.c | ~247 | adapt | CO-001 + CC-001 + PR-013 | done | (next) | O(iters·(V+E)) | C:2 / py:2 / R:2 |
| ALGO-CO-006 | Edge betweenness community detection (Girvan-Newman 2002) — Brandes per-removal unweighted edge-betweenness + reverse-dendrogram replay with best-Q tracking (`edge_betweenness_community`; `EdgeBetweennessResult` carrying membership / nb_clusters / removed_edges / edge_betweenness / merges / bridges / modularity) | community/edge_betweenness.c | ~370 | adapt | CO-001 + CORE-001a/b/d | done | (next) | O(\|V\|·\|E\|²) | C:2 / py:2 / R:2 |
| ALGO-CO-007 | Fast greedy modularity (Clauset-Newman-Moore 2004) — greedy `ΔQ`-merge dendrogram via lazy-deletion global max-heap over per-community `BTreeMap` neighbour maps; tracks best-Q cut and full dendrogram (`fast_greedy_modularity`, `fast_greedy_modularity_weighted`; `FastGreedyResult` carrying membership / nb_clusters / merges / modularity) | community/fast_modularity.c | ~1072 | adapt | CO-001 + PR-014 (multi-edge detect) + CORE-001a/b/d | done | (next) | O(\|V\|·\|E\|·log²V) | C:2 / py:2 / R:2 |
| ALGO-CO-006b | Weighted edge betweenness community detection — Brandes-Dijkstra per-removal weighted edge-betweenness + reverse-dendrogram replay with weighted-modularity best-Q tracking (`edge_betweenness_community_weighted`; reuses `EdgeBetweennessResult`); rejects non-finite/negative weights | community/edge_betweenness.c | ~430 | adapt | CO-006 + modularity_weighted (CO-001) | done | (next) | O(\|V\|·\|E\|·(\|E\|+\|V\| log \|V\|)) | C:2 / py:2 / R:2 |
| ALGO-CO-006c | Directed Girvan-Newman edge betweenness community — extends CO-006/CO-006b with directed Brandes (forward via OUT-incidence, back-pass via populated predecessor lists), no halving of directed `eb`, per-level Q dispatch via `modularity_directed` / `modularity_weighted_directed` (the latter newly introduced); also exports `modularity_weighted_directed` | community/edge_betweenness.c | ~120 (in-place extension) | adapt | CO-006 + CO-006b + modularity_directed | done | (next) | O(\|V\|·\|E\|²) directed / O(\|V\|·\|E\|·(\|E\|+\|V\| log \|V\|)) directed-weighted | C:3 / py:3 / R:3 |
| ALGO-CO-009 | Voronoi-based community detection (Deritei et al. 2014, Molnár et al. 2024) — greedy generator picking via Local Relative Density (LRD): every vertex computes the average distance to all other reachable vertices, the lowest-LRD unassigned vertex becomes a generator and "claims" every vertex within radius `r·avg_dist`, repeat until exhausted; ties broken by smallest vertex id (deterministic, no RNG). For `r = -1` (auto-r), wraps the inner pick+assign in a Brent quadratic-fit 1D optimizer over `r ∈ (lo·d̄, hi·d̄)` with up to 25 iterations, picking the `r` that maximises Newman-Girvan modularity (weighted when `weights` is `Some`). Reuses ALGO-SP-007 `voronoi()` as the final cell-assignment step. Supports unweighted / weighted edge-length costs, directed `mode = IN/OUT/ALL`. (`community_voronoi`; `CommunityVoronoiResult { membership, generators, modularity }`) | community/voronoi.c | ~750 | adapt | CO-001 + SP-007 + CORE-001a/b/d | done | (next) | O(k·(V+E·log V)) per pick (k = generator count), ×25 for auto-r | C:4 / py:4 / R:4 |
| ALGO-CO-008 | Walktrap community detection (Pons-Latapy 2005) — random walks of length t (default 4) define a between-vertex L2 distance r²_{ij} weighted by 1/sqrt(deg); greedy agglomerative merges that minimise Δσ produce a binary dendrogram, and the best Newman-Girvan Q step is returned as the final partition. Internal graph synthesizes a per-vertex self-loop (mean incident weight, 1.0 if isolated — same fix as upstream #2043). Hand-rolled binary min-heap with a lazy-refine pattern (entries marked `!exact` are refined on pop via the triangle formula `Δσ' = ((s₁+s_k)·d₁ + (s₂+s_k)·d₂ − s_k·Δσ_old) / (s₁+s₂+s_k)` or the chain lower bound `−1/min(\|adj(new)\|, \|adj(k)\|)`). Modularity trajectory matches the C `community_walktrap.out` reference (triangle / bug-2042 / 6-ring weighted / isolated) to ~1e-15. (`walktrap`, `walktrap_weighted`, `walktrap_with_options`; `WalktrapOptions`, `WalktrapResult` carrying membership / nb_clusters / merges / modularity; `WALKTRAP_DEFAULT_STEPS = 4`) | community/walktrap/*.cpp | ~1200 | adapt | CO-001 + CORE-001a/b/d | done | (next) | O(t·V·E + V²·log V) | C:3 / py:3 / R:3 |
| ALGO-CO-017 | Leading eigenvector community detection (Newman 2006) — recursively bisects graph by sign of leading eigenvector of modularity matrix `B = A − k·k^T/(2m)` restricted to each community with diagonal correction for zero row sums. Hand-rolled symmetric Lanczos eigensolver (thick-restart, full reorthogonalization, QL tridiagonal solver). Starts from weakly connected components, queues communities for splitting, validates each split via eigenvalue positivity + sign-vector modularity-increase check. (`leading_eigenvector`, `leading_eigenvector_weighted`; `LeadingEigenvectorResult { membership, eigenvalues, merges, modularity }`) | community/leading_eigenvector.c | ~465 | adapt | CO-001 + CC-001 + CORE-001a/d | done | (next) | O(steps·(V+E)·k) where k = Lanczos subspace dim (≤20) | unit×8 |
| ALGO-CO-018 | Infomap community detection (Rosvall & Bergstrom 2008) — map equation minimization via greedy Louvain-style moves. Computes PageRank flows with teleportation (τ=0.15) via power iteration, then iteratively moves vertices between modules to minimize the two-level map equation codelength L(M) = q↷·H(Q) + Σᵢ(qᵢ↷ + pᵢ)·H(Pᵢ). Multi-trial with SplitMix64 PRNG for deterministic seeded traversal, returns the best partition across all trials. Supports directed/undirected graphs, optional edge weights, configurable nb_trials/seed. (`infomap`, `infomap_weighted`, `infomap_with_options`; `InfomapResult { membership, codelength }`) | external Infomap C++ lib (wrapper: community/infomap.cpp ~275 lines) | ~750 (from scratch) | self-roll | CO-001 + CORE-001a/d | done | 2026-06-04 | O(trials·iters·(V+E)) | unit×10, integration×16, proptest×4 |
| ALGO-CO-019 | Spinglass community detection (Reichardt & Bornholdt 2006) — Potts model with simulated annealing. Vertices assigned spin states (1..q), Hamiltonian minimized via heat-bath updates at decreasing temperatures. Two null models: Simple (G(n,p) baseline) and Config (degree-preserving configuration model). Two update modes: Sequential (random node per step) and Parallel (all nodes per sweep). Supports edge weights, configurable spins/temperatures/gamma/cooling/seed. Requires connected graph. (`spinglass`, `spinglass_weighted`, `spinglass_with_options`; `SpinglassResult { membership, modularity, temperature, nb_clusters, csize }`; `SpinglassOptions`; `SpinglassUpdateRule`) | community/spinglass/pottsmodel_2.cpp (~3300 lines C++) | ~750 (from scratch) | self-roll | CO-001 + CORE-001a/d | done | 2026-06-04 | O(sweeps·spins·(V+E)) | unit×14, integration×27, proptest×5 |
| ALGO-CM-013 | `community_to_membership` (dendrogram → membership). Pure-function helper mirroring `igraph_community_to_membership` — cuts a binary dendrogram at `steps` merges and returns a densified `0..k` membership + per-cluster `csize`. Walks merges top-down (`steps-1 → 0`) with 1-based supercluster ids in a `tmp` slot per row, an `already_merged` bitmap to detect double-merges, propagates ids down through internal nodes; final pass densifies untouched leaves as singletons. O(steps + n). Errors on `steps > rows`, double-merge, out-of-range entry (`≥ nodes + i`), or usize overflow. (`community_to_membership` returning `CommunityToMembershipResult { membership, csize }`) | community/community_misc.c | ~120 | adapt | (none — pure helper) | done | (next) | O(steps + n) | C:2 / py:2 / R:2 |
| ALGO-CM-014 | `reindex_membership` (densify membership labels). Pure-function helper mirroring `igraph_reindex_membership` / `igraph_i_reindex_membership_large` — relabels a `membership[v] = c` vector with arbitrary `u32` ids into a contiguous `0..k` labelling in first-occurrence order, optionally reporting the original id behind each new id. Two-branch single pass: fast path (`max_id < n`) uses a flat `Vec<u32>` lookup with 0 as the "unseen" sentinel; sparse fallback uses `BTreeMap<u32, u32>` when ids overflow the window. O(n) avg / O(n log k) sparse, single allocation of size n. (`reindex_membership` returning `ReindexMembershipResult { membership, new_to_old }`) | community/community_misc.c | ~140 | adapt | (none — pure helper) | done | (next) | O(n) avg / O(n log k) sparse | C:2 / py:2 / R:2 |
| ALGO-CM-015 | `compare_communities` (5 partition-distance metrics). Pure-function helper mirroring `igraph_compare_communities` — given two membership vectors of equal length, computes one of `VariationOfInformation` (Meilă 2003), `NormalizedMutualInformation` (Danon 2005), `SplitJoin` (van Dongen 2000), `Rand` (Rand 1971), or `AdjustedRand` (Hubert-Arabie 1985). Densifies via `reindex_membership` then builds a sparse `HashMap<(u32, u32), u32>` confusion matrix; per-method post-processing uses natural-log entropy (VI/NMI), max-row + max-col (SJ), or pair counts (Rand/AR). O(n + S) where S = observed sparse cells. (`compare_communities(&[u32], &[u32], CommunityComparison) -> IgraphResult<f64>`) | community/community_misc.c | ~250 | adapt | CM-014 | done | (next) | O(n + S) | C:2 / py:2 / R:2 |
| ALGO-CM-016 | `split_join_distance` (asymmetric projection-distance pair). Pure-function helper mirroring `igraph_split_join_distance` — given two membership vectors of equal length, returns the asymmetric pair `SplitJoinDistance { d12, d21 }` where `d12 = n − Σᵢ max_j n_ij` (project comm1 onto comm2) and `d21 = n − Σⱼ max_i n_ij`; the symmetric scalar `d12 + d21` matches `compare_communities(_, _, SplitJoin)`. `d12 == 0` ⇔ comm1 is a sub-partition of comm2 (and vice versa), so the pair detects refinement relationships the scalar collapses. Reuses CM-014's `reindex_membership` for densification + CM-015's `split_join_distances(pub(crate))` helper for the confusion-matrix walk. O(n + S) where S = observed sparse cells. (`split_join_distance(&[u32], &[u32]) -> IgraphResult<SplitJoinDistance>`) | community/community_misc.c | ~250 | adapt | CM-014, CM-015 | done | (next) | O(n + S) | C:2 / py:2 / R:2 |
| ALGO-PR-015 | Coreness / k-core decomposition (`coreness`, undirected) | centrality/coreness.c | 157 | adapt | - | done | (next) | O(V+E) Batagelj-Zaversnik | C:1 / py:1 / R:1 |
| ALGO-PR-015b | `coreness_with_mode` (directed IN/OUT) | centrality/coreness.c | ~80 | adapt | PR-015 | done | (next) | O(V+E) | C:1 / py:1 / R:1 |
| ALGO-PR-020 | `is_dag` (Kahn topological peel) | properties/dag.c (lines 151-220) | 70 | adapt | CORE-001a/b | done | (next) | O(V+E) | C:2 / py:2 / R:2 |
| ALGO-PR-021 | `topological_sorting` (Kahn peel, OUT/IN mode, ignores self-loops) | properties/dag.c (lines 54-123) | 70 | adapt | CORE-001a/b | done | (next) | O(V+E) | C:2 / py:2 / R:2 |
| ALGO-PR-022 | `is_acyclic` (directed→is_dag; undirected→union-find cycle detect) | properties/trees.c (lines 753-762) | 10 + helper | adapt | PR-020 | done | (next) | O(V+E·α(E)) | C:2 / py:2 / R:2 |
| ALGO-PR-023 | `is_tree` (mode-aware: out-/in-arborescence or undirected; ecount=N-1 + DFS reach-all) | properties/trees.c (lines 251-392) | ~140 | adapt | PR-022 | done | (next) | O(V+E) | C:2 / py:2 / R:2 |
| ALGO-PR-024 | `is_forest` (mode-aware: forest-of-arborescences or undirected; ecount≤N-1 + per-component DFS reach-all + cycle detect) | properties/trees.c (lines 520-725) | ~270 | adapt | PR-023 | done | (next) | O(V+E) | C:2 / py:2 / R:2 |
| ALGO-PR-016 | `is_complete` (cardinality short-circuit + simple-fast-path + unique-neighbour scan) | properties/complete.c (lines 43-155) | ~110 | adapt | PR-013 | done | (next) | O(V+E) | C:2 / py:2 / R:2 |
| ALGO-PR-017 | `hub_and_authority_scores` (Kleinberg HITS, unweighted; directed → power-iter on AA^T; undirected → delegate to PR-012) | centrality/hub_authority.c (lines 260-505) | ~200 | adapt | PR-012 | done | (next) | O(iter·(V+E)) power | C:2 / py:2 / R:2 |
| ALGO-PR-017b | `hub_and_authority_scores_weighted` (Kleinberg HITS, weighted; directed → power-iter on W·W^T; undirected → shifted power-iter on weighted symmetric W) | centrality/hub_authority.c (lines 130-176, 333-505) | ~150 | adapt | PR-017 | done | (next) | O(iter·(V+E)) power | C:1 / py:1 / R:1 |
| ALGO-PR-018 | `is_perfect` (perfect-graph test via the Strong Perfect Graph Theorem: a graph is perfect iff neither it nor its complement contains an induced odd hole of length ≥ 5; faithful port of upstream's decision cascade — directed/non-simple input errors; trivial perfect cases (`<5` vertices, `≤4` edges or within 5 of complete); bipartite or chordal ⇒ perfect; Weak Perfect Graph Theorem lets the same bipartite/chordal probes run on the complement; a finite odd girth `>3` in the graph or its complement ⇒ not perfect; otherwise an induced odd-hole search via induced LAD subgraph isomorphism over `ring(cycle_len)` for odd `cycle_len` from `min(girth, comp_girth)+1` up to `n`; composed entirely from already-ported primitives — `is_simple`, `is_bipartite`, `is_chordal` (MCS), `complementer`, `girth`, `ring_graph`, `subisomorphic_lad` (induced); undirected simple scope mirrors VF2/LAD; py conformance skipped — python-igraph 0.11.9 has no `Graph.is_perfect` binding, so verdicts come from the upstream C unit test (authentic-by-reading-source) + rigraph `is_perfect`) | properties/perfect.c (igraph_is_perfect, lines 55-190) | ~190 | adapt | PR-001 (girth), PR-013 (is_simple), is_bipartite, CL-002 (is_chordal/MCS), OP-003 (complementer), CN-006 (ring), ISO-020 (subisomorphic_lad induced) | done | 2026-05-30 | worst case exponential (induced odd-hole LAD search); bipartite/chordal/girth fast paths resolve most inputs in O(V+E) (plus a dense-complement pass for non-perfect verdicts) | C:6 / py:0 (no binding) / R:3 + proptest (Weak-PGT complement invariance + bipartite⇒perfect) + unit + doctest = 9 total |
| ALGO-PR-019 | `power_law_fit` (Clauset–Shalizi–Newman MLE power-law fit / igraph plfit port: returns `{continuous, alpha, xmin, log_likelihood, ks_statistic}`; p-value skipped, matching igraph's `PLFIT_P_VALUE_SKIP`; continuous case uses the closed-form MLE `alpha = 1 + m / Σ ln(x_i/xmin)` with KS-minimised xmin via the same stratified-sampling scan as upstream; discrete case maximises `-alpha·Σln x_i − m·ln ζ(alpha, xmin)` over a golden-section search (vs upstream L-BFGS — equivalent to ~1e-8 on the flat ridge, with the maximised L itself matching to ~1e-13), Hurwitz zeta via Euler–Maclaurin (shift 10, order 32, reverse summation); finite-size correction `alpha·(n−1)/n + 1/n` for `n<50`; auto continuous/discrete detection from integrality, `force_continuous` override; data-vector algorithm — no graph input) | nongraph/power_law_fit.c + vendored plfit (plfit.c, lbfgs.c, zeta) | ~430 | adapt | none (self-contained numerics) | done | 2026-05-30 | continuous ~O(num_uniques·n) xmin scan; discrete ~O(num_uniques·golden_iters·zeta) | C:3 / py:6 / R:2 + proptest (closed-form alpha + discrete detection + force-continuous) + 12 unit + doctest = 23 total; source-aware conformance tolerance (c/r 5-decimal `.out`, py full-precision 1e-9 with discrete alpha/D at 1e-6) |
| ALGO-EM-003 | `laplacian_spectral_embedding` (spectral decomposition of a Laplacian matrix for graph embedding; three types: D−A (combinatorial), D⁻¹ᐟ²AD⁻¹ᐟ² (normalized adjacency / DAD), I−D⁻¹ᐟ²AD⁻¹ᐟ² (symmetric normalized / IDAD); computes top-k eigenpairs via Lanczos; supports edge weights, all three SpectralWhich modes, optional sqrt(|λ|) scaling; isolated vertices handled gracefully (1/√0 → 0); undirected scope; reuses Lanczos top-k from ALGO-CO-017) | misc/embedding.c (lines 906-1093) | ~280 | adapt | CO-017 (Lanczos), EM-002 (shared types) | done | 2026-06-01 | O(no · iter · (V+E)) Lanczos | 16 unit + 1 doctest |
| ALGO-EM-002 | `adjacency_spectral_embedding` (spectral decomposition of the adjacency matrix for graph embedding; computes top-k eigenpairs of A + diag(cvec) via Lanczos; supports LargestAlgebraic/SmallestAlgebraic/LargestMagnitude eigenvalue selection, optional edge weights, diagonal augmentation (scalar or per-vertex cvec), and scaled embedding (X = U D^{1/2}); undirected scope — treats directed graphs as undirected; zapsmall to zero out numerical noise; reuses the Lanczos top-k solver from ALGO-CO-017) | misc/embedding.c (lines 872-904, 598-804) | ~250 | adapt | CO-017 (Lanczos) | done | 2026-06-01 | O(no · iter · (V+E)) Lanczos | 10 unit + 1 doctest |
| ALGO-EM-001 | `dim_select` (Zhu & Ghodsi 2006 profile-likelihood dimensionality selection — given an ordered vector of importance measures (e.g. singular values), models them as a two-component equal-variance Gaussian mixture and returns the split point `d ∈ 1..=n` that maximises the profile log-likelihood (the "elbow of the scree plot"); single O(n) running-sum scan over the *given* order (does not sort); empty input errors, n==1 → 1, degenerate n==2 falls through to the single-group fallback → 2; data-vector algorithm — no graph input; first member of the Embedding (EM) cluster) | misc/embedding.c (lines 1130-1195) | ~120 | adapt | none (self-contained numerics) | done | 2026-05-30 | O(n) single running-sum pass | C:0 (no dedicated `.out`) / py:0 (python-igraph 0.11.9 does not expose) / R:2 (rigraph `dim_select`, anchor `dim_select(1:100)==50` from test-embedding.R) + 3 proptest + 7 unit + doctest = 13 total |
| ALGO-PR-027 | `neighborhood_size` (BFS-based k-hop ball count, OUT/IN/ALL mode, mindist filter) | properties/neighborhood.c (lines 70-164) | ~140 | adapt | CORE-001a/b | done | (next) | O(V·(V+E)) BFS-per-vertex | C:2 / py:2 / R:2 |
| ALGO-PR-027b | `neighborhood` (BFS-based k-hop vertex lists, OUT/IN/ALL mode, mindist filter) | properties/neighborhood.c (lines 208-303) | ~160 | adapt | PR-027 | done | (next) | O(V·(V+E)) BFS-per-vertex | C:2 / py:2 / R:2 |
| ALGO-PR-028 | `convergence_degree` (per-edge `(In−Out)/(In+Out)` measure of shortest-path source/sink balance) | properties/convergence_degree.c (lines 21-93) | ~80 | adapt | CORE-001a/b | done | (next) | O(V·(V+E)) BFS-per-vertex | C:2 / py:2 / R:2 |
| ALGO-PR-029 | `global_efficiency` (Latora–Marchiori average inverse pairwise distance) | paths/shortest_paths.c (lines 392-486) | ~95 | adapt | SP-001 | done | (next) | O(V·(V+E)) BFS-per-vertex | C:2 / py:2 / R:2 |
| ALGO-PR-030 | `local_efficiency` + `average_local_efficiency` (per-vertex inverse-distance among neighbours in `G\{v}`, plus its mean) | paths/shortest_paths.c (lines 688-867) | ~180 | adapt | PR-029 | done | (next) | O(V·\|N(v)\|·(V+E)) BFS-per-source | C:4 / py:0 (skipped — py-igraph 0.11 lacks API) / R:4 |
| ALGO-PR-031 | `ecc` (Radicchi 2004 edge clustering coefficient, k=3 triangle + k=4 square, offset & normalize flags, loop-aware degree, NaN for degenerate denominators) | properties/ecc.c (lines 33-385) | ~330 | adapt | core/Graph | done | (next) | O(E·d_max) intersection-per-edge | C:5 / py:4 (hand-derived — py-igraph 0.11 lacks API) / R:4 (hand-derived — R only exports internal `ecc_impl`) |
| ALGO-PR-032 | `constraint` (Burt's structural holes constraint — measures redundancy of a vertex's connections; higher constraint = fewer structural holes; supports optional edge weights and directed/undirected graphs; isolated vertices return NaN) | properties/constraint.c (lines 1-288) | ~288 | adapt | core/Graph | done | 2026-05-27 | O(V + E + n·d²) where d is max degree | C:0 / py:0 / R:0 (unit + proptest only) |
| ALGO-PR-033 | `centralization` + `centralization_degree_tmax` + `centralization_betweenness_tmax` + `centralization_closeness_tmax` + `centralization_eigenvector_tmax` + `CentralizationMode` + `LoopMode` (igraph `igraph_centralization*` family — graph-level centralization index `C = Σ_v (max_u c_u - c_v)` optionally normalized by the theoretical maximum for the most centralized structure (star graph); 5 functions: generic score aggregation, plus 4 `tmax` helpers for degree/betweenness/closeness/eigenvector centralization; `CentralizationMode` enum {In, Out, All} and `LoopMode` enum {NoLoops, LoopsOnce, LoopsTwice} control directed/loop variants of degree tmax) | centrality/centralization.c (lines 1-723) | ~205 | adapt | — (pure math, no graph dependency) | done | 2026-05-27 | O(n) score scan for centralization(); O(1) for all tmax functions | C:0 / py:0 / R:0 (unit + proptest only) |
| ALGO-PR-034 | `is_graphical` + `is_bigraphical` + `EdgeTypeFilter` (igraph `igraph_is_graphical` + `igraph_is_bigraphical` — degree-sequence realizability tests; `EdgeTypeFilter` enum {Simple, LoopsSimple, Multi, LoopsMulti} selects edge constraints; undirected simple uses Erdős–Gallai theorem via Cloteaux 2015 linear-time algorithm with Zverovich sufficient-condition shortcut; undirected loopy-simple uses Cairns–Mendan modification; directed simple uses Fulkerson–Chen–Anstee theorem with Berger 2014 relaxation; directed loopy-simple reduces to bigraphical simple; bigraphical simple uses Gale–Ryser theorem with Berger 2014 relaxation; multi variants use sum/parity checks; pure math functions with no graph dependency) | misc/graphicality.c (lines 1-928) | ~480 | adapt | — (pure math, no graph dependency) | done | 2026-05-27 | O(n) for all variants | C:0 / py:0 / R:0 (unit + proptest only) |
| ALGO-PR-035 | `strength` + `strength_with_mode` + `diversity` + `StrengthMode` (igraph `igraph_strength` + `igraph_diversity` — weighted vertex degree and structural diversity index; `strength` sums incident edge weights per vertex for undirected graphs; `strength_with_mode` extends to directed graphs with `StrengthMode` enum {Out, In, All} and a `loops` flag controlling self-loop weight contribution (IGRAPH_LOOPS_TWICE semantics: each self-loop contributes its weight twice under All mode for undirected); `diversity` computes the Eagle-Macy-Claxton 2010 normalized Shannon entropy of incident edge weights per vertex: `D(i) = H(i) / ln(k_i)` where `H(i) = ln(s) - (Σ w·ln(w)) / s`, single-pass formulation; diversity requires undirected simple graphs with non-negative finite weights and degree ≥ 2 for well-defined entropy; single-pass O(V+E) for strength, single-pass O(V+E) for diversity) | degrees.c (lines 537-674) + basic_properties.c (lines 193-278) | ~600 | adapt | core/Graph | done | 2026-05-27 | O(\|V\| + \|E\|) single-pass for both strength and diversity | C:0 / py:0 / R:0 (unit + proptest only; 24 unit + 4 proptest + 3 doctest) |
| ALGO-PR-036 | `trussness` (igraph `igraph_trussness` — per-edge k-truss decomposition via Wang-Cheng 2012 bucket-peeling; a k-truss is a maximal subgraph where every edge participates in at least k-2 triangles; trussness of an edge = highest k-truss it belongs to; edges in no triangle get trussness 2; rejects multigraphs, accepts self-loops (trussness 2); uses mark-based triangle enumeration to compute per-edge support, then iterative bucket-peeling from lowest support up, decrementing support of neighbor edges via sorted adjacency list intersection) | centrality/truss.cpp (lines 1-288) | ~210 | adapt | core/Graph | done | 2026-05-27 | O(\|E\|^1.5) time, O(\|E\|) space | C:2 / py:2 / R:2 (6 three-source conformance + 16 unit + 3 proptest + 3 doctest) |
| ALGO-PR-040 | `rich_club_sequence` (per-vertex rich-club coefficient sequence: invert vertex_order permutation, distribute edge weights (or unit counts) to `res[min(order_of[v1], order_of[v2])]`, reverse-cumsum, optionally normalize by `total_possible_edges(vcount - i, directed, loops)`; directed flag silently false-folded when graph is undirected; warns once when `normalized && !loops` and a self-loop is encountered; returns `f64::NAN` at the all-removed tail when `normalized`) | properties/rich_club.c (lines 32-166) | ~135 | adapt | core/Graph | done | 2026-05-26 | O(\|V\| + \|E\|) | C: rich_club.out 3a/6a/7a / py: hand-derived (no binding) / R: aaa-auto.md:3068-3081 |
| ALGO-PR-041 | `neighborhood_graphs` + `neighborhood_graphs_with_mode` (per-vertex induced subgraphs of k-hop neighbourhoods; composes `neighborhood_with_mode` + `induced_subgraph`; supports directed mode IN/OUT/ALL and mindist filter; returns `Vec<Graph>` of length `vcount`; when neighbourhood equals full graph, returns clone instead of re-building) | properties/neighborhood.c (lines 306-457) | ~100 | adapt | PR-027b + OP-007 | done | 2026-05-28 | O(V·(V+E)) BFS-per-vertex + induced subgraph | C:0 / py:0 / R:0 (unit + doctest only) |
| ALGO-PR-042 | `local_scan_0` + `local_scan_1_ecount` + `local_scan_0_them` + `local_scan_1_ecount_them` + `local_scan_subset_ecount` + `local_scan_k_ecount` + `local_scan_k_ecount_them` (full local scan statistics family — k=0 degree/strength, k=1 mark-and-crawl with directed mode support, k-general BFS-based neighbourhood edge count, two-graph variants using `us` neighbourhood and `them` edges, subset edge count for arbitrary vertex sets; extends existing ALGO-PR-051 `local_scan_1` and ALGO-PR-053 `local_scan_k`) | misc/scan.c (lines 1-766) | ~766 | adapt | core/Graph | done | 2026-05-29 | O(V·(V+E)) k-scan BFS / O(E·deg) k=1 mark-crawl | C:0 / py:0 / R:0 (unit + doctest only) |
| ALGO-PR-043 | `centralization_degree_wrapper` + `centralization_betweenness_wrapper` + `centralization_closeness_wrapper` + `centralization_eigenvector_wrapper` (convenience wrappers composing per-vertex score functions with tmax + centralization formula; returns `CentralizationResult {scores, centralization, theoretical_max}`; degree wrapper supports `DegreeMode` and loop control via `strength_with_mode`; closeness maps `None` → `NaN` for disconnected graphs; eigenvector uses `eigenvector_centrality_full` with mode derived from directedness) | centrality/centralization.c (lines 138-647) | ~200 | compose | PR-033 + PR-008 + PR-007 + PR-012 + PR-035 | done | 2026-05-29 | O(score-fn) + O(V) | C:0 / py:0 / R:0 (unit + doctest only) |
| ALGO-MST-001 | `minimum_spanning_tree` (Prim/Kruskal/Unweighted/Automatic — Vec<EdgeId> of tree edges, treats directed as undirected) | misc/spanning_trees.c (lines 30-484) | ~480 | adapt | core/Graph | done | (now) | O(\|E\| log \|V\|) Prim / O(\|E\| log \|E\|) Kruskal / O(V+E) BFS | C:3 (hand-derived — upstream uses non-portable RNG seed) / py:3 / R:3 (hand-derived, rigraph-style) |
| ALGO-GN-001 | `erdos_renyi_gnp` + `erdos_renyi_gnm` (4 (directed × loops) cases, Batagelj–Brandes geometric-skip sampling for G(n,p), Floyd distinct-sample for G(n,m); SplitMix64 PRNG promoted to `core::rng`) | games/erdos_renyi.c (lines 25-352) | ~330 | adapt | core/Graph | done | (now) | O(n + m_expected) gnp / O(n + m) gnm | C:3 (hand-derived — upstream RNG not portable) / py:4 (hand-derived bands) / R:3 (hand-derived bands) |
| ALGO-GN-014 | `preference_game` + `asymmetric_preference_game` (per-vertex categorical / joint type assignment with Batagelj–Brandes per-pair sampling via `vids_by_type` indirection; symmetric variant supports `fixed_sizes` deterministic equal-split + arbitrary `type_dist`; asymmetric is always directed and reuses SBM's `PairShape` for joint pair iteration) | games/preference.c (lines 27-474) | ~500 | adapt | GN-010 (PairShape) | done | (now) | O(n + Σ pair-cells × m_pair) | C:3 + 3 / py:3 + 3 / R:3 + 3 |
| ALGO-GN-015 | `establishment_game` (Caldarelli et al. 2002 sample-traits growing model: each new vertex `i ≥ k` is assigned a categorical type from `type_dist` (uniform when `None`), Floyd-distinct samples `k` previous vertices, and accepts each candidate edge `(i, j)` independently with `pref_matrix[type[i]][type[j]]`; deterministic SplitMix64; pref-matrix symmetry required when `directed = false`; output is always simple-by-construction — no self-loops, no multi-edges) | games/citations.c (lines 159-274) | ~590 | adapt | GN-014 (PairShape patterns) | done | (now) | O(n · k · log n) Floyd + cumdist | C:3 (hand-derived bands) / py:3 / R:3 |
| ALGO-GN-016 | `callaway_traits_game` (Callaway et al. 2001 growing-traits model: ALL `n` vertex types categorical-sampled up front (uniform when `type_dist=None`); then for each step `i ∈ [1, n)` draw two endpoints uniformly from `[0, i]` *inclusive* and accept the candidate edge with `pref_matrix[t_a][t_b]` — `edges_per_step` independent attempts per step; deterministic SplitMix64; pref-matrix symmetry required when `directed=false`; self-loops and multi-edges ARE allowed by construction — key contrast to `establishment_game`) | games/citations.c (lines 95-156) | ~520 | adapt | GN-015 (sampling patterns) | done | (now) | O(n · eps) two uniform draws + cumdist + Bernoulli per step | C:3 (hand-derived bands) / py:3 / R:3 |
| ALGO-GN-017 | `cited_type_game` (Redner 1998 / igraph `igraph_cited_type_game` — growing citation network where vertex types are **input** (not sampled); for each new vertex `i ∈ [1, n)` add `edges_per_step` outgoing citations to previously-added vertices with probability proportional to `pref[type[v]]` via an incrementally-grown cumulative-sum vector + `partition_point` inverse-transform; multi-edges allowed when `eps ≥ 2`; self-loops only via the `sum = 0` fallback; deterministic SplitMix64; batched `add_edges` keeps per-step cost out of the O(m²) trap) | games/citations.c (lines 246-335) | ~90 | adapt | GN-016 (citation-family patterns) | done | (now) | O((n−1)·eps·log n) rng.gen_unit + cumsum.partition_point per citation | C:3 (hand-derived bands) / py:3 / R:3 |
| ALGO-GN-018 | `lastcit_game` (igraph `igraph_lastcit_game` — recency-decay citation network: each step `i ≥ 1` emits `edges_per_node` outgoing citations to previously-added vertices weighted by `preference[bucket(age)]` where `bucket = min(⌊(i − lastcit[v]) / binwidth⌋, agebins − 1)` for already-cited vertices and `bucket = agebins` for never-cited; `binwidth = n / agebins + 1`; on every step an age-sweep demotes vertices that crossed a bin boundary; sampling uses an inline Fenwick BIT (`PsumTree`) with binary-lifting prefix-search for O(log n) `set`/`search`; cited vertices' weights refresh to `preference[0]`; NEVER self-loops by construction; multi-edges allowed when `eps ≥ 2`; deterministic SplitMix64; batched `add_edges`) | games/citations.c (lines 28-92) | ~290 | adapt | GN-017 (citation-family + cumdist patterns) | done | (now) | O(n · (eps + agebins) · log n) psumtree set/search per step | C:3 (hand-derived bands) / py:3 / R:3 |
| ALGO-GN-019 | `recent_degree_game` (igraph `igraph_recent_degree_game` — sliding-window preferential attachment: each step `i ≥ 1` emits `m` (or `outseq[i]`) outgoing edges to previously-added vertices weighted by `pow(recent_in_degree, power) + zero_appeal`; edges added at step `i − time_window` are expired from the BIT-tree at step `i` (history queue uses `None` sentinels per-step); `outpref` toggles whether the source's own emitted citations count toward its recent in-degree; sampling uses an inline Fenwick BIT (`PsumTree`) with binary-lifting prefix-search; NEVER self-loops by construction; multi-edges allowed when `m ≥ 2`; uniform fallback when total weight is zero — covers `time_window=0` and `zero_appeal=0` start-of-run cases; deterministic SplitMix64; batched `add_edges`) | games/recent_degree.c (lines 24-200) | ~480 | adapt | GN-018 (PsumTree + windowed-history patterns) | done | (now) | O(n · m · log n) psumtree set/search per step + amortized O(m) expiry | C:3 (hand-derived bands) / py:3 / R:3 |
| ALGO-GN-020 | `barabasi_game_psumtree` + `barabasi_game_psumtree_multiple` (igraph `igraph_i_barabasi_game_psumtree*` — Fenwick-BIT preferential attachment with `attraction(deg) = pow(deg, power) + A` weight; **simple** variant zeros each chosen target inside the step's `m` draws and refreshes weights at end-of-step — no within-step duplicates by construction; **multiple** variant snapshots the BIT sum once per step so duplicates may occur; both variants use `search_bounded(target, bound)` clamped to `[0, i)` to avoid FP-drift self-loop on the new vertex; NEVER self-loops by construction; `outpref` (forced `true` when `directed=false`) feeds the source's own out-degree back into the attraction; `outseq` per-step out-degree override; deterministic SplitMix64; batched `add_edges`) | games/barabasi.c (lines 166-414) | ~750 | adapt | GN-019 (PsumTree + bounded prefix-search) | done | (now) | O(n · m · log n) psumtree set/search per step (both variants) | C:3 (hand-derived bands) / py:3 / R:3 |
| ALGO-GN-021 | `barabasi_aging_game` (igraph `igraph_barabasi_aging_game` — preferential attachment with vertex aging: each step `i ≥ 1` emits `m` (or `outseq[i]`) outgoing edges weighted by `weight(v) = (deg_coef · pow(deg(v), pa_exp) + zero_deg_appeal) · (age_coef · pow(age(v), aging_exp) + zero_age_appeal)`; `age(v) = (i − v)/binwidth + 1` with `binwidth = nodes/aging_bins + 1`; age-sweep at every `k · binwidth ≤ i` boundary refreshes vertex at position `i − k · binwidth` with age `k + 2`; preserves C's `pow(0, 0) == 1` special case on the degree term; sampling uses the same Fenwick BIT (PsumTree + bounded prefix-search) as GN-020 — NEVER self-loops by construction; C source does NOT zero picks per draw, so within-step multi-edges are possible when m ≥ 2; uniform fallback when total weight is zero; `outpref` feeds new vertex's own out-degree back into the BIT; `outseq` per-step out-degree override; deterministic SplitMix64; batched `add_edges`) | games/barabasi.c (lines 606-841) | ~770 | adapt | GN-020 (PsumTree + bounded prefix-search), GN-018 (age-sweep idiom) | done | (now) | O((n + n/aging_bins) · log n + \|E\|) | C:3 (hand-derived bands) / py:3 / R:3 |
| ALGO-GN-022 | `dot_product_game` (igraph `igraph_dot_product_game` — random dot-product graph (Nickel 2006 / Young–Scheinerman 2007): every vertex `i` carries a latent position vector `v_i ∈ R^d`; for every pair `(i, j)` with `i < j` (undirected) or `i ≠ j` (directed) the edge probability is `prob = v_i · v_j` and the edge is sampled Bernoulli(prob); clamp semantics mirror the C reference exactly — `prob < 0` adds no edge but raises a one-shot `negative_warning` flag; `prob > 1` deterministically adds the edge and raises a one-shot `over_one_warning` flag; otherwise a single `RNG_UNIF01` draw decides; NEVER self-loops by construction; ALWAYS simple by construction — each ordered/unordered pair is inspected exactly once; deterministic SplitMix64; bounded edges (`n(n−1)/2` undirected, `n(n−1)` directed)) | games/dotproduct.c (lines 59-102) | ~110 | adapt | GN-012 (static-fitness — fitness/weight validation patterns) | done | (now) | O(n² · d) dot-product per pair | C:3 (hand-derived bands) / py:3 / R:3 |
| ALGO-GN-023 | `correlated_game` + `correlated_pair_game` (igraph `igraph_correlated_game` + `igraph_correlated_pair_game` — Pearson-correlated Erdős–Rényi pair generator: given a simple `old_graph` with density `p`, sample a new graph whose adjacency-vector correlation with the original is exactly `corr ∈ [0, 1]`; uses derived conditional probabilities `q = p + corr·(1 − p)`, `p_del = 1 − q` (existing edge dropped) and `p_add = (1 − q)·p/(1 − p)` (non-edge becomes edge); per-position Bernoullis sampled via the same Batagelj–Brandes geometric-skip used in GN-001 ER (deletion stream over the `m` existing edges in canonical code order, addition stream over the `n(n−1)/2` undirected (or `n(n−1)` directed) candidate non-edges, then 3-way merge); special cases `corr = 0` → `erdos_renyi_gnp`, `corr = 1` → relabeled copy of `old_graph`; optional `permutation` (`perm[i]` = source vertex in `old_graph` that becomes vertex `i` in new graph) inverted and applied to the final edge list; old-graph simple-ness required and verified; ALWAYS simple by construction; directed/undirected inherited from `old_graph`; deterministic SplitMix64. `correlated_pair_game` is the convenience wrapper that builds an ER `(n, p)` and a correlated counterpart from a single seed) | games/correlated.c (lines 90-338) | ~340 | adapt | GN-001 (ER G(n,p) + Batagelj–Brandes + code/decode), is_simple property | done | (now) | O(m + (#added) + (#deleted) + m·log m sort) | C:4 (2 corr + 2 pair, hand-derived bands) / py:4 / R:4 = 12 total |
| ALGO-GN-024 | `degree_sequence_game_configuration` (igraph `igraph_degree_sequence_game(..., IGRAPH_DEGSEQ_CONFIGURATION)` — classical configuration-model multigraph generator: realises a prescribed degree sequence by stub matching (Bender–Canfield 1978 / Bollobás 1980); expand each vertex `i` into `d_i` half-edge stubs in a flat `Vec<VertexId>`, then repeatedly draw two stubs uniformly at random and pair them; undirected mode uses one bag (requires `Σ out` even); directed mode uses two bags (out + in, requires `Σ out = Σ in`); `Vec::swap_remove` gives O(1) random-access pop; result is a multigraph — self-loops and multi-edges admitted by construction; deterministic SplitMix64; signature `(out_degrees: &[u32], in_degrees: Option<&[u32]>, seed: u64) -> IgraphResult<Graph>`; first of a 5-AWU split of `igraph_degree_sequence_game` — VL / FAST_HEUR_SIMPLE / CONFIGURATION_SIMPLE / EDGE_SWITCHING_SIMPLE follow as GN-025..028) | games/degree_sequence.c (lines 37-123) | ~440 | adapt | GN-001 (SplitMix64 + Graph::add_edges patterns) | done | (now) | Θ(n + Σd_i) bag build + Θ(\|E\|) swap_remove pairing | C:3 (hand-derived) / py:3 / R:3 = 9 total |
| ALGO-GN-025 | `degree_sequence_game_vl` (igraph `igraph_degree_sequence_game(..., IGRAPH_DEGSEQ_VL)` — Viger–Latapy 2005 uniform sampler for **simple connected** graphs with a prescribed degree sequence: (1) Erdős–Gallai + Hakimi connected-graphical pre-check (rejects Σd < 2(n−1) when all positive), (2) Hakimi-style greedy realisation seeded by largest-degree-first heuristic, (3) `make_connected` 2-swap merging across the components produced by step 2, (4) edge-switch MCMC chain of `5·2·|E|` proposals with a windowed snapshot/restore rollback whenever the chain disconnects the graph mid-flight (window = `max(16, 2|E|)`); undirected only; guarantees exact degree preservation, no self-loops, no multi-edges, weak connectivity; deterministic SplitMix64 (not portable to igraph C / NumPy / R, so conformance asserts structural invariants only); signature `(degrees: &[u32], seed: u64) -> IgraphResult<Graph>`; second of the 5-AWU split — FAST_HEUR_SIMPLE / CONFIGURATION_SIMPLE / EDGE_SWITCHING_SIMPLE follow as GN-026..028) | games/degree_sequence_vl.c | ~950 | adapt | GN-024 (degree-sequence validation + Σd parity), GN-001 (SplitMix64) | done | (now) | Θ(Σd log Σd) realisation + Θ(\|E\| · BFS) MCMC body with windowed snapshot/restore | C:2 / py:3 / R:3 = 8 total |
| ALGO-GN-026 | `degree_sequence_game_fast_heur_simple` (igraph `igraph_degree_sequence_game(..., IGRAPH_DEGSEQ_FAST_HEUR_SIMPLE)` — fast-heuristic sampler for **simple** graphs with a prescribed degree sequence (NO connectivity guarantee, traded away for 21–54× speedup vs. GN-025 VL): (1) Erdős–Gallai (undirected) or Fulkerson–Chen–Anstee (directed) pre-check rejects non-graphical inputs; (2) build a stub bag of size Σd, Fisher–Yates shuffle once; (3) walk the shuffled pairs left-to-right; reject self-loops and pairs that duplicate an existing edge (`O(log Δ)` sorted-Vec adjacency lookup), bump residual back and continue on rejection; (4) on stuck (no further legal pair) restart from scratch, capped at `MAX_OUTER_ATTEMPTS = 1024`; both undirected (one stub bag, `in_degrees = None`) and directed (separate out/in stub bags, `Some(in_seq)`) branches share the same outer loop; no MCMC, no per-window connectivity check; deterministic SplitMix64; signature `(out_degrees: &[u32], in_degrees: Option<&[u32]>, seed: u64) -> IgraphResult<Graph>`; third of the 5-AWU split — CONFIGURATION_SIMPLE / EDGE_SWITCHING_SIMPLE follow as GN-027..028) | games/degree_sequence.c (lines 160-198 + helpers) | ~520 | adapt | GN-024 (Σd / Σout=Σin validation + stub bag idiom), GN-001 (SplitMix64) | done | (now) | Θ(Σd) shuffle + Θ(Σd · log Δ) pair walk per attempt (typically 1 attempt for graphical inputs) | C:4 / py:3 / R:3 = 10 total |
| ALGO-GN-027 | `degree_sequence_game_configuration_simple` (igraph `igraph_degree_sequence_game(..., IGRAPH_DEGSEQ_CONFIGURATION_SIMPLE)` — uniform sampler for **simple** graphs with a prescribed degree sequence via configuration-model rejection: (1) graphicality pre-check (Erdős–Gallai undirected / Fulkerson–Chen–Anstee directed); (2) build a flat stub bag of size Σd; undirected branch: at edge index `i` draw two indices `k1 = RNG_INTEGER(2i, sc-1)` and `k2 = RNG_INTEGER(2i+1, sc-1)`, swap into positions `2i` and `2i+1` (two-swap-per-edge incremental Fisher–Yates), take the pair as a candidate edge, reject and restart the whole attempt on a self-loop or multi-edge (per-source `Vec<HashSet<u32>>` adjacency tracker); directed branch: one FY swap per edge on the out-stub bag, pair against in-stubs in construction order, multi-arc detection via a bumped-counter trick (`Vec<u64>` indexed by target vertex, mark bumped per attempt for O(1) reset-free check); restart budget capped at `MAX_OUTER_ATTEMPTS = 1024`; **uniform** over the space of simple realisations (key distinguisher vs. GN-026 fast_heur) but rejects aggressively at high density — expected attempts ≈ `exp(O((Σd/n)²))` so suitable only for bounded-density sequences; NOT guaranteed connected (use GN-025 VL for connectivity); deterministic SplitMix64; signature `(out_degrees: &[u32], in_degrees: Option<&[u32]>, seed: u64) -> IgraphResult<Graph>`; fourth of the 5-AWU split — EDGE_SWITCHING_SIMPLE follows as GN-028) | games/degree_sequence.c (lines 200-294 + helpers, sample_simple_undirected / sample_simple_directed) | ~580 | adapt | GN-026 (shared EG/FCA + checked_sum helpers via `pub(crate)`), GN-001 (SplitMix64) | done | (now) | Θ(Σd) per attempt × E[attempts] = exp(O((Σd/n)²)) — practically Θ(Σd) for density ≲ 4 | C:4 / py:3 / R:3 = 10 total |
| ALGO-GN-028 | `degree_sequence_game_edge_switching_simple` (igraph `igraph_degree_sequence_game(..., IGRAPH_DEGSEQ_EDGE_SWITCHING_SIMPLE)` — edge-switching MCMC sampler for **simple** graphs with a prescribed degree sequence: (1) graphicality pre-check (Erdős–Gallai undirected / Fulkerson–Chen–Anstee directed); (2) **seed phase** — Havel–Hakimi (undirected) / Kleitman–Wang (directed) INDEX-deterministic realisation via residual-sort-per-hub, producing the initial simple graph that already realises the input degree sequence; (3) **rewire phase** — `REWIRE_TRIALS_PER_EDGE · |E| = 10 · |E|` degree-preserving edge-switching MCMC trials, each O(1) amortised: draw two distinct edge indices `e1, e2`, undirected branch flips endpoints with prob ½, reject no-op (`a==c \|\| b==d`), self-loop (`a==d \|\| b==c`), multi-edge (`adj[a].contains(d) \|\| adj[c].contains(b)`); on success swap `(a,b),(c,d) → (a,d),(c,b)` and update `Vec<HashSet<u32>>` adjacency tracker; **detailed-balance preserved** — both successful AND failed swap trials count toward the budget; directed branch uses out-adjacency-only set, no c↔d flip; output is simple, exactly realises the input sequence, NOT guaranteed connected; deterministic SplitMix64; linear-in-\|E\| wall-clock at ANY density (vs. GN-027 exponential cliff); signature `(out_degrees: &[u32], in_degrees: Option<&[u32]>, seed: u64) -> IgraphResult<Graph>`; **fifth and final** of the 5-AWU split of `igraph_degree_sequence_game` (GN-024..028) — the full upstream C entry point is now ported) | games/degree_sequence.c (lines 296-413 + helpers, sample_edge_switching_simple_undirected/directed) | ~720 | adapt | GN-026 (shared EG/FCA + checked_sum via `pub(crate)`), GN-025 (Havel–Hakimi seed idiom), GN-001 (SplitMix64) | done | (now) | O(n² + \|E\|) — Θ(n²) Havel–Hakimi/Kleitman–Wang seed + Θ(\|E\|) MCMC rewire | C:4 / py:3 / R:3 = 10 total |
| ALGO-GN-029 | `citing_cited_type_game` (igraph `igraph_citing_cited_type_game` — citing×cited-type growing citation network: generalises `cited_type_game` (GN-017) by letting the **citing** vertex's type also matter; vertex `i` cites `edges_per_step` earlier vertices, drawing each target from a per-citing-type Fenwick BIT weighted by `pref[type[i]][type[j]]`; signature `pub fn citing_cited_type_game(nodes: u32, types: &[u32], pref: &[&[f64]], edges_per_step: u32, directed: bool, seed: u64) -> IgraphResult<Graph>`; algorithm: validate inputs (T = max(types)+1; `pref` must be T×T finite non-negative; `types.len() == nodes`), maintain T parallel `PsumTree`s of size `nodes` and a parallel `sums: Vec<f64>` accumulator, pre-seed vertex 0 in all trees so step 1 has a non-empty draw pool, then per step `i ∈ [1, n)`: (1) let `ti = type[i]`, perform `edges_per_step` independent draws — if `sums[ti] > 0` call `sumtrees[ti].search_bounded(rng.gen_unit() * sums[ti], i)` (upper bound `i` blocks any FP drift into not-yet-inserted vertices), else fall back to `(rng.next_u64() % i) as usize` for the uniform-over-`[0, i)` regime, push `(i, target)`; (2) insert vertex `i` into all T trees with weight `pref[j][ti]`; **never self-loops, structurally** — even the uniform fallback samples `[0, i)` BEFORE vertex `i` is appended; **multi-edges ARE allowed** when `eps ≥ 2`; per-module private `PsumTree` following established crate pattern (see `barabasi_psumtree.rs:96-97`); deterministic SplitMix64; cost `O((n-1) · (eps · log n + T · log n))`; per-bench measurements at n=5000/T=4/eps=3 directed: 1.74 ms ≈ 2.88 Melem/s, undirected n=1000/T=2/eps=4: 344 µs ≈ 2.90 Melem/s; conformance is structural (vcount exact, directed exact, ecount in band, no_self_loops) since RNG is not portable across C/py/R bindings) | games/citations.c (igraph_citing_cited_type_game, lines 340-499) | ~600 | adapt | GN-017 (cited_type pattern + validate idiom), GN-001 (SplitMix64), `PsumTree` (per-module duplicate per `barabasi_psumtree.rs` precedent) | done | (now) | O((n-1) · (eps · log n + T · log n)) — eps · log n per draw + T · log n per append | C:3 / py:3 / R:3 = 9 total |
| ALGO-GN-030 | `bipartite_game_gnp` + `bipartite_game_gnm` (igraph `igraph_bipartite_game_gnp` + `igraph_bipartite_game_gnm` — bipartite Erdős–Rényi G(n₁, n₂, p) and G(n₁, n₂, m) generators producing a `BipartiteGraph { graph, types }` where vertices `0..n₁` carry `type=false` (bottom) and `n₁..n₁+n₂` carry `type=true` (top); 3-mode `BipartiteMode` enum: `Out` (bottom→top arcs only, directed), `In` (top→bottom arcs only, directed), `All` (mutual arcs allowed for directed case, equivalent to single edge in undirected case); pair space = `n₁·n₂` for `Out`/`In`/undirected and `2·n₁·n₂` for directed+`All` (each ordered cross-pair sampled independently); decoder `to = idx/n₁, from = idx − to·n₁, to += n₁` for bottom→top, swap `(from, to)` for In, second half of pair space encodes top→bottom for directed+All; gnp uses Batagelj–Brandes geometric-skip walk (GN-001 idiom) for sparse `p`; gnm uses Floyd's distinct-sample (module-private duplicate of GN-001 helper per PsumTree precedent); validates `p ∈ [0, 1]` finite, rejects `m > max_edges`, rejects `m > 0` when `n₁ == 0` or `n₂ == 0`; ALWAYS simple by construction — each pair sampled once; NEVER self-loops by construction — pair space excludes within-partition edges; deterministic SplitMix64; MVP scope: simple bipartite graphs only — multigraph (gnm with `multiple=true`) and edge-labelled `IEA` deferred to future AWUs to mirror erdos_renyi MVP boundary) | misc/bipartite.c (lines 1237-1784: bipartite_gnm_simple, igraph_bipartite_game_gnm, gnp_bipartite_large, igraph_bipartite_game_gnp) | ~734 | adapt | GN-001 (ER + Batagelj–Brandes geometric-skip + Floyd distinct-sample, duplicated per-module per crate precedent), core/Graph | done | (now) | O((n₁ + n₂) + p·n₁·n₂) gnp / O((n₁ + n₂) + m) gnm | C:3 / py:3 / R:3 = 9 total |
| ALGO-GN-031 | `iea_game` (igraph `igraph_iea_game` Independent Edge Allocation random multigraph generator — given vertex count `n`, edge count `m`, a directedness flag and a `loops` flag, materialises a random multigraph on `n` vertices with **exactly** `m` edges where each edge is independently assigned to an *ordered* vertex pair drawn uniformly at random; signature `pub fn iea_game(n: u32, m: u64, directed: bool, loops: bool, seed: u64) -> IgraphResult<Graph>`; semantics: every edge is a fresh independent SplitMix64 draw, so the model (a) always emits exactly `m` edges (no rejection — the inner loop has no retry path), (b) admits multi-edges by construction (the same vertex pair may be drawn repeatedly), (c) optionally admits self-loops; loops=true branch draws two indices `u = rng.gen_index(n)` and `v = rng.gen_index(n)` and pushes `(u, v)`; loops=false branch draws `u = rng.gen_index(n)`, then `v = rng.gen_index(n - 1)`, then applies the **shift-by-one trick** `if v >= u { v += 1 }` to lift the sample uniformly onto `[0, n) \ {u}` — equivalent to rejection sampling with one less PRNG draw and no branch mispredict; not uniform over the space of multigraphs — equivalent to uniformly sampling **edge-labelled** graphs where the `m` edges are distinguishable, and the probability of any multigraph is proportional to `1 / Π_{i,j} A_ij!` (factorial correction for self-loops on undirected); for the special case of simple realisations (no repeated pairs) the IEA model gives every simple graph equal weight — i.e. conditional on simplicity, equivalent to `erdos_renyi_gnm` without replacement; validation: `m > IEA_ECOUNT_MAX = u32::MAX` rejected, `m > 0 ∧ n == 0` rejected (impossible to place edges on no vertices), `m > 0 ∧ n == 1 ∧ loops == false` rejected (no legal pair space for a single vertex without self-loops), `m == 0` short-circuits to the empty `Graph::new(n, directed)`; deterministic SplitMix64; **upstream's `IGRAPH_EXPERIMENTAL` tag refers to API stability, not algorithm correctness** — the IEA model itself is textbook (Janson, "Random graphs and trees"; sometimes called "edge-labelled Erdős–Rényi" or "random multigraph with given size"); structural properties verified by 5 unit + 4 proptest: vcount equals input `n` exactly, ecount equals input `m` exactly (no rejection), directedness flag round-trips, no_self_loops invariant holds when `loops == false` (proptest sweeps every emitted edge and asserts `u != v`), `loops == true` produces ~m/n self-loops on average (sanity check, not asserted strictly), determinism across same-seed re-runs (proptest); cost: O(|V| + |E|) — one PRNG draw per edge for the loops=true branch (two for directed-with-loops to draw both endpoints), one draw + one comparison + one conditional increment for loops=false; conformance: igraph C's `igraph_iea_game` is gated behind `IGRAPH_EXPERIMENTAL` so RNG state is not portable across language bindings (the C entry advances the RNG one increment per edge per endpoint, while our SplitMix64 advances differently), so all three sources use structural-only invariant compare — vcount EXACT, ecount EXACT (not band — IEA always emits exactly m by construction), directedness flag, and `no_self_loops` invariant via `assert_ne!(a, b)` per edge when `loops == false`; fixture catalogue covers C:3 (directed_loops_n40_m100, directed_no_loops_n20_m80, undirected_no_loops_n15_m30), py:3 (directed_loops_n50_m200, undirected_loops_n30_m120, directed_no_loops_n25_m50), R:3 (directed_loops_n35_m140, undirected_no_loops_n18_m45, directed_loops_n10_m0_empty — the empty-edge degenerate); deterministic SplitMix64) | games/erdos_renyi.c (lines 480-542: igraph_iea_game wrapper) | ~85 | adapt | GN-001 (ER + SplitMix64 `gen_index` helper) | done | (now) | O(\|V\| + \|E\|) — one PRNG draw per edge (two for directed+loops), no rejection; bench: ~18–24 Melem/s across all three variants on M-series silicon (m=100k → ~5.4 ms, m=10k → ~460 µs, m=1k → ~41 µs); cost dominated by SplitMix64 draws + Vec::push of `(u32, u32)` staging buffer; no py oracle baseline (RNG not portable, IGRAPH_EXPERIMENTAL gate) | C:3 / py:3 / R:3 = 9 total |
| ALGO-GN-032 | `recent_degree_aging_game` (igraph `igraph_recent_degree_aging_game` — hybrid of GN-019 recent-degree sliding window and GN-021 vertex aging: each step `i ≥ 1` emits `m` (or `outseq[i]`) outgoing edges; vertex weight `(deg(v)^pa_exp + zero_appeal) · age(v)^aging_exp` where `deg(v)` counts edges received from `v` in the last `time_window` steps and `age(v) = (i − v)/binwidth + 1`; sampling uses the same Fenwick BIT (PsumTree + bounded prefix-search) as GN-019/GN-021; **window expiry**: when `i ≥ time_window`, pop history entries from the front until the per-step sentinel — decrement each popped vertex's recent-degree counter and refresh its weight with the **age at that moment** (matching C line 319's `(pow(deg, pa_exp) + zero_appeal) * pow(age + 1, aging_exp)`); **age-sweep** at every bin boundary `k · binwidth ≤ i` refreshes vertex at position `i − k · binwidth` with age `k + 2`; preserves C's `pow(0, 0) == 1` special case on the degree term; NEVER self-loops by construction (BIT range is `[0, i)`); within-step multi-edges allowed when `m ≥ 2` (no within-step zeroing — matches upstream); uniform fallback over `[0, i)` when BIT total is zero (e.g. very negative `aging_exp` + zero appeal); `outpref` feeds new vertex's own out-degree back into the BIT but **does NOT enter the history deque** — so the `outpref` contribution to `deg(i)` is permanent for the lifetime of vertex `i` (matches C lines 349-354 which update degree+psumtree but not history); `outseq` per-step out-degree override (first entry ignored, matching C lines 282-283 which subtract `outseq[0]` from the edge total); deterministic SplitMix64; batched `add_edges`) | games/recent_degree.c (lines 228-381: igraph_recent_degree_aging_game) | ~280 | adapt | GN-019 (recent_degree FIFO + sliding window), GN-021 (age binwidth + age-sweep), PsumTree | done | (now) | O((\|V\| + \|V\|/aging_bins + \|E\|) · log \|V\|) per igraph C doc — BIT ops dominate, sliding-window expiry is amortised O(\|E\|) | C:3 / py:3 / R:3 = 9 total |
| ALGO-CN-001 | `ring_graph` + `path_graph` + `cycle_graph` (igraph `igraph_ring` deterministic constructor family — single O(\|V\|) forward sweep `(i, i+1)` emits the `n−1` "spine" edges; optional `circular` flag appends the wrap-around `(n−1, 0)` to close the cycle; optional `mutual` flag emits the back-arc `(i+1, i)` per link in directed mode, including the wrap-around; 4-flag truth table `(n, directed, mutual, circular)` covers path (no wrap), cycle (wrap), directed cycle (forward only), directed-mutual cycle (both directions on every link); `path_graph(n, directed, mutual)` is a thin wrapper for `circular=false`; `cycle_graph(n, directed, mutual)` wraps `circular=true`; degenerate inputs: `n=0` → empty graph, `n=1` + `circular` → singleton self-loop `(0,0)`, `n=2` + `circular` + undirected → parallel edges between vertex 0 and 1; deterministic, no RNG; canonical (`min, max`) undirected edge canonicalisation handled by `Graph::add_edges`; rejects `directed=false && mutual=true` per igraph C contract) | constructors/regular.c (igraph_ring + igraph_lattice path branch) | ~140 | adapt | core/Graph | done | (now) | O(\|V\|) edge enumeration + Graph::add_edges | C:6 / py:4 / R:4 = 14 total |
| ALGO-CN-002 | `star_graph` (igraph `igraph_star` deterministic constructor — O(\|V\|) single sweep over leaves visited in raw vertex-id order `[0, center) ∪ (center, n)`; 4-mode `StarMode` enum: `Out` (centre → leaf), `In` (leaf → centre), `Mutual` (both arcs per leaf, forward first), `Undirected` (single edge per leaf, stored as canon `(min, max)`); edge counts `n − 1` for Out/In/Undirected, `2(n − 1)` for Mutual; degenerate inputs: `n=0` → empty graph, `n=1` → no edges; rejects `center >= n` with `InvalidArgument`; deterministic, no RNG) | constructors/regular.c (igraph_star, lines 75-141) | ~310 | adapt | core/Graph | done | (now) | O(\|V\|) edge enumeration + Graph::add_edges | C:6 / py:4 / R:4 = 14 total |
| ALGO-CN-003 | `wheel_graph` (igraph `igraph_wheel` deterministic constructor — composes `star_graph` spoke layer with a rim cycle through the leaves in raw vertex-id order skipping `center`; 4-mode `WheelMode` enum maps 1-to-1 onto `StarMode`: `Out` (centre → leaf + rim `prev → next`), `In` (leaf → centre + rim `prev → next`), `Mutual` (both arcs on spokes AND rim, rim reverse arcs appended in **reverse-discovery order** matching upstream C `i → end-i` indexing), `Undirected` (single edge per spoke + per rim edge, canonicalised `(min, max)`); edge counts `2(n − 1)` for Out/In/Undirected, `4(n − 1)` for Mutual; degenerate inputs: `n=0` / `n=1` reduce to star, `n=2` produces a self-loop on the rim vertex (rim collapses to 1-cycle), `n=3` produces parallel rim edges `(1,2)` and `(2,1)` (rim collapses to 2-cycle); rejects `center >= n` via spoke layer; deterministic, no RNG) | constructors/regular.c (igraph_wheel, lines 145-287) | ~430 | adapt | CN-002 (star_graph spoke layer) | done | (now) | O(\|V\|) edge enumeration + Graph::add_edges | C:6 / py:4 / R:4 = 14 total |
| ALGO-CN-004 | `kary_tree` (igraph `igraph_kary_tree` deterministic constructor — emits a rooted tree on `n` vertices in BFS order: vertex `0` is the root, at each parent index `p` up to `children` consecutive `(parent, to)` pairs are pushed with `to` advancing by one each push and the last parent possibly emitting a short batch; 3-mode `TreeMode` enum: `Out` (parent → child), `In` (child → parent), `Undirected` (parent–child stored as canon `(min, max)`); edge count `n − 1` in all modes; degenerate inputs: `n=0` → empty graph, `n=1` → singleton, `children=1` → linear chain (path), `children >= n − 1` → collapses to star anchored at vertex 0; rejects `children == 0` with `InvalidArgument`; deterministic, no RNG) | constructors/regular.c (igraph_kary_tree, lines 608-706) | ~310 | adapt | core/Graph | done | (now) | O(\|V\|) edge enumeration + Graph::add_edges | C:6 / py:4 / R:4 = 14 total |
| ALGO-CN-005 | `symmetric_tree` (igraph `igraph_symmetric_tree` deterministic constructor — takes a slice of branching factors and applies one per BFS depth: `branches[d]` children at depth `d`; reuses the shared 3-mode `TreeMode` enum: `Out` (parent → child), `In` (child → parent), `Undirected` (canon `(min, max)`); BFS level walk snapshots `level_end = child` before the inner loop so only current-level parents are expanded; vertex count `1 + Σ Π branches` computed with `checked_mul`/`checked_add` to surface overflow as `InvalidArgument`; edge count `n − 1` for non-empty branches, 0 for empty; degenerate inputs: `branches=[]` → singleton root, `branches=[1,1,…]` → linear chain (path), `branches=[k]` → star K1,k anchored at vertex 0; rejects any `branches[d] == 0` with `InvalidArgument`; deterministic, no RNG) | constructors/regular.c (igraph_symmetric_tree, lines 706-808) | ~350 | adapt | CN-004 (TreeMode enum) | done | (now) | O(\|V\|) edge enumeration + Graph::add_edges | C:6 / py:4 / R:4 = 14 total |
| ALGO-CN-006 | `regular_tree` (igraph `igraph_regular_tree` deterministic constructor — Bethe lattice of height `h` and degree `k`: every non-leaf vertex has total degree exactly `k` (root has `k` children, internal non-root has `k − 1` children + 1 parent edge, leaves sit at depth `h` with degree 1); built as a thin wrapper that constructs `branches = [k, k-1, k-1, ..., k-1]` of length `h` and delegates to `symmetric_tree`; reuses the shared 3-mode `TreeMode` enum: `Out` (parent → child), `In` (child → parent), `Undirected` (canon `(min, max)`); vertex count `1 + k + k(k-1) + … + k(k-1)^{h-1}` computed inside `symmetric_tree` via `checked_mul`/`checked_add` so overflow surfaces as `InvalidArgument`; degenerate inputs: `h=1` → star K1,k anchored at vertex 0, `k=2` → linear chain because `branches = [2, 1, 1, …]` collapses to a path after the root; rejects `h < 1` or `k < 2` with `InvalidArgument`; deterministic, no RNG) | constructors/regular.c (igraph_regular_tree, lines 843-865) | ~280 | adapt | CN-005 (symmetric_tree) | done | (now) | O(\|V\|) edge enumeration + Graph::add_edges (delegated) | C:6 / py:4 / R:4 = 14 total |
| ALGO-CN-007 | `hypercube` (igraph `igraph_hypercube` deterministic constructor — n-dimensional hypercube graph `Q_n` with `2^n` vertices and `2^(n-1)·n` edges; two vertices are connected iff their zero-based IDs differ in exactly one bit; enumerate all vertices `v ∈ [0, 2^n)`, for each bit `i ∈ [0, n)` toggle to get `u = v ^ (1 << i)` and emit the canonical edge `(v, u)` only when `v < u` so each edge is produced exactly once; `MAX_HYPERCUBE_DIMENSION = 30` cap keeps `1u32 << n` always well-defined and the edge `Vec` allocation within 64-bit `usize`; bound-checked via `checked_shl`/`checked_mul` so any future bound change cannot silently wrap; supports `directed` flag — both variants do identical enumeration work, the flag only changes how the underlying `Graph` stores arc orientation; degenerate inputs: `n = 0` → singleton, `n = 1` → K_2, `n = 2` → 4-cycle, `n = 3` → 8-vertex cube; structural properties: `n`-regular, bipartite split by parity of `popcount(id)`, every edge has Hamming distance 1, every emitted pair canonically ordered `u < v`; deterministic, no RNG) | constructors/regular.c (igraph_hypercube, lines 983-1003) | ~330 | adapt | CN-001..006 (constructors family pattern) | done | (now) | O(2^n · n) = O(\|E\|) edge enumeration + Graph::add_edges | C:6 / py:4 / R:4 = 14 total |
| ALGO-CN-008 | `hamming` (igraph `igraph_hamming` deterministic constructor — d-dimensional Hamming graph `H(n, q)` over alphabet `{0..q-1}` of size `q`; `q^n` vertices indexed by length-`n` strings via little-endian base-`q` encoding `Σᵢ xᵢ·qⁱ`; edge iff strings differ in exactly one position; edge count `q^n·(q-1)·n/2`; algorithm: for each `v ∈ [0, q^n)` walk digit positions `pos = 1, q, q², …`, extract `dig = (v / pos) mod q`, for every higher digit value emit canonical `(v, v + j·pos)` so each edge is produced exactly once with `u < v` ordering; runtime overflow protection via `u32::checked_pow` (vcount) and chained `checked_mul` (ecount product); degenerate inputs: `n = 0` → singleton (even when `q = 0`), `n > 0` ∧ `q = 0` → null graph, `q = 1` → singleton, `q = 2` ≡ hypercube `Q_n` (verified edge-by-edge against CN-007 in tests); structural properties: `n·(q-1)`-regular, every edge differs in exactly one base-`q` digit, every emitted pair canonically ordered `u < v`; deterministic, no RNG) | constructors/regular.c (igraph_hamming, lines 1070-1153) | ~330 | adapt | CN-007 (hypercube — `q=2` equivalence) | done | (now) | O(n · q^(n+1)) edge enumeration + Graph::add_edges | C:6 / py:4 / R:4 = 14 total |
| ALGO-CN-009 | `square_lattice` (igraph `igraph_square_lattice` deterministic constructor — d-dimensional Cartesian-product grid over dimension shape `[n_0, n_1, …, n_{d-1}]`; `Π n_i` vertices indexed in little-endian coords `id = i_0 + n_0·i_1 + n_0·n_1·i_2 + …`; per-axis edge emission: for each vertex and each axis emit forward neighbour `(v, v + stride_d)` whenever `coord_d + 1 < n_d`, plus optional wrap edge `(v, v − (n_d − 1)·stride_d)` when `periodic[d]` is set; wrap edges are **suppressed for dimension size 2** so the cycle does not duplicate the forward edge into a parallel pair; `directed && mutual` flag combo additionally emits the symmetric back arc for each forward and wrap edge so undirected edges become bidirectional arc pairs (`directed && !mutual` is the same forward set without back arcs); `nei = 1` is the only currently supported neighbourhood radius — `nei >= 2` (which igraph C handles via `igraph_connect_neighborhood` post-hoc) returns `InvalidArgument` and defers to a future AWU; runtime overflow protection: `dim.len()` ≤ `u32::MAX`, vcount product chained through `checked_mul` so overflow surfaces as `InvalidArgument`, stride walk uses `checked_mul`; degenerate inputs: `dim=[]` → singleton (0-d), `dim=[1, …]` → singleton chain, `dim=[k]` non-periodic → path `P_k`, `dim=[k]` periodic → cycle `C_k` (or `P_2` when `k=2` because wrap is suppressed), `dim=[2,2,2]` → cube `Q_3` (matches CN-007 edge-by-edge); structural properties: undirected non-periodic has `Σ_d (Π_{i≠d} n_i)·(n_d − 1)` edges, undirected periodic doubles each cycle dim (subject to `n_d > 2` suppression rule), 2-D 3×3 torus is 4-regular, `directed && mutual` doubles every undirected edge count; emission order is little-endian dim-by-dim and is NOT portable across upstream sources, so conformance tests use canonicalised multiset compare; deterministic, no RNG) | constructors/lattice.c (igraph_square_lattice, lines 60-298) | ~440 | adapt | CN-007 (hypercube — 3-D cube equivalence) | done | (now) | O(\|V\| · d) = O(\|E\|) edge enumeration + Graph::add_edges | C:8 / py:4 / R:4 = 16 total |
| ALGO-CN-010 | `generalized_petersen` (igraph `igraph_generalized_petersen` deterministic constructor — generalized Petersen graph `G(n, k)` over `2n` vertices organised in two layers: outer cycle `v_0..v_{n-1}` on ids `0..n` and inner circulant `u_0..u_{n-1}` of shift `k` on ids `n..2n`, joined by `n` rung edges `v_i — u_i`; emission per `i ∈ [0, n)`: push outer edge `(i, (i+1) mod n)`, rung `(i, i+n)`, and inner circulant `(i+n, ((i+k) mod n) + n)`, in that order, for a total of exactly `3n` edges with `Graph::add_edges` canonicalising endpoints to `lo, hi`; always undirected (the C constructor is fixed `IGRAPH_UNDIRECTED`); constraints `n ≥ 3` and `0 < k < n/2` (strict — `2k < n`; `2k = n` would emit parallel inner edges); overflow protection: `2*n` via `checked_mul` for vcount, `3*n` via `checked_mul` for ecount; famous specializations: `G(5,2)` = classic Petersen (10v, 15e, 3-regular, girth 5), `G(8,3)` = Möbius–Kantor (16v, 24e, bipartite, girth 6), `G(10,3)` = Desargues (20v, 30e, bipartite), `G(12,5)` = Nauru (24v, 36e, symmetric); structural invariants verified by proptest sweep: every `G(n,k)` is 3-regular, simple (no loops, no parallels), the outer-layer subgraph is exactly `C_n`, the rungs cover every `i`, the inner layer is exactly the circulant with shift `k`; emission order is internal-loop-driven and differs from python-igraph `Graph.Famous('Petersen')` / R-igraph `make_graph('Petersen')` — conformance test uses canonicalised multiset compare, and only Petersen (G(5,2)) shares the canonical vertex labelling across all three sources, so py/R fixtures cover Petersen only while C fixtures sweep 6 small parameter pairs; deterministic, no RNG) | constructors/generalized_petersen.c (igraph_generalized_petersen, lines 58-95) | ~120 | adapt | CN-006 (ring — outer cycle pattern), CN-009 (square_lattice — canonicalised multiset compare for emission-order-sensitive constructors) | done | (now) | O(\|V\|) = O(n) edge enumeration + Graph::add_edges | C:6 / py:1 / R:1 = 8 total |
| ALGO-CN-011 | `circulant` (igraph `igraph_circulant` deterministic constructor — circulant graph `G(n, shifts)` on `n` vertices where for each `s ∈ shifts` (after normalisation) edge `(v_j, v_{(j+s) mod n})` is emitted for every `j ∈ [0, n)`; signature `pub fn circulant(n: u32, shifts: &[i64], directed: bool) -> IgraphResult<Graph>`; shift normalisation: `s := s mod n`, lifted to `[0, n)`, then for `!directed` shifts `s ≥ (n+1)/2` fold to `n − s` so e.g. shift 3 on n=5 collapses to shift 2 (avoids duplicating undirected edges); zero shift always suppressed (would emit self-loops); duplicate shifts deduped via linear `seen.contains` scan (kept linear because `|shifts|` is typically small); emission per canonical shift: for `j ∈ [0, limit)` push `(j, (j + s) mod n)` where `limit = n/2` when `!directed && n even && s == n/2` (even-n antipodal shift halves to avoid duplicating the matching), else `limit = n`; runtime overflow protection: shift mod `n_i64` via signed arithmetic then `u32::try_from`, `n * |canonical shifts|` capacity via `checked_mul`; canonical generalisations: `circulant(n, &[1], false) ≡ ring_graph(n) = C_n`, `circulant(n, &[k], false)` equals inner layer of `generalized_petersen(n, k)` for `0 < k < n/2`, `circulant(n, &[1, 2, …, n/2], false) ≡ K_n` (complete graph specialisation through every distinct undirected shift); structural properties verified by proptest sweep: result is always simple (no loops, no parallels), vertex-regular with degree equal to `2·|canonical shifts after halving|` for the undirected case, negative shifts always equivalent to positive normalisation; conformance: python-igraph 0.11.x does NOT expose `Graph.Circulant` so py fixtures use the canonical equivalents `Graph.Ring(n)` (≡ `circulant(n, [1], false)`) and `Graph.Famous('Tetrahedral')` (≡ `circulant(4, [1, 2], false) = K_4`); R-igraph mirrors the same approach via `make_ring()` and `make_full_graph()`; cross-source test compares edge multisets, canonicalising `(lo, hi)` for undirected and preserving orientation for directed; deterministic, no RNG) | constructors/circulant.c (igraph_circulant, lines 70-170) | ~112 | adapt | CN-006 (ring — single-shift specialisation), CN-010 (generalized_petersen — inner-circulant equivalence) | done | (now) | O(n · \|canonical shifts\|) edge enumeration + Graph::add_edges; throughput ~21–27 Medge/s for n ≥ 1000 | C:6 / py:2 / R:2 = 10 total |
| ALGO-CN-012 | `de_bruijn` (igraph `igraph_de_bruijn` deterministic constructor — De Bruijn graph `B(m, n)` over alphabet of size `m`: `m^n` vertices, each a length-`n` string in little-endian base-`m` encoding `Σᵢ xᵢ·mⁱ`; an arc goes from `v = (a_1, …, a_n)` to `w = (a_2, …, a_n, b)` for every alphabet symbol `b`, giving out-degree exactly `m` and in-degree exactly `m` for `n ≥ 1`; total arc count `m^(n+1)`; always directed (matches the fixed `IGRAPH_DIRECTED` of the C constructor); arithmetic encoding makes the rewrite a pure integer formula: from vertex `i`, the `m` successors are `((i*m) mod m^n) + b` for `b ∈ [0, m)`, computed once per source vertex via `basis = (i*m) mod m^n` hoisted out of the inner alphabet loop and a u64 widening that removes any need for `checked_mul` inside the inner loop; degenerate inputs: `n == 0` → singleton directed (one length-0 string, no arcs), `m == 0` → null graph (0 vertices), `m == 1 ∧ n == 1` → single self-loop `(0, 0)`, `B(2, 1)` = directed K_2 with both self-loops; runtime overflow protection: `u32::checked_pow(m, n)` for vcount (rejects `m=2 ∧ n=32` etc.) and `usize::checked_mul` for ecount so the edge buffer cannot be sized over `usize::MAX`; structural properties verified by proptest sweep: exactly `m^n` vertices, exactly `m^(n+1)` arcs, every vertex has out-degree `m` and in-degree `m`, every arc `(u, v)` satisfies the rewrite `v ∈ [(u*m) mod m^n, (u*m) mod m^n + m)`; conformance: python-igraph exposes `Graph.De_Bruijn(m, n)` and R-igraph exposes `make_de_bruijn_graph(m, n)` — both dispatch to the same C entry point so arc emission order is identical across all three sources, and the conformance test compares the raw ordered arc list rather than the canonicalised multiset (any drift in emission order would be a real regression); deterministic, no RNG) | constructors/de_bruijn.c (igraph_de_bruijn, lines 58-113) | ~113 | adapt | CN-007 (hypercube — bit/digit rewrite pattern), CN-011 (circulant — `(i*m) mod n` arithmetic) | done | (now) | O(\|V\| + \|E\|) = O(m^(n+1)) edge enumeration + Graph::add_edges; throughput ~21–23 Marc/s for m^(n+1) ≥ 10^5 | C:6 / py:2 / R:2 = 10 total |
| ALGO-CN-014 | `full_graph` (igraph `igraph_full` deterministic complete-graph constructor — `K_n` on `n` vertices with the `(directed, loops)` flag pair giving four distinct emission cells: `(false, false)` → `n·(n−1)/2` undirected edges in canonical `i < j` order, `(false, true)` → `n·(n+1)/2` edges in canonical `i ≤ j` order (every self-loop included), `(true, false)` → `n·(n−1)` arcs in source-major order with per-row `j ∈ [0, i) ∪ (i, n)` (the inner loop is split into two passes that skip the diagonal, matching upstream byte-for-byte), `(true, true)` → `n²` arcs in full row-major order; degenerate inputs handled inline — `n == 0` returns the empty graph regardless of flags, `n == 1 ∧ loops == false` yields a singleton with no edges, `n == 1 ∧ loops == true` yields a singleton with a single self-loop `(0, 0)`; runtime overflow protection: `usize::checked_mul` for the `n·n`, `n·(n−1)`, and `n·(n+1)` products plus `usize::checked_add` for the `n + 1` increment, so the worst-case `n²` edge buffer cannot be sized over `usize::MAX`; structural properties verified by proptest sweep: ecount equals the closed form per cell, the `loops` flag exactly toggles the presence of self-loops (loop count is `n` with loops, `0` without), and for the `(false, false)` cell every unordered pair `{i, j}` appears exactly once; conformance: python-igraph exposes `Graph.Full(n, directed, loops)` and R-igraph exposes `make_full_graph(n, directed, loops)` — both dispatch to the same upstream `igraph_full()` C entry point so edge emission order is identical across all three sources, and the conformance test compares the raw ordered edge list rather than the canonicalised multiset (any drift would be a real regression); deterministic, no RNG; ALGO-CN-013 `kautz` previously inlined the `(true, false)` emission for its `n == 0 → K_{m+1}` degenerate path — that inlined helper is preserved for now but the cross-check test in this AWU asserts the two emission paths agree on `K_4`) | constructors/full.c (igraph_full, lines 54-124) | ~70 | adapt | CN-013 (kautz — directed K_{m+1} cross-check) | done | (now) | O(\|V\| + \|E\|) = O(n²) edge enumeration + Graph::add_edges; throughput ~30–42 Melem/s across all four cells (n=64: ~44 µs ud-noloops, ~100 µs d-loops; n=512: ~4.1 ms ud-noloops, ~9.0 ms d-loops), ~1.8× faster than python-igraph Graph.Full(512, True, True) on the same host | C:7 / py:4 / R:3 = 14 total |
| ALGO-CN-015 | `linegraph` (igraph `igraph_linegraph` constructor over an existing graph — produces the line graph `L(G)` where `|V(L)| = |E(G)|` and edge id maps bijectively to L-vertex id; signature `pub fn linegraph(graph: &Graph) -> IgraphResult<Graph>`; dispatches on input directedness: undirected branch builds the per-vertex incidence list under `IGRAPH_LOOPS == IGRAPH_LOOPS_TWICE` (a self-loop `(u, u)` appears twice in `incident[u]`) and for each vertex emits `(j, k)` for every `j < k` index pair in its incidence vector — so two non-loop edges sharing `u` yield exactly one L-edge, two parallel self-loops on `u` yield three L-edges (the two-elements-each cross product divided by the order-preserving `j < k` filter), and `n` non-loop edges incident on `u` yield `n·(n−1)/2` L-edges; directed branch builds per-vertex IN-incidence lists once and per source vertex `u` walks OUT-incidence emitting arc `(out_eid, in_eid_at_target)` for every `(out, in)` pair where `target(out) = u`, matching `head(out_eid) == tail(in_eid)`; result preserves input directedness (`Graph::new(vcount, directed)`); runtime overflow protection: `u32::try_from(ecount)` for vertex id reuse, `usize::checked_mul`/`checked_add` for the cross-product capacity estimate; degenerate inputs handled inline — empty input → empty L, single isolated vertex → empty L (no edges), single self-loop undirected → one L-vertex with a single self-loop emitted from the cross product of the two loop copies; structural properties verified by proptest sweep: vcount equals input ecount, no L-edge references a non-existent L-vertex, undirected L-graph stays loopless when input is loopless, multi-edge multiplicity preserved in L; conformance: cross-source ordering of L-edges differs (igraph C uses incidence-vector order; python-igraph and R-igraph share that order since they all dispatch through `igraph_linegraph()` but cross-version emission isn't byte-stable), so conformance test compares the L-edge multiset canonicalised to `(min, max)` for undirected and as-ordered for directed, mirroring `igraph_is_same_graph` semantics; fixture catalogue covers empty graph, path P_4, triangle K_3, K_4, star, ring C_5, multi-edge, self-loop, directed chain, directed cycle across the three sources; deterministic, no RNG) | constructors/linegraph.c (igraph_linegraph + igraph_i_linegraph_undirected + igraph_i_linegraph_directed, lines 35-150) | ~150 | adapt | CN-014 (full_graph — input dense K_n test fixtures), CN-006 (ring — input cycle fixtures), CN-005 (star — input star fixtures) | done | (now) | O(\|V(G)\| + Σ_v deg(v)²) for undirected, O(\|V(G)\| + Σ_v deg_in(v)·deg_out(v)) for directed; throughput ~16 Melem/s on path inputs, ~20 M L-edges/s on dense K_n inputs, ~24 M L-edges/s on star inputs, ~26 M arcs/s on directed chains | C:5 / py:5 / R:3 = 13 total |
| ALGO-CN-017 | `tree_from_parent_vector` (igraph `igraph_tree_from_parent_vector` decoder — given a length-`n` parent vector where `parents[v]` names `v`'s parent vertex id and any negative entry marks `v` as a root, materialises the unique rooted tree or forest on `n` vertices with three possible arc-orientation flavours selected by `TreeMode`: `Out` directed parent→child, `In` directed child→parent, `Undirected` single canonical `(min,max)` edge per parent link; signature `pub fn tree_from_parent_vector(parents: &[i64], mode: TreeMode) -> IgraphResult<Graph>`; linear-time `O(n)` decoder ported byte-for-byte from `constructors/tree.c:igraph_tree_from_parent_vector` — outer index `i ∈ [0, n)` walks up the parent chain starting from `v = i`, emitting one edge per non-root step and stopping at the first vertex that was already visited in a previous round (`seen[v] != 0`) so each vertex is visited at most twice across all rounds; emission order in the result mirrors the upstream byte-for-byte (per-round emission of newly-discovered ancestors); validation: rejects any parent id `>= n` with `InvalidArgument`, allows any negative value as a root sentinel (matches C semantics where igraph's enum value `-2` and the user's `-1` are both negative), rejects `i64::MIN` only via the `try_from` overflow path (defensive — bounds above the conversion already prove safety), and uses `u32::try_from(...).map_err(...)` rather than `as u32` to stay free of unwrap/expect per project conventions; degenerate inputs handled inline — empty parent vector → empty graph, all-roots vector → isolated-vertex edgeless graph regardless of mode, single-root chain → straight path with edges emitted in vertex order, multi-root parent vector → disconnected forest where each tree appears in its own contiguous edge block; rigraph wrapper performs `parents <- as.numeric(parents) - 1` before dispatching to C so R fixture `c(-1, 1, 2, 3)` becomes C-level `[-2, 0, 1, 2]` yielding chain `0 → 1 → 2 → 3`; python-igraph does NOT bind `igraph_tree_from_parent_vector` directly so py-side conformance uses synthetic fixtures grounded in identical C semantics; structural properties verified by proptest sweep over `n ∈ [1, 30)`: every result is acyclic (union-find probe), has at most `n − 1` edges, has zero self-loops, edge count equals the number of non-root entries in `parents`, undirected mode keeps the multiset deduplicated, and an out-of-range `parents[v] >= n` strategy always errors; conformance: directed fixtures compare the ordered edge multiset (upstream emission order is the contract), undirected fixtures compare the canonical `(min,max)` multiset (rigraph re-canonicalises) — fixture catalogue covers 14 cases across the three sources including the upstream-C `[4,4,1,-2,3]` shape in OUT/IN/Undirected, two-root forest, edgeless graph, null graph, chain/star/forest variants, and the rigraph snapshot chain; deterministic, no RNG) | constructors/tree.c (igraph_tree_from_parent_vector, lines 200-310) | ~100 | adapt | CN-009 (kary_tree — shares `TreeMode` enum and edge-orientation dispatch) | done | (now) | O(n) per upstream — each vertex visited at most twice; throughput ~53 Melem/s on `forest` shape (short alternating chains, L1-cache-resident), ~35 Melem/s on `chain` (deepest leaf walked once, rest short-circuit), ~33 Melem/s on `star` (no cascade per i), ~18 Melem/s on `random` (cache-unfriendly seen[] access from random parents); `time(n=131072)/time(n=16384)` ratios 4.94/5.56/5.80/7.20 across chain/star/forest/random vs ideal 8.0 confirm O(n) scaling with sub-linear cache effects beyond L2 | C:6 / py:5 / R:3 = 14 total |
| ALGO-CN-016 | `from_prufer` (igraph `igraph_from_prufer` Prüfer-sequence decoder — given a length-`L` sequence `prufer[0..L]` over the alphabet `{0, 1, …, n-1}` with `n = L + 2`, produces the unique labelled undirected tree on `n` vertices and `n − 1` edges that the sequence encodes (Prüfer bijection); signature `pub fn from_prufer(prufer: &[u32]) -> IgraphResult<Graph>`; linear-time `O(n)` Micikevičius–Caminiti–Deo decoder ported byte-for-byte from `constructors/prufer.c:55-125` — pre-fill `degree[w]` = residual occurrences of vertex `w` in the unread tail, then for each `i ∈ [0, n)` walk `u = i` and cascade: while `degree[u] == 0 ∧ u ≤ i ∧ k < L`, emit `(prufer[k], u)`, decrement `degree[prufer[k]]`, advance `u ← prufer[k]`; after the main loop, scan `(last_i + 1)..n` for the lone surviving leaf `last_u ≠ v` and emit the final closing edge `(v, last_u)`; degenerate inputs handled inline — empty Prüfer (`L = 0`, `n = 2`) skips the main loop entirely with `last_i = 0`, `v = prufer[0]` was never assigned so `v = 0`, scan finds `last_u = 1`, emits `(0, 1)` yielding `P_2` (the 2-vertex path); always undirected — `Graph::new(n, false)` matches the upstream `IGRAPH_UNDIRECTED` flag; runtime safety: `u32::try_from(prufer.len()).ok().and_then(|l| l.checked_add(2))` rejects sequences whose `L + 2` would overflow `u32` (the graph's vcount type), and a single pre-decode pass validates every `prufer[i] < n` returning `InvalidArgument` with the offending value; structural properties verified by proptest sweep over `n ∈ [2, 30)`: result has exactly `n` vertices and `n − 1` edges, no self-loops, no duplicate edges (multiset uniqueness via `BTreeSet`), single connected component (union-find with path compression), and an out-of-range `prufer[i] ≥ n` strategy always errors; conformance: python-igraph exposes `Graph.Prufer(seq)` (note: capitalised `P`) and R-igraph exposes `make_from_prufer(prufer)`, both dispatching to the same `igraph_from_prufer()` C entry — cross-source edge emission order is **not** byte-stable across language bindings (R re-canonicalises edges), so conformance test compares the canonical `(min, max)` edge multiset rather than the raw ordered list; fixture catalogue covers the two upstream-C `igraph_from_prufer.out` cases (`[2,3,2,3]` → 6-vertex, `[0,2,4,1,1,0]` → 8-vertex) plus the empty sequence across all three sources, with additional py-side coverage of singleton/star/path/mixed shapes and an R-side round-trip from `make_tree(13, 3, undirected) → to_prufer → from_prufer` confirming the bijection; deterministic, no RNG) | constructors/prufer.c (igraph_from_prufer, lines 55-125) | ~80 | adapt | none | done | (now) | O(n) per Micikevičius–Caminiti–Deo; throughput ~60 Melem/s on `path` worst-case cascade at n=131072, ~51 Melem/s on `star` (no cascade), ~16 Melem/s on `random` (cache-unfriendly degree[] access from random parents); `time(n=131072)/time(n=16384)` ratios 7.83/7.81/9.14 confirm near-linear scaling across topologies | C:3 / py:5 / R:3 = 11 total |
| ALGO-CN-018 | `lcf` (igraph `igraph_lcf` deterministic LCF (Lederberg-Coxeter-Frucht) cubic-graph constructor — given vertex count `n`, an integer shift pattern `shifts[0..k]` (positive or negative offsets relative to a Hamilton cycle), and a positive `repeats` count, materialises the unique 3-regular simple undirected graph encoded by the LCF notation `[shifts]^repeats`; signature `pub fn lcf(n: u32, shifts: &[i64], repeats: u32) -> IgraphResult<Graph>`; algorithm: (1) emit Hamilton-cycle backbone `0–1–2–…–(n−1)–0` covering exactly `n` undirected edges (skip when `n ≤ 1`); (2) chord pass — for each `sptr ∈ [0, repeats·shifts.len())` compute `u = sptr mod n`, `s = shifts[sptr mod shifts.len()]`, `v = ((u as i64) + s).rem_euclid(n as i64) as u32`, and emit candidate chord `(u, v)`; (3) inline simplify via `BTreeSet<(u32, u32)>` with canonical `(min, max)` insert — self-loops where `u == v` are silently dropped and any chord already in the seen-set is a no-op, mirroring upstream's terminal `igraph_simplify(loops=true, multi=true)` collapse; result is always `Graph::new(n, false)` (the C constructor is fixed `IGRAPH_UNDIRECTED`); LCF notation references: `[5, -5]^6` on n=12 → Franklin (18 edges, bipartite cubic), `[5, -5]^7` on n=14 → Heawood (21 edges, girth-6 cage), `[-5,-2,-4,2,5,-2,2,5,-2,-5,4,2]^1` on n=12 → Frucht (only 3-regular planar graph with trivial automorphism group), `[2, 6, -2, -6]^3` on n=12 → Truncated tetrahedron, `[3, -7, 7, -3]^6` on n=24 → Truncated octahedron; degenerate inputs handled inline — `lcf(0, [], 0)` → empty graph (upstream bug #996 regression, explicitly covered), `lcf(n, [], 0)` → pure Hamilton cycle `C_n` (no chord pass), `lcf(n, shifts, 0)` → pure Hamilton cycle (repeats=0 short-circuits chord pass), `lcf(2, _, _)` → backbone (0,1) plus every chord collapsing to self-loop or duplicate of backbone (ecount=1 guaranteed); validation: shifts containing 0 are accepted but silently collapse to self-loops which the simplify pass drops, shifts ≥ n or ≤ -n are accepted via `rem_euclid` wrapping; runtime overflow protection: `repeats.checked_mul(shifts.len() as u32)` capacity estimate, `i64` widening for the `(u as i64) + s` chord arithmetic so a `shifts = [i64::MAX]` adversarial input cannot silently wrap before the `rem_euclid` modular reduction; structural properties verified by proptest sweep over `n ∈ [0, 30]`, `shifts.len() ∈ [0, 6]`, `repeats ∈ [0, 4]`: result is always undirected, no self-loops survive, no parallel edges survive (canonical `(min, max)` multiset uniqueness), edge count is bounded by `n + repeats·shifts.len()` (chord-pass upper bound before simplify), pure-cycle invariant when `repeats == 0` or `shifts` is empty, `n == 0` always yields empty regardless of pattern, `n == 1` always yields singleton, unrolled-shifts equivalence (`lcf(n, shifts, k) ≡ lcf(n, shifts.repeat(k), 1)` as edge multisets), and maximum degree is bounded by `2 + 2·shifts.len()` per vertex (backbone contributes 2, each shift position can contribute up to 2 chord endpoints before dedup); conformance: python-igraph exposes `Graph.LCF(n, shifts, repeats)` and R-igraph exposes `graph_from_lcf(n, shifts, repeats)`, both dispatching to the same `igraph_lcf()` C entry — cross-source emission order is NOT byte-stable (upstream's terminal simplify rewrites the edge list), so conformance test compares canonical `(min, max)` edge multisets; fixture catalogue covers Franklin, Heawood, Truncated tetrahedron, single shift `[-2]^4` on n=8, n=2 self-loop collapse, repeats=0 pure-cycle, empty-shifts pure-cycle, single-shift repeats=3, and the bug #996 `lcf(0, [], 0)` null-graph regression across all three sources; deterministic, no RNG) | constructors/lcf.c (igraph_lcf + igraph_lcf_vector + igraph_i_lcf_simplify, lines 40-180) | ~180 | adapt | CN-006 (ring — Hamilton-cycle backbone pattern), CN-011 (circulant — chord arithmetic via `(j + s) mod n`) | done | (now) | O((n + repeats·shifts.len()) · log(n + repeats·shifts.len())) from inline `BTreeSet` simplify (matches upstream `igraph_simplify(loops=true, multi=true)` envelope); throughput on famous shapes ~7.8–9.4 Melem/s, cycle-only `n=131072` 12.578 ms ≈ 10.4 Melem/s, chord-heavy `n=131072` (shifts.len=5, repeats=26214) 27.531 ms ≈ 4.8 Melem/s; cycle-only scaling 17.9×/20.7×/9.0× and chord-heavy 23.7×/20.0×/9.0× per 16×/16×/8× `n` step confirm near-linear with mild log overhead from BTreeSet inserts | C:6 / py:5 / R:3 = 14 total |
| ALGO-CN-022 | `create` (igraph `igraph_create` foundational edge-list constructor — given a flat edge list, a target vertex count `n`, and a directed flag, materialises the unique graph on `max(n, max_endpoint(edges) + 1)` vertices whose edge multiset equals the input in emission order; signature `pub fn create(edges: &[(u32, u32)], n: u32, directed: bool) -> IgraphResult<Graph>`; type-level error elimination — the upstream C entry takes `igraph_vector_int_t` of length `2·|E|` and can return `IGRAPH_EINVAL` (odd-length) and `IGRAPH_EINVVID` (negative ids), both of which become statically unreachable once the input is `&[(u32, u32)]`: odd-length cannot exist (pairs are tuples) and negative ids cannot exist (`u32`); semantics: `n == 0` infers vcount from `max(u, v) + 1` over the edge list (returning the empty graph when `edges` is empty), `n < max_endpoint + 1` silently extends to `max_endpoint + 1` via the upstream `igraph_add_vertices(graph, max_endpoint + 1 − n)` path, `n ≥ max_endpoint + 1` keeps the requested vertex count verbatim (any unreferenced vertex stays isolated), and emission order is preserved byte-for-byte (the slice is passed straight to `Graph::add_edges`); algorithm: a single linear scan over the edge slice to compute `max_endpoint`, one `Graph::new(vcount, directed)` allocation, one bulk `Graph::add_edges` call — no intermediate buffers, no deduplication, no canonicalisation (self-loops `(u, u)` and parallels survive verbatim, matching upstream); runtime overflow protection: the inferred vcount computation guards against `u32::MAX` endpoints via `max_endpoint.checked_add(1)` returning `IgraphError::Overflow` rather than silently wrapping to 0 (the demo shows `create(&[(0, u32::MAX)], 0, false)` triggering this path), and a saturating `n.max(max_endpoint + 1)` widens to `u32` before the `Graph::new` call so the upstream "silent extend" rule cannot truncate; degenerate inputs handled inline — empty `edges` with `n == 0` returns the null graph, empty `edges` with `n > 0` returns `n` isolated vertices, single self-loop `(0, 0)` with `n == 0` infers vcount 1 and one self-loop, directed input with `(u, v)` and `(v, u)` keeps both arcs as separate ids with the first arc receiving id 0; structural properties verified by unit + proptest sweep: vcount equals `max(n, max_endpoint + 1)` exactly, ecount equals `edges.len()` exactly (no dedup), edge ids are assigned in emission order (`graph.edge(i) == edges[i]` for every `i`), the directed flag round-trips, isolated vertex padding works for any `n`, and the upstream-C example from `references/igraph/examples/simple/igraph_create.c` (edges `[(0,1), (1,2), (2,3), (2,2)]`, `n=0`) reproduces vcount=4 / ecount=4 / 1 self-loop on vertex 2; 12 unit + 4 proptest cover (a) upstream example bit-exact match, (b) `n == 0` empty-edges null graph, (c) `n == 0` self-loop inference, (d) `n > max+1` keeps padding, (e) `n < max+1` silent extend, (f) directed arc-order preserved, (g) self-loops and parallels survive, (h) `u32::MAX` endpoint triggers `Overflow`, plus the proptest sweep checking edge multiset equality, vcount projection, ecount equals input length, and edge-id order preservation across random `n ∈ [0, 50]`, edge counts `∈ [0, 100]`, directed flag both ways; conformance: cross-source emission order is byte-stable (all three bindings dispatch through `igraph_create()` and preserve input order), python-igraph exposes `Graph(edges=..., n=..., directed=...)` and R-igraph exposes `make_graph(c(u1, v1, u2, v2, ...) + 1, n=..., directed=...)` (R adds 1-based offset on input, our extractor strips it); fixture catalogue covers C:7 (upstream example, n=10 padded, n=3 silent extend, directed arc-order, empty null, empty 5-isolated, self-loops+parallels), py:6 (simple n=0, explicit n keeps, directed arcs, empty null, star via edges, self-loop), R:6 (make_graph 0-based variants: n=0 infers, explicit n kept, two arcs directed, empty isolated, parallel edges, P_4 path); conformance test decodes `params.edges/n/directed` and compares vcount + ecount + directed flag + canonical edge multiset (directed-aware key: `if directed || u <= w { (u,w) } else { (w,u) }`); deterministic, no RNG, no allocations beyond `Graph::new` itself) | constructors/create.c (igraph_create, lines 47-95) | ~80 | adapt | none (foundational — every other generator-like API ultimately reduces to it) | done | (now) | O(\|V\| + \|E\|) — single max-scan + `Graph::new` + bulk `add_edges`; bench: path n=100 ~2.9 µs / n=100k ~1.53 ms (≈15 ns/edge), star n=100k ~1.57 ms, dense K_400 ~2.36 ms; 31.0× faster than python-igraph `Graph(edges, n, directed)` at n=100 path (90 µs Cython entry overhead amortises away at scale: 8.4× / 3.2× / 2.6× across n ∈ {1k, 10k, 100k}; dense narrows to 4.8×→1.4× as Cython per-call cost becomes negligible vs the upstream C edge-array packing which is already optimal) | C:7 / py:6 / R:6 = 19 total |
| ALGO-CN-021 | `atlas` + `ATLAS_SIZE` (igraph `igraph_atlas` deterministic Read-Wilson graph-atlas constructor — given an index `number ∈ [0, 1253)` returns the `number`-th of the 1253 simple unlabelled undirected graphs on 0..7 vertices in the ordering of Ronald C. Read and Robin J. Wilson, *An Atlas of Graphs* (Oxford University Press, 1998); ordering is canonical: (1) by ascending vertex count, (2) for fixed vertex count by ascending edge count, (3) for fixed vertex/edge count by ascending lexicographic degree sequence, (4) for fixed degree sequence by ascending automorphism count; signatures `pub fn atlas(number: u32) -> IgraphResult<Graph>` and `pub const ATLAS_SIZE: u32 = 1253`; per-vertex-count starting offsets follow upstream `references/igraph/src/constructors/atlas.c:50-52`: `STARTS = [0, 1, 2, 4, 8, 19, 53, 209, 1253]` so indices `0`, `1`, `2`, `4`, `8`, `19`, `53`, `209` are the null graphs on 0..7 vertices respectively, and the last entry in each non-trivial cell is the complete graph `K_n` — specifically `atlas(3) = K_2` (1 edge), `atlas(7) = K_3` (triangle), `atlas(18) = K_4` (6 edges, 3-regular), `atlas(52) = K_5`, `atlas(208) = K_6`, `atlas(1252) = K_7` (21 edges, 6-regular); storage: two `#[rustfmt::skip] pub(super) const &[u32]` arrays in `src/algorithms/constructors/atlas_edges.rs` — `ATLAS_EDGES_POS` of length 1253 (starting offsets into the flat data block) and `ATLAS_EDGES` of length 27146 (flat header+edge data with layout `[vcount, ecount, edge0_a, edge0_b, …]` per graph), both transliterated byte-for-byte from upstream `references/igraph/src/constructors/atlas-edges.h` so the per-index dispatch reproduces the C, python-igraph (`Graph.Atlas(number)`), and R-igraph (`graph_from_atlas(n) → igraph_atlas`) emission identically; dispatch is O(1) — read `pos = ATLAS_EDGES_POS[number]`, read header `[vcount, ecount]` at `pos..pos+2`, slice payload `pos+2..pos+2+2·ecount`, `Graph::new(vcount, false)` + `add_edges(chunks_exact(2))`; degenerate inputs handled inline — `atlas(0)` returns the null graph (0v/0e), every cell-start index returns the null graph on its respective vertex count; runtime overflow protection: `usize::checked_mul(ecount, 2)` guards the edge buffer length (per-graph upper bound is `2·21 = 42` so overflow is impossible in practice but the check stays for adversarial position-table corruption), defensive bounds-check on `pos + 2 ≤ ATLAS_EDGES.len()` and `pos + 2 + 2·ecount ≤ ATLAS_EDGES.len()` returns `InvalidArgument` rather than panicking on a corrupt position table, `u32::try_from` on payload entries inherits the static `&[u32]` guarantee so no fallible narrowing; validation: `number ≥ ATLAS_SIZE` returns `InvalidArgument("atlas: graph index N is out of range (must be < 1253)")`; structural properties: all 1253 graphs are undirected, simple, vcount ∈ 0..=7 — verified by the `all_atlas_graphs_construct_and_are_undirected` unit test which walks the entire 1253-graph catalogue and asserts `vcount ≤ 7`, `!is_directed`, and `Σ_v deg(v) == 2·|E|` for every entry (this exhaustive sweep is strictly stronger than any sample-based proptest, so no proptest was added); 8 unit tests cover (a) out-of-range error path (number = 1253, 1254, u32::MAX), (b) `ATLAS_SIZE` matches upstream count, (c) null-graph cell-start indices for all 8 vertex counts, (d) small-known-graph identities for atlas(3)=K_2, atlas(7)=K_3, atlas(18)=K_4 with canonical edge multiset compare, (e) atlas(1252)=K_7 with 6-regularity check, (f) no self-loops or repeated edges across a 19-index representative spread, (g) exhaustive 1253-graph walk for degree-sum invariant + undirected flag, (h) every index in `start[n]..start[n+1]` has exactly `n` vertices; conformance: cross-source edge emission order is byte-stable across all three bindings since they all read the same on-disk `atlas-edges.h` table, but R-igraph re-canonicalises 1-based→0-based — fixture catalogue covers C:8 (null0, k2_single_edge, triangle, k4, v6_null, k6_last_6v, v7_null, k7_last_atlas_graph), py:7 (null0, k2, k4, idx70_skipped_in_upstream, idx180_skipped_in_upstream, k6, k7_last — python-igraph cells skip a few interior indices the C test catalogue omits), R:4 (null0, triangle, k4, k7_last); conformance test compares canonical `(min, max)` edge multisets; deterministic, no RNG, no allocations beyond a single `Vec<(u32, u32)>` of at most 21 entries per call) | constructors/atlas.c (igraph_atlas, lines 50-87) + atlas-edges.h (the 1253-graph flat data table) | ~30 (function) + ~1274 (atlas_edges.rs transliterated data) | adapt | none (self-contained constructor with static data tables) | done | (now) | per-call O(ecount) — single position-table indirection + bulk `add_edges` of at most 21 edges (K_7); bench: per-call cost 226 ns (null) → 1141 ns (K_7), full 1253-graph catalogue walk 1.06 ms; 87.7×–375.8× faster than python-igraph `Graph.Atlas(number)` baseline (~85 μs/call fixed Cython+Graph-object overhead regardless of graph size); 101.4× speedup on the full 1253-graph walk | C:8 / py:7 / R:4 = 19 total |
| ALGO-CN-020 | `famous` + `famous_names` (igraph `igraph_famous` deterministic named-graph catalogue — given a case-insensitive ASCII name returns the canonical labelled copy of one of 31 small graphs that appear repeatedly in graph-theory literature: Bull, Chvátal, Coxeter, Cubical, Diamond, Dodecahedron, Folkman, Franklin, Frucht, Grötzsch, Heawood, Herschel, House, HouseX, Icosahedron, Krackhardt Kite, Levi, McGee, Meredith, Noperfectmatching, Nonline, Octahedron, Petersen, Robertson, Smallestcyclicgroup, Tetrahedron, Thomassen, Tutte, Uniquely3colorable, Walther, Zachary; signatures `pub fn famous(name: &str) -> IgraphResult<Graph>` and `pub fn famous_names() -> &'static [&'static str]`; storage: each graph is a `#[rustfmt::skip] const DATA_<NAME>: &'static [u32]` array transliterated byte-for-byte from `references/igraph/src/constructors/famous.c:26-249`, with layout `[vcount, ecount, directed_flag, edge0_a, edge0_b, edge1_a, edge1_b, ...]`; dispatch: a 36-entry static `TABLE` of `(canonical_name, data)` pairs is linear-scanned with case-insensitive ASCII compare (`a.eq_ignore_ascii_case(b)`), so a name like `"PETERSEN"` resolves to the same row as `"petersen"` — overhead stays sub-microsecond because the table fits in L1; aliases follow upstream: `dodecahedral ≡ dodecahedron`, `grotzsch ≡ groetzsch`, `icosahedral ≡ icosahedron`, `octahedral ≡ octahedron`, `tetrahedral ≡ tetrahedron`; materialisation: `materialize(data: &[u32]) -> IgraphResult<Graph>` reads `[v, e, dir, edges...]`, allocates `Graph::new(v, dir != 0)`, then bulk-inserts via `Graph::add_edges` reading the flat edge slice; all 31 graphs are undirected (`dir = 0`) — the directed flag is part of the on-disk format for future-proofing but currently unused; structural invariants: every graph is simple (no self-loops, no multi-edges) and `famous_names()` returns the same 31-entry slice every call; validation: unknown name returns `IgraphError::InvalidArgument("famous: 'X' is not a known graph; see famous_names() for the list")`; size envelope: smallest is Diamond (4 vertices, 5 edges), largest is Meredith (70 vertices, 140 edges); 10 unit tests cover (a) every catalogue entry round-trips via `famous(name).vcount() == DATA[0]` and `ecount() == DATA[1]`, (b) case-insensitive dispatch (`Petersen == petersen == PETERSEN`), (c) every alias resolves to the same graph as its canonical name, (d) `famous("NotARealGraph")` returns `InvalidArgument`, (e) `famous_names().len() == 31`, (f) Zachary karate club has 34 vertices / 78 edges matching the published count, (g) Petersen has 10 vertices / 15 edges and is 3-regular, (h) every graph is simple undirected; conformance: python-igraph 0.11.x exposes `Graph.Famous(name)` and R-igraph exposes `make_graph("<name>")` which dispatches to `famous_impl()` → `igraph_famous` C — all three sources share the per-graph emission order since they read the same static C table, so conformance test compares the canonical `(min, max)` edge multiset across all 16 fixtures (7 C-lane + 5 py-lane + 4 R-lane covering Bull, Petersen, Meredith counts, Zachary counts, dodecahedron case-insensitive, grotzsch alias, tetrahedron alias, krackhardt kite lowercase); deterministic, no RNG, no allocations beyond the single bulk `add_edges` call) | constructors/famous.c (igraph_famous + igraph_i_famous + per-graph data tables, lines 26-498) | ~430 | adapt | none (self-contained constructor with static data tables) | done | (now) | per-call linear-scan over 36-entry case-insensitive table + single bulk `add_edges`; bench: Bull 619 ns, Petersen 982 ns, Meredith 5.4 μs end-to-end, walk-all-31-catalogue 50.6 μs; 15.7×–207× faster than python-igraph `Graph.Famous(name)` baseline (88.2× speedup on Petersen, 52.4× on the full 31-graph walk) because Rust avoids both the Cython entry path and the per-call Graph object construction overhead | C:7 / py:5 / R:4 = 16 total |
| ALGO-CN-019 | `mycielskian` + `mycielski_graph` (igraph `igraph_mycielskian` + `igraph_mycielski_graph` deterministic Mycielski-construction pair — given a graph `G = (V, E)` and a non-negative iteration count `k`, the Mycielski construction `μ(G)` produces a triangle-free larger graph whose chromatic number is one higher than `G`'s; signatures `pub fn mycielskian(graph: &Graph, k: u32) -> IgraphResult<Graph>` and `pub fn mycielski_graph(k: u32) -> IgraphResult<Graph>`; per-iteration recurrence `(v', e') = (2v + 1, 3e + v)` is exact and the construction preserves directedness (directed input produces directed output); single-iteration emission: for input with `n` vertices and edge list `[(u_0, v_0), …, (u_{m-1}, v_{m-1})]`, output has `2n + 1` vertices labelled `0..n` (originals) ∪ `n..2n` (shadow copies) ∪ `{2n}` (apex), and edges (1) `(u_i, v_i)` for `i ∈ [0, m)` — the original edge multiset reproduced verbatim, (2) `(u_i, v_i + n)` and `(u_i + n, v_i)` for `i ∈ [0, m)` — each original edge replicated twice with one endpoint promoted to its shadow (`2m` cross edges total), (3) `(j + n, 2n)` for `j ∈ [0, n)` — every shadow vertex connected to the apex (`n` apex edges total); arithmetic check `m + 2m + n = 3m + n` matches the recurrence; promotion chain `null → singleton (k=1) → P_2 (k=2)` handles degenerate inputs — for `vcount == 0` the first iteration emits a single isolated vertex (the apex with no shadows), the second emits `P_2` since there are now original ∪ shadow ∪ apex but no edges, and subsequent iterations build M_3 = C_5 = `mycielski_graph(3)` etc.; parameter-only `mycielski_graph(k)` bootstraps M_2 = P_2 explicitly (`Graph::new(2, false)` + `add_edge(0, 1)`) and dispatches to `mycielskian(M_2, k - 2)` for `k ≥ 3`, with `k ∈ {0, 1}` returning explicit null/singleton (matches upstream `IGRAPH_EINVAL` for `k < 2` reverted to "minimal canonical answer" per project convention of in-band degenerate handling); algorithm: per-iteration cost is `O(e + v)` — `stage_input_edges` copies the current row into a flat `Vec<u32>` once (O(e)), `project_counts` pre-flight grows the output edge buffer via `Vec::resize` (O(1) amortised), then a single source-major sweep emits original/cross/apex edges in three contiguous segments via direct pointer arithmetic on the staging buffer, and a final `Graph::add_edges` bulk call performs the actual edge insert; runtime overflow protection: `u32::checked_mul(2)` + `checked_add(1)` for the `2v + 1` projection on every iteration step, `usize::checked_mul(3)` + `checked_add(v)` for the `3e + v` edge-count projection, and `try_from` between `u32` and `usize` at every cross-domain boundary so `mycielskian(C_{u32::MAX/4}, 1)` reports `Overflow` rather than silently truncating; structural properties verified by proptest sweep (`n ∈ [0, 8]`, edge density up to `n·(n-1)/2`, `k ∈ [0, 4]`): every iteration result has exactly `2v + 1` vertices and `3e + v` edges per the recurrence, originals are reproduced exactly (multiset compare on first `m` emitted edges vs input), every shadow vertex `n..2n` has degree `deg_orig + 1` (deg_orig from cross edges + 1 from apex), the apex has degree `n`, the directed flag is preserved, and iterating mycielskian k times equals applying the per-iteration map k times to the running output; conformance: python-igraph 0.11.x exposes NO `Graph.Mycielski` / `Graph.Mycielskian` binding (still pending), so py-lane fixtures use the synthetic-but-canonical recurrence-derived shape with origin note "no upstream binding in python-igraph 0.11.x"; rigraph snapshot at `references/rigraph/tests/testthat/_snaps/aaa-auto.md` provides R-source truth for `mycielskian(P_3, k=1)` (7v/9e) and `mycielskian(P_3, k=2)` (15v/34e), edges hand-translated 1-based→0-based; igraph C unit tests have NO dedicated mycielski test (constructors/mycielskian.c has no companion file under `tests/unit/`), so C-lane uses synthetic-but-canonical fixtures grounded in the published recurrence (same pattern as ALGO-CN-018 lcf for the bug #996 regression); fixture catalogue covers M_0 → M_5 across `mycielski_graph(k)` (k=5 uses counts-only check via `edges=null` due to 71-edge fixture size), arbitrary-input variants `mycielskian(P_3, 1)`, `mycielskian(C_5, 1) ≡ Grötzsch`, `mycielskian(null, 2) ≡ P_2`, `mycielskian(K_3, 0)` identity, and the rigraph-validated `mycielskian(P_3, 2)`; cross-source emission order is **not** byte-stable (upstream may rewrite edge order), so conformance tests compare canonical `(min, max)` edge multisets; deterministic, no RNG) | constructors/mycielskian.c (igraph_mycielskian + igraph_mycielski_graph, lines 60-280) | ~220 | adapt | none (self-contained constructor pair) | done | (now) | per-iteration O(e + v); `mycielski_graph(10)` builds a 513-vertex/4099-edge graph in ~1.05ms; `mycielskian(C_n, 2)` scales linearly in n (~0.51μs/vertex — n=32: 15μs, n=64: 31μs, n=128: 65μs); no external baseline (python-igraph 0.11.x lacks the binding) | C:10 / py:4 / R:3 = 17 total |
| ALGO-CN-023 | `triangular_lattice` (igraph `igraph_triangular_lattice` deterministic planar triangular-lattice constructor — given a `dims` slice that doubles as a shape selector (`[n]` → triangle of side `n` with `n(n+1)/2` vertices, `[size_x, size_y]` → quasi-rectangle of `size_x` rows × `size_y` vertices per row, `[size_x, size_y, size_z]` → hexagon with side lengths matching), materialises the unique undirected (or directed-mutual / directed-unilateral) planar triangular lattice where every vertex `(i, j)` is joined to `(i+1, j)`, `(i, j+1)`, and `(i-1, j+1)` whenever those neighbours fall inside the lattice — every interior vertex thus has degree exactly six and the graph is the planar dual of the hexagonal lattice over the same `dims`; signature `pub fn triangular_lattice(dims: &[u32], directed: bool, mutual: bool) -> IgraphResult<Graph>`; type-level error elimination — the upstream C entry takes `igraph_vector_int_t` of any length and can return `IGRAPH_EINVAL` on negative dims or length-not-in-{1,2,3}, both of which collapse here: negative dims cannot exist (`&[u32]`) so the upstream "any dim < 0" path becomes statically unreachable, and the length check returns the upstream "size of dimension vector must be 1, 2 or 3" error verbatim; algorithm: byte-for-byte port of `constructors/lattices.c:90-156` (generic `triangular_lattice` helper) + the three private shape helpers — `triangle_shape(n)` builds `row_lengths[j] = n - j` and `row_start[j] = 0`, `rectangle_shape(size_x, size_y)` sets every row length to `size_y` and uses `row_start[j] = (row_count - j) / 2` (C integer division — produces the characteristic skewed-rectangle outline), `hex_shape(size_x, size_y, size_z)` runs the three-phase growth/middle/shrink update (`first_threshold = min(sy,sz) - 1`, `second_threshold = max(sy,sz) - 1`, `sgn_flag = sy < sz ? 0 : -1`) on `row_count = sy + sz - 1` rows; vertex indexing follows the upstream `lex_ordering = false` rule: `vertex_index(i, j) = prefix_sum[j] + (i - row_start[j])` with `prefix_sum` computed via `i64.checked_add` (so adversarial `(size_x, size_y) = (u32::MAX, u32::MAX)` reports `Overflow` rather than silently wrapping), and emission is source-major per-vertex with the three-candidate boundary check `ADD_EDGE_IJ_KL_IF_EXISTS(l in [0, row_count) ∧ k in [row_start[l], row_end(l)])` ported verbatim; semantics: `directed = false` emits each lattice edge once with `min(src, dst)` first, `directed = true, mutual = false` emits each lattice edge as a single canonical-direction arc (same edge set as undirected), `directed = true, mutual = true` emits each lattice edge as a forward+reverse arc pair (edge count doubles); special cases handled inline ahead of any work — `dims = []` returns `InvalidArgument` (matching the upstream length check), `dims.len() != 1, 2, 3` returns `InvalidArgument` likewise, and any zero in `dims` short-circuits to `Graph::new(0, directed)` (upstream `igraph_vector_int_contains_zero` guard); runtime overflow protection: `i64.checked_add` on prefix sums, `u32::try_from` on the `total_vertices` and per-edge vertex ids so any overflow surfaces as `IgraphError::InvalidArgument("triangular_lattice: vertex count overflows u32")` rather than truncating; structural properties verified by 12 unit + 6 proptest: dims = [n] triangle vcount is the n-th triangular number, dims = [sx, sy] rectangle vcount equals `sx · sy`, dims = [3, 4, 5] hexagon vcount = 36 / ecount = 87 (canonical .out fixture), triangle side 5 directed reproduces the upstream 30-arc multiset byte-for-byte (from `references/igraph/tests/unit/igraph_triangular_lattice.out`), rectangle 4×5 directed-mutual hits vcount=20 / ecount=86 (43 undirected × 2 mutual orientations), `directed && mutual` doubles undirected ecount exactly, `directed && !mutual` preserves undirected ecount exactly, max degree ≤ 6 across every shape, directedness flag round-trips, and `dims = [3, 0]` collapses to the null graph; conformance: python-igraph exposes `Graph.Triangular_Lattice(dims, directed=False, mutual=False)` and R-igraph exposes `triangular_lattice_impl(dimvector, directed, mutual)`, both dispatching to the same `igraph_triangular_lattice()` C entry — cross-source emission order is NOT byte-stable across the three sources (different lattice walks pick different per-vertex emission orders), so conformance test compares directed-aware multiset (`(u, v)` vs `(v, u)` distinct when directed, canonical `(min, max)` when undirected); fixture catalogue covers C:7 (triangle side 1 single vertex, triangle side 3 undirected, triangle side 5 directed full 30-arc reproduction, rectangle 2×2 undirected, rectangle 2×2 directed-mutual 10-arc, hexagon 2,2,2 undirected, zero-dim guard), py:3 (2×2 undirected, 2×2 directed-unilateral, 2×2 directed-mutual matching `testTriangularLattice`), R:3 (2×2 undirected, 2×2 directed-mutual snapshot from `_snaps/aaa-auto.md:220-229`, triangle side 3 cross-check); deterministic, no RNG, no allocations beyond per-row metadata vectors + the final `Graph::add_edges`) | constructors/lattices.c (igraph_triangular_lattice + triangle_shape + rectangle_shape + hex_shape + generic helper, lines 90-319) | ~230 | adapt | CN-009 (square_lattice — sibling lattice constructor with `dims` dispatch + canonicalised multiset conformance pattern) | done | (now) | O(\|V\| + \|E\|) — single source-major sweep with 3 candidate boundary checks per vertex; bench: triangle_100 ~693 µs / triangle_200 ~2.85 ms / rect_100x100 ~1.43 ms / rect_200x100 ~3.03 ms / hex_30_30_30 ~330 µs — all shapes hover 18–23 Medge/s (undirected) and 18–22 Medge/s (directed_mutual) on M-series silicon; comparable to square_lattice's ~30 Medge/s with the gap explained by the extra `up-left` candidate per vertex and the i64→u32 conversions kept in the inner loop for overflow safety | C:7 / py:3 / R:3 = 13 total |
| ALGO-CN-024 | `hexagonal_lattice` (igraph `igraph_hexagonal_lattice` deterministic planar hexagonal-lattice (honeycomb) constructor — given a `dims` slice that doubles as a shape selector (`[n]` → triangular outline of `n` hexagons per side, `[size_x, size_y]` → quasi-rectangle of `size_x × size_y` hexagons, `[size_x, size_y, size_z]` → hexagonal outline with three sides carrying `size_x`, `size_y`, `size_z` hexagons respectively), materialises the unique undirected (or directed-mutual / directed-unilateral) planar hexagonal lattice where every vertex `(i, j)` is joined to `(i + 1, j)` always and to `(i - 1, j + 1)` only when `i` is odd — every vertex thus has degree at most three and the graph is the planar dual of the triangular lattice over the same `dims`; signature `pub fn hexagonal_lattice(dims: &[u32], directed: bool, mutual: bool) -> IgraphResult<Graph>`; type-level error elimination — the upstream C entry takes `igraph_vector_int_t` of any length and can return `IGRAPH_EINVAL` on negative dims or length-not-in-{1,2,3}, both of which collapse here: negative dims cannot exist (`&[u32]`) so the upstream "any dim < 0" path becomes statically unreachable, and the length check returns the upstream "size of dimension vector must be 1, 2 or 3" error verbatim; algorithm: byte-for-byte port of `constructors/lattices.c:350-601` (generic `hexagonal_lattice` helper) + the three private shape helpers — `triangle_shape(size)` uses upstream's exact `row_count_raw = size + 2; for i in 0..(row_count_raw - 1)` loop with `len = 2 · (row_count_raw - i) - (3 if i == 0 else 1)` (subtle off-by-one trap: the loop runs over `size + 1` rows, not `size`, and the first row sheds 3 instead of 1; my first translation read `row_count = size + 1` and produced 12-arc / 20-vertex outputs instead of the .out-canonical 60-arc / 46-vertex for `dims=[5]` — fixed by adopting the upstream form verbatim), `rectangle_shape(size_x, size_y)` builds `row_count = size_x + 1`, `row_lengths[j] = 2 · size_y + (j == 0 || j == row_count - 1 ? 0 : 1)`, `row_start[j] = (j + 1) / 2`, `hex_shape(size_x, size_y, size_z)` runs the three-phase growth/middle/shrink update on `row_count = 2 · (size_y + size_z) - 1` rows with `first_threshold = 2 · min(sy, sz) - 1`, `second_threshold = 2 · max(sy, sz) - 1`, and a per-row parity flag controlling the `+1` shed; vertex indexing follows the upstream `lex_ordering = false` rule: `vertex_index(i, j) = prefix_sum[j] + (i - row_start[j])` with `prefix_sum` computed via `i64.checked_add` (so adversarial `(size_x, size_y) = (u32::MAX, u32::MAX)` reports `Overflow` rather than silently wrapping), and emission is source-major per-vertex with the two-candidate boundary check (right `(k+1, j)` always; up-left `(k-1, j+1)` only when `k` is odd) ported verbatim; semantics: `directed = false` emits each lattice edge once with `min(src, dst)` first, `directed = true, mutual = false` emits each lattice edge as a single canonical-direction arc, `directed = true, mutual = true` emits each lattice edge as a forward+reverse arc pair (edge count doubles); special cases handled inline ahead of any work — `dims = []` returns `InvalidArgument`, `dims.len() != 1, 2, 3` returns `InvalidArgument`, and any zero in `dims` short-circuits to `Graph::new(0, directed)` (upstream `igraph_vector_int_any_smaller(dims, 1)` guard); runtime overflow protection: `i64.checked_add` on prefix sums, `u32::try_from` on `total_vertices` and per-edge vertex ids so any overflow surfaces as `IgraphError::InvalidArgument("hexagonal_lattice: vertex count overflows u32")` rather than truncating; structural properties verified by 12 unit + 6 proptest: `dims=[1]` directed yields the 6-vertex C_6 hexagon (the .out-canonical single-hexagon fixture), `dims=[5]` directed reproduces the upstream 60-arc / 46-vertex sweep byte-for-byte (from `references/igraph/tests/unit/igraph_hexagonal_lattice.out`), `dims=[2, 2]` undirected matches the python-igraph `testHexagonalLattice` 16-vertex / 19-edge multiset, `dims=[4, 5]` directed-mutual hits vcount=58 / ecount=154 (77 undirected × 2 mutual), `dims=[3, 4, 5]` hexagon undirected+mutual collapses to 94 vertices / 129 edges (the .out fixture — undirected silently collapses the mutual flag), `directed && mutual` doubles undirected ecount exactly, `directed && !mutual` preserves undirected ecount exactly, max degree ≤ 3 across every shape (the hexagonal-lattice invariant), directedness flag round-trips, and `dims = [3, 0]` collapses to the null graph; conformance: python-igraph exposes `Graph.Hexagonal_Lattice(dims, directed=False, mutual=False)` but R-igraph has NO `hexagonal_lattice` wrapper (verified via `references/rigraph/R/`), so this AWU runs a two-source rather than three-source conformance test — cross-source emission order is NOT byte-stable across C and py (different lattice walks pick different per-vertex emission orders), so conformance test compares directed-aware multiset (`(u, v)` vs `(v, u)` distinct when directed, canonical `(min, max)` when undirected); fixture catalogue covers C:7 (single hexagon directed, triangle side 5 directed full 60-arc reproduction, rectangle 4×5 directed-mutual 154-arc, hexagon 3,4,5 undirected-mutual 129-edge canonical .out match, two zero-dim guards, rectangle 2×2 undirected), py:3 (2×2 undirected 19-edge multiset, 2×2 directed-unilateral, 2×2 directed-mutual matching `Graph.Hexagonal_Lattice` runtime), R:absent (no wrapper); deterministic, no RNG, no allocations beyond per-row metadata vectors + the final `Graph::add_edges`) | constructors/lattices.c (igraph_hexagonal_lattice + triangle_shape + rectangle_shape + hex_shape + generic helper, lines 350-601) | ~290 | adapt | CN-023 (triangular_lattice — sibling lattice constructor with `dims` dispatch + canonicalised multiset conformance pattern, planar dual) | done | (now) | O(\|V\| + \|E\|) — single source-major sweep with 2 candidate boundary checks per vertex; bench: triangle_50 ~133 µs / triangle_100 ~575 µs / rect_100x100 ~1.19 ms / rect_200x100 ~2.52 ms / hex_30_30_30 ~303 µs — 24–30 Medge/s undirected and 17–22 Medge/s directed_mutual on M-series silicon; slightly faster than triangular_lattice on similar \|V\|+\|E\| budgets because hexagonal vertices carry degree ≤ 3 (vs triangular's ≤ 6) so per-vertex candidate evaluations are fewer | C:7 / py:3 / R:0 = 10 total |
| ALGO-CN-029 | `adjacency` + `AdjacencyMode` + `LoopsMode` (igraph `igraph_adjacency` deterministic dense integer adjacency-matrix constructor — given an `n × n` integer matrix `matrix: &[&[i64]]` whose entries are non-negative edge multiplicities, an [`AdjacencyMode`] selecting one of seven dispatch flavours (`Directed`, `Undirected`, `Max`, `Min`, `Plus`, `Upper`, `Lower`), and a [`LoopsMode`] controlling how diagonal entries become self-loops (`NoLoops` zeros the diagonal, `Once` treats `A(i,i)` as the loop count, `Twice` treats it as twice the loop count requiring even entries), materialises the unique graph whose edge multiset matches the per-mode rule: `Directed` emits one arc `i → j` per unit of `A(i, j)` for every off-diagonal pair; `Undirected` requires `A == Aᵀ` and delegates to `Max` after symmetry check (consumers explicitly opting into multiplicity-`max(A(i,j), A(j,i))` semantics for the undirected case); `Max` / `Min` / `Plus` emit `max(A(i,j), A(j,i))` / `min(A(i,j), A(j,i))` / `A(i,j) + A(j,i)` undirected edges per pair `i < j`; `Upper` / `Lower` walk only the strict upper / strict lower triangle (plus the diagonal as self-loops if `loops != NoLoops`); signature `pub fn adjacency(matrix: &[&[i64]], mode: AdjacencyMode, loops: LoopsMode) -> IgraphResult<Graph>`; type-level error elimination — the upstream C entry takes `igraph_matrix_t` (a 2D dense matrix of `igraph_real_t` that may carry NaN or non-integer entries despite being interpreted as multiplicities), all of which become statically unreachable here since `&[&[i64]]` rules out non-integer and NaN entries; the surviving structural errors are "ragged matrix" (a row's length differs from the row count → `InvalidArgument`), "negative entry" (any `A(i,j) < 0` → `InvalidArgument` since multiplicities cannot be negative), "asymmetric matrix in Undirected mode" (`A != Aᵀ` → `InvalidArgument`), and "odd diagonal under LoopsMode::Twice on symmetric modes" (`A(i,i) % 2 != 0` → `InvalidArgument`); semantics: the per-mode `Twice → Once` collapse for `Directed`, `Upper` and `Lower` matches upstream lines 84, 168, 209 (`igraph_i_adjust_loop_edge_count` does not halve the diagonal for these layouts because the matrix only stores one copy of each loop), so `adjacency(matrix, Upper, Twice)` produces the same edges as `adjacency(matrix, Upper, Once)`; algorithm: byte-for-byte port of `references/igraph/src/constructors/adjacency.c:335-386` — (1) validate `matrix.len() == matrix[0].len()` (square), `matrix.iter().all(|row| row.len() == matrix.len())` (ragged check), `matrix.iter().all(|row| row.iter().all(|&x| x >= 0))` (non-negative), (2) `nrow as u32` cast guarded by `u32::try_from(nrow)` so adversarial `nrow > u32::MAX` reports `InvalidArgument` rather than truncating, (3) dispatch on `mode` to one of six per-mode emit helpers (`emit_directed`, `emit_max`, `emit_min`, `emit_plus`, `emit_upper`, `emit_lower` — `Undirected` checks `A == Aᵀ` then falls through to `emit_max`), each helper traversing the matrix in a mode-specific order and calling `adjust_loop_edge_count(diagonal_entry, mode, loops)` to compute the actual self-loop count per vertex (`NoLoops → 0`, `Twice on collapsing mode → entry`, `Twice on non-collapsing mode → entry / 2 after even-check`, `Once → entry`); (4) accumulate edges into `Vec<(u32, u32)>`, single bulk `Graph::add_edges` call; runtime overflow protection: `usize.checked_add(1)` on the per-vertex loop count expansion (cannot in practice overflow for `i64 ≤ 2^31`, but the check stays), `usize.checked_mul` on the edge buffer capacity pre-size, `u32::try_from` at every cross-domain `i64 → u32` cast; structural properties verified by 20 unit + 6 proptest: vcount equals matrix side length, every mode produces zero edges on the zero matrix, `Directed` round-trips with `Plus` on symmetric inputs (since `A + Aᵀ = 2A` when `A = Aᵀ`), `Max(A, A) == A`, `Min(A, A) == A`, `Upper + Lower` on the same matrix equals the asymmetric `Directed` walk (off-diagonal only), `LoopsMode::Twice` with even diagonal produces exactly half as many self-loops as `LoopsMode::Once` on the same matrix in symmetric modes, `LoopsMode::Twice` with odd diagonal in symmetric mode returns `InvalidArgument`, `Undirected` rejects asymmetric matrix, ragged matrix rejected, negative entry rejected, `LoopsMode::Twice` short-circuits to `Once` semantically for `Directed`/`Upper`/`Lower` (proptest verifies output equality), proptest sweep across `n ∈ [0, 6]`, every cell `∈ [0, 4]`, with all 7 modes × 3 loops combinations checked for invariant preservation (vcount == n, ecount matches independently-derived closed-form per mode, no panics, deterministic re-runs produce identical output); conformance: python-igraph exposes `Graph.Adjacency(matrix, mode='directed')` with only 4 of the 7 modes (`directed`/`undirected`/`max`/`plus`) plus an implicit `loops=True/False` boolean (no native `Twice` mode — uses `Once` semantics on `True`) and R-igraph exposes `graph_from_adjacency_matrix(adjmatrix, mode='directed', diag=TRUE)` with all 7 modes but only `diag={TRUE,FALSE}` (no native `Twice` — uses `Once` semantics on `TRUE`); cross-source emission order is NOT byte-stable (each binding walks the matrix in its own per-mode order), so conformance test compares directed-aware edge multiset (`(u, v)` distinct from `(v, u)` when directed, canonical `(min, max)` when undirected); fixture catalogue covers C:11 (the three matrices from `references/igraph/tests/unit/igraph_adjacency.c` — M3=`[[4,2,0],[3,0,4],[0,5,6]]`, M3_SYM=`[[4,2,0],[2,0,4],[0,4,6]]`, M3_MIN=`[[4,2,0],[3,0,5],[0,4,6]]` — across all 7 modes plus the empty `0×0` and singleton `1×1` shapes + the `Twice → Once` collapse on `Directed` and `Upper`), py:4 (3×3 directed with `loops=False` from `testAdjacency`, 3×3 max with `loops=False`, 3×3 plus with `loops=True` exercising the `Once` path, 3×3 upper with `loops=True` exercising the collapsed `Twice` path), R:3 (3×3 directed `diag=FALSE`, 3×3 undirected `diag=TRUE`, 3×3 lower `diag=FALSE` — no native `Twice` mode in R wrapper so those cases skip the R lane); conformance test decodes `params.matrix/mode/loops` JSON shapes into the typed Rust enums and compares vcount + ecount + directed flag + canonical multiset; deterministic, no RNG, no allocations beyond `Vec::with_capacity(estimated_edges)` for the edge buffer + the single bulk `Graph::add_edges` call) | constructors/adjacency.c (igraph_adjacency + per-mode emit helpers + adjust_loop_edge_count, lines 75-386) | ~620 | adapt | none (foundational — every dense-matrix-input constructor reduces to it) | done | (now) | O(\|V\|² + \|E\|) — full matrix traversal + bulk `Graph::add_edges`; bench: M3 (3x3) all-mode dispatch 550-860 ns (10-20 Melem/s — Graph::new + add_edges + mode dispatch overhead dominates at this size), n=128 sparse directed (\|E\|=128) ~27.3 µs (4.7 Melem/s — n² traversal dominates over emission), n=128 dense directed (\|E\|≈16k) ~466 µs / max ~217 µs (~35-37 Melem/s — emission throughput sweet spot, edge buffer L1-resident), n=512 dense max (\|E\|≈130k) ~4.32 ms (~30 Melem/s — slight tail-off attributable to L1 misses on per-vertex adjacency Vec growth); python-igraph wraps as `Graph.Adjacency` but the per-call cost is dominated by the Cython bridge so the cross-source ns/edge comparison is not meaningful | C:11 / py:4 / R:3 = 18 total |
| ALGO-CN-030 | `weighted_adjacency` (igraph `igraph_weighted_adjacency` deterministic dense real-valued adjacency-matrix constructor — sibling of CN-029 that consumes an `n × n` `f64` matrix `matrix: &[&[f64]]` whose entries are *signed* edge weights and returns `(Graph, Vec<f64>)` where the second vector aligns one-to-one with the emitted edge ids; reuses the seven-flavoured `AdjacencyMode` enum (`Directed`, `Undirected`, `Max`, `Min`, `Plus`, `Upper`, `Lower`) and three-flavoured `LoopsMode` enum (`NoLoops`, `Once`, `Twice`) from CN-029 so signature `pub fn weighted_adjacency(matrix: &[&[f64]], mode: AdjacencyMode, loops: LoopsMode) -> IgraphResult<(Graph, Vec<f64>)>` slots into the same dispatch path; key differences from CN-029 — (a) zero is now the "no-edge" sentinel rather than a multiplicity (so `A(i, j) != 0.0` triggers a *single* edge emission with weight `A(i, j)`, not `A(i, j)` copies of an unweighted edge), (b) negative entries are *accepted* as signed weights (the integer-multiplicity "non-negative" guard from CN-029 is intentionally absent), (c) NaN handling is explicit per-mode following upstream `references/igraph/src/constructors/adjacency.c:550-680`: MAX uses `m1 < m2 || m2.is_nan()` (NaN propagates from the second argument), MIN uses `m1 > m2 || m2.is_nan()` (symmetric), PLUS is bare `f64 + f64` (NaN propagates per IEEE 754), and the Undirected symmetry check is NaN-tolerant via `m1 != m2 && !(m1.is_nan() && m2.is_nan())` so `[[NaN], [NaN]]` is considered symmetric, (d) loop-weight adjustment under `LoopsMode` differs from CN-029's multiplicity halving: `NoLoops` zeros every diagonal entry as before, `Twice` halves the diagonal weight via `w / 2.0` for non-collapsing modes (`Undirected`, `Max`, `Min`, `Plus`) since the matrix stores `2·w_loop` in that layout (matching upstream's `weight / 2.0` rather than CN-029's "even integer" check), and `Once` / `Twice→Once` for collapsing modes (`Directed`, `Upper`, `Lower`) keeps the diagonal weight unhalved; type-level error elimination — the upstream C entry takes `igraph_matrix_t` (a 2D dense matrix of `igraph_real_t`); the only surviving structural errors are "ragged matrix" → `InvalidArgument`, "asymmetric matrix in Undirected mode" → `InvalidArgument` (NaN-tolerant compare per (c)); no "odd diagonal" error survives because halving by 2 always succeeds for `f64`; algorithm: byte-for-byte port of `references/igraph/src/constructors/adjacency.c:485-680` — (1) validate `matrix.len() == matrix[0].len()` (square + ragged check), (2) `nrow as u32` cast guarded by `u32::try_from(nrow)`, (3) dispatch on `mode` to one of six per-mode emit helpers (`emit_directed`, `emit_max`, `emit_min`, `emit_plus`, `emit_upper`, `emit_lower` — `Undirected` checks `A == Aᵀ` (NaN-tolerant) then falls through to `emit_max`), each helper traversing the matrix in a mode-specific order and computing the per-cell weight via the mode's reduction (`Max`/`Min`/`Plus` reduce `(A(i,j), A(j,i))` to a single off-diagonal value, `Upper`/`Lower` skip the symmetric half), then `adjust_loop_weight(diagonal_entry, mode, loops)` produces the actual self-loop weight per vertex; (4) accumulate edges into `Vec<(u32, u32)>` paired with a `Vec<f64>` weights buffer (both `Vec::with_capacity(estimated_edges)`), single bulk `Graph::add_edges` call returns the edge ids, and the weights vector aligns to `g.edge(eid)` for every `eid ∈ 0..ecount`; runtime overflow protection: `u32::try_from` at every cross-domain `usize → u32` cast (only theoretically reachable for adversarial `nrow > u32::MAX`), `usize::checked_mul` on the edge buffer capacity pre-size; structural properties verified by 18 unit + 5 proptest: vcount equals matrix side length, zero-matrix yields zero edges across every mode, NaN inputs propagate per-mode (MAX/MIN return NaN when either operand is NaN, PLUS returns NaN bare), `Twice → Once` short-circuit for collapsing modes verified by output equality across all signed-weight test matrices, halving rule for non-collapsing modes verified by `loops=Twice` weights being exactly half of `loops=Once` weights on identical diagonal entries, Undirected with NaN-symmetric input does NOT reject, Undirected with asymmetric input rejects, `Max(A, A) == A` (with NaN propagation), `Min(A, A) == A` (likewise), `Upper + Lower` weights match the asymmetric `Directed` walk on off-diagonal cells, proptest sweep across `n ∈ [0, 4]`, every cell `∈ f64::arbitrary()` (including signed and zero values), all 7 modes × 3 loops combinations checked for invariant preservation (vcount == n, ecount bounded by `n*(n-1)/2` for symmetric modes and `n*(n-1)` for `Directed`, no panics, deterministic re-runs produce identical output); conformance: python-igraph exposes `Graph.Weighted_Adjacency(matrix, mode='directed', attr='weight', loops='once')` (note: the C `IGRAPH_NO_LOOPS`/`IGRAPH_LOOPS_ONCE`/`IGRAPH_LOOPS_TWICE` map to py strings `'ignore'`/`'once'`/`'twice'`) and R-igraph exposes `graph_from_adjacency_matrix(adjmatrix, mode='directed', weighted=TRUE, diag=TRUE)` with the same 7-mode dispatch but only `diag={TRUE,FALSE}` (no native `Twice` mode — uses `Once` semantics on `TRUE`); cross-source emission order is NOT byte-stable, so conformance test compares `((canonical_u, canonical_v), weight.to_bits())` multiset; fixture catalogue covers C:11 (M3=`[[2.0,0.5,0.0],[1.5,0.0,2.0],[0.0,2.5,3.0]]` and M3_SYM symmetric variant across all 7 modes plus the empty `0×0` and singleton `1×1` shapes + the `Twice → Once` collapse on `Directed` and `Upper`), py:4 (3×3 directed `loops='ignore'`, 3×3 max `loops='ignore'`, 3×3 plus `loops='once'`, 3×3 upper `loops='twice'` collapsed), R:3 (3×3 directed `diag=FALSE`, 3×3 undirected `diag=TRUE`, 3×3 lower `diag=FALSE`); conformance test decodes `params.matrix/mode/loops` JSON shapes into the typed Rust enums and verifies `((canonical_u, canonical_v), weight.to_bits())` multiset equality (NaN compares equal under bit-aware key); deterministic, no RNG, no allocations beyond `Vec::with_capacity(estimated_edges)` for the edge and weights buffers + the single bulk `Graph::add_edges` call) | constructors/adjacency.c (igraph_weighted_adjacency + per-mode weighted emit helpers + adjust_loop_weight, lines 485-680) | ~700 | adapt | CN-029 (adjacency — shares `AdjacencyMode` / `LoopsMode` enums + per-mode dispatch shape; this AWU is the f64-weights sibling) | done | (now) | O(\|V\|² + \|E\|) — full matrix traversal + bulk `Graph::add_edges`; bench: M3 (3×3) all-mode dispatch 343-668 ns, n=128 sparse directed (\|E\|=128) ~14.7 µs (8.7 Melem/s, ~2× faster than CN-029 sibling because each weight emit is one `f64` write rather than CN-029's multiplicity push loop), n=128 dense directed (\|E\|=128·127) ~468 µs / dense max (\|E\|=128·127/2) ~211 µs (~34-39 Melem/s, +10-20% over CN-029's integer baseline at the same dense shape because no per-cell multiplicity multiplier), n=512 dense max (\|E\|=130k) ~4.25 ms (~30 Melem/s — same L1-pressure tail-off as CN-029); python-igraph wraps as `Graph.Weighted_Adjacency` but the per-call cost is dominated by the Cython bridge so the cross-source ns/edge comparison is not meaningful | C:11 / py:4 / R:3 = 18 total |
| ALGO-CN-028 | `extended_chordal_ring` (igraph `igraph_extended_chordal_ring` deterministic chordal-ring (a.k.a. "extended cycle with chord matrix") constructor — given a vertex count `nodes: u32`, a chord-offset matrix `w: &[&[i64]]` where each row defines a family of chord offsets and the row length is the "period" `p` (a divisor of `nodes`), and a `directed: bool` flag, materialises the union of (a) the cycle `C_nodes` and (b) for every vertex `i ∈ [0, nodes)` and every row `r ∈ [0, m)` of `w`, the chord edge `(i, (i + w[r][i mod p]) mod nodes)`; signature `pub fn extended_chordal_ring(nodes: u32, w: &[&[i64]], directed: bool) -> IgraphResult<Graph>`; type-level error elimination — the upstream C entry takes `igraph_matrix_t` (a 2D dense matrix that can hold non-integer or NaN entries even though it is interpreted as offsets), which becomes statically unreachable here since `&[&[i64]]` rules out non-integer offsets; the only structural error that survives is "ragged matrix" (rows of different length) which returns `IgraphError::InvalidArgument("extended_chordal_ring: matrix W must have rows of equal length")`; semantics: `directed = true` emits each cycle edge and each chord edge as a single forward arc (cycle as `(0, 1), (1, 2), …, (n-1, 0)`; chords as `(i, target)`), `directed = false` produces the same edge multiset but with `Graph::edge()` returning canonical `(min, max)` endpoints; negative chord offsets `w[r][j] < 0` are accepted and computed via `i64::rem_euclid(nodes)` so `w = [[-3]]` for `n = 5` gives the same chord set as `w = [[+2]]` (the upstream `(i + w) mod n` with C's signed modulo is replicated faithfully); the algorithm is a verbatim byte-for-byte port of `references/igraph/src/constructors/regular.c:868-963`: (1) validate `nodes >= 3` and `nrow(w) > 0 → period > 0 ∧ nodes % period == 0` with `InvalidArgument` per upstream guard, (2) pre-size the edge buffer `Vec<(u32, u32)>::with_capacity(nodes + nodes · nrow(w))` with `usize::checked_mul` + `checked_add` overflow protection so adversarial `nodes = u32::MAX` reports `Overflow` rather than silently truncating, (3) emit the cycle in emission order `(0, 1), (1, 2), …, (n-2, n-1), (n-1, 0)` via a single `for i in 0..(nodes - 1)` loop + the explicit wrap-around push, (4) for every vertex `i ∈ [0, nodes)` and every row `r ∈ [0, m)` of `w` compute `target = (i + w[r][i mod period]).rem_euclid(nodes)` and push `(i, u32::try_from(target)?)` — chord emission order is per-vertex then per-row (matches upstream's `for (i = 0; i < nodes; i++) for (j = 0; j < nrow; j++)`); degenerate inputs handled inline ahead of the cycle emission — `nodes < 3` returns `InvalidArgument("extended_chordal_ring: nodes must be at least 3")` (matching upstream's `igraph_extended_chordal_ring(…, nodes < 3) → IGRAPH_EINVAL` cycle-too-short guard), `nrow(w) == 0` (empty matrix) returns the pure cycle `C_nodes` skipping the chord pass entirely (matches upstream's `if (nrow == 0) skip chord pass`), `period == 0` (a row exists but has length 0) returns `InvalidArgument("extended_chordal_ring: period (W row length) must be positive when W has rows")`, `nodes % period != 0` returns `InvalidArgument("extended_chordal_ring: period must divide nodes")`; runtime overflow protection: `i64::rem_euclid` for safe negative-offset modular arithmetic, `u32::try_from` on every chord target so any algorithmic invariant break (target < 0 after rem_euclid would be a bug, not adversarial input) surfaces as `IgraphError::Internal` rather than truncating, `usize::checked_mul(nodes as usize, nrow + 1)` for the edge buffer capacity; structural properties verified by 17 unit + 6 proptest: vcount always equals `nodes`, ecount always equals `nodes + nodes · nrow(w)` (cycle + per-vertex per-row chord), empty W yields pure cycle with `ecount == nodes` (verified on n ∈ {3, 5, 8, 12}), pentagram pos/neg-equivalent invariant `extended_chordal_ring(5, [[+2]], dir) ≡ extended_chordal_ring(5, [[-3]], dir)` byte-for-byte on canonical multiset, article-12-multigraph reproduces the 36-edge multiset of upstream `tests/unit/igraph_extended_chordal_ring.c` case 2 byte-for-byte (12 backbone × 1 + 12 distinct chords × 2 = 36 edges, every chord appearing twice as endpoints i and target both emit it), `nodes < 3` rejected, `period == 0` rejected (when nrow > 0), `period ∤ nodes` rejected, ragged W (rows of different length) rejected, directed and undirected variants share the same canonical edge multiset, proptest sweep across `nodes ∈ [3, 20], period ∈ [1, nodes], nrow ∈ [0, 3]` with random integer offsets `∈ [-50, 50]`; conformance: python-igraph 0.11.x exposes NO `Graph.Extended_Chordal_Ring` binding (verified via `hasattr(igraph.Graph, 'Extended_Chordal_Ring') == False` against the live `.venv/` installation), so this AWU runs a two-source rather than three-source conformance test (mirroring the precedent set by CN-024 hexagonal_lattice and CN-027 turan for absent-binding lanes); R-igraph exposes `make_chordal_ring(n, w, directed = FALSE)` which dispatches to `igraph_extended_chordal_ring()` C entry sharing the per-call cycle + per-vertex per-row chord emission with the C lane; cross-source emission order is byte-stable across C and R, but the conformance test compares directed-aware multiset (`(u, v)` distinct from `(v, u)` when directed, canonical `(min, max)` when undirected) for safety; fixture catalogue covers C:3 (the three cases from `references/igraph/tests/unit/igraph_extended_chordal_ring.c` — pentagram-positive `(n=5, W=[[+2]], directed)`, pentagram-negative-equivalent `(n=5, W=[[-3]], directed)`, article-12-multigraph `(n=12, W=[[4,2],[8,10]], undirected)`), py:0 (no binding), R:3 (`make_chordal_ring(8, matrix(2))` n=8 period-1, `make_chordal_ring(9, matrix(c(2,3,4), 1, 3))` n=9 period-3 single-row, `make_chordal_ring(10, matrix(c(2,3,4,5), 2, 2))` n=10 period-2 two-row); deterministic, no RNG, no allocations beyond the pre-sized edge buffer + the single bulk `Graph::add_edges` call) | constructors/regular.c (igraph_extended_chordal_ring, lines 868-963) | ~520 | adapt | CN-001 (ring_graph — cycle backbone precedent) | done | (now) | O(\|V\| + \|E\|) — single cycle emission + 2-deep per-vertex per-row chord pass; bench: n=5 pentagram (10 edges) 11.6 Melem/s (Graph::new + add_edges overhead dominates), n=12 article multigraph (36 edges) 18.6 Melem/s, n=256 single-row (512 edges) 27.8 Melem/s, n=512 period-4 3-row (2048 edges) 28.5 Melem/s (sweet spot — edge buffer L1-resident), n=4096 single-row (8192 edges) 24.3 Melem/s, n=2048 period-4 3-row (8192 edges) 24.5 Melem/s (~8k-edge buffer pushes out of L1 but stays L2-resident); python-igraph has no `Graph.Extended_Chordal_Ring` baseline (`hasattr` check fails on the .venv install) so the cross-source delta is N/A by construction | C:3 / py:0 / R:3 = 6 total |
| ALGO-CN-027 | `turan` (igraph `igraph_turan` deterministic Turán graph constructor — given a vertex count `n: u32` and a partition count `r: u32`, materialises the unique (up to isomorphism) `n`-vertex graph that maximises the number of edges subject to containing no clique of size `r + 1` (Turán's 1941 theorem); concretely the complete `r'`-partite graph with maximally balanced partition sizes where `r' = min(r, n)`, `q = n / r'`, `s = n % r'`, the first `s` partitions get size `q + 1`, and the remaining `r' − s` partitions get size `q`; signature `pub fn turan(n: u32, r: u32) -> IgraphResult<FullMultipartite>`; returns `FullMultipartite { graph: Graph, types: Vec<u32> }` so the partition layout is recoverable from the result (mirroring the precedent set by CN-026); semantics: always undirected (matching upstream — Turán graphs are not defined as directed objects); algorithm: thin wrapper over `full_multipartite(&partitions, false, MultipartiteMode::All)` — compute `r_effective = r.min(n)`, allocate `partitions: Vec<u32>` of length `r_effective` via a single `for i in 0..r_effective` loop that pushes `q + 1` when `i < s` and `q` otherwise (matches upstream's `igraph_vector_int_fill(quotient)` + per-index `++` post-loop), then dispatch — so the entire inter-partition edge emission path is shared with CN-026 and the implementation is one helper away from the wrapped call; degenerate inputs handled inline ahead of any work — `n == 0` short-circuits to `full_multipartite(&[], false, All)` returning the empty graph with empty `types` regardless of `r` (matching upstream's `if (n == 0) return IGRAPH_SUCCESS` after `igraph_empty`), `r == 0 ∧ n > 0` returns `IgraphError::InvalidArgument("turan: number of partitions must be positive when n > 0")` (mirroring upstream's `IGRAPH_EINVAL`), `r > n` silently caps to `r_effective = n` yielding `K_n` with `r' = n` singleton partitions and `types = [0, 1, …, n − 1]` (matching upstream's `r = n` reassignment); runtime overflow protection: `quotient.checked_add(1)` for the `q + 1` partition size so adversarial inputs cannot silently wrap (the only arithmetic-overflow risk in the wrapper itself); the wrapped `full_multipartite` carries the heavier `u64.checked_add` for the `n_acc` prefix sums and `usize::checked_mul` for the edge buffer capacity; structural properties verified by 15 unit + 6 proptest: vcount always equals `n`, ecount matches the closed-form `E = ½ · Σ n_i · (N − n_i)` (verified independently for `T(13, 4) = 63`, `T(8, 3) = 21`, `T(6, 3) = 12`, `T(12, 4) = 54`), partition sizes always differ by at most one (the Turán-balance invariant), types vector is partition-major monotone non-decreasing, no edge crosses inside a single partition (the multipartite invariant — `types[u] ≠ types[v]` for every emitted edge), graph is always undirected, `T(n, n + extra) ≡ T(n, n)` for every `extra ≥ 0` (the r-capping invariant), `T(13, 4)` is isomorphic to `full_multipartite(&[4, 3, 3, 3], false, All)` byte-for-byte (same edge multiset + same types), all 6 upstream `.out` fixtures reproduce byte-for-byte (`igraph_turan(0, 10)` empty, `igraph_turan(10, 1)` 10 isolated vertices, `igraph_turan(4, 6)` capped to K_4, `igraph_turan(13, 4)` with `types = [0,0,0,0,1,1,1,2,2,2,3,3,3]` and 63 edges, `igraph_turan(8, 3)` with `types = [0,0,0,1,1,1,2,2]` and 21 edges, `igraph_turan(6, 3)` the octahedron `K_{2,2,2}` with 12 edges), and proptest sweep across `n ∈ [0, 30], r ∈ [1, 10]`; conformance: python-igraph 0.11.x exposes NO `Graph.Turan` binding (verified via `hasattr(igraph.Graph, 'Turan') == False` against the live `.venv/` installation), so this AWU runs a two-source rather than three-source conformance test (mirroring the precedent set by CN-024 hexagonal_lattice for absent-binding lanes); R-igraph exposes `make_turan(n, r)` which dispatches to `turan_impl()` → `igraph_turan()` C entry, sharing the per-call edge emission with the C lane; cross-source emission order is byte-stable across C and R (both walk the wrapped `full_multipartite` source-major sweep), but the conformance test compares canonical `(min, max)` edge multiset + exact types vector for safety; fixture catalogue covers C:6 (the 6 `.out` cases from `references/igraph/tests/unit/igraph_turan.out` — n=0/r=10 empty guard, n=10/r=1 isolated set, n=4/r=6 K_4 cap, n=13/r=4 balanced-with-remainder canonical, n=8/r=3 with sizes [3,3,2], n=6/r=3 octahedron), py:0 (no binding), R:3 (n=10/r=3 with sizes [4,3,3], n=7/r=4 with sizes [2,2,2,1], n=9/r=3 balanced K_{3,3,3}); deterministic, no RNG, no allocations beyond `Vec::with_capacity(r_effective)` for the partition sizes + whatever `full_multipartite` allocates internally) | constructors/full.c (igraph_turan, lines 281-325) | ~115 | adapt | CN-026 (full_multipartite — entire edge emission path) | done | (now) | O(\|V\| + \|E\|) — dominated by the wrapped `full_multipartite` call; bench: T(6, 3) octahedron 12.6 Melem/s (12 edges — overhead-dominated by `Graph::new` + `add_edges` + partition `Vec<u32>` allocation), T(13, 4) 28.3 Melem/s (63 edges — partial L1 warm-up), T(96, 3) balanced 42.3 Melem/s / T(128, 4) balanced 41.6 Melem/s (~3-6k edges, edge buffer L1-resident — sweet spot), T(101, 3) skewed 42.0 Melem/s (the per-partition branch is a single index compare, indistinguishable from balanced), T(200, 7) skewed 35.2 Melem/s (17k-edge buffer spills out of L1 but stays L2-resident); python-igraph has no `Graph.Turan` baseline (`hasattr` check fails on the .venv install) so the cross-source delta is N/A by construction | C:6 / py:0 / R:3 = 9 total |
| ALGO-CN-026 | `full_multipartite` + `FullMultipartite` + `MultipartiteMode` (igraph `igraph_full_multipartite` deterministic complete multipartite (and bipartite) constructor — given a slice of per-partition sizes `partitions: &[u32]` of length `k ≥ 0`, a `directed` flag, and a `MultipartiteMode` ∈ {`All`, `Out`, `In`}, materialises the unique complete multipartite graph `K_{n_0, n_1, …, n_{k-1}}` on `N = Σ n_i` vertices where every pair `(u, v)` with `u` in partition `t_u`, `v` in partition `t_v`, and `t_u ≠ t_v` is connected; returns `FullMultipartite { graph: Graph, types: Vec<u32> }` with the `types` vector mapping every vertex id to its 0-based partition index; signature `pub fn full_multipartite(partitions: &[u32], directed: bool, mode: MultipartiteMode) -> IgraphResult<FullMultipartite>`; type-level error elimination — the upstream C entry takes `igraph_vector_int_t` and validates "no negative entries" with `IGRAPH_EINVAL`, which becomes statically unreachable here (`&[u32]` cannot be negative); `MultipartiteMode` is a local enum (matching the project pattern of per-constructor mode enums next to `StarMode`, `WheelMode`, `TreeMode`) that maps directly to `IGRAPH_OUT` / `IGRAPH_IN` / `IGRAPH_ALL`; semantics: `directed = false` ignores the mode and emits each cross-partition pair once in canonical `(min, max)` order, `directed = true, mode = Out` emits each cross-partition pair as a single arc in source-major order with the source always belonging to the lower-indexed partition (`from_type < to_type`), `directed = true, mode = In` emits the same pairs with endpoints reversed (so the source belongs to the higher-indexed partition), `directed = true, mode = All` emits both directions for every cross-partition pair (arc count doubles); algorithm: byte-for-byte port of `references/igraph/src/constructors/full.c:154-247` — first compute `n_acc[v] = Σ_{i<v} n_i` for `v ∈ [0, k]` via `u64.checked_add` (saving the partition-prefix-sum so the types vector can be built in one O(N) pass and the inner emission can index "next-partition-start" in O(1)), then build the types vector with a `while v < no_of_types && (i as u64) == n_acc[v] { v += 1; }` loop (the `while` — not `if` — is critical: it correctly handles consecutive empty partitions like `partitions = [2, 0, 3]` where two adjacent `n_acc` entries are equal and the empty middle partition still occupies a label slot, yielding `types = [0, 0, 2, 2, 2]` rather than `[0, 0, 1, 1, 1]`), pre-size the edge buffer via `Vec::with_capacity(E)` where `E` is derived from the upstream `IGRAPH_SAFE_MULT` formula `(1/2) · Σ_i n_i · (N - n_i)` (mode-aware: `All` doubles, `In`/`Out` stays as the undirected count), then run the 4-deep emission walk `for from_type in 0..k { for i in n_acc[from_type]..n_acc[from_type + 1] { for to_type in from_type + 1..k { for j in n_acc[to_type]..n_acc[to_type + 1] { emit (i, j) or (j, i) per mode } } } }` matching `full.c:201-225`; mode dispatch is inside the innermost loop with three branches (one branch per mode) — undirected collapses to the `Out` branch since the directed-vs-undirected flag is encoded only at `Graph::new` time; runtime overflow protection: `u64.checked_add` for `n_acc` cumulative sum (so adversarial `partitions = [u32::MAX, u32::MAX]` reports `IgraphError::Overflow` rather than silently wrapping), `u32::try_from` on the final `N = n_acc[k]` so vcount cannot exceed `u32::MAX`, and `usize::checked_mul` on the edge buffer capacity so the pre-sizing cannot truncate; degenerate inputs handled inline — `partitions = []` returns the empty graph with empty types regardless of `directed`/`mode` (matching upstream early-return), `partitions = [n]` returns `n` isolated vertices in partition 0 with no edges (the inner emission loop has nothing to iterate since `from_type + 1..k` is empty), `partitions` containing zeros simply skips that partition at the edge level while still occupying a label slot in the types vector; structural properties verified by 14 unit + 6 proptest: vcount equals partition sum, every emitted edge satisfies `types[u] ≠ types[v]` (the multipartite invariant), types vector is monotone non-decreasing (partition-major), `directed = true, mode = All` ecount equals `2 · directed-Out` ecount, `Out` and `In` are reverse-of-each-other when intersected as ordered arc sets, undirected canonical multiset equals directed-Out canonical multiset, partition-skip rule for `partitions = [2, 0, 3]` produces `types = [0, 0, 2, 2, 2]` and `ecount = 12` (the 2×6 mutual arcs of `K_{2,3}`), all 7 upstream `.out` fixtures reproduce byte-for-byte (`igraph_full_multipartite.c` tests `[2,0,3] undir`, `[2,0,3] dir all`, `[2,3,4,2] undir`, `[2,3,4,2] dir all`, `[2,3,4,2] dir in` with **44** arcs not 45 — verified by direct .out read, `[2,3,4,2] dir out`, `[] dir`), and proptest sweep verifies the invariants across random `partitions` shapes; conformance: python-igraph exposes `Graph.Full_Multipartite(n, directed=False, mode='all')` plus the bipartite shortcut `Graph.Full_Bipartite(n1, n2, directed=False, mode='all')` and R-igraph exposes `make_full_multipartite(c(n1, n2, …), directed=FALSE, mode='all')` plus `make_full_bipartite_graph(n1, n2, directed=FALSE, mode='all')` — all dispatch to the same `igraph_full_multipartite()` C entry, but cross-source edge emission order is NOT byte-stable (R re-canonicalises endpoints), so conformance test compares the canonical `(min, max)` edge multiset (directed-aware: `(u, v)` distinct from `(v, u)` when `directed`, canonical-pair when undirected) plus the exact types vector; fixture catalogue covers C:7 (the seven `igraph_full_multipartite.out` cases including the empty-graph guard and the empty-middle-partition `[2, 0, 3]` skip), py:3 (`Full_Multipartite([2, 3, 4], False, 'all')` from `testFullMultipartite`, `Full_Bipartite(3, 4, False)` bipartite shortcut from `testBipartite`, `Full_Multipartite([2, 3, 4, 2], True, 'out')` directed-out cross-mode check), R:3 (`make_full_multipartite(c(2, 3, 4), False, 'all')`, `make_full_bipartite_graph(3, 4, False, 'all')`, `make_full_multipartite(c(2, 3, 4, 2), True, 'all')` directed-all); deterministic, no RNG, no allocations beyond `Vec::with_capacity(E)` for the edge buffer + `Vec::with_capacity(N)` for the types vector + the single `Graph::add_edges` call) | constructors/full.c (igraph_full_multipartite, lines 154-247) | ~95 | adapt | CN-014 (full_graph — K_n multiset cross-check when k = N, every partition size 1) | done | (now) | O(\|V\| + \|E\|) — single source-major sweep with mode-aware emission; bench: K_{2,3} ~8.5–8.8 Melem/s (Graph::new + add_edges overhead dominates), K_{8,8,8} ~33–38 Melem/s (emit loop L1-resident, per-call setup amortizes), K_{32}^4 ~38–42 Melem/s (sweet spot — 6144 edges/buffer comfortably L1-resident), K_{16}^6 ~42 Melem/s (most balanced multipartite shape — 3840 edges spread across C(6,2)=15 partition pairs); directed-All slightly slower than the others because it emits 2× edges per pair (extra Vec pushes + larger add_edges sort) | C:7 / py:3 / R:3 = 13 total |
| ALGO-CN-025 | `full_citation` (igraph `igraph_full_citation` deterministic complete-graph constructor — given vertex count `n` and `directed` flag, materialises the unique complete graph on `n` vertices with the canonical citation-order emission: for each `i ∈ [1, n)` and each `j ∈ [0, i)` push edge `(i, j)`; signature `pub fn full_citation(n: u32, directed: bool) -> IgraphResult<Graph>`; semantics: `directed = true` yields the complete DAG where every arc `(i, j)` satisfies `i > j` — the "citation" invariant: newer paper cites older — and the edge multiset is `{(i, j) : 0 ≤ j < i < n}`; `directed = false` yields the undirected `K_n` whose edge multiset is identical to `full_graph(n, false, false)` but in descending-source-major emission order (vs `full_graph`'s ascending-source-major); edge count is exactly `n·(n-1)/2` for both directed and undirected variants (the directed walk emits each undirected pair `{i, j}` as a single arc `i → j` rather than the two arcs `i → j` and `j → i` that `full_graph(n, true, false)` would emit); degenerate inputs handled inline — `n == 0` returns the empty graph regardless of flag, `n == 1` returns a singleton with no edges; runtime overflow protection: `usize::checked_mul(n, n - 1)` followed by `/ 2` so the pre-sized edge buffer cannot exceed `usize::MAX` (mirroring upstream's `IGRAPH_SAFE_MULT(n, n-1, &no_of_edges2)` guard), and `usize` widening of `n` happens via `try_from` so an adversarial `n = u32::MAX` reports `IgraphError::Overflow` rather than silently truncating; structural properties verified by 12 unit + 6 proptest: every arc `(u, v)` in the directed variant satisfies `u > v` (the citation invariant — verified across `n ∈ [2, 30]`), `directed = true` yields exactly `n·(n-1)/2` arcs with zero self-loops and `in_degree(k) == n - 1 - k`, `out_degree(k) == k` (verified via local `in_out_counts` helper since `Graph` exposes only combined `degree`), `directed = false` yields the same edge multiset as `full_graph(n, false, false)` under canonical `(min, max)` multiset compare while differing in raw emission order, the directed and undirected variants always agree on ecount, `vcount()` round-trips for every `n`, and emission order matches the upstream byte-for-byte (`full_citation(4, true) == [(1, 0), (2, 0), (2, 1), (3, 0), (3, 1), (3, 2)]`); conformance: python-igraph exposes `Graph.Full_Citation(n, directed)` and R-igraph exposes `make_full_citation_graph(n, directed)` — both dispatch to the same upstream `igraph_full_citation()` C entry point, but cross-source edge emission order is **not** byte-stable (R re-canonicalises endpoints), so conformance test compares the canonical `(min, max)` multiset for undirected and the raw `(u, v)` multiset for directed, mirroring the precedent set by `check_linegraph_fixture`; fixture catalogue covers C:4 (`n=4` directed canonical DAG matching `igraph_full_citation.c`, `n=4` undirected K_4, `n=1` directed singleton, `n=0` directed empty), py:3 (`n=6` directed/undirected from `testFullCitation`, `n=2` directed degenerate single arc `1 → 0`), R:3 (`n=4` directed/undirected from `make_full_citation_graph`, `n=1` directed singleton); deterministic, no RNG, no allocations beyond the pre-sized edge buffer + the bulk `add_edges` call) | constructors/full.c (igraph_full_citation, lines 348-376) | ~60 | adapt | CN-014 (full_graph — K_n multiset cross-check) | done | (now) | O(\|V\| + \|E\|) = O(n²) — single pair-walk + `Graph::add_edges`; bench: n=8 ~20 Melem/s (Graph::new + add_edges overhead dominates), n=64 ~44 Melem/s (sweet spot — emit loop L1-resident yet per-call setup amortizes), n=512 ~31–32 Melem/s (130 816 edges; cost dominated by Graph::add_edges populating from/to/oi/ii arrays and sort-by-key into the CSR-like adjacency); directed vs undirected indistinguishable since the emission body is identical | C:4 / py:3 / R:3 = 10 total |
| ALGO-CN-013 | `kautz` (igraph `igraph_kautz` deterministic Kautz graph constructor — directed graph `K(m, n)` on `(m+1)·m^n` vertices labelled by length-`n+1` strings over an alphabet of `m+1` letters subject to the constraint that no two consecutive letters in a string may be equal; an arc goes from `v = (a_0, …, a_n)` to every `w = (a_1, …, a_n, b)` with `b ≠ a_n`, giving out-degree exactly `m` and in-degree exactly `m` for `n ≥ 1` (total arc count `m·(m+1)·m^n`) and zero self-loops because the rewrite forces source-`lastdigit` ≠ target-`lastdigit`; always directed (matches the fixed `IGRAPH_DIRECTED` of the C constructor); two-table encoding: `index1` of size `(m+1)^(n+1)` is a 1-based sentinel mapping sparse base-`(m+1)` string codes to dense vertex ids (`index1[code] = id+1`, `0` means "string is not valid"), and `index2` of size `vcount` is its inverse, both built by a single in-order cursor walk over the valid strings via `digits[]` + a positional-weight `table[i] = (m+1)^(n-i)` so each cursor advance does one digit increment with a skip-when-equal-prev rule; arc emission per source vertex `i` is source-major and target-ascending: `lastdigit = fromvalue mod (m+1)`, `basis = (fromvalue·(m+1)) mod allstrings` (hoisted once per source), then for `digit ∈ [0, m]` skip `digit == lastdigit` and emit `(i, index1[basis + digit] − 1)`; degenerate inputs handled by direct dispatch ahead of any table allocation — `n == 0` → directed `K_{m+1}` with no self-loops (the C source delegates to `igraph_full`; we inline the emission to avoid pulling in a not-yet-ported full-graph helper), `m == 0 ∧ n ≥ 1` → empty 0-vertex graph (single-symbol alphabet cannot satisfy the no-consecutive-equal constraint), `m == 0 ∧ n == 0` → singleton; runtime overflow protection: `u32::checked_pow` / `u32::checked_mul` / `u32::checked_add` on every dimension (`m+1`, `n+1`, `m^n`, `(m+1)·m^n`, `(m+1)^(n+1)`), `usize::checked_mul` for the edge buffer length, and a `u64` widening for the `(fromvalue·(m+1)) mod allstrings` basis so the intermediate product never silently truncates; structural properties verified by proptest sweep: exactly `(m+1)·m^n` vertices, exactly `m·(m+1)·m^n` arcs, every vertex has out-degree `m` and in-degree `m`, and the loopless-and-simple invariant holds (no `(v, v)` arcs and no duplicated `(u, v)` pairs); conformance: python-igraph exposes `Graph.Kautz(m, n)` and R-igraph exposes `make_kautz_graph(m, n)` — both dispatch to the same upstream `igraph_kautz()` C entry point so arc emission order is identical across all three sources; the conformance test compares the raw ordered arc list rather than the canonicalised multiset (any drift in emission order would be a real regression) and the fixture catalogue covers `K(2,1)`, `K(0,10)`, `K(0,0)`, `K(5,0)`, `K(3,1)`, `K(2,2)`, `K(3,2)` from the C oracle plus `K(2,1)`, `K(3,2)` from python-igraph and `K(2,1)`, `K(2,2)` from R; deterministic, no RNG) | constructors/kautz.c (igraph_kautz, lines 61-210) | ~210 | adapt | CN-012 (de_bruijn — sibling string-rewrite constructor) | done | (now) | O(\|V\| + \|E\|) = O((m+1)^(n+1)) two-pass (index tables + arc emission); throughput ~30–38 Marc/s in the L2-friendly band (k_4_4 / k_5_5) and ~24 Marc/s at the largest binary shape k_2_14 where the (m+1)/m × vcount sparse table dominates allocator pressure | C:7 / py:2 / R:2 = 11 total |

## Phase 3 — Centrality + Eigensolver (~65 AWU)

> Eigensolver scaffolding (ALGO-LA-*) is the prerequisite for the starred (★)
> centrality algorithms. Full per-AWU table to be expanded when Phase 2 nears
> completion. See MASTER_PLAN.md §5.2 Phase 3.

| ID | Task | C source | LOC | Complexity | Deps | Status | Commit | Big-O | Conformance |
|----|------|----------|-----|------------|------|--------|--------|-------|-------------|
| ALGO-EIG-001 | `eigen_matrix_symmetric` + `EigenWhich` + `EigenDecomposition` — public API for computing selected eigenvalues/eigenvectors of a real symmetric matrix defined by a matrix-vector product closure; wraps existing Lanczos solver (`community/lanczos.rs`); supports `LargestAlgebraic`, `SmallestAlgebraic`, `LargestMagnitude` spectrum targeting | eigen/symmetric.rs (wraps community/lanczos.rs) | ~100 | adapt | community/lanczos | done | 2026-06-02 | O(n·k·max_iter) Lanczos | 5 unit + 1 doctest |
| ALGO-EIG-002 | `eigen_adjacency` — compute selected eigenvalues of a graph's adjacency matrix; pre-builds edge list for allocation-free matvec; directed graphs use symmetrized `(A+A^T)/2`; delegates to `eigen_matrix_symmetric` | eigen/adjacency.rs | ~100 | adapt | ALGO-EIG-001 | done | 2026-06-02 | O(E·k·max_iter) Lanczos | 6 unit + 2 doctest |
| ALGO-EIG-003 | `eigen_matrix` + `GeneralEigenWhich` + `GeneralEigenDecomposition` — public API for computing selected eigenvalues/eigenvectors of a general (non-symmetric) real matrix; Arnoldi iteration builds upper Hessenberg projection, Francis double-shift QR iteration extracts eigenvalues (real or complex); supports `LargestMagnitude`, `SmallestMagnitude`, `LargestReal`, `SmallestReal`; complex eigenvalues returned as `(re, im)` pairs; inverse iteration for eigenvector recovery | eigen/general.rs | ~500 | novel | - | done | 2026-06-02 | O(n·m·max_iter) Arnoldi + O(m³) QR | 8 unit + 2 doctest |

## Phase 4-10 — see MASTER_PLAN.md

Each phase's per-AWU table is materialized here as work approaches.

### Phase 5 — Flow + Cut + MST

| ID | Task | C source | LOC | Complexity | Deps | Status | Commit | Big-O | Conformance |
|----|------|----------|-----|------------|------|--------|--------|-------|-------------|
| ALGO-FL-002 | `max_flow_value` (Dinic's algorithm — BFS level graph + DFS blocking flow; returns scalar max-flow value matching the igraph C push-relabel reference value bit-for-bit on unit-capacity fixtures and within `1e-9` on weighted fixtures; rejects `source == target` and `source/target >= vcount`; null capacity = unit capacity per edge) | flow/flow.c (igraph_maxflow_value entry; Dinic is self-rolled, not ported — picked over push-relabel for simpler code while preserving the unique optimum value) | ~400 | rewrite | core/Graph | done | 2026-05-26 | O(V²·E) | C: flow.c examples / py: maxflow_value / R: max_flow |
| ALGO-FL-010 | `st_mincut_value` (scalar s-t minimum-cut value; thin wrapper over `max_flow_value` justified by Ford-Fulkerson max-flow / min-cut duality — igraph C's counterpart at flow.c:1127 is a 5-line redirect; same error contract as the delegate) | flow/flow.c:1127 (`igraph_st_mincut_value`) | ~5 | wrapper | ALGO-FL-002 | done | 2026-05-26 | inherits FL-002 = O(V²·E) | C: tests/unit/igraph_st_mincut_value.c / py: test_flow.py:MinCutTests.testMinCutValue / R: tests/testthat/test-flow.R via `min_cut` |
| ALGO-FL-011 | `st_edge_connectivity` (number of edges whose removal disconnects `source` from `target`; thin wrapper over `max_flow_value` with NULL/unit capacity, cast to integer — mirrors `igraph_st_edge_connectivity` at flow.c:2219 (15 lines: rejects `source == target`, then `(igraph_int_t) max_flow_value`); returns `i64` to match igraph C's `igraph_int_t`) | flow/flow.c:2219 (`igraph_st_edge_connectivity`) | ~5 | wrapper | ALGO-FL-002 | done | 2026-05-26 | inherits FL-002 = O(V²·E) on unit caps = O(V³) per igraph C doc | C: tests/unit/igraph_st_edge_connectivity.c / py: test_flow.py:CutTests.testEdgeConnectivity / R: tests/testthat/test-flow.R (`edge_connectivity` with source+target) |
| ALGO-FL-012 | `edge_disjoint_paths` (maximum number of pairwise edge-disjoint `source → target` paths; Menger's theorem (1927) — thin wrapper over `max_flow_value` with NULL/unit capacity, cast to integer; mirrors `igraph_edge_disjoint_paths` at flow.c:2326 (15-line wrapper: rejects `source == target`, then `(igraph_int_t) max_flow_value`); equivalent to `st_edge_connectivity` by Menger, kept as separate Rust function for naming parity with igraph C) | flow/flow.c:2326 (`igraph_edge_disjoint_paths`) | ~5 | wrapper | ALGO-FL-002 | done | 2026-05-26 | inherits FL-002 = O(V²·E) on unit caps = O(V³) per igraph C doc | C: tests/unit/igraph_edge_disjoint_paths.c / py: test_flow.py:CutTests.testEdgeDisjointPaths / R: tests/testthat/test-flow.R (`edge_disjoint_paths`) |
| ALGO-FL-014 | `vertex_disjoint_paths` (maximum number of pairwise internally vertex-disjoint `source → target` paths; thin Menger wrapper over `st_vertex_connectivity` with `VconnNei::Ignore` plus the count of direct `source → target` arcs added back — mirrors `igraph_vertex_disjoint_paths` at flow.c:2374 (~30 lines: rejects `source == target`, computes `get_all_eids_between`, delegates to `igraph_i_st_vertex_connectivity_{directed,undirected}` with `IGRAPH_VCONN_NEI_IGNORE`, then adds direct-edge count); returns `i64` to match igraph C's `igraph_int_t`; cross-validated against `st_vertex_connectivity(VconnNei::NumberOfNodes)` upper bound — both encode "every direct edge counts as one path") | flow/flow.c:2374 (`igraph_vertex_disjoint_paths`) | ~15 | wrapper | ALGO-FL-013 | done | 2026-05-26 | inherits FL-013 = O(V·E²) on unit caps | C: tests/unit/igraph_vertex_disjoint_paths.c / py: test_flow.py:CutTests.testVertexDisjointPaths / R: tests/testthat/test-flow.R (`vertex_disjoint_paths`) |
| ALGO-FL-013 | `st_vertex_connectivity` (minimum number of internal vertices whose removal disconnects `source` from `target`; vertex-splitting reduction (Even §5.5): replace each vertex `v` with `v_out → v_in` arcs, rewrite each input arc `u → v` as `u_out → v_in`, zero out arcs incident on `source_in` and `target_out`, then unit-cap max-flow from `source_out` to `target_in` equals s-t vertex connectivity by Menger; `VconnNei::{Error, Negative, NumberOfNodes, Ignore}` parity with igraph C `igraph_vconn_nei_t` for direct-edge handling; undirected input is `IGRAPH_TO_DIRECTED_MUTUAL`-converted first; cross-validated against `st_edge_connectivity` upper bound via Whitney 1932) | flow/flow.c:1922 (`igraph_st_vertex_connectivity` + `_directed/_undirected/_check_errors` + `flow_conversion.c:61 igraph_i_split_vertices`) | ~250 | rewrite | ALGO-FL-002, ALGO-FL-011 | done | 2026-05-26 | O(V·E²) on unit caps via FL-002 backend | C: tests/unit/igraph_st_vertex_connectivity.c / py: test_flow.py:CutTests.testVertexDisjointPaths / R: tests/testthat/test-flow.R (`vertex_connectivity` with source+target) |
| ALGO-FL-015 | `vertex_connectivity` (global graph cohesion: minimum number of internal vertices whose removal disconnects some pair of distinct vertices — equal to `min_{s ≠ t} st_vertex_connectivity(s, t, NumberOfNodes)`; `checks: bool` parameter mirrors igraph C — when set, short-circuits via cheap helpers: (i) empty graph or disconnected (weak for undirected, strong for directed) → 0, (ii) any vertex with `min(out, in) = 1` → 1, (iii) complete graph → `vcount - 1`; otherwise pairwise iteration of FL-013 with `VconnNei::NumberOfNodes` mode, early-exit when min reaches 0; mirrors `igraph_vertex_connectivity` at flow.c:2158 + `igraph_i_vertex_connectivity_{directed,undirected}` at flow.c:1942-2067 + `igraph_i_connectivity_checks` at flow.c:2076; also exposes `igraph_cohesion` alias verbatim. Per igraph docstring "Vertex connectivity = group cohesion (Douglas R. White and Frank Harary, Sociological Methodology 31:305-359, 2001)") | flow/flow.c:2158 (`igraph_vertex_connectivity`) + flow.c:2076 (`_connectivity_checks`) + flow.c:1942-2067 (`_vertex_connectivity_{directed,undirected}`) + flow.c:2470 (`igraph_cohesion` alias) | ~120 | rewrite | ALGO-FL-013, ALGO-CC-001, ALGO-CC-002, ALGO-PR-066 | done | 2026-05-26 | O(V²) × FL-013 = O(V³·E²) on unit caps, igraph C doc says O(V⁵) | C: tests/unit/igraph_cohesion.c / py: test_flow.py:CutTests.testEdgeConnectivity (`vertex_connectivity()` no source/target) / R: tests/testthat/test-flow.R (`vertex_connectivity()` no source/target) |
| ALGO-FL-016 | `edge_connectivity` (global graph adhesion: minimum number of edges whose removal disconnects some pair of distinct vertices — equal to `min_{s ≠ t} st_edge_connectivity(s, t)`; `checks: bool` mirrors igraph C — when set, short-circuits via the shared `_connectivity_checks` helper: (i) empty/singleton graph or disconnected (weak undirected, strong directed) → 0, (ii) any vertex with `min(out, in) = 1` → 1. Deliberately does **not** short-circuit on complete graphs because completeness alone does not determine edge connectivity for multigraphs (see flow.c:2168-2180 comment). Otherwise runs the same fixed-vertex iteration as `igraph_mincut_value`: pick vertex 0 and call `st_edge_connectivity(0, v)` for every `v ≠ 0` (undirected) or both `(0, v)` and `(v, 0)` (directed); track the running minimum with early-exit at 0. This works because every global min-cut separates 0 from at least one other vertex. Mirrors `igraph_edge_connectivity` at flow.c:2270 → `igraph_mincut_value` at flow.c:1692 (without porting Stoer-Wagner). Also exposes the `igraph_adhesion` alias (flow.c:2433-2440)) | flow/flow.c:2270 (`igraph_edge_connectivity`) + flow.c:1692 (`igraph_mincut_value` directed branch) + flow.c:2076 (`_connectivity_checks`, shared with FL-015) + flow.c:2433 (`igraph_adhesion` alias) | ~110 | rewrite | ALGO-FL-002, ALGO-FL-011, ALGO-CC-001, ALGO-CC-002 | done | 2026-05-26 | undirected: O(V) × FL-011 = O(V³) on unit caps; directed: O(2V) × FL-011 = O(V³); igraph C doc says O(log V·V²) undirected (Stoer-Wagner, not ported) and O(V⁴) directed | C: tests/unit/igraph_edge_connectivity.c / py: test_flow.py:CutTests.testEdgeConnectivity (`edge_connectivity()` no source/target) / R: tests/testthat/test-flow.R (`edge_connectivity()` no source/target) |
| ALGO-FL-017 | `mincut_value` (global minimum-cut value — weighted generalisation of FL-016: returns the total minimum capacity of edges whose removal makes the graph not strongly connected; undirected ⇔ weakly disconnected; `capacity: Option<&[f64]>` mirrors igraph C — `None` means unit capacities. For `vcount ≤ 1` returns `f64::INFINITY` (mirrors `IGRAPH_INFINITY` init at flow.c:1699); otherwise runs the fixed-vertex iteration: pick vertex 0, call `max_flow_value(graph, 0, v, capacity)` for every `v ≠ 0` (undirected) or both `(0, v)` and `(v, 0)` (directed); track the running minimum with early-exit at `0.0`. Same correctness argument as FL-016 (every global min-cut separates 0 from some other vertex). Unlike igraph C, we **do not** port Stoer-Wagner for the undirected case — fixed-vertex iteration via FL-002 covers both directed and undirected with one code path, at the cost of asymptotic optimality on dense undirected inputs. Mirrors `igraph_mincut_value` at flow.c:1692-1730) | flow/flow.c:1692 (`igraph_mincut_value`) — directed branch (lines 1706-1723) used for both undirected and directed in our port | ~90 | rewrite | ALGO-FL-002 | done | 2026-05-26 | O(V) × FL-002 = O(V²·E²) undirected, O(2V) × FL-002 = O(V²·E²) directed; igraph C doc says O(log V · V²) undirected (Stoer-Wagner, not ported) and O(V⁴) directed | C: tests/unit/igraph_mincut.c / py: test_flow.py:CutTests.testMincutValue / R: tests/testthat/test-flow.R (`min_cut()` no source/target) |
| ALGO-FL-018 | `st_mincut` (s-t **minimum-cut partition** — value + cut edge set + source/target bipartition; thin wrapper over a refactored Dinic backend that exposes the residual graph. Mirrors `igraph_st_mincut` at flow.c:1176 (5-line redirect to `igraph_maxflow` with `flow=NULL`). Returns `StMincut { value: f64, cut: Vec<u32>, partition: Vec<u32>, partition2: Vec<u32> }`: `value` matches `st_mincut_value`; `partition` is the source-side vertex set (always contains `source`) computed by BFS from `source` in the residual graph after Dinic; `partition2 = V \ partition` (always contains `target`); `cut` is the set of edge IDs whose canonical endpoint ordering crosses the partition with positive original capacity (i.e., saturated cut edges). Refactors `max_flow.rs` into `pub(crate) fn max_flow_with_residual(...) -> IgraphResult<(f64, ResidualState)>` shared with `max_flow_value` (which becomes a thin wrapper that drops the residual). Rejects `source == target` and out-of-range vertex IDs, same as FL-002.) | flow/flow.c:1176 (`igraph_st_mincut`) + flow.c:980-1100 (`igraph_maxflow` partition-extraction tail) | ~180 | rewrite | ALGO-FL-002 | done | 2026-05-26 | inherits FL-002 = O(V²·E) + O(V + E) partition BFS | C: tests/unit/igraph_st_mincut.c / py: test_flow.py:CutTests.testMinCut (`mincut(source, target, capacity)` returns `Cut` object) / R: tests/testthat/test-flow.R (`min_cut(g, source, target, capacity)` returns list with value/cut/partition) |
| ALGO-FL-020 | `gomory_hu_tree` (Gomory-Hu cut tree — compact `V`-vertex / `V-1`-edge **weighted undirected** tree on the same vertex set as the input graph, encoding the s-t min-cut value for **every** vertex pair: the min cut between `u` and `v` in the original graph equals the *minimum* edge weight on the unique tree path from `u` to `v`. Uses **Gusfield's algorithm** (1990) — exactly `V - 1` calls to s-t max-flow / min-cut; each call drives a parent-pointer update over a `Vec<u32>`. Driver reuses the FL-018 `st_mincut` backend (returns the source-side partition + value the loop needs), so the Rust port carries no separate maxflow-with-partition shim. Output is `GomoryHuTree { tree: Graph, flows: Vec<f64> }` where `flows.len() == tree.ecount() == max(vcount - 1, 0)` and `flows[eid]` is the min-cut value for the two endpoints of tree edge `eid`. Rejects directed graphs with `InvalidArgument` (matching igraph C's `EINVAL` at flow.c:2533). `capacity: Option<&[f64]>` matches FL-002/FL-010/FL-017/FL-018 convention; `None` = unit weights. Empty graph (`vcount == 0`) returns an empty tree and empty flows; `vcount == 1` returns a one-vertex tree and empty flows.) | flow/flow.c:2479-2616 (`igraph_gomory_hu_tree`) | ~120 | rewrite | ALGO-FL-002, ALGO-FL-018 | done | 2026-05-26 | O(V) × FL-018 = O(V³·E) on weighted = O(V⁴) per igraph C doc | C: tests/unit/igraph_gomory_hu_tree.c / py: test_flow.py:CutTests.testGomoryHu (`gomory_hu_tree(capacity=...)`) / R: tests/testthat/test-flow.R (`gomory_hu_tree(g, capacity)`) |
| ALGO-FL-030 | `dominator_tree` (Lengauer-Tarjan dominator tree of a directed **flowgraph**. Given a directed graph and a root `r`, vertex `v` *dominates* vertex `w` iff every path from `r` to `w` passes through `v`; the *immediate dominator* `idom(w)` is the unique dominator of `w` closest to `w`. The edges `{(idom(w), w) | w ≠ r}` form a directed tree rooted at `r`. Implements the classical Lengauer-Tarjan 1979 algorithm with bucket-driven semi-dominator computation and LINK/COMPRESS/EVAL path-compression primitives: (1) one DFS from `root` to assign DFS numbers (`semi`), DFS-order vertex array (`vertex`), and parent pointers; (2) reverse pass `i = component_size-1 .. 1` computing semi-dominators via in-neighbours and `EVAL`, inserting into per-vertex buckets, then linking and processing buckets for the parent; (3) forward fixup `i = 1 .. component_size-1` propagating the immediate dominator when it differs from the semi-dominator. Returns `DominatorTree { idom: Vec<i32>, tree: Graph, leftout: Vec<u32> }`: `idom[v] = -1` for `root`, `-2` for vertices unreachable from `root`, else the immediate-dominator vertex id; `tree` is a directed graph with the same vcount as the input — edge orientations follow `mode` (`Out` ⇒ parent→child, `In` ⇒ child→parent) and unreachable vertices appear as isolates; `leftout` lists unreachable vertex ids in ascending order. `mode: DominatorMode` is `Out` (follow out-edges from `root`, standard control-flow dominator) or `In` (treat in-edges as forward, used for post-dominator trees); the `IGRAPH_ALL` mode is rejected by igraph C and equivalently disallowed by the enum. Rejects undirected graphs with `InvalidArgument`. Implementation lives in the crate (does not depend on the trimmed `dfs` AWU); the DFS is written inline as the iterative neighbour-cursor stack so we can capture both `parent` and the discovery order in one pass and avoid allocating callback boilerplate.) | flow/st-cuts.c:434-609 (`igraph_dominator_tree`); helpers st-cuts.c:286-375 (`dbucket`, LINK/COMPRESS/EVAL) | ~180 | rewrite | ALGO-CORE-001 | done | 2026-05-26 | O(\|V\| + \|E\| · α(\|E\|, \|V\|)) with path-compression `EVAL`, where α is the inverse Ackermann | C: tests/unit/igraph_dominator_tree.c (3 fixtures: 13-vertex `IGRAPH_OUT`, 13-vertex `IGRAPH_IN` reversed, 20-vertex with unreachable) + examples/simple/dominator_tree.c (10-vertex `IGRAPH_IN` with leftout) / py: test_flow.py (`dominator_tree(root, mode=)` on `Graph`) / R: tests/testthat/test-dominator-tree.R (`dominator_tree(g, root, mode)` returning list with `$dom`, `$domtree`, `$leftout`) |
| ALGO-FL-031 | `all_st_cuts` (list **every** (s,t) edge cut of a directed graph via the Provan-Shier 1996 paradigm. Enumerates each distinct minimal edge cut between `source` and `target` exactly once by walking a binary search tree over *closed* vertex sets `S` (the source side). At each node a **pivot** vertex `v` and its *closure increment* `I(S,v)` are computed from the post-dominator tree of the residual `Sbar = V \ S` (induced subgraph rooted at `target`, `DominatorMode::In`): recursing "right" adds `I(S,v)` to `S`, recursing "left" forbids `v` (pushes it on the `T` stack). A leaf with a proper non-trivial `S` (`0 < |S| < |V|`) yields one cut. Returns `StCuts { cuts: Vec<Vec<EdgeId>>, partition1s: Vec<Vec<VertexId>> }`: aligned per-cut lists where `partition1s[i]` is the source-side set (contains `source`, never `target`, sorted ascending) and `cuts[i]` lists the ids of edges leaving it (`from ∈ S`, `to ∉ S`, sorted ascending). Rejects undirected graphs and `source == target` with `InvalidArgument`; out-of-range endpoints with `VertexOutOfRange`. Reuses the `dominator_tree` (ALGO-FL-030) and `induced_subgraph` AWUs; the `igraph_marked_queue_int_t` batch stack and `igraph_estack_t` are reimplemented inline as private `MarkedQueue` / `EStack` structs. The `all_st_cuts_minimal` step is simplified to a direct idom-chain walk, proven equivalent to igraph's DFS-callback approach.) | flow/st-cuts.c:1030 (`igraph_all_st_cuts`); helpers st-cuts.c:665 (`minimal`), 722 (`pivot`), 928 (`provan_shier_list_recursive`) | ~460 | rewrite | ALGO-FL-030 | done | 2026-05-29 | O(\|cuts\| · (\|V\| + \|E\|)) — one residual induced-subgraph + dominator-tree build per recursion node, output-sensitive in the number of cuts | C: tests/unit/igraph_all_st_cuts.c (6-node canonical 9-cut fixture + diamond) / py: test_flow.py (`Graph.all_st_cuts(source, target)` → list of `Cut`, `cut.partition[0]` / `cut.es.indices`) / R: tests/testthat/test-flow.R (`all_st_cuts(graph, source, target)`) |
| ALGO-FL-032 | `all_st_mincuts` (list every **minimum** (s,t) edge cut of a directed graph via the Provan-Shier 1996 paradigm specialised to minimum cuts. Pipeline: (1) compute a maximum `source -> target` flow (value + per-edge flow); (2) build the **reverse residual** graph of that flow; (3) contract the residual by its **strongly connected components** and `simplify`, then `topological_sorting` + `permute_vertices` to relabel into a DAG where every minimum cut is a *closed set* (down-set); (4) mark projected vertices that are endpoints of a positive-flow edge as *active*; (5) run the shared Provan-Shier recursion (`provan_shier_list`, factored into `flow/provan_shier.rs`) with a max-flow/active-set **pivot** that walks in-neighbours of the topologically ordered residual; (6) map each closed set back to original vertices, cut edges = positive-flow edges leaving the source side. Returns `StMinCuts { value: f64, cuts: Vec<Vec<EdgeId>>, partition1s: Vec<Vec<VertexId>> }`: `value` is the min-cut/max-flow value, aligned per-cut lists where `partition1s[i]` is the source-side set (contains `source`, never `target`, sorted ascending) and `cuts[i]` lists the saturated edges leaving it (sorted ascending). The set of minimum cuts is **unique**, so conformance/unit comparison is done as a SET of (partition, cut) pairs, independent of enumeration order. `capacity: Option<&[f64]>` defaults to unit caps; all caps must be strictly positive. Rejects undirected graphs and `source == target` with `InvalidArgument`; out-of-range endpoints with `VertexOutOfRange`. Shares the `MarkedQueue` / `EStack` / `Pivot` scaffolding with ALGO-FL-031.) | flow/st-cuts.c:1335 (`igraph_all_st_mincuts`); helpers st-cuts.c:1156 (`igraph_i_all_st_mincuts_pivot`), 1110 (`minimal`) | ~580 | rewrite | ALGO-FL-031 | done | 2026-05-29 | O(\|mincuts\| * (\|V\| + \|E\|)) after one max-flow + SCC-contraction setup, output-sensitive in the number of minimum cuts | C: tests/unit/igraph_all_st_mincuts.c (path5 / parallel-middle / branching-chain fixtures) / py: test_flow.py (`Graph.all_st_mincuts(source, target)` -> list of `Cut`, `cut.value` / `cut.partition[0]` / `cut.es.indices`) / R: tests/testthat/test-flow.R (`all_st_mincuts(graph, source, target)`) |
| ALGO-CN-031 | `minimum_size_separators` (list **every minimum-size vertex separator** of an undirected graph — Arkady Kanevsky, "Finding all minimum-size separating vertex sets in a graph", Networks 23 (1993) 533–541. Mirrors `igraph_minimum_size_separators` (igraph C 0.10.16). Pipeline: (1) compute vertex connectivity `k = vertex_connectivity(graph, checks=true)`; (2) three early exits — `k ≤ 0` (already disconnected) ⇒ no separators; `k == 1` ⇒ the articulation points each as a singleton; `k == n-1` (complete graph) ⇒ every `(n-1)`-subset of vertices; (3) main branch `2 ≤ k ≤ n-2`: work on a `simplify`-ied copy, take the `k` largest-degree vertices `X`, record `X` itself if it separates, then for each `x ∈ X` and each non-adjacent `j` compute a max-flow on the Even–Tarjan reduction; whenever the flow value equals `k` enumerate **all** minimum `(x, j)` vertex cuts via `all_st_mincuts` and deduplicate, adding edge `(x, j)` after each pair so later flows only surface new separators. Returns `Vec<Vec<VertexId>>`: each separator sorted ascending, the collection unique with no duplicates, order arbitrary — so conformance compares a canonical SET of sorted vertex lists. Rejects directed graphs with `InvalidArgument`.) | connectivity/separators.c (`igraph_minimum_size_separators`) | ~340 | rewrite | ALGO-FL-032 (all_st_mincuts), ALGO-OP-029 (even_tarjan_reduction), ALGO-FL-015 (vertex_connectivity), ALGO-CC-010 (articulation_points), simplify, are_adjacent | done | 2026-05-29 | O(k · n) max-flows on the Even–Tarjan reduction + one all_st_mincuts enumeration per non-adjacent pair; output-sensitive in the number of separators | C: tests/unit/igraph_minimum_size_separators.c (star7 / K_{2,3} / authentic 10-vertex 0.10.16 fixture) / py: test_flow.py (`Graph.minimum_size_separators()` → list of vertex-set lists) / R: tests/testthat/test-flow.R (`minimum_size_separators(graph)`) — C:3 / py:3 / R:3 = 9 total |
| ALGO-CN-032 | `cohesive_blocks` (hierarchical **cohesive blocking** of an undirected graph — James Moody & Douglas R. White, "Structural Cohesion and Embeddedness: A Hierarchical Concept of Social Groups", American Sociological Review 68 (2003) 103–127. Mirrors `igraph_cohesive_blocks` (igraph C 0.10.16). Maintains a work queue of candidate vertex sets seeded with the whole graph; for each candidate it computes `vertex_connectivity`, enumerates all `minimum_size_separators` (Kanevsky), removes each separator and re-decomposes the remainder into components (re-attaching bordering separator vertices), and queues every resulting subset whose induced connectivity strictly exceeds the parent's. Candidates are deduplicated against already-queued sets via a sorted-subset containment test; a final prune+compaction pass drops any block that is a subset of another block with ≥ its cohesion, then rewrites the parent pointers and builds the block-inclusion tree. Returns `CohesiveBlocks { blocks: Vec<Vec<VertexId>>, cohesion: Vec<i64>, parent: Vec<i64>, block_tree: Graph }`: index-aligned, `blocks[0]` is the whole graph with `parent[0] = -1`, each block sorted ascending, the directed `block_tree` has one edge per non-root block. Block enumeration order is implementation-defined, so conformance compares a canonical SET of (sorted-block, cohesion) pairs.) | connectivity/cohesive_blocks.c (`igraph_cohesive_blocks`) | ~430 | rewrite | ALGO-CN-031 (minimum_size_separators), ALGO-FL-015 (vertex_connectivity), induced_subgraph, max_degree, is_simple, create | done | 2026-05-30 | per queued candidate: one vertex_connectivity + one minimum_size_separators (Kanevsky) call, each itself output-sensitive max-flow work; total cost is dominated by the per-block separator enumeration multiplied by the number of blocks discovered | C: tests/unit/igraph_cohesive_blocks.c (Moody-White 23-vertex figure / 'tricky' 8-vertex / Zachary-style science-camp) / py: test_structural.py (`Graph.cohesive_blocks()` → `CohesiveBlocks` VertexCover, `list(cb)` / `cb.cohesions()`) / R: tests/testthat/test-cohesive.R (`cohesive_blocks(graph)`, `blocks()` / `cohesion()`) — C:3 / py:3 / R:3 = 9 total |

### Phase 6 — Coloring + Matching + Isomorphism

| ID | Task | C source | LOC | Complexity | Deps | Status | Commit | Big-O | Conformance |
|----|------|----------|-----|------------|------|--------|--------|-------|-------------|
| ALGO-CL-001 | `vertex_coloring_greedy` (heuristic greedy vertex coloring with two ordering strategies: `ColoredNeighbors` (CN — pick uncolored vertex with most already-colored neighbors, mirrors `igraph_i_vertex_coloring_greedy_cn`) and `DSatur` (pick vertex with highest saturation degree, breaking ties by degree in uncolored subgraph, mirrors `igraph_i_vertex_coloring_dsatur`); lazy-deletion `BinaryHeap` replaces igraph C's custom indexed heap (`igraph_2wheap_t` / `igraph_gen2wheap_t`); CN uses 1-based internal colors (0 = uncolored) matching the C reference, subtracts 1 at return; DSATUR uses `Option<u32>` with `None` = uncolored, 0-based from start; `all_neighbors()` merges out+in for directed graphs, `all_neighbors_simple()` dedupes + removes self-loops for DSATUR adjacency; also provides three coloring validators: `is_vertex_coloring` (proper vertex coloring check, self-loops ignored), `is_bipartite_coloring` (bipartite 2-color check with directed edge-direction mode `Out`/`In`/`All` detection), `is_edge_coloring` (proper edge coloring check, self-loops not adjacent to themselves); all handle self-loops, multi-edges, directed graphs) | misc/coloring.c (519 lines: `igraph_i_vertex_coloring_greedy_cn` lines 32-138, DSATUR helpers 142-274, dispatcher 302-311, `igraph_is_vertex_coloring` 330-363, `igraph_is_bipartite_coloring` 388-453, `igraph_is_edge_coloring` 473-519) | ~600 | rewrite | core/Graph | done | 2026-05-27 | CN: O(V·d_max + E), DSATUR: O(V²·d_max) with lazy heap, validators: O(E) | C:3 / py:2 / R:2 |
| ALGO-CL-002 | `maximum_cardinality_search` + `is_chordal` (MCS: Tarjan-Yannakakis 1984 bucket-list maximum cardinality search producing a perfect elimination ordering `alpha` and its inverse `alpham1`; uses doubly-linked bucket lists with 1-based sentinel pointers matching igraph C; O(V+E). `is_chordal`: tests whether a graph is chordal using a pre-computed or freshly-computed MCS ordering; path-compression forest with mark/index arrays detects zero-fill-in; returns `ChordalResult { chordal: bool, fill_in: Vec<(u32, u32)> }` listing missing edges. `build_simple_adj` creates undirected simple adjacency for both directed and undirected input since chordality ignores edge directions.) | misc/chordality.c (477 lines: `igraph_maximum_cardinality_search` 67-231, `igraph_is_chordal` 274-477) | ~548 | adapt | core/Graph | done | 2026-05-27 | O(V + E) for MCS and is_chordal | C:2 / py:2 / R:2 |
| ALGO-CL-003 | `maximum_bipartite_matching` + `is_matching` + `is_maximal_matching` + `maximum_bipartite_matching_weighted` (Push-relabel algorithm for unweighted maximum cardinality bipartite matching with greedy initialization and global relabeling every n/2 steps; Hungarian algorithm (Kuhn-Munkres) for weighted maximum weight bipartite matching with phantom edges and label updates. `is_matching` validates matching vector: length, mutual pairing, edge existence, bipartite type check. `is_maximal_matching` additionally verifies no unmatched vertex has an unmatched cross-partition neighbor. Returns `MatchingResult { matching_size, matching_weight, matching: Vec<Option<u32>> }`.) | misc/matching.c (1013 lines: `igraph_is_matching` 71-125, `igraph_is_maximal_matching` 154-195, `igraph_maximum_bipartite_matching` 272-297, unweighted push-relabel 317-455, global relabel 457-512, weighted Hungarian 528-1009) | ~700 | rewrite | core/Graph | done | 2026-05-27 | Unweighted: O(√V · E), Weighted: O(V · E) | C:2 / py:2 / R:2 |
| ALGO-CL-004 | `cliques` + `independent_vertex_sets` (enumerate all cliques/independent vertex sets within a size range; recursive backtracking over vertices in order, extending current clique if candidate is adjacent to all existing members; `independent_vertex_sets` uses complement adjacency; `max_results` parameter for early termination; directed graphs treated as undirected) | cliques/cliques.c (`igraph_cliques` lines 144-236, `igraph_independent_vertex_sets` lines 357-430) | ~120 | adapt | core/Graph | done | 2026-05-28 | O(3^(V/3)) worst case (Bron-Kerbosch bound) | unit tests only |
| ALGO-CL-005 | `maximal_cliques_subset` (enumerate maximal cliques containing at least one vertex from a given subset; runs full Bron-Kerbosch then filters by subset membership, size bounds, and optional max_results cap; directed graphs treated as undirected; isolated vertices in subset produce size-1 cliques) | cliques/maximal_cliques_template.h (`IGRAPH_MAXIMAL_CLIQUES_SUBSET` macro variant, lines 1-378) | ~70 | adapt | core/Graph, maximal_cliques | done | 2026-06-01 | O(3^(V/3)) worst case (BK enumeration + linear filter) | unit tests (9) + doctest (1) |
| ALGO-CL-020 | `graphlets_candidate_basis` + `graphlets_project` + `graphlets` (graphlet decomposition: model edge weights as a superposition of dense overlapping subgraphs; `graphlets_candidate_basis` recursively finds maximal cliques at weight thresholds then filters non-maximal subsets to produce a `GraphletBasis { cliques, thresholds }`; `graphlets_project` iteratively projects weights onto the basis via NNLS-like clamped residual update producing non-negative µ coefficients; `graphlets` combines both steps; `build_subclique_graph` constructs subgraphs induced by each clique and recursively descends at the next weight threshold; faithful port of `igraph_graphlets` / `igraph_graphlets_candidate_basis` / `igraph_graphlets_project` from glet.c) | cliques/glet.c (882 lines: `igraph_i_subclique_next` 45-150, `igraph_i_graphlets` 153-257, `igraph_graphlets_candidate_basis` 260-311, `igraph_i_graphlets_filter` 336-433, `igraph_i_graphlets_project` 438-831, `igraph_graphlets` 860-882) | ~620 | adapt | core/Graph, maximal_cliques | done | 2026-05-31 | O(iterations · \|basis\| · max_clique_size²) for projection; basis discovery depends on clique enumeration | unit tests (18) + doctest (3) |
| ALGO-ISO-030 | `simplify_and_colorize` — turn a graph with self-loops/multi-edges into a vertex- and edge-colored simple graph for VF2-style backends: `vertex_color[v]` = number of self-loops on `v`, `edge_color[e]` = multiplicity of merged parallel edge `e` (each ≥1); reproduces `IGRAPH_EDGEORDER_FROM` by sorting edge ids by stored `(from,to)` so parallels group consecutively; undirected endpoints are canonicalised (`from≤to`) so `(u,v)`/`(v,u)` merge, directed arcs stay distinct; preserves vcount (isolated vertices survive) and directedness; result via `create`; returns `SimplifyAndColorize { graph, vertex_color, edge_color }` | isomorphism/isomorphism_misc.c:54-114 | ~135 | adapt | core/Graph, CN-create | done | 2026-05-29 | O(V + E log E) | unit tests + proptest + doctest |
| ALGO-ISO-001 | VF2 graph-isomorphism core + wrappers (`isomorphic_vf2`, `count_isomorphisms_vf2`, `get_isomorphisms_vf2`): backtracking VF2 engine over a candidate-pair search tree with feasibility pruning, optional vertex/edge color constraints and user compat callbacks; thin callback wrappers stop-at-first / count / collect-all mappings | isomorphism/vf2.c:137-1022 | ~885 | rewrite | core/Graph | done | 2026-05-29 | O(V!·V) worst case | C:2 / py:2 / R:2 |
| ALGO-ISO-002 | VF2 subgraph-isomorphism core + wrappers (`subisomorphic_vf2`, `count_subisomorphisms_vf2`, `get_subisomorphisms_vf2`): backtracking VF2 engine finding (non-induced) copies of pattern `graph2` inside target `graph1`; `>=` degree/terminal-set/look-ahead feasibility, one-directional edge-consistency, optional vertex/edge colour constraints; thin callback wrappers stop-at-first / count / collect-all mappings | isomorphism/vf2.c:1023-1741 | ~490 | rewrite | core/Graph, ISO-001 | done | 2026-05-29 | O(V1^V2 · V2) worst case | C:2 / py:2 / R:2 |
| ALGO-ISO-003 | `canonical_permutation` — hand-rolled individualization-refinement (I-R, nauty/bliss family) engine producing a canonical vertex labeling for simple graphs (directed + undirected, optional vertex colors); color refinement (1-WL equitable partition) → target-cell individualization → DFS over the I-R tree keeping the lexicographically-maximal relabeled-adjacency certificate; the shared engine also yields a generating set of Aut(G) and \|Aut(G)\| (consumed by ISO-004/005). Loops allowed, multi-edges rejected (mirrors bliss). Canonical labeling is implementation-defined, so no three-source oracle — proptest checks relabeling invariance | isomorphism/bliss.cc (reimplemented, not line-ported) | ~330 | rewrite | core/Graph, operators/permute_vertices | done | 2026-05-30 | exponential worst case (graph symmetry driven) | unit tests + proptest + doctest |
| ALGO-ISO-004 | `count_automorphisms` — order \|Aut(G)\| of the automorphism group, returned as `f64` (matches `igraph_count_automorphisms`'s `igraph_real_t`, exact below 2^53); thin wrapper over the shared I-R engine (ALGO-ISO-003): \|Aut(G)\| equals the number of I-R leaves whose certificate equals the canonical one. Optional per-vertex colours; loops allowed, multi-edges rejected | isomorphism/bliss.cc:igraph_count_automorphisms (reimplemented) | ~50 | rewrite | core/Graph, ISO-003 | done | 2026-05-30 | exponential worst case (graph symmetry driven) | C:2 / py:3 / R:3 + unit + doctest |
| ALGO-ISO-005 | `automorphism_group` — a small generating set of Aut(G), each generator a vertex permutation `g[v]` = image of `v`; built from the shared I-R engine (ALGO-ISO-003) by collecting all leaf automorphisms then greedily selecting a generating subset closed under composition. Generators are non-unique across implementations, so conformance derives and compares the **group order** (closure size), not the generator list. Optional per-vertex colours; loops allowed, multi-edges rejected | isomorphism/bliss.cc:igraph_automorphism_group (reimplemented) | ~60 | rewrite | core/Graph, ISO-003 | done | 2026-05-30 | exponential worst case (graph symmetry driven) | C:2 / py:3 / R:3 + proptest + unit + doctest |
| ALGO-ISO-006 | `isomorphic_bliss` — graph isomorphism yes/no plus a concrete vertex mapping via canonical forms: both graphs are canonicalized by the shared I-R engine (ALGO-ISO-003) and are isomorphic iff their certificates (and, when colours are given, the raw colour sequence in canonical order) coincide; the map is recovered by composing the two canonical labelings. Reuses the `Vf2Isomorphism { iso, map12, map21 }` result. BLISS matches vertex colours by **raw value** (not class structure). Loops allowed, multi-edges rejected | isomorphism/bliss.cc:igraph_isomorphic_bliss (reimplemented) | ~75 | rewrite | core/Graph, ISO-003, ISO-001 | done | 2026-05-30 | exponential worst case (graph symmetry driven) | C:4 / py:7 / R:5 + proptest + unit + doctest |
| ALGO-ISO-007 | `isomorphic` + `subisomorphic` — generic dispatchers mirroring `igraph_isomorphic` / `igraph_subisomorphic`. `isomorphic`: directedness mismatch → `InvalidArgument`; if either graph has multi-edges both are reduced by `simplify_and_colorize` (ALGO-ISO-030) and compared by colour-aware VF2 (self-loops → vertex colours, parallel-edge multiplicities → edge colours); else differing vcount/ecount → `false`; else the BLISS canonical-form test (ALGO-ISO-006) decides (upstream's tiny-graph isoclass shortcut yields the identical verdict, so the port uses BLISS uniformly). `subisomorphic`: delegates verbatim to the VF2 subgraph engine (graph1 = target, graph2 = pattern). Only the yes/no verdict is observable | isomorphism/queries.c:igraph_isomorphic + igraph_subisomorphic (reimplemented) | ~50 | rewrite | ISO-001, ISO-006, ISO-030, ISO-002 | done | 2026-05-30 | exponential worst case (backend driven) | C:6+3 / py:6+4 / R:4+2 + proptest + unit + doctest |
| ALGO-ISO-008 | Weisfeiler-Lehman graph hashing (`wl_hash`, `wl_hash_iterations`, `wl_isomorphic`) — 1-WL color refinement fingerprint for vertices and entire graph; initial colors from vertex labels or degree; iterates until stabilization or max_iterations; deterministic; useful for GNN kernels and fast isomorphism pre-check | — (novel, no C upstream) | ~170 | novel | core/Graph | done | (next) | O(iterations · (V + E) · log V) | no three-source (novel); 18 unit tests + 3 doctests |
| ALGO-ISO-020 | `subisomorphic_lad` + `get_subisomorphisms_lad` — Solnon's LAD subgraph-isomorphism CSP engine: per-pattern-vertex domains over target vertices, AllDifferent global constraint enforced by GAC (Hopcroft-Karp bipartite matching + residual-graph SCC pruning), forward-checking, degree/loop compatibility, fixpoint `filter` propagation, recursive branch-and-propagate `solve`. `induced` toggles non-induced (edge-preserving monomorphism) vs induced (also non-edge-preserving) search; optional caller `domains` restrict candidate images. `subisomorphic_lad` stops at first map; `get_subisomorphisms_lad` enumerates all. Map order implementation-defined (compared as sets) | isomorphism/lad.c | ~640 | rewrite | core/Graph | done | 2026-05-30 | exponential worst case (CSP backtracking) | C:3+3 / py:4+4 / R:2+1 + proptest + unit + doctest |

### Phase 9 — File I/O

| ID | Task | C source | LOC | Complexity | Deps | Status | Commit | Big-O | Conformance |
|----|------|----------|-----|------------|------|--------|--------|-------|-------------|
| ALGO-IO-001 | `read_gml` + `write_gml` (GML recursive key-value format; hand-rolled tokenizer + recursive descent parser) | io/gml.c | ~300 | rewrite | core/Graph | done | 2026-05-27 | O(N) | unit tests only |
| ALGO-IO-002 | `read_ncol` + `write_ncol` (NCOL symbolic named edge list; named vertices, optional weights, always undirected) | io/ncol.c | ~200 | rewrite | core/Graph | done | 2026-05-27 | O(N) | unit tests only |
| ALGO-IO-003 | `read_lgl` + `write_lgl` (LGL adjacency-list format; hub-header + neighbours, optional weights) | io/lgl.c | ~200 | rewrite | core/Graph | done | 2026-05-27 | O(N) | unit tests only |
| ALGO-IO-004 | `read_dimacs` + `write_dimacs_flow` (DIMACS flow/edge format; problem/node/arc lines, 1-based IDs) | io/dimacs.c | ~350 | rewrite | core/Graph | done | 2026-05-27 | O(N) | unit tests only |
| ALGO-IO-005 | `read_pajek` + `write_pajek` (Pajek .net subset; *Vertices/*Edges/*Arcs sections, labels, weights) | io/pajek.c | ~350 | rewrite | core/Graph | done | 2026-05-27 | O(N) | unit tests only |
| ALGO-IO-006 | `write_dot` (DOT/Graphviz writer; label escaping, isolated vertex emission, directed/undirected) | io/dot.c | ~150 | rewrite | core/Graph | done | 2026-05-27 | O(V+E) | unit tests only |
| ALGO-IO-007 | `write_leda` (LEDA native graph format writer; structured text with attribute type decls) | io/leda.c | ~120 | copy | core/Graph | done | 2026-05-28 | O(V+E) | unit tests only |
| ALGO-IO-008 | `read_graphdb` (GraphDB binary reader; packed LE-16 adjacency for isomorphism benchmarks) | io/graphdb.c | ~80 | copy | core/Graph | done | 2026-05-28 | O(V+E) | unit tests only |
| ALGO-IO-009 | `read_dl` (UCINET DL reader; fullmatrix/edgelist1/nodelist1, labels, weights) | io/dl.c | ~250 | rewrite | core/Graph | done | 2026-05-28 | O(V+E) | unit tests only |
| ALGO-IO-010 | `write_edgelist` (plain edge-list writer; completes read_edgelist round-trip) | io/edgelist.c | ~30 | copy | core/Graph | done | 2026-05-28 | O(E) | unit tests only |
| ALGO-IO-011 | `write_graphml` (`GraphML` XML writer; node/edge topology, optional labels, XML escaping) | io/graphml.c | ~100 | rewrite | core/Graph | done | 2026-05-28 | O(V+E) | unit tests only |
| ALGO-IO-012 | `read_leda` (LEDA native graph format reader; parses header/types/directedness/vertices/edges, returns LedaGraph with labels+weights) | io/leda.c | ~120 | rewrite | core/Graph | done | 2026-06-03 | O(V+E) | unit tests only |
| ALGO-IO-013 | `write_dl` (UCINET DL edgelist1 writer; 1-based IDs, optional labels section, optional edge weights) | io/dl.c | ~60 | rewrite | core/Graph | done | 2026-06-03 | O(V+E) | unit tests only |
| ALGO-IO-014 | `read_dot` (DOT/Graphviz reader; parses digraph/graph, edge/node attributes, labels, weights) | io/dot.c | ~200 | rewrite | core/Graph | done | 2026-05-28 | O(V+E) | unit tests only |
| ALGO-IO-015 | `read_graphml` (GraphML XML reader; parses nodes/edges/attributes, returns GraphmlGraph) | io/graphml.c | ~200 | rewrite | core/Graph | done | 2026-05-28 | O(V+E) | unit tests only |
| ALGO-GEO-003 | `gabriel_graph` (Gabriel proximity graph of a spatial point set: connect points A,B iff no other point C lies in the closed ball with AB as diameter, i.e. `dist²(C, mid(A,B)) ≥ (dist(A,B)/2)²` for all C; the lune-based β-skeleton with β=1, equal to RNG ⊆ Gabriel ⊆ Delaunay so it always contains the EMST and is connected/planar in 2-D; arbitrary dimensionality inferred from the first row; returns an undirected graph; faithful closed-ball tolerance `(1 + 128·DBL_EPSILON)²` so co-circular lattice corners drop the diagonal, exactly matching the upstream `is_closed=true` filter; brute-force over all O(n²) pairs with an O(n) empty-ball test is provably edge-equivalent to the Delaunay-pruned C version, so no Delaunay kernel/dependency is needed; third member of the spatial GEO cluster) | spatial/beta_skeleton.cpp (igraph_gabriel_graph, line 744) | ~135 | adapt | core/Graph | done | 2026-05-30 | O(n³·d) brute-force candidate + empty-ball test | C:2 (authentic `beta_skeletons.out` golden edge lists — 25-point random + rotated square lattice) / py:0 (python-igraph 0.11.9 does not expose) / R:0 (rigraph does not expose) + 2 proptest (permutation invariance + EMST-connectivity) + 10 unit + doctest = 14 total |
| ALGO-GEO-004 | `relative_neighborhood_graph` (RNG of a spatial point set: connect points A,B iff no other point C is strictly closer to both than they are to each other, i.e. `dist(A,B) ≤ max(dist(C,A), dist(C,B))` for all C — equivalently the open lune, the intersection of the two open balls of radius `dist(A,B)` centred at A and B, is empty; the **open** lune-based β-skeleton with β=2, so RNG ⊆ Gabriel ⊆ Delaunay and it contains the EMST → always connected; arbitrary dimensionality inferred from the first row; returns an undirected graph; faithful open-lune deflation `(1 − 128·DBL_EPSILON)²` so a co-equidistant third point on the lune boundary keeps the edge, exactly matching the upstream `is_closed=false` filter — the boundary case that keeps a triangular lattice fully connected while the closed β=2 lune skeleton on the same points is empty; brute-force over all O(n²) pairs with an O(n) empty-lune test is provably edge-equivalent to the Delaunay-pruned C version, so no Delaunay kernel/dependency is needed; fourth member of the spatial GEO cluster) | spatial/beta_skeleton.cpp (igraph_relative_neighborhood_graph, line 798) | ~148 | adapt | core/Graph | done | 2026-05-30 | O(n³·d) brute-force candidate + empty-lune test | C:1 (authentic `beta_skeletons.out` golden edge list — triangular lattice, the canonical open-lune RNG witness) / py:0 (python-igraph 0.11.9 does not expose) / R:0 (rigraph does not expose) + 3 proptest (permutation invariance + RNG⊆Gabriel subgraph + EMST-connectivity) + 10 unit + doctest = 14 total |
| ALGO-GEO-005 | `nearest_neighbor_graph` (k-nearest-neighbor graph of a spatial point set: each point gets a directed arc to at most `k` of its nearest OTHER points lying strictly within `cutoff` distance; reuses the `DistanceMetric` enum — Euclidean compares squared distances against `cutoff²`, Manhattan compares raw L1 sums against `cutoff`; `cutoff < 0` ⇒ no cutoff (INFINITY), `k < 0` ⇒ unlimited (all in-cutoff others), `k == 0` ⇒ edgeless; `directed=false` COLLAPSES reciprocal/parallel arcs into one undirected edge; empty point set ⇒ `Graph::new(0, directed)` with 0-dim allowed only when n==0, else inconsistent/0-dim ⇒ InvalidArgument; the C version walks a nanoflann k-d tree whose k-boundary distance ties are resolved by tree visit order (explicitly unspecified upstream), so our deterministic (dist,index) brute-force O(n²·d) all-pairs scan is output-equivalent only on tie-free configs — conformance fixtures use hand-verified tie-free golden cases and proptests assert tie-robust invariants only; fifth member of the spatial GEO cluster) | spatial/nearest_neighbor.cpp (igraph_nearest_neighbor_graph) | ~330 | adapt | core/Graph, DistanceMetric | done | 2026-05-31 | O(n²·d) brute-force all-pairs scan + per-source partial sort | C:4 (authentic `igraph_nearest_neighbor_graph.out` golden edge lists — 2d k=2 cutoff=5 L2, 3d k=2 no-cutoff L2, 4d k=1 no-cutoff L2, 2d k=2 cutoff=5 L1) / py:0 (python-igraph 0.11.9 does not expose) / R:0 (rigraph does not expose) + 3 proptest (unlimited-k permutation invariance + cutoff/degree-bound contract + undirected-collapse equivalence) + 13 unit + doctest = 17 total |
| ALGO-GEO-006 | `lune_beta_skeleton` (lune-based β-skeleton of a spatial point set for shape parameter β>0: connect points A,B iff the lune — the intersection of two equal balls governed by β — contains no other point; for β≥1 (any dimension) the balls have radius `(β/2)·dist(A,B)` with centres sliding apart along AB as `a_center[d]=a[d]+(r−1)(a[d]−b[d])`, `b_center[d]=b[d]+(r−1)(b[d]−a[d])` where `r=β/2`, so β=1 collapses both centres onto the midpoint and recovers the **Gabriel graph**; for β<1 (**2-D only**, else `Unsupported` mirroring `IGRAPH_UNIMPLEMENTED`) the centres sit symmetrically off the line AB at perpendicular offset `(a−b)·sqrt(r²−¼)` with `r=1/(2β)`, giving a denser graph; larger β enlarges the empty region → sparser graph (β-skeleton nesting); boundary is **closed** (`is_closed=true`) with inflation `(1+128·DBL_EPSILON)²` so a point exactly on the lune boundary counts as inside and removes the edge; arbitrary dimensionality inferred from the first row; returns an undirected graph; brute-force over all O(n²) pairs with an O(n·d) empty-lune test is edge-equivalent to the Delaunay-pruned C version for β≥1, and matches the reference's complete-graph start for β<1, so no Delaunay kernel/dependency is needed; sixth member of the spatial GEO cluster) | spatial/beta_skeleton.cpp (igraph_lune_beta_skeleton, line 444) | ~185 | adapt | core/Graph | done | 2026-05-31 | O(n³·d) brute-force candidate + empty-lune test | C:4 (authentic `beta_skeletons.out` golden edge lists — 25-point random 2-D at β=2, β=1 Gabriel reduction, β=0.5 perpendicular construction + 10-point 3-D at β=2) / py:0 (python-igraph 0.11.9 does not expose) / R:0 (rigraph does not expose) + 3 proptest (permutation invariance + β=1≡Gabriel + β-nesting subgraph) + 13 unit + doctest = 17 total |
| ALGO-GEO-007 | `circle_beta_skeleton` (circle-based β-skeleton of a 2-D spatial point set for shape parameter β>0: like the lune skeleton, connect points A,B iff a β-governed empty region contains no other point, but **2-D only in every regime** — non-2-D input returns `Unsupported`, mirroring the reference's unconditional `IGRAPH_UNIMPLEMENTED`; both regimes use two **perpendicular** centres `mid ± (a−b)·sqrt(r²−¼)` rotated 90°; differs from the lune variant **only for β≥1**, where `r=β/2` and the test is **union-empty** — a point in **either** circle (radius `r·dist(A,B)`) removes the edge — with a single `(1+128·DBL_EPSILON)` boundary inflation; β=1 collapses both centres onto the midpoint so the union of the two identical circles is the Gabriel diametral ball, recovering the **Gabriel graph**; for β<1 the construction is **identical to the lune skeleton** — `r=1/(2β)`, same perpendicular centres, **intersection-empty** test (a point removes the edge only when in **both** circles), `(1+128·DBL_EPSILON)²` inflation; larger β enlarges the union empty-region → sparser graph (β-skeleton nesting); boundary is **closed**; returns an undirected graph; brute-force over all O(n²) pairs with an O(n) two-circle empty-region test is edge-equivalent to the Delaunay-pruned C version for β≥1 and matches the reference's complete-graph start for β<1, so no Delaunay kernel/dependency is needed; seventh member of the spatial GEO cluster) | spatial/beta_skeleton.cpp (igraph_circle_beta_skeleton, line 494) | ~180 | adapt | core/Graph, gabriel_graph | done | 2026-05-31 | O(n³) brute-force candidate + two-circle empty-region test | C:2 (authentic `beta_skeletons.out` golden edge lists — 25-point random 2-D at β=1.1 circle-based + 2-point degenerate at β=1.1) / py:0 (python-igraph 0.11.9 does not expose) / R:0 (rigraph does not expose) + 3 proptest (permutation invariance + β=1≡Gabriel + β-nesting subgraph) + 9 unit + doctest = 13 total |
| ALGO-GEO-008 | `beta_weighted_gabriel_graph` (β-weighted Gabriel graph of a spatial point set: build the **Gabriel graph** and weight each edge by the **threshold β** at which it leaves the lune-based β-skeleton — the smallest β for which some other point first enters the edge's lune — capped at a `max_beta` cutoff; an edge whose threshold is ≥ `max_beta` (or which no point ever blocks) gets weight `+∞`; per-edge BetaFinder computes, for edge A,B with `ab²=dist²(A,B)` and each other point P, `ap²,bp²` (swapped so `ap²≤bp²`), `denom=ab²+ap²−bp²`, `β=denom≤0 ? +∞ : 2·ap²/denom`, collapsing `β<1+128·DBL_EPSILON` to 0 (P inside the closed Gabriel ball ⇒ non-Gabriel edge dropped); `max_beta` must be ≥1 (or +∞) since every threshold is ≥1; arbitrary dimensionality inferred from the first row; duplicate points ⇒ InvalidArgument; returns `BetaWeightedGabriel { graph, weights }` with `weights[e]` aligned to edge e in increasing `(min,max)` order; the upstream Delaunay-seed + k-d-tree `dist<max_radius` prune is a pure search bound (reduces to `x(x²+y²−h²)≤0`, holds strictly for every point yielding finite β≥1), so the brute-force over all O(n²) pairs and all points is output-equivalent in edges AND weights — no Delaunay kernel/dependency needed; eighth member of the spatial GEO cluster) | spatial/beta_skeleton.cpp (igraph_beta_weighted_gabriel_graph, line 651) | ~205 | adapt | core/Graph, gabriel_graph | done | 2026-05-31 | O(n³·d) brute-force candidate + per-edge BetaFinder point scan | C:4 (authentic `beta_skeletons.out` golden weight vectors — 25-point random 2-D and 10-point 3-D, each at max_beta=∞ and max_beta=5; order-independent sorted finite-weight multiset + ∞-count match) / py:0 (python-igraph 0.11.9 does not expose) / R:0 (rigraph does not expose) + 3 proptest (edge-set≡Gabriel + weights≥1∨∞ + cutoff caps weights) + 13 unit + doctest = 17 total |
| ALGO-NG-002 | `sample_sphere_surface` (sample `n` points uniformly from the surface of a `dim`-dimensional sphere of given radius; Muller 1959 method via gen_normal; optional positive-orthant restriction; second member of the nongraph utility cluster) | random/sampling.c (igraph_rng_sample_sphere_surface) | ~40 | translate | core/rng::SplitMix64 | done | 2026-06-01 | O(n·dim) | C:0 (RNG-dependent) / py:0 / R:0 + 8 unit + doctest = 9 total |
| ALGO-NG-003 | `sample_sphere_volume` (sample `n` points uniformly from the interior of a `dim`-dimensional ball; sphere-surface sampling scaled by U^(1/dim); third member of the nongraph utility cluster) | random/sampling.c (igraph_rng_sample_sphere_volume) | ~25 | translate | core/rng::SplitMix64 | done | 2026-06-01 | O(n·dim) | C:0 (RNG-dependent) / py:0 / R:0 + 4 unit (shared with NG-002 tests) + doctest = 5 total |
| ALGO-NG-004 | `sample_dirichlet` (sample `n` vectors from a Dirichlet distribution with concentration parameters alpha; Gamma-based method; fourth member of the nongraph utility cluster) | random/sampling.c (igraph_rng_sample_dirichlet) | ~30 | translate | core/rng::SplitMix64 | done | 2026-06-01 | O(n·dim) | C:0 (RNG-dependent) / py:0 / R:0 + 6 unit + doctest = 7 total |
| ALGO-NG-001 | `random_sample` (generates an increasing sorted random sequence of `length` distinct integers from `[l, h]` without replacement using Vitter's Algorithm D (1987) with Algorithm A fallback; O(length) expected time regardless of interval size; deterministic via SplitMix64 seed; first member of the nongraph utility cluster) | random/random.c (igraph_random_sample) | ~160 | translate | core/rng::SplitMix64 | done | 2026-06-01 | O(length) expected | C:0 (RNG-dependent) / py:0 / R:0 + 16 unit + doctest = 17 total |
| ALGO-GEO-009 | `delaunay_graph` (Delaunay triangulation graph of a 1-D or 2-D point set: connect points with an edge whenever they share a face in the Delaunay triangulation — no other point lies inside the circumscribed circle of any triangle; 1-D reduces to sorted-adjacent path in O(n log n); 2-D uses Bowyer-Watson incremental insertion with an adaptively-sized super-triangle — for n≤20 the super-triangle radius is 3× the maximum circumradius over all O(n³) point triples, for n>20 it is 100× the maximum pairwise distance; strict `<` in-circumcircle test ensures co-circular points get exactly one diagonal; collinear 2-D inputs are detected and handled as a projected 1-D path; duplicate points ⇒ InvalidArgument; ≥3-D returns InvalidArgument; returns an undirected Graph; ninth member of the spatial GEO cluster) | spatial/delaunay.c (igraph_delaunay_graph) | ~210 | hand-roll | core/Graph | done | 2026-05-31 | O(n²) avg Bowyer-Watson; O(n³) super-triangle setup for n≤20 | C:0 (C version delegates to Qhull; labeling is implementation-defined) / py:0 / R:0 + 1 proptest (planar edge-count bounds) + 18 unit + doctest = 19 total |

### Phase 8 — Spectral + Embedding + HRG

| ID | Task | C source | LOC | Complexity | Deps | Status | Commit | Big-O | Conformance |
|----|------|----------|-----|------------|------|--------|--------|-------|-------------|
| ALGO-HRG-001 | `HrgTree` data structure + `hrg_create` + `from_hrg_dendrogram` — binary dendrogram representation: `left: Vec<i32>`, `right: Vec<i32>`, `prob: Vec<f64>`, `vertices: Vec<i32>`, `edges: Vec<i32>` (negative = internal node, non-negative = leaf); `hrg_create` builds an HrgTree from a Graph using recursive splitting by connected components; `from_hrg_dendrogram` converts an HrgTree into a Graph dendrogram with edge weights = connection probabilities | hrg/hrg.cc (igraph_hrg_create, igraph_from_hrg_dendrogram) + hrg/hrg_types.cc (dendro class internals) | ~400 | rewrite | core/Graph | done | 2026-06-04 | O(V + E) construction | unit + conformance + doctest |
| ALGO-HRG-002 | `hrg_sample` + `hrg_game` + `hrg_sample_many` — sample random graphs from a fitted HRG model: for each internal node with probability p, independently connect each pair of leaves in left/right subtrees with probability p; `hrg_game` is the single-sample entry point; `hrg_sample_many` generates multiple samples efficiently | hrg/hrg.cc (igraph_hrg_sample, igraph_hrg_game, igraph_hrg_sample_many) + hrg/hrg_types.cc (dendro::graph_from_hrg) | ~300 | rewrite | ALGO-HRG-001, core/rng::SplitMix64 | done | 2026-06-04 | O(V² · log V) per sample | unit + conformance + doctest |
| ALGO-HRG-003 | `hrg_fit` + `hrg_consensus` + `hrg_predict` — MCMC fitting of HRG model to observed graph: Metropolis-Hastings tree-space search with SPR (subtree prune-regraft) proposals, likelihood = Π p^e · (1-p)^(n-e) at each internal node; `hrg_consensus` averages over MCMC samples to produce consensus dendrogram; `hrg_predict` identifies missing edges with highest average connection probability across MCMC samples | hrg/hrg.cc (igraph_hrg_fit, igraph_hrg_consensus, igraph_hrg_predict) + hrg/hrg_types.cc (dendro::monteCarloMove, dendro::makeConsensus) | ~800 | rewrite | ALGO-HRG-001, ALGO-HRG-002, core/rng::SplitMix64 | done | 2026-06-05 | O(mcmc_steps · V) per fit | unit + conformance + doctest |
| ALGO-LAP-001 | `laplacian` — graph Laplacian matrix (combinatorial L = D − A, or normalized variants); dense output; supports directed mode (Out/In/All) and 4 normalizations (unnormalized, symmetric, left, right); optional edge weights | misc/laplacian.c (igraph_get_laplacian, igraph_get_laplacian_sparse) | ~200 | adapt | core/Graph, EIG-001 | done | 2026-06-05 | O(V + E) | unit + doctest |
| ALGO-EMB-001 | `adjacency_spectral_embedding` — spectral embedding via Lanczos eigendecomposition of the adjacency matrix; d-dimensional vertex coordinates from top-d eigenvectors; supports directed (symmetrized) and undirected | embedding/ase.c (igraph_adjacency_spectral_embedding) | ~250 | rewrite | EIG-001, LAP-001 | done | 2026-06-05 | O(E·d·max_iter) Lanczos | unit + doctest |
| ALGO-EMB-002 | `laplacian_spectral_embedding` — spectral embedding via eigenvectors of the graph Laplacian; d-dimensional vertex coordinates from bottom-d non-trivial eigenvectors; supports unnormalized and normalized Laplacians | embedding/lse.c (igraph_laplacian_spectral_embedding) | ~250 | rewrite | EIG-001, LAP-001 | done | 2026-06-05 | O(E·d·max_iter) Lanczos | unit + doctest |
| ALGO-EMB-003 | `dim_select` — automatic dimensionality selection for spectral embedding via profile likelihood (Zhu & Ghodsi 2006); finds the elbow in an eigenvalue sequence | embedding/dim_select.c (igraph_dim_select) | ~80 | adapt | - | done | 2026-06-05 | O(n²) | unit + doctest |

---

## Counters

| Phase | done | wip | todo | native | total | Conformance fixtures |
|-------|------|-----|------|--------|-------|----------------------|
| 0 (BOOT) | 37 | 0 | 0 | 0 | 37 | bfs: 4 (C:2, py:1, R:1) |
| 1 | 206 | 0 | 0 | 5 (DS-V/M/S/SEL/ADJ) | 211 | dfs: 3; cc: 4; scc: 4; distances: 3; is_eulerian: 5 (py skipped); articulation: 3; bridges: 4; is_biconnected: 4; girth: 4; ecc/radius/diameter: 9; ecc/radius/diameter_with_mode: 9; triangles+transitivity: 10; transitivity_barrat: 3; density+mean_distance: 6; eulerian_path: 3 (py skipped); count_reachable: 3; reciprocity: 3; knn: 3; assortativity: 3; assortativity_values: 7 (C:2, py:3, R:2); CORE-001c: no fixtures; CORE-001d: no fixtures; CORE-001f: no fixtures (internal cache); reachability_matrix: 3; transitive_closure: 3; closeness: 3; harmonic: 3; betweenness: 3; edge_betweenness: 3; pagerank: 3; biconnected_components: 3; eigenvector: 3; simplify: 3; modularity: 3; is_simple: 3; has_loop+has_multiple: 6; is_loop+is_multiple: 6; disjoint_union: 3; dijkstra_distances: 3; complementer: 3; closeness_weighted: 3; harmonic_centrality_weighted: 3; betweenness_weighted: 3; edge_betweenness_weighted: 3; pagerank_weighted: 3; assortativity_degree_weighted: 3; assortativity_degree_directed_weighted: 3; floyd_warshall_distances: 3; decompose: 3; union: 3; intersection: 3; difference: 3; dijkstra_paths+path_to+cutoff: 9; dijkstra_with_mode+all_shortest_paths: 6; ecc/radius/diameter_weighted_with_mode: 9; a_star_path: 3; bellman_ford_distances: 9; johnson_distances: 6; widest_path_widths: 6; widest_path: 6; widest_path_widths_floyd_warshall: 6; widest_paths_to: 6; widest_paths: 6; edgelist_percolation: 6; bond_percolation: 6; site_percolation: 6; is_same_graph: 6; is_dag: 6; topological_sorting: 6; is_acyclic: 6; is_tree: 6; is_forest: 6; is_complete: 6; neighborhood_size: 6; neighborhood: 6; convergence_degree: 6; count_loops: 6; count_multiple: 6; count_adjacent_triangles: 6; global_efficiency: 6; local_efficiency+average_local_efficiency: 8 (py skipped); hub_and_authority_scores: 6; hub_and_authority_scores_weighted: 3; eigenvector_centrality_weighted: 3; eigenvector_centrality_directed: 3; louvain: 6; leiden: 6; label_propagation: 6; fluid_communities: 6; edge_betweenness_community: 6; fast_greedy_modularity: 6; edge_betweenness_community_weighted: 6; walktrap: 9; community_to_membership: 6; reindex_membership: 6; compare_communities: 6; split_join_distance: 6; erdos_renyi_gnp: 10; erdos_renyi_gnm: 10; barabasi_game_bag: 10; growing_random_game: 10; tree_game_lerw: 10; grg_game: 10; forest_fire_game: 9; simple_interconnected_islands_game: 9; k_regular_game: 9; watts_strogatz_game: 9; sbm_game: 9; hsbm_game: 9; hsbm_list_game: 9; chung_lu_game: 27; static_fitness_game: 18; static_power_law_game: 18; preference_game: 9; asymmetric_preference_game: 9; establishment_game: 9; callaway_traits_game: 9; cited_type_game: 9; lastcit_game: 9; recent_degree_game: 9; dot_product_game: 9; correlated_game+correlated_pair_game: 12; degree_sequence_game_configuration: 9; degree_sequence_game_vl: 8; degree_sequence_game_fast_heur_simple: 10; degree_sequence_game_configuration_simple: 10; degree_sequence_game_edge_switching_simple: 10; ring_graph: 14; star_graph: 14; wheel_graph: 14; kary_tree: 14; symmetric_tree: 14; regular_tree: 14; hypercube: 14; hamming: 14; square_lattice: 16; generalized_petersen: 8; circulant: 10; de_bruijn: 10; kautz: 11; full_graph: 14; linegraph: 13; from_prufer: 11; tree_from_parent_vector: 14; lcf: 14; mycielskian: 17; famous: 16; atlas: 19; create: 19; triangular_lattice: 13; hexagonal_lattice: 10 (R skipped — no wrapper); full_citation: 10; full_multipartite: 13; turan: 9 (py skipped — no `Graph.Turan` binding); extended_chordal_ring: 6 (py skipped — no `Graph.Extended_Chordal_Ring` binding); adjacency: 18; weighted_adjacency: 18; rich_club_sequence: 9; pagerank_linsys: shared with PR-011; max_flow_value: 9; st_mincut_value: 9; st_edge_connectivity: 11; edge_disjoint_paths: 9; st_vertex_connectivity: 13; vertex_disjoint_paths: 9; vertex_connectivity: 10; edge_connectivity: 10; mincut_value: 10; st_mincut: 10; gomory_hu_tree: 12; dominator_tree: 12; bipartite_game_gnp: 9; bipartite_game_gnm: 9; iea_game: 9; recent_degree_aging_game: 9; constraint: 0 (unit + proptest only); centralization: 0 (unit + proptest only); is_graphical+is_bigraphical: 0 (unit + proptest only); strength+diversity: 0 (unit + proptest only); trussness: 6; path_length_hist: 7; all_simple_paths: 6; layout: 6; coloring: 7; chordal: 6; matching: 6; minimum_size_separators: 9; cohesive_blocks: 9; power_law_fit: 11 (C:3, py:6, R:2); get_shortest_path_astar: 7 (C:2, py:3, R:2); get_all_shortest_paths_dijkstra: 7 (C:2, py:3, R:2); get_shortest_paths_dijkstra: 7 (C:2, py:3, R:2) |
| 6 | 4 | 0 | 0 | 0 | 4 | CL-001: C:3/py:2/R:2; CL-002: C:2/py:2/R:2; CL-003: C:2/py:2/R:2; CL-020: unit tests + doctest |
| 9 (I/O) | 15 | 0 | 0 | 0 | 15 | IO-001..015: unit tests only |
| 8 (spectral+HRG) | 10 | 0 | 0 | 0 | 10 | EIG-001..003: unit + doctest; LAP-001: unit + doctest; EMB-001..003: unit + doctest; HRG-001..003: unit + conformance + doctest |
| 2-10 (rest) | 0 | 0 | ~529 | 0 | ~529 | - |

> **Note on `native` rows.** rust-igraph borrows from igraph C / python-igraph
> / R-igraph as references but does *not* aim for a 1:1 C translation. The
> DS-* family (Vector / Matrix / SparseMatrix / Selector / AdjList) is
> covered by Rust standard-library types (`Vec`, `HashMap`, iterator
> protocol) and the `core/` modules introduced organically by algorithm
> AWUs. We mark them `native` rather than rolling them into the `done` /
> `todo` count, since they would otherwise distort the algorithm-progress
> ratio.

**Phase 0 — complete (37/37)**. **Phase 1 complete**: 420 algorithms done —
Graph core (CORE-001a/b/d), DFS (TR-002), weak CC (CC-001), strong CC
(CC-002), unweighted distances (SP-006), Eulerian existence (CC-040),
articulation points (CC-010), bridges (CC-014), is_biconnected
(CC-013), girth (PR-001), eccentricity/radius/diameter (SP-020),
triangle count + global transitivity (PR-002), local transitivity
per-vertex (PR-002b), density + mean_distance (PR-003), Eulerian
path/cycle (CC-041 + CC-042), reachability counts (CC-020),
reachability matrix (CC-021), transitive closure (CC-022),
reciprocity (PR-004), BFS multi-output (TR-001), knn (PR-005),
degree assortativity (PR-006), edge query helpers (CORE-001d),
closeness centrality (PR-007), harmonic centrality (PR-009), betweenness
centrality (PR-008), edge betweenness (PR-010), PageRank (PR-011),
biconnected components multi-output (CC-011), eigenvector centrality
(PR-012), simplify (OP-001), modularity (CO-001), is_simple
(PR-013), has_loop + has_multiple (PR-014), per-edge is_loop +
is_multiple (PR-014b), disjoint_union (OP-002), dijkstra_distances (SP-001),
complementer (OP-003), weighted closeness (PR-007b),
weighted harmonic (PR-009b), weighted betweenness (PR-008b),
weighted edge_betweenness (PR-010b), weighted PageRank
(PR-011b), weighted assortativity (PR-006b),
Floyd-Warshall all-pairs (SP-004), coreness / k-core
(PR-015), reciprocity ratio mode + ignore_loops
(PR-004b), weighted modularity (CO-001c), is_simple
directed-as-undirected mode (PR-013b),
disjoint_union_many (OP-002b),
directed coreness IN/OUT (PR-015b),
directed assortativity (PR-006c),
directed modularity (CO-001b),
biconnected component_edges output (CC-012),
weighted knn + knnk + knnk_weighted (PR-005b),
Barrat weighted transitivity (PR-002c),
decompose (CC-003 weak slice),
union (OP-004 two-graph max-multiplicity),
intersection (OP-005 two-graph min-multiplicity),
difference (OP-006 clamped multiset subtract),
mode-aware ecc/radius/diameter (SP-021abc),
Dijkstra paths/path_to/cutoff/multi-source (SP-001b),
Dijkstra IN/ALL + all-shortest-paths (SP-001c),
weighted ecc/radius/diameter (SP-021..023 weighted),
directed weighted assortativity (PR-006d),
A* shortest path with admissible heuristic (SP-005),
random walk (TR-003, deterministic SplitMix64 PRNG),
is_complete (PR-016, cardinality + simple-fast-path + unique-neighbour scan),
neighborhood_size (PR-027, BFS-based k-hop ball count with mindist filter),
neighborhood (PR-027b, BFS-based k-hop vertex lists with mindist + mode),
convergence_degree (PR-028, per-edge shortest-path source/sink balance),
count_loops + count_multiple (PR-014c, scalar + per-edge multiplicity),
count_adjacent_triangles (PR-002d, per-vertex adjacent triangle count),
global_efficiency (PR-029, Latora–Marchiori average inverse pairwise distance),
local_efficiency + average_local_efficiency (PR-030, per-vertex inverse-distance among neighbours of `G\{v}` and its mean),
property cache subsystem (CORE-001f, bit-packed boolean cache + selective invalidation; is_dag/is_forest/has_loop/has_multiple now O(1) on repeat),
hub_and_authority_scores (PR-017, Kleinberg HITS — directed power-iter on A·Aᵀ; undirected delegates to eigenvector_centrality),
hub_and_authority_scores_weighted (PR-017b, weighted Kleinberg HITS — directed power-iter on W·Wᵀ via incident-edge walks; undirected self-rolled shifted power-iter on W+I until PR-012b lands),
PR-012b (directed + weighted eigenvector centrality — `eigenvector_centrality_weighted`, `eigenvector_centrality_directed`, `eigenvector_centrality_directed_weighted`, `eigenvector_centrality_full` master; self-rolled shifted-power-iter on (M+σI) with σ = max_row_norm+1 — Perron-Frobenius guarantees the largest-real eigenvalue of non-negative M becomes the unique largest-magnitude eigenvalue of M+σI so plain power iter converges; DAG short-circuit returns 1s on sinks (Out) / sources (In); Rayleigh quotient for λ; negative-weight path uses signed-pivot for largest-real convergence; bench: karate ~15µs/28µs, directed ring(500) ~17µs unweighted / 6.2ms weighted),
CO-002 (Louvain multilevel community detection — `louvain`, `louvain_weighted`, `louvain_with_options`; Blondel et al. 2008 fast-unfolding via per-pass local moves + super-graph aggregation; SplitMix64 + Fisher-Yates shuffle for deterministic seed-driven traversal; IGRAPH_LOOPS_TWICE convention preserved through aggregation so self-loops at level k+1 still contribute 2w to k_v; internal Q matches standalone `modularity()` to 1e-9; configurable γ resolution; Newman-Girvan modularity used as the gain function; karate Q ≈ 0.39..0.42, ring-of-cliques(4×5) Q ≈ 0.66 with k=4; bench: karate ~16.8µs / weighted 12.1µs / fixed seed 14.8µs / ring-of-cliques(8×10) ~29µs — 2.5×–3.6× faster than python-igraph),
CO-003 (Leiden community detection — `leiden`, `leiden_weighted`, `leiden_with_options`; Traag-Waltman-van Eck 2019 three-phase loop guaranteeing well-connected communities — queue-based fastmove → singleton-init refinement with exp(diff/β) sampling → aggregation where super-vertices are *refined* subclusters, not original clusters; three objectives: Modularity / CPM / ER via `LeidenObjective` enum + `LeidenOptions` (γ resolution, β randomness, n_iterations, seed, optional start membership); negative weights accepted for CPM only; IGRAPH_LOOPS convention so Leiden's internal Q matches standalone `modularity()` exactly on loop-free graphs; same SplitMix64 + Fisher-Yates determinism story as Louvain; karate Q ≈ 0.42 with k=4, ring-of-cliques(4×5) Q ≈ 0.66 with k=4; bench: karate ~37.5µs / weighted 43.9µs / fixed seed 53.0µs / CPM 56.2µs / ER 32.3µs / ring-of-cliques(8×10) ~93µs — 1.5× faster than python-igraph on karate, 0.84× on ring-of-cliques where the hand-tuned igraph C `igraph_community_leiden` still leads),
CO-004 (Label propagation community detection — `label_propagation`, `label_propagation_weighted`, `label_propagation_with_options`; three variants via `LpaVariant`: Fast (Traag-Šubelj 2023 queue-based, single-sweep convergence with in-queue bitmap), Dominance (Raghavan-Albert-Kumara 2007 alternating control/update iters), Retention (keep current label when still dominant); `LpaOptions` carries `variant`, `seed`, optional `initial` membership (with `-1` for unlabelled isolates) and `fixed` mask for semi-supervised LPA; IGRAPH_LOOPS_ONCE adjacency so self-loops contribute exactly once to a vertex's own label sum; final dense-relabel + BFS-fill so unlabelled connected components each receive a fresh contiguous label; SplitMix64 + Fisher-Yates shuffle for fully deterministic seeded traversal; quality reported via standalone `modularity()` since LPA does not optimise an objective; karate Q ≈ 0.34–0.40 with k≈3, ring-of-cliques(4×5) k=4, K3+K3+thin-bridge weighted partition cuts at the bridge as expected; bench: karate Fast ~3.9µs / unit-weighted 3.9µs / fixed seed 3.6µs / Dominance 8.8µs / Retention 6.1µs / ring-of-cliques(8×10) Fast ~19.2µs — 4.5× faster than python-igraph on karate, 1.27× on ring-of-cliques),
CO-005 (Fluid Communities Parés et al. 2017 — `fluid_communities`, `fluid_communities_with_options`; user pins the number of communities `k`, the algorithm seeds `k` "fluids" at random vertices each with density `1/size`, then per-iteration shuffles vertex order and re-evaluates each vertex's label by tallying density contributions from itself+neighbours with ε=1e-4 tie tolerance and same-label retention; runs until no vertex changes label or `max_iterations` (default 1000) is reached; rejects non-simple, disconnected, and `k=0 || k>vcount` graphs; IEEE-754 inf semantics of `1/0` quietly recover an emptied community on the next sweep, no special-case needed; SplitMix64 + Fisher-Yates seeded shuffle for determinism; quality via standalone `modularity()` since Fluid does not optimise an objective; karate Q ≈ 0.37 with k=2, ring-of-4-K5-cliques recovers the ground-truth k=4 partition with Q ≈ 0.66, two-K4-with-bridge splits cleanly with k=2; bench: karate k=2 ~12.8µs / k=4 ~9.6µs / k=3 fixed seed 9.6µs / ring-of-cliques(8×10) k=8 ~44.6µs; python-igraph 0.11.9 does not expose this binding so no cross-language baseline captured),
CO-006 (Edge betweenness community detection Girvan-Newman 2002 — `edge_betweenness_community`; iteratively remove the highest-betweenness edge via a Brandes unweighted edge-betweenness pass over a per-removal *active* edge set, recording removal order in `removed_edges`, the corresponding `edge_betweenness` at removal (halved for undirected to match centrality convention, left un-halved for directed graphs to match the C reference), then walks removals in reverse to build the binary dendrogram — each removal that re-joins two distinct components is a merge with new cluster id `n + i`, modularity is recomputed via standalone `modularity()` / `modularity_directed()` (depending on graph orientation) at every merge, and the best-Q membership is densified to `0..nb_clusters` and returned; `EdgeBetweennessResult` also exposes `merges`, `bridges`, `modularity` per merge so callers can replay any cut point; edges are masked via private per-vertex `Vec<EdgeId>` incidence lists with `retain`, never via `graph.delete_edges`, so original edge IDs stay stable for the entire run; ties broken by smallest active edge id matching upstream `igraph_i_which_max_active_ratio`; directed graphs supported (see CO-006c); karate Q ≈ 0.40 (k=5) with chosen partition matching standalone `modularity()` to 1e-9, ring-of-4-K5-cliques splits ground-truth k=4, two-K4-with-bridge removes the bridge first; bench: path-10 ~13.5µs / two-K4-bridge ~14.0µs / ring-of-cliques(4×5) ~115.5µs / karate ~998µs — karate runs in ~71% of python-igraph 0.11.9's wall time, ring-of-cliques within noise (Rust 115µs vs python 128µs)),
CO-007 (Fast greedy modularity Clauset-Newman-Moore 2004 — `fast_greedy_modularity`, `fast_greedy_modularity_weighted`; greedy ΔQ-merge dendrogram driven by a lazy-deletion global `BinaryHeap<HeapEntry>` over per-community `BTreeMap<u32, f64>` neighbour maps (safe-Rust replacement for the upstream cross-pointer indexed max-heap), per-merge update applies the three CNM rules — Triangle `ΔQ' = ΔQ_to_k + ΔQ_from_k`, Chain-1 `ΔQ' = ΔQ_to_k − 2·a[from]·a[k]`, Chain-2 `ΔQ' = ΔQ_from_k − 2·a[to]·a[k]` — to every shared neighbour, mirrors into both directions, decays `a[merged] = a[c1] + a[c2]`, and tracks the best-Q cut as merges proceed; rejects directed input and multigraphs (`InvalidArgument`); `FastGreedyResult { membership, nb_clusters, merges, modularity }` carries the dense-labelled best-Q partition (`0..nb_clusters`), the full binary dendrogram (`merges[i] = [c1, c2]` produces super-cluster `n + i`), and the per-step Q trajectory (length = `merges.len() + 1`); karate Q = 0.380671 (k=3), two-K5-bridge Q = 0.452381 (k=2), K4+K4+iso Q = 0.5 (k=3), two-disjoint-10-rings Q = 0.54 (k=4), 6v8e Q = 0.179688, 2v+2loops Q = 0.5 — every value matches the upstream igraph C unit-test number bit-for-bit on the best-Q cut; bench: path-10 ~2.54µs / two-K5-bridge ~4.72µs / ring-of-cliques(4×5) ~10.33µs / karate ~36.57µs — Rust karate runs in ~81% of python-igraph 0.11.9 wall time; ring-of-cliques(4×5) ~3.3× faster, two-K5-bridge ~4.8× faster),
CO-006b (Weighted edge betweenness community detection — `edge_betweenness_community_weighted`; same Girvan-Newman framework as CO-006 but per-removal betweenness is computed via Brandes over Dijkstra shortest paths with a `BinaryHeap<Frontier>` min-heap (Frontier uses `f64::total_cmp` after rejecting NaN/negative/non-finite weights up front, so the heap stays deterministic) and dendrogram-level modularity uses `modularity_weighted` / `modularity_weighted_directed` so the best-Q partition reflects `m = Σ w_e` rather than `m = ecount`; reuses the same `EdgeBetweennessResult` shape (membership / nb_clusters / removed_edges / edge_betweenness / merges / bridges / modularity) and the same per-vertex `Vec<EdgeId>` masking pattern (private incidence lists with `retain`, original `EdgeId`s stay stable across the run), tie-breaks by smallest active edge id matching the upstream linear scan, directed graphs supported (see CO-006c); unit weights through this entry must reproduce the CO-006 dendrogram bit-for-bit — enforced by `unit_weights_match_unweighted_on_karate`, `unit_weights_match_unweighted_on_two_triangles_bridge`, `unit_weights_match_unweighted_path_5` and two proptests over `arb_graph(10)` (membership / removed_edges / merges identical, modularity within 1e-9); cheap-bridge fixtures (two K4 + bridge w=0.1, two K3 + bridge w=0.1) confirm the bridge is removed first and the K4/K3 components remain internally connected at the best cut, karate weighted-Q ≥ 0.35 self-consistent with `modularity_weighted` of the returned partition; bench: path-10 unit ~35.7µs / two-K4-bridge unit ~36.4µs / ring-of-cliques(4×5) unit ~218µs / karate unit ~1.76ms — Rust karate runs in ~79% of python-igraph 0.11.9 wall time (1.76ms vs 2.23ms) and within noise on ring-of-cliques (218µs vs 206µs); ~2.6× slower than the unweighted CO-006 on karate as expected from the Dijkstra-vs-BFS heap overhead),
CO-006c (Directed Girvan-Newman edge betweenness community detection — extends CO-006 and CO-006b in place rather than adding a third entry point: `edge_betweenness_community` and `edge_betweenness_community_weighted` now accept directed graphs, with the forward Brandes/Brandes-Dijkstra pass walking the OUT-incidence list (`Graph::incident(_, Out)`), the back-pass driven by predecessor lists populated during the forward sweep, no halving of per-pair edge contributions for directed (`if (!directed) eb /= 2.0;` mirrors the C reference), and per-level Q dispatched via `modularity_directed` (unweighted) or the newly-added `modularity_weighted_directed` (weighted) so the best-Q cut reflects the Leicht-Newman 2008 directed objective `Σ_c [e_{cc}/m − (s^{out}_c/m)·(s^{in}_c/m)]` rather than the symmetric undirected sum; also exports `modularity_weighted_directed(graph, membership, resolution, weights)` falling through to `modularity_weighted` when the graph is undirected so callers have a single weighted entry; directed 4-path eb=4.0 (un-halved) sanity test, directed 6-path 0→1→2→3→4→5 cleanly cuts the middle directed edge first (eb=9 unique) producing {0,1,2}|{3,4,5} at Q=8/25=0.32, directed two-triangles+bridge documents the lowest-eid tie-break under a 3-way Brandes tie; weighted unit-weight directed runs match unweighted directed runs bit-for-bit (proptest invariant); bench: directed-path-10 ~16.4µs / directed-two-triangles-bridge ~8.3µs / weighted directed-path-10 unit ~11.6µs / weighted directed-two-triangles-cheap-bridge ~7.5µs; undirected cells track the CO-006/CO-006b baseline within ±15% (in-place extension does not regress undirected throughput)),
CO-008 (Walktrap community detection Pons-Latapy 2005 — `walktrap`, `walktrap_weighted`, `walktrap_with_options`; random walks of length t (default 4 via `WALKTRAP_DEFAULT_STEPS`) define a between-vertex distance r²_{ij} = Σ_k (P^t_{ik} − P^t_{jk})²/deg(k); greedy agglomerative merges that minimise Δσ produce a binary dendrogram, the best Newman-Girvan Q step is returned as the final partition; internal graph synthesizes a per-vertex self-loop with weight = mean incident edge weight (1.0 if isolated) — same fix as upstream issue #2043 — so the probability vectors handle leaf/isolated vertices without numerical pathology; probability representation is a dense `Vec<f64>` sized to vcount (the C reference switches between full and sparse Map<u32,f64> at runtime — deferred to alpha.3); hand-rolled binary min-heap with `heap_push`/`heap_pop`/`sift_up`/`sift_down`, lazy-refine pattern (entries with `!exact` are popped, refined via the triangle Δσ formula `Δσ' = ((s₁+s_k)·d₁ + (s₂+s_k)·d₂ − s_k·Δσ_old) / (s₁+s₂+s_k)` or chain lower-bound `−1/min(|adj(new)|, |adj(k)|)`, marked exact, re-inserted) — no external heap dependency, no `unsafe`; `WalktrapResult { membership, nb_clusters, merges, modularity }` mirrors `igraph_community_walktrap`: dendrogram `[c1, c2]` rows for the merge tree, full Q trajectory from singletons to one community, densified `[0, k)` best-Q membership; modularity trajectory matches the C `community_walktrap.out` reference (triangle / bug-2042 / 6-ring weighted / isolated) to ~1e-15; conformance: Zachary karate (Q ∈ [0.30, 0.45], k ∈ [3, 6]), K5+K5+bridge / two-K4-bridge (k=2), ring-of-4-cliques (k=4), 6-ring weighted (k=3, Q ≈ 0.146); bench: triangle ~10.8µs / ring-6 weighted ~26.4µs / two-K5-bridge ~412µs / ring-of-cliques(4×5) ~262µs / karate ~932µs — Rust wins decisively at smallest scales (triangle ~2× faster than python-igraph 0.11.9, ring-6 weighted ~7× faster), python-igraph still wins on karate / ring-of-cliques ~3-5× because of dense-vs-sparse probability vectors and per-merge Δσ recomputation; both optimisations are accuracy-preserving and tracked for alpha.3),
CM-013 (`community_to_membership(merges, nodes, steps) -> Result<CommunityToMembershipResult>` — pure-function helper mirroring `igraph_community_to_membership` in `references/igraph/src/community/community_misc.c`; cuts a binary dendrogram at `steps` merges and returns a densified `0..k` membership vector + per-cluster `csize`; algorithm walks merges top-down (`steps-1 → 0`) assigning 1-based supercluster ids via a `tmp` slot per row, marks `already_merged` to detect double-merges, propagates ids down through internal nodes; final pass densifies leaves with `cid == 0` as fresh singletons; O(steps + n) total work — allocates one `Vec<u32>` of size `n`, one `Vec<bool>` of size `n + steps`, one `Vec<u32>` of size `steps`; errors `InvalidArgument` on `steps > rows`, double-merge, out-of-range entry (`≥ nodes + i`), or usize overflow of `nodes + steps`; 11 unit tests (zero steps, full collapse, intermediate cut, untouched leaves, chained merges, empty graph, all 3 error paths, partial dendrogram, end-to-end against `walktrap` on two K4 + bridge) + 3 proptests (zero-steps singletons, full collapse, cut invariants: csize sums to n / cluster count = n − steps / every membership entry < cluster count) + 6 three-source conformance fixtures (2 each from c/py/r, partition-equivalence comparison because cluster labels can differ between implementations); bench: chain-64 full collapse ~470ns / balanced-256 full ~1.7µs / balanced-1024 full ~6.6µs / balanced-1024 half cut ~2.95µs / balanced-1024 zero steps ~1.26µs — ~28-30× faster than python-igraph 0.11.9 `VertexDendrogram.as_clustering` on full collapses, ~106× on a half cut, ~342× at zero steps; example `community_to_membership_walktrap_karate` cuts the walktrap karate dendrogram at 0/8/16/29/33 steps and reports Q at each depth).
CM-014 (`reindex_membership(&[u32]) -> Result<ReindexMembershipResult>` — pure-function helper mirroring `igraph_reindex_membership` + `igraph_i_reindex_membership_large` in `references/igraph/src/community/community_misc.c`; relabels a `membership[v] = c` vector with arbitrary `u32` ids into a contiguous `0..k` labelling assigned in first-occurrence order, also reporting the original cluster id behind each new id via `new_to_old`; two-branch single pass: fast path (`max_id < n`) uses a flat `Vec<u32>` lookup of length `n` with 0 as the "unseen" sentinel and stores `new_id + 1` so the zero-init is reusable; sparse fallback uses a `BTreeMap<u32, u32>` keyed by old cluster id when ids overflow the `[0, n)` window so the O(n) flat-Vec path stays bounded; O(n) avg / O(n log k) in the sparse branch; allocates one `Vec<u32>` of size `n` (output) plus the lookup (fast: `Vec<u32>` of size `n`; sparse: `BTreeMap<u32, u32>` of size `k`) plus a `Vec<u32>` of size `k` (`new_to_old`); infallible today but returns `IgraphResult` for API symmetry with other community helpers; 11 unit tests (empty, identity, singleton collapse, gaps compressed, reverse-order, all-singletons, large-id triggers BTreeMap, fast-path edge `max == n−1`, sparse-path edge `max == n`, partition-preserved over 3 mixed inputs incl. `u32::MAX`, fast-path-equals-sparse-path bookkeeping check) + 4 proptests over 80 cases each (partition_preserved across all id-kind branches, ids_are_contiguous in `[0, k)`, new_to_old_round_trips so `r.new_to_old[r.membership[i]] == input[i]`, idempotent_when_already_dense) + 6 three-source conformance fixtures (2 each from c/py/r, partition-equivalence comparison via canonical first-occurrence relabel + new_to_old round-trip check because cluster numbering can differ between C/py/R implementations); bench: fast 256v/8c ~0.32µs / fast 1024v/32c ~0.98µs / fast 10000v/100c ~8.23µs / sparse 1024v/32c ~5.00µs / sparse 10000v/100c ~63.44µs — 320× / 160× / 107× faster than python-igraph 0.11.9 `VertexClustering(g, membership=...).membership` on the fast path and 32× / 13× on the sparse path; example `reindex_membership_walktrap_karate` builds a messy karate membership with cluster ids 1_000 / 4_242 / 9_999 (forcing the sparse branch), densifies to `0..k-1`, and verifies modularity Q is preserved bit-for-bit before and after).
CM-016 (`split_join_distance(comm1, comm2) -> IgraphResult<SplitJoinDistance>` — pure-function helper mirroring `igraph_split_join_distance` in `references/igraph/src/community/community_misc.c`; returns the asymmetric pair `SplitJoinDistance { d12: u64, d21: u64 }` with `total() -> u64` giving `d12 + d21` which matches the symmetric scalar from `compare_communities(_, _, SplitJoin)`; computes `d12 = n − Σᵢ max_j n_ij` projecting comm1 onto comm2, `d21 = n − Σⱼ max_i n_ij` projecting comm2 onto comm1, with `n = comm1.len() = comm2.len()`; `d12 == 0` ⇔ comm1 is a sub-partition of comm2 (every comm1-cluster fits entirely inside one comm2-cluster), and vice versa for `d21 == 0` — the asymmetric pair preserves the refinement-relationship information that the scalar collapses; densifies both inputs via `reindex_membership` (CM-014) then delegates to the shared `split_join_distances(pub(crate))` helper promoted from CM-015 which walks a sparse `HashMap<(u32, u32), u32>` confusion matrix; empty inputs return `(0, 0)`; errors only on length mismatch (`InvalidArgument`); 8 unit tests (empty inputs zero pair, length-mismatch error, identical partition zero, full disagreement 2x2 `d12=d21=2 total=4` matching CM-015's `SplitJoin=4`, sub-partition asymmetry `[0,0,1,2]` vs `[0,0,0,1]` → `d12=0, d21=1`, cross-check against `compare_communities(_, _, SplitJoin)` on 8-vertex mixed partition, relabel-invariance under arbitrary cluster-id remap, all-singletons partition `(0..5)`) + 5 proptests over 256 cases each (non-negative components in `[0, n]`, total equals `compare_communities` with `SplitJoin`, relabel-invariance under wrapping_add/wrapping_mul, identical partitions → `(0, 0)`, coarsened sub-partition `b = a / 2` → `d12 == 0`); 6 three-source conformance fixtures (2 each from c/py/r, two scenarios shared: sub-partition / refinement → asymmetric pair where one side is 0, and 2x2 full-disagreement `d12=d21=2`; expected emitted as `{"d12", "d21"}` object — conformance test asserts exact equality on both components; from_c calls igraph_split_join_distance directly, from_py declares fixed references derived from confusion-matrix decomposition since python-igraph exposes only the symmetric scalar, from_r mirrors R-igraph's `split_join_distance()` which returns the length-2 integer vector); bench: small 256v/8c ~5.29µs / medium 10000v/100c ~174.74µs / subpartition coarsening 10000v/100c ~172.92µs — 3.31× / 3.94× / 3.57× faster than python-igraph 0.11.x `igraph.compare_communities(method='split_join')` baseline (the symmetric scalar has the same internal cost as returning the pair, so this is an apples-to-apples comparison; speedup is consistent with CM-015 since both share the confusion-matrix machinery and the FFI boundary is the dominant python-side cost); example `split_join_distance_louvain_vs_leiden_karate` runs louvain + leiden + leiden(iter=6) on Zachary's karate club, prints the asymmetric pair with a one-line interpretation per pair ("identical / sub-partition / neither is a refinement"), demonstrates that louvain vs leiden(default) yields `d12=d21=3 total=6` (neither refines the other) while leiden default and leiden iter=6 produce identical partitions; perf snapshot at `.codefuse/tracking/perf/ALGO-CM-016.json`).

CM-015 (`compare_communities(comm1, comm2, CommunityComparison) -> IgraphResult<f64>` — pure-function helper mirroring `igraph_compare_communities` in `references/igraph/src/community/community_misc.c`; given two membership vectors of equal length, computes one of five partition-distance measures: `VariationOfInformation` (Meilă 2003, `VI = H(C₁|C₂) + H(C₂|C₁)`, natural-log basis matching igraph C / python-igraph), `NormalizedMutualInformation` (Danon 2005, `2·I(C₁,C₂)/(H₁+H₂)` clamped to `[0,1]` with the `H₁ = H₂ = 0` degenerate case returning 1), `SplitJoin` (van Dongen 2000, `2n − Σᵢmax_j n_ij − Σⱼmax_i n_ij`), `Rand` (Rand 1971, agreeing pairs / total pairs in `[0,1]`), `AdjustedRand` (Hubert-Arabie 1985, `(RI − E[RI])/(max RI − E[RI])` with `0/0 → 1` per sklearn for single-cluster pairs); densifies both inputs via `reindex_membership` (CM-014) then builds a sparse `HashMap<(u32, u32), u32>` confusion matrix (O(observed) memory rather than dense O(k₁·k₂)); per-method post-processing uses the dense `p₁`/`p₂` marginals plus a single sparse-matrix walk; empty inputs yield VI=0 / NMI=1 / SJ=0 / Rand and AR error out; 11 unit tests (identical 6v partition, identical with relabel, two-class disagreement closed-form `VI=2·ln(2)`, four-vertex full disagreement closed-form `Rand=1/3` `AR=−0.5` `SJ=4`, single-cluster vs single-cluster `NMI=1` and `AR=1` 0/0 case, three-way subpartition split-join, empty input degenerate cases, the three error paths) + 7 proptests over 64 cases each (NMI in `[0,1]`, VI ≥ 0, Rand in `[0,1]`, AR ≤ 1, relabel-invariance under arbitrary id remap, NMI symmetric in arguments, identical-partition extremals: VI=0 / NMI=1 / SJ=0 / Rand=1 / AR=1) + 6 three-source conformance fixtures (2 each from c/py/r covering all 5 methods at least once, expected scalar comparison within 1e-9 to closed-form arithmetic; from_c emits `{"value": f64}` and snake_case method names matching igraph C, from_py mirrors that, from_r uses bare scalar + CamelCase enum names — the conformance test accepts both shapes); bench: vi/nmi small 256v/8c ~6.4µs medium 10000v/100c ~197µs; rand/adjusted_rand small ~5.5µs medium ~183µs; split_join small ~5.5µs medium ~208µs — 2.5-3× faster than python-igraph 0.11.9 `igraph.compare_communities(method=...)` across all 5 methods at both sizes (modest because python-igraph itself delegates to the C core, so the gap mostly reflects FFI overhead avoided); example `compare_communities_walktrap_louvain_karate` runs walktrap and louvain on Zachary's karate club, prints all 5 metrics for the walktrap-vs-louvain pair (NMI ≈ 0.82, AR ≈ 0.77) plus a walktrap-stability cell at steps=4 vs steps=8 (AR ≈ 0.53)).
PR-032 (`constraint(graph, weights) -> IgraphResult<Vec<f64>>` — Burt's structural holes constraint; measures how much a vertex's connections are redundant via proportional tie strengths and two-hop indirect paths; isolated vertices return NaN; supports optional edge weights and directed/undirected graphs; O(V + E + n·d²) where d is max degree; 13 unit tests + 3 proptests).
LO-001 (`layout_random`, `layout_random_3d`, `layout_circle`, `layout_star`, `layout_grid`, `layout_grid_3d`, `layout_sphere` — simple deterministic/random graph layouts in 2D and 3D; `layout_random`/`layout_random_3d` place vertices uniformly in `[-1,1]²`/`[-1,1]³` via `SplitMix64`; `layout_circle` places vertices on a unit circle with optional ordering; `layout_star` places center at origin, rest on unit circle; `layout_grid`/`layout_grid_3d` place on regular 2D/3D grids with auto-width/height; `layout_sphere` uses the Saff-Kuijlaars spiral algorithm for approximately uniform distribution on a unit sphere; all O(V); 27 unit tests + 4 proptests + 7 doctests).
PR-033 (`centralization` + `centralization_degree_tmax` + `centralization_betweenness_tmax` + `centralization_closeness_tmax` + `centralization_eigenvector_tmax` + `CentralizationMode` + `LoopMode` — graph-level centralization index C = Σ_v (max_u c_u - c_v) with optional normalization by theoretical maximum for star graphs; degree tmax covers directed/undirected × 3 loop modes; betweenness/closeness/eigenvector tmax cover directed/undirected × mode variants; pure math functions with no graph dependency; O(n) for score aggregation, O(1) for all tmax functions; 26 unit tests + 3 proptests + 5 doctests).
PR-034 (`is_graphical` + `is_bigraphical` + `EdgeTypeFilter` — degree-sequence realizability tests; `EdgeTypeFilter` enum {Simple, LoopsSimple, Multi, LoopsMulti} selects edge constraints; undirected simple uses Erdős–Gallai theorem via Cloteaux 2015 linear-time algorithm with Zverovich sufficient-condition shortcut; undirected loopy-simple uses Cairns–Mendan modification; directed simple uses Fulkerson–Chen–Anstee theorem with Berger 2014 relaxation; directed loopy-simple reduces to bigraphical simple via Gale–Ryser; bigraphical simple uses Gale–Ryser with Berger 2014 relaxation; multi variants use sum/parity checks; pure math functions with no graph dependency; O(n) for all variants; 36 unit tests + 6 proptests + 2 doctests).
PR-035 (`strength` + `strength_with_mode` + `diversity` + `StrengthMode` — weighted vertex degree (sum of incident edge weights) with directed mode support (Out/In/All) and self-loop control (IGRAPH_LOOPS_TWICE semantics), plus Eagle-Macy-Claxton 2010 structural diversity index (normalized Shannon entropy of incident edge weights); single-pass O(V+E) for both; diversity requires undirected simple graphs with non-negative finite weights; 24 unit tests + 4 proptests + 3 doctests).

CY-001 (`find_cycle` + `CycleMode` + `CycleResult` — iterative DFS cycle detection for directed and undirected graphs; `CycleMode` enum {Out, In, All} controls edge traversal direction; returns first cycle found as vertex + edge lists with first==last vertex; directed uses gray/black coloring, undirected tracks parent to avoid trivial back-edges; O(V+E); 11 unit tests + 1 doctest).
CY-003 (`simple_cycles` + `SimpleCycleMode` + `SimpleCycle` — Johnson's 1975 algorithm for enumerating all elementary circuits; iterative DFS with blocking/unblocking mechanism; min/max cycle length filters; max_results early termination; undirected duplicate suppression via canonical edge ordering; O((V+E)(C+1)) where C is the number of cycles; 15 unit tests + 1 doctest).
MO-002 (`triad_census` + `TriadCensus` + `TriadType` — 16-class Davis-Leinhardt triad classification; enumerates all `n*(n-1)*(n-2)/6` vertex triples and classifies each into one of the 16 MAN isomorphism types using a flat `n×n` dyad matrix; undirected edges treated as mutual; O(n³); 23 unit tests + 1 doctest).
CC-050 (`is_connected` + `ConnectednessMode` — connectivity predicate without full component computation; weak mode uses BFS over symmetric adjacency built from edge iteration; strong mode uses forward+reverse BFS from vertex 0; O(V+E); 12 unit tests + 1 doctest).
CC-051 (`subcomponent` + `SubcomponentMode` — BFS reachability from a source vertex with direction control; Out/In/All modes; returns vertices in BFS discovery order; builds adjacency from edge iteration; O(V+E); 12 unit tests + 1 doctest).
OP-020 (`to_undirected` + `ToUndirectedMode` — directed→undirected conversion; Each/Collapse/Mutual modes; O(E log E) for Collapse/Mutual due to canonical sorting; 13 unit tests + 1 doctest).
OP-021 (`to_directed` + `ToDirectedMode` — undirected→directed conversion; Mutual/Arbitrary modes; O(E); 11 unit tests + 1 doctest).
PR-036 (`degree_sequence` + `max_degree` + `min_degree` + `max_degree_vertex` + `DegreeMode` — batch degree computation with Out/In/All modes; handles self-loops per IGRAPH_LOOPS_TWICE convention; O(E); 15 unit tests + 4 doctests).

CN-035 (`full_bipartite` + `FullBipartite` — complete bipartite graph K_{n1,n2} constructor; reuses `BipartiteMode` from games::bipartite (Out/In/All); returns graph + per-vertex bool type vector; supports undirected, directed-out, directed-in, and directed-mutual; O(n1*n2) edge emission; 12 unit tests + 1 doctest).

CN-036 (`create_bipartite` — validated bipartite graph constructor from type vector + edge list; verifies every edge crosses partition boundary; rejects same-type edges and out-of-range vertex IDs; O(V+E); 11 unit tests + 1 doctest).

CN-037 (`biadjacency` + `BiadjacencyResult` — bipartite graph from n1×n2 biadjacency matrix; supports multiple=true (integer multiplicities) and multiple=false (any non-zero → single edge); directed mode Out/In/All with mutual arcs; returns graph + types vector; O(n1*n2); 13 unit tests + 1 doctest).

PR-042 (`get_edgelist` — returns all edges as Vec<(VertexId, VertexId)> in edge-ID order; O(E); 6 unit tests + 1 doctest).

PR-043 (`get_biadjacency_matrix` + `GetBiadjacencyResult` — extract biadjacency matrix from bipartite graph given types vector; returns n1×n2 matrix + row_ids + col_ids; silently ignores same-partition edges; O(V+E); 13 unit tests + 1 doctest).

PR-044 (`are_adjacent` — check if two vertices share at least one edge; delegates to Graph::find_eid; O(deg(min)); 8 unit tests + 1 doctest).

PR-045 (`joint_degree_distribution` — joint degree distribution matrix P_ij; configurable from/to degree modes, directed_neighbors flag, optional normalization, max degree clipping; O(E); 11 unit tests + 1 doctest).

PR-046 (`get_biadjacency_weighted` + `GetBiadjacencyWeightedResult` — weighted biadjacency matrix extraction; sums edge weights per (row,col) pair; O(V+E); 9 unit tests + 1 doctest).

CN-038 (`weighted_biadjacency` + `WeightedBiadjacencyResult` — bipartite graph from weighted n1×n2 biadjacency matrix; non-zero entries become edges with weight=value; supports directed Out/In/All modes with mutual arcs; returns graph + types + weights; O(n1*n2); 11 unit tests + 1 doctest).

PR-047 (`closeness_cutoff` + `ClosenessCutoffResult` — range-limited closeness centrality; BFS depth bounded by cutoff; returns scores + reachable_count + all_reachable flag; handles normalization by reachable component size; O(V*(V+E)); 11 unit tests + 1 doctest).

PR-048 (`harmonic_centrality_cutoff` — range-limited harmonic centrality; sum of inverse distances within cutoff hops; optional normalization by n-1; O(V*(V+E)); 11 unit tests + 1 doctest).

PR-049 (`betweenness_cutoff` — range-limited vertex betweenness via depth-bounded Brandes algorithm; BFS stops expanding at cutoff depth; undirected results halved; O(V*(V+E)); 11 unit tests + 1 doctest).

PR-050 (`edge_betweenness_cutoff` — range-limited edge betweenness centrality via depth-bounded Brandes; pred stores (VertexId, EdgeId) tuples; uses incident()+edge_other(); undirected results halved; O(V*(V+E)); 9 unit tests + 1 doctest).

PR-051 (`betweenness_subset` — subset-limited vertex betweenness centrality; modified Brandes where only target vertices seed +1 in back-propagation; supports directed/undirected; handles directed-graph-as-undirected via combined out+in neighbors; undirected results halved; O(|S|*(V+E)); 12 unit tests + 1 doctest).

PR-052 (`edge_betweenness_subset` — subset-limited edge betweenness centrality; Brandes with target-only seeding; pred stores (VertexId, EdgeId) tuples; directed-graph-as-undirected via combined incident+incident_in; O(|S|*(V+E)); 10 unit tests + 1 doctest).

PR-053 (`personalized_pagerank_vs` — vertex-set convenience wrapper for personalized PageRank; constructs uniform reset vector over specified vertices then delegates to personalized_pagerank; O(V+E) per iteration; 4 unit tests + 1 doctest).

CL-004 (`maximal_cliques_count` + `clique_size_hist` — counting maximal cliques without full enumeration and histogram of clique sizes; Bron-Kerbosch variants that increment counters instead of storing cliques; O(3^(n/3)) worst case; 10 unit tests + 2 doctests).

---

**Phase 1 complete (420 algorithms done).** Next: Phase 2 (I/O formats for practical utility).

CC-023 (`reachability` — SCC-based per-component reachability bitsets; condenses directed graph into DAG of SCCs, propagates bitsets in reverse topological order; O(|C|·|V|/w + |V| + |E|); supports Out/In/All modes; 14 unit tests + 1 doctest).

CL-005 (`maximal_cliques_subset` — enumerate maximal cliques containing at least one vertex from a given subset; full BK enumeration then filter by subset membership + size bounds + max_results cap; directed graphs treated as undirected; O(3^(V/3)) worst case; 9 unit tests + 1 doctest).

CY-004 (`feedback_vertex_set` + `FvsAlgorithm` — greedy heuristic for feedback vertex set: iteratively finds a cycle via DFS with a removed-vertex mask, removes the highest-degree (or highest-weight) vertex from it, and repeats until acyclic; supports directed and undirected graphs, optional per-vertex weights; `FvsAlgorithm::Greedy` accepted for API parity with the C enum — the C version only has `IGRAPH_FVS_EXACT_IP` which requires GLPK; our greedy heuristic is dependency-free and produces reasonable approximations; O((V+E)·|FVS|); 18 unit tests + 1 doctest).

VC-001 (`minimum_vertex_cover` + `is_vertex_cover` — greedy 2-approximation for minimum vertex cover: repeatedly picks an uncovered edge and adds both endpoints; marks all incident edges of newly-covered vertices to skip redundant work; guaranteed at most 2× optimal (König 1931 / Gavril 1974); `is_vertex_cover` validator checks every edge has at least one endpoint in the set; supports directed and undirected graphs, self-loops, parallel edges; O(V+E); 16 unit tests + 2 doctests).

VC-002 (`maximum_independent_set` — greedy heuristic for maximum independent set: repeatedly picks the vertex with minimum effective degree among remaining candidates, adds it to the independent set, removes it and all its neighbors; uses both in- and out-neighbors for directed graphs; pre-excludes self-loop vertices; O(V·(V+E)); 14 unit tests + 2 doctests).

VC-003 (`minimum_dominating_set` + `is_dominating_set` — greedy O(ln n)-approximation for minimum dominating set: precomputes closed neighborhoods (vertex + all neighbors, both directions for directed), repeatedly picks the vertex that dominates the most undominated vertices; `is_dominating_set` validator checks every vertex is either in the set or adjacent to a set member; supports directed and undirected graphs, self-loops; O(V·(V+E)); 17 unit tests + 2 doctests).

COL-002 (`edge_coloring_greedy` + `edge_chromatic_number` — greedy edge coloring: iterates edges in order, assigns the smallest color not used by any adjacent edge; handles directed graphs (both in/out incidence), self-loops (not self-adjacent per igraph convention), parallel edges; `edge_chromatic_number` returns the count of distinct colors used; up to 2·Δ-1 colors in the worst case; O(E·Δ); 14 unit tests + 2 doctests).

MA-002 (`minimum_edge_cover` + `is_edge_cover` — minimum edge cover via greedy maximal matching + unmatched vertex extension; an edge cover is a set of edges covering all vertices; errors on isolated vertices (no edge cover exists); handles directed graphs (both in/out incidence for coverage), self-loops, parallel edges; optimal by König's theorem: |cover| = n - |matching|; O(V+E); 16 unit tests + 2 doctests).

FL-040 (`maximum_cut` + `cut_value` + `MaxCutResult` — greedy 0.5-approximation for maximum cut: partitions vertices into two sets to maximize crossing edges; processes vertices in order, placing each on the side with fewer already-placed neighbors; `cut_value` counts edges crossing a given partition; handles directed graphs (merged in/out neighbors), self-loops (skipped — never cross), parallel edges; guaranteed ≥ |E|/2 crossing edges; O(V+E); 14 unit tests + 2 doctests).

PR-070 (`is_regular` + `regularity` — check whether a graph is k-regular (all vertices have same degree); for directed graphs checks both out-degree and in-degree uniformity and equality; `regularity` returns the common degree k or None; O(V+E); 18 unit tests + 2 doctests).

COL-003 (`vertex_chromatic_number` + `chromatic_number_upper_bound` — vertex chromatic number from a coloring vector and convenience DSatur upper bound; `vertex_chromatic_number` counts distinct colors (max+1); `chromatic_number_upper_bound` runs DSatur greedy coloring and returns the count; O(V²·d_max) via DSatur; 14 unit tests + 2 doctests).

PR-071 (`is_cactus_graph` — check whether a graph is a cactus (connected, every biconnected component is a single edge or simple cycle); uses biconnected_components; handles directed (weak connectivity + undirected bicomp), self-loops, parallel edges; O(V+E); 17 unit tests + 1 doctest).

PR-072 (`is_split_graph` — check whether a graph is a split graph via Hammer-Simeone degree-sequence characterization; sort degrees descending, find m = max{i : d_i >= i-1}, split iff sum(d_1..d_m) = m(m-1) + sum(d_{m+1}..d_n); returns false for directed/non-simple graphs; O(V+E); 16 unit tests + 1 doctest).

PR-073 (`is_threshold_graph` — check whether a graph is a threshold graph via degree-sequence peeling; sort degrees descending, repeatedly remove dominating (degree n-1) or isolated (degree 0) vertices; if top degree is neither 0 nor n-1 at any step, graph is not threshold; returns false for directed/non-simple graphs; O(V log V); 20 unit tests + 1 doctest).

PR-074 (`is_block_graph` — check whether a graph is a block graph (every biconnected component is a clique); uses biconnected_components; each block with k vertices must have k(k-1)/2 edges; requires connectivity; returns false for directed graphs; O(V+E); 19 unit tests + 1 doctest).

PR-075 (`is_pseudo_forest` — check whether every connected component has at most one cycle; equivalently |E| <= |V| per component; uses connected_components; handles self-loops and parallel edges; returns false for directed graphs; O(V+E); 19 unit tests + 1 doctest).

PR-076 (`is_self_complementary` — check whether a graph is isomorphic to its complement; fast edge-count filter (need n(n-1)/4 edges) then VF2 isomorphism test; returns false for directed graphs; O(V^2) complement + VF2; 14 unit tests + 1 doctest).

PR-077 (`is_cluster_graph` — check whether a graph is a disjoint union of cliques (P3-free); each connected component must be complete; uses connected_components; rejects self-loops and parallel edges; returns false for directed graphs; O(V+E); 16 unit tests + 1 doctest).

PR-078 (`is_geodetic` — check whether every pair of reachable vertices has a unique shortest path; BFS from each vertex tracking shortest-path counts; supports directed graphs; O(V*(V+E)); 18 unit tests + 1 doctest).

PR-079 (`is_distance_hereditary` — check whether a graph is distance-hereditary via pruning; repeatedly remove pendant vertices (degree 1), true twins (adjacent with identical closed neighborhoods), and false twins (non-adjacent with identical open neighborhoods); per-component via connected_components; returns false for directed graphs; O(V^2) worst case; 17 unit tests + 1 doctest).

PR-080 (`is_cograph` — check whether a graph is a cograph (P4-free / complement-reducible); recursive characterization: G is a cograph iff G or complement(G) is disconnected and all components are cographs; uses connected_components and complementer; returns false for directed graphs; 20 unit tests + 1 doctest).

PR-081 (`is_ptolemaic` — check whether a graph is ptolemaic (chordal ∩ distance-hereditary); composes is_chordal and is_distance_hereditary; returns false for directed graphs; 17 unit tests + 1 doctest).

PR-082 (`is_strongly_regular` — check whether a graph is strongly regular; returns optional `StronglyRegularParams(n, k, λ, μ)`; checks regularity then verifies all adjacent pairs share λ common neighbors and all non-adjacent pairs share μ; handles complete/edgeless degenerate cases; returns None for directed/non-simple graphs; 16 unit tests + 1 doctest).

PR-083 (`is_biclique` — check whether a graph is a complete bipartite graph (biclique); uses is_bipartite to partition, then verifies edge count equals part_a × part_b; returns false for directed graphs; handles empty/single-vertex/K_{m,0} degenerate cases; 16 unit tests + 1 doctest).

PR-084 (`is_tournament` — check whether a directed graph is a tournament (complete asymmetric); verifies directed, simple, n(n-1)/2 edges, no mutual edges; returns false for undirected graphs; 13 unit tests + 1 doctest).

PR-085 (`is_wheel` — check whether a graph is a wheel graph; finds hub vertex of degree n-1, verifies all rim vertices have degree 3, walks the rim to confirm it forms a Hamiltonian cycle; returns false for directed/non-simple graphs and n < 4; 13 unit tests + 1 doctest).

PR-086 (`is_caterpillar` — check whether a graph is a caterpillar tree; verifies is_tree then checks that all non-leaf vertices have at most 2 non-leaf neighbors (spine forms a path); returns false for directed graphs; 15 unit tests + 1 doctest).

PR-087 (`is_lobster` — check whether a graph is a lobster tree; verifies is_tree then double-prunes leaves: first pass identifies leaf1 (degree 1) and computes pruned degrees, second pass identifies leaf2 (pruned_deg ≤ 1) and checks remaining vertices have ≤ 2 non-leaf1-non-leaf2 neighbors; returns false for directed graphs; 13 unit tests + 1 doctest).

PR-088 (`is_path` — check whether a graph is a path graph; verifies is_tree and all vertices have degree ≤ 2; returns false for directed graphs; 13 unit tests + 1 doctest).

PR-089 (`is_cycle` — check whether a graph is a cycle graph; verifies n ≥ 3, exactly n edges, all vertices have degree 2, and connected; returns false for directed graphs; 14 unit tests + 1 doctest).

PR-090 (`is_complete_multipartite` — check whether a graph is a complete multipartite graph; computes complement, verifies each connected component of complement is a clique (= independent set in original), validates edge count; returns `Some(parts)` with sorted part sizes or `None`; returns false for directed/non-simple graphs; 17 unit tests + 1 doctest).

PR-091 (`is_star` — check whether a graph is a star graph K_{1,n-1}; verifies is_tree and exactly one vertex has degree n-1 with all others having degree 1; returns false for directed graphs and empty graph; 12 unit tests + 1 doctest).

PR-092 (`is_windmill` — check whether a graph is a windmill graph Wd(k,n); finds hub vertex of degree n-1, verifies all non-hub vertices have uniform degree k-1, validates edge count, then walks clique structure; returns `Some((k, n))` or `None`; returns false for directed/non-simple graphs; 12 unit tests + 1 doctest).

PR-093 (`is_unicyclic` — check whether a graph is unicyclic (connected with exactly one cycle); verifies connected + exactly n edges; returns false for directed graphs; 13 unit tests + 1 doctest).

PR-094 (`is_trivially_perfect` — check whether a graph is trivially perfect (quasi-threshold); recursively verifies every connected component has a universal vertex; returns false for directed graphs; 17 unit tests + 1 doctest).

PR-095 (`is_semicomplete` — check whether a directed graph is semicomplete (every pair of distinct vertices has at least one arc); builds adjacency matrix and checks all pairs; returns false for undirected graphs; 12 unit tests + 1 doctest).

PR-096 (`is_claw_free` — check whether a graph has no induced `K_{1,3}` (claw); for each vertex checks if any three neighbors are mutually non-adjacent; returns false for directed graphs; 14 unit tests + 1 doctest).

PR-097 (`is_diamond_free` — check whether a graph has no induced diamond (`K_4` minus one edge); for each edge checks if any pair of common neighbors is non-adjacent; returns false for directed graphs; 14 unit tests + 1 doctest).

PR-098 (`is_gem_free` — check whether a graph has no induced gem (fan `F_{1,3}`: `P_4` + universal vertex); for each high-degree vertex checks if its neighborhood contains an induced `P_4`; returns false for directed graphs; 11 unit tests + 1 doctest).

PR-099 (`is_paw_free` — check whether a graph has no induced paw (triangle + pendant edge); for each triangle checks if any vertex is adjacent to exactly one member; returns false for directed graphs; 13 unit tests + 1 doctest).

PR-100 (`is_bull_free` — check whether a graph has no induced bull (triangle with two pendant edges from distinct triangle vertices); enumerates triangles and checks all 3 pairs for pendant neighbors; returns false for directed graphs; 13 unit tests + 1 doctest).

PR-101 (`is_c4_free` — check whether a graph has no induced `C_4` (chordless 4-cycle); for each edge (a,b) finds c adjacent to b but not a, then d adjacent to both c and a but not b; `K_4` is `C_4`-free, Petersen is `C_4`-free (girth 5); returns false for directed graphs; 14 unit tests + 1 doctest).

PR-102 (`is_c5_free` — check whether a graph has no induced `C_5` (chordless pentagon); for each edge (a,b) chains through non-adjacent neighbors to find a 5-cycle with no chords; Petersen graph has induced `C_5` (girth 5); returns false for directed graphs; 14 unit tests + 1 doctest).

PR-103 (`is_bowtie_free` — check whether a graph has no induced bowtie (butterfly/hourglass: two triangles sharing a single vertex); for each vertex collects triangle-partner pairs among neighbors, then checks if any two triangle pairs are vertex-disjoint with no cross-edges; `K_5` is bowtie-free (cross-edges present); returns false for directed graphs; 14 unit tests + 1 doctest).

PR-104 (`is_cricket_free` — check whether a graph has no induced cricket (triangle with two pendant edges from the *same* vertex); for each triangle tries each vertex as pendant hub and checks for two non-adjacent pendant neighbors; differs from bull where pendants hang from different vertices; returns false for directed graphs; 13 unit tests + 1 doctest).

PR-105 (`is_house_free` — check whether a graph has no induced house (`C_5` with one chord, equivalently triangle + `P_2` extension completing a 5-cycle); for each triangle tries all 6 orientations of the chord/roof and searches for roof vertices d, e; Petersen graph is house-free (triangle-free); returns false for directed graphs; 14 unit tests + 1 doctest).

PR-106 (`is_banner_free` — check whether a graph has no induced banner (flag: `C_4` + pendant edge); finds each induced `C_4` then checks if any cycle vertex has a pendant neighbor outside; `K_{3,3}` is banner-free (outside vertices connect to 2 cycle vertices); returns false for directed graphs; 14 unit tests + 1 doctest).

PR-107 (`is_dart_free` — check whether a graph has no induced dart (diamond + pendant from a wing/degree-2 vertex); finds diamonds via common-neighbor pairs of edges, then checks wing vertices for pendants; pendant from spine vertex does NOT form a dart; returns false for directed graphs; 14 unit tests + 1 doctest).

PR-108 (`is_fork_free` — check whether a graph has no induced fork (claw `K_{1,3}` with one edge subdivided); for each induced claw checks if any arm has a pendant neighbor forming the fork; star graphs are fork-free (leaves have no pendants); returns false for directed graphs; 13 unit tests + 1 doctest).

PR-109 (`is_p5_free` — check whether a graph has no induced `P_5` (path on 5 vertices); chains through edges finding a-b-c-d-e with no chords; `C_5` is `P_5`-free (closing edge is a chord), `C_6` is not; returns false for directed graphs; 14 unit tests + 1 doctest).

PR-110 (`is_co_bipartite` — check whether a graph is co-bipartite (complement is bipartite, vertex set partitions into two cliques); builds adjacency matrix, 2-colors complement via BFS; `K_n` and `C_4` are co-bipartite; `C_5` is not (self-complementary odd cycle); 3 isolated vertices not (complement `K_3` not bipartite); returns false for directed graphs; 14 unit tests + 1 doctest).

PR-111 (`is_chordal_bipartite` — check whether a bipartite graph is chordal bipartite (no induced `C_6` or longer); uses bisimplicial elimination ordering on adjacency matrix; handles disconnected graphs via component decomposition; complete bipartite graphs are chordal bipartite; `C_6` and `C_8` are not; returns false for non-bipartite or directed graphs; 14 unit tests + 1 doctest).

PR-112 (`is_well_covered` — check whether all maximal independent sets have the same cardinality; Bron-Kerbosch enumeration with early termination on size mismatch; complete graphs, odd cycles, and complete bipartite graphs are well-covered; stars with ≥ 3 leaves are not; directed graphs treated as undirected; 14 unit tests + 1 doctest).

PR-113 (`is_strongly_chordal` — check whether a chordal graph admits a strong elimination ordering; uses MCS to find PEO then verifies later-neighborhood inclusion ordering; complete graphs and trees are strongly chordal; sun graphs (chordal but with induced trampoline) are not; directed graphs treated as undirected; 14 unit tests + 1 doctest).

PR-114 (`is_chain_graph` — check whether a bipartite graph is a chain graph (neighborhoods in each part totally ordered by inclusion, equivalently no induced `2K_2`); partitions via bipartite check then verifies nested neighborhoods using sorted inclusion test; complete bipartite and star graphs are chain graphs; `C_6` and `2K_2` are not; returns false for non-bipartite or directed graphs; 16 unit tests + 1 doctest).

PR-115 (`is_proper_interval` — check whether a graph is a proper interval graph (unit interval graph); equivalent to chordal + claw-free (Roberts/Wegner characterization); paths, complete graphs, and diamonds are proper interval; stars and cycles ≥ 4 are not; returns false for directed graphs; 15 unit tests + 1 doctest).

PR-116 (`is_k_degenerate` + `degeneracy` — check whether every subgraph has a vertex of degree ≤ k (equivalently coreness ≤ k); uses existing coreness decomposition; 0-degenerate = edgeless, 1-degenerate = forest; `degeneracy` returns the smallest such k; directed graphs treated as undirected; 13 unit tests + 2 doctests).

PR-117 (`is_spider` — check whether a graph is a spider (tree with at most one vertex of degree ≥ 3); stars, paths, and subdivided stars are spiders; caterpillars with two branching vertices are not; directed graphs treated as undirected; 14 unit tests + 1 doctest).

PR-118 (`satisfies_dirac` — check Dirac's condition for Hamiltonicity: every vertex has degree ≥ ⌈n/2⌉; `K_n`, `C_4`, `K_{3,3}` satisfy; `C_5`, paths, and stars do not; returns false for directed or n < 3; 13 unit tests + 1 doctest).

PR-119 (`satisfies_ore` — check Ore's condition for Hamiltonicity: for every non-adjacent pair u,v, deg(u)+deg(v) ≥ n; strictly weaker than Dirac; `K_5 - 2 edges` satisfies Ore but not Dirac; returns false for directed or n < 3; 13 unit tests + 1 doctest).

PR-120 (`is_apex_forest` — check whether a graph is an apex forest: removing some single vertex yields a forest; triangles, diamonds, theta graphs are apex forests; `K_4`, wheels, and two disjoint triangles are not; directed graphs treated as undirected; 14 unit tests + 1 doctest).

PR-121 (`is_weakly_chordal` — check whether a graph is weakly chordal (weakly triangulated): neither G nor its complement has an induced `C_k` for k ≥ 5; chordal and co-chordal graphs are weakly chordal; `C_4` allowed but `C_5`, `C_6` are not; uses DFS-based chordless cycle detection on both G and complement adjacency matrices; directed graphs treated as undirected; 14 unit tests + 1 doctest).

PR-122 (`is_complete_bipartite` — check whether a graph is complete bipartite `K_{m,n}`: uses bipartite check to find two parts, then verifies edge count = m × n; `K_{1,1}`, `C_4` = `K_{2,2}`, stars = `K_{1,n}` are complete bipartite; `C_6`, triangles, disconnected graphs are not; directed treated as undirected; 15 unit tests + 1 doctest).

PR-123 (`is_cubic` — check whether a graph is cubic (3-regular): every vertex has degree exactly 3; `K_4`, `K_{3,3}`, Petersen graph, prism graph are cubic; `C_4` (2-regular), `K_5` (4-regular), stars are not; returns false for directed or n < 4; 13 unit tests + 1 doctest).

PR-124 (`is_co_chordal` — check whether a graph is co-chordal: complement of G is chordal; builds complement graph and runs chordality check; complete, edgeless, `C_4`, and star graphs are co-chordal; `C_5`, `C_6`, `P_5` are not; directed treated as undirected; 13 unit tests + 1 doctest).

PR-125 (`is_net_free` — check whether a graph has no induced net subgraph: a net is a triangle with a pendant edge on each vertex (6 vertices, 6 edges); precomputes pendant candidates per triangle vertex for efficiency; triangles, complete graphs, cycles, paths are net-free; directed treated as undirected; 14 unit tests + 1 doctest).

PR-126 (`is_biregular` — check whether a graph is biregular (semiregular bipartite): bipartite and all vertices in the same partition have the same degree; uses `is_bipartite` for partition, then checks degree uniformity per side; `K_{m,n}`, regular bipartite, stars are biregular; `P_4`, mixed-component disconnected graphs are not; directed treated as undirected; 15 unit tests + 1 doctest).

PR-127 (`is_series_parallel` — check whether a graph is series-parallel: no `K_4` minor (Duffin 1965); iterative reduction algorithm: peel degree-0/1 vertices, series-reduce degree-2 vertices, collapse parallel edges until fixpoint; graph is SP iff it reduces to at most one edge; trees, cycles, diamonds, `K_{2,3}` are SP; `K_4`, `K_5`, Petersen, wheels are not; directed treated as undirected; 20 unit tests + 1 doctest).

PR-128 (`is_apex_tree` — check whether a graph is an apex tree: removing some single vertex yields a tree; stricter than apex forest (requires connected remainder); uses adjacency-matrix removal + tree-check per candidate; trees, triangles, diamonds, theta graphs are apex trees; `K_4`, wheels, two disjoint triangles, edgeless graphs with n >= 4 are not; directed treated as undirected; 16 unit tests + 1 doctest).

PR-129 (`is_outerplanar` — check whether a graph is outerplanar: embeddable in the plane with all vertices on the outer face; equivalent to no `K_4` minor and no `K_{2,3}` minor; uses `is_series_parallel` for `K_4`-freeness plus a tagged SP-reduction for `K_{2,3}` detection — edges created by series contraction are tagged synthetic, and 3+ synthetic parallel edges between any pair certify a `K_{2,3}` minor; forests, cycles, diamonds, triangulated polygons are outerplanar; `K_4`, `K_{2,3}`, Petersen are not; directed treated as undirected; 18 unit tests + 1 doctest).

PR-130 (`is_mutual` + `has_mutual` + `count_mutual` — per-edge mutual/reciprocal classification for directed graphs: `is_mutual` returns a bool vector indicating whether each edge has a reciprocal partner; `has_mutual` returns true if any edge is mutual; `count_mutual` returns the count of mutual edges; self-loops optionally treated as mutual; undirected graphs: all edges mutual; O(E); 15 unit tests + 1 doctest).

PR-131 (`is_clique` + `is_independent_vertex_set` — vertex-set property tests: `is_clique` checks whether a given vertex set induces a complete subgraph; `is_independent_vertex_set` checks whether no two vertices in the set are adjacent; supports directed mode; O(|S|^2); 15 unit tests + 2 doctests).

PR-132 (`is_triangle_free` — check whether a graph has no triangle (3-clique); iterates edges and checks common neighbors; returns false for directed graphs; O(E * sqrt(E)); 15 unit tests + 1 doctest).

PR-133 (`degree_correlation_vector` — degree-degree correlation vector (average neighbor degree per degree class); supports Out/In/All modes; O(V+E); 12 unit tests + 1 doctest).

PR-134 (`joint_degree_matrix` — joint degree matrix counting edges between degree-i and degree-j vertices; optional edge weights; O(E); 12 unit tests + 1 doctest).

PR-135 (`joint_type_distribution` — joint type/attribute distribution matrix; generalizes joint_degree_matrix to arbitrary vertex types; O(E); 10 unit tests + 1 doctest).

PR-136 (`katz_centrality` — Katz centrality: weighted sum of walks of all lengths with attenuation factor alpha; power-iteration with convergence check; supports optional initial scores and edge weights; O(V+E) per iteration; 7 unit tests + 1 doctest).

PR-137 (`list_triangles` — enumerate all triangles as sorted vertex triples; directed graphs treated as undirected; O(E * sqrt(E)); 12 unit tests + 1 doctest).

PR-138 (`mean_distance_weighted` — weighted average shortest-path distance using Dijkstra all-pairs; handles directed/undirected; returns NaN for disconnected components when unconditional=false; O(V * E log V); 13 unit tests + 1 doctest).

PR-139 (`cocitation` + `bibcoupling` + `similarity_jaccard_pairs` + `similarity_dice_pairs` + `similarity_inverse_log_weighted_pairs` — pairwise vertex similarity measures: co-citation and bibliographic coupling counts; Jaccard, Dice, and inverse-log-weighted similarity for given vertex pairs; supports directed mode; O(V * d^2) for co-citation/bibcoupling; O(|pairs| * d) for pairwise measures; 40 unit tests + 5 doctests).

PR-140 (`sort_vertices_by_degree` — returns vertex indices sorted by degree in ascending or descending order; supports Out/In/All modes; O(V log V); 9 unit tests + 1 doctest).

PR-141 (`get_stochastic` — row- or column-stochastic matrix from graph adjacency; normalizes rows/columns to sum to 1; handles isolated vertices (all-zero rows); O(V+E); 11 unit tests + 1 doctest).

PR-142 (`graph_summary` + `graph_summary_string` — summary statistics struct for a graph: vertex/edge counts, density, components, diameter, average degree, clustering coefficient, reciprocity; `graph_summary_string` formats as human-readable text; O(V+E); 9 unit tests + 2 doctests).

PR-143 (`unfold_tree` — BFS-based tree unfolding: converts a general graph into a tree rooted at a given vertex by duplicating shared vertices; returns unfolded tree + vertex mapping back to original; supports root selection; O(V+E); 10 unit tests + 1 doctest).

PR-144 (`running_mean` + `expand_path_to_pairs` — sliding-window running mean over a data vector; `expand_path_to_pairs` utility converts a vertex path to consecutive edge pairs; O(n); 11 unit tests + 2 doctests).

PR-145 (`get_eids` — batch edge-ID lookup for vertex pairs; returns edge IDs for given (from, to) pairs; directed-aware with optional error on missing edges; O(|pairs| * d); 7 unit tests + 1 doctest).

PR-146 (`assortativity_nominal` — nominal assortativity coefficient for categorical vertex attributes; Newman 2003 mixing-matrix formulation; O(E); 12 unit tests + 1 doctest).

MO-001 (`motifs_randesu` + `motifs_randesu_no` + `motifs_randesu_estimate` + `motifs_randesu_callback` — subgraph census (motif counting) via Wernicke 2006 RAND-ESU enumeration; counts all subgraphs of size 3 or 4 by isomorphism class; `motifs_randesu_no` returns total count; `motifs_randesu_estimate` samples a fraction; `motifs_randesu_callback` invokes user callback per motif; size 3: 16 classes directed / 4 undirected; size 4: 218 classes directed / 11 undirected; O(V * d^(size-1)); 29 unit tests + 4 doctests).

MO-003 (`dyad_census` — Davis-Holland-Leinhardt dyad census: counts mutual, asymmetric, and null dyads in a directed graph; `DyadCensus { mutual, asymmetric, null }`; undirected: all non-null dyads are mutual; O(V+E); 13 unit tests + 1 doctest).

MO-004 (`graph_count` — number of labeled graphs on n vertices (directed or undirected); `2^(n*(n-1)/2)` undirected, `2^(n*(n-1))` directed; pure combinatorial formula; O(1); 4 unit tests + 1 doctest).

CY-002 (`fundamental_cycles` — fundamental cycle basis from a spanning tree; BFS spanning tree + one non-tree edge per cycle; returns cycles as vertex lists; O(V+E) for tree + O(C * V) for cycle extraction; 14 unit tests + 1 doctest).

CY-005 (`minimum_cycle_basis` — minimum weight cycle basis using Horton's algorithm; enumerates candidate cycles via shortest paths through each edge, then Gaussian elimination over GF(2) to select linearly independent minimum-weight set; optional edge weights; O(E^2 * V + E * V^2 * log V); 16 unit tests + 1 doctest).

CY-006 (`feedback_arc_set` + `FasAlgorithm` — feedback arc set: minimum set of edges whose removal makes a directed graph acyclic; `FasAlgorithm::EadesLinSmyth` greedy heuristic: alternately peel sinks and sources, place remaining by net degree delta; supports optional edge weights; O(V+E); 16 unit tests + 1 doctest).

EP-001 (`sir` — SIR (Susceptible-Infected-Recovered) epidemic simulation on graphs; discrete-time stochastic process with infection rate beta and recovery rate gamma; returns time series of S/I/R counts per simulation; deterministic via SplitMix64 seed; O(V * T) per simulation; 11 unit tests + 1 doctest).

MA-003 (`solve_lsap` — solve the Linear Sum Assignment Problem (Hungarian algorithm); finds minimum-cost perfect matching in a bipartite graph given an n×n cost matrix; returns optimal column assignment vector; O(n^3); 10 unit tests + 1 doctest).

OP-022 (`contract_vertices` — vertex contraction: merges vertices sharing the same mapping value; preserves edges between contracted super-vertices; O(V+E); 9 unit tests + 1 doctest).

OP-023 (`line_graph` — line graph construction: vertices of L(G) are edges of G, two L(G)-vertices adjacent iff their edges share an endpoint; O(E * d); 9 unit tests + 1 doctest).

OP-024 (`rewire_edges` + `rewire_directed_edges` — random edge rewiring: each edge independently rewired with probability p; `rewire_edges` rewires both endpoints; `rewire_directed_edges` rewires only source or target; self-loops optional; deterministic via SplitMix64 seed; O(E); 17 unit tests + 1 doctest).

OP-025 (`subgraph_from_edges` — extract a subgraph induced by a set of edge IDs; optionally deletes non-selected vertices (reindex) or keeps all vertices; O(V+|S|); 13 unit tests + 1 doctest).

OP-026 (`bipartite_projection` + `BipartiteProjectionResult` — one-mode projections of a bipartite graph: projects onto type-0 and/or type-1 vertices; two type-X vertices are connected iff they share a type-Y neighbor; optional edge multiplicity weights; O(V * d^2); 10 unit tests + 1 doctest).

OP-027 (`bipartite_projection_size` — compute sizes (vertex/edge counts) of bipartite projections without constructing them; O(V * d); 9 unit tests + 1 doctest).

OP-028 (`residual_graph` + `reverse_residual_graph` — max-flow residual graph construction: given a flow assignment, builds the residual graph with forward (capacity - flow) and backward (flow) arcs; `reverse_residual_graph` builds the reverse-arc residual; O(V+E); 12 unit tests + 1 doctest).

SP-037 (`distances_from` + `distances_from_with_mode` — single-source unweighted shortest distances via BFS; returns `Vec<Option<u32>>` with None for unreachable vertices; direction-aware (Out/In/All); convenience wrapper over distances with single-source semantics; O(V+E); 10 unit tests + 1 doctest).

SP-038 (`spanner` — graph spanner: sparse subgraph that approximately preserves distances; greedy algorithm selecting edges whose removal would increase stretch beyond threshold; supports optional edge weights; O(E * V); 12 unit tests + 1 doctest).

SP-039 (`graph_center` + `pseudo_diameter` — graph center: vertices with minimum eccentricity; `pseudo_diameter` approximation via BFS double-sweep; supports directed modes; O(V * (V+E)); 17 unit tests + 2 doctests).

SP-040 (`vertex_path_from_edge_path` — convert an edge-ID path to a vertex-ID path; reconstructs the vertex sequence by walking edge endpoints; O(|path|); 15 unit tests + 1 doctest).

ST-001 (`random_spanning_tree` — Wilson's algorithm (loop-erased random walk) for uniform random spanning tree; deterministic via SplitMix64 seed; returns tree as edge-ID vector; O(V * E) expected; 15 unit tests + 1 doctest).

GEO-010 (`convex_hull_2d` — 2-D convex hull via Andrew's monotone chain algorithm; returns vertices in counterclockwise order + hull polygon; handles collinear points; O(n log n); 11 unit tests + 1 doctest).

GEO-011 (`spatial_edge_lengths` — compute Euclidean edge lengths from vertex coordinates; returns `Vec<f64>` aligned with edge IDs; arbitrary dimensionality; O(E * d); 13 unit tests + 1 doctest).

CN-039 (`realize_degree_sequence` — construct a simple undirected graph with a given degree sequence; Erdos-Gallai realizability check then stub-matching construction; O(V^2); 14 unit tests + 1 doctest).

CN-040 (`realize_bipartite_degree_sequence` — construct a simple bipartite graph with given degree sequences for both parts; Gale-Ryser realizability check; O(n1 * n2); 13 unit tests + 1 doctest).

CN-041 (`realize_directed_degree_sequence` — construct a simple directed graph with given in-degree and out-degree sequences; Fulkerson realizability check; O(V^2); 14 unit tests + 1 doctest).

CO-009 (`modularity_matrix` — Newman's modularity matrix B_{ij} = A_{ij} - k_i * k_j / 2m; returns dense V×V matrix; used internally by leading_eigenvector community detection; O(V^2); 12 unit tests + 1 doctest).

CO-018 (Infomap community detection Rosvall & Bergstrom 2008 — `infomap`, `infomap_weighted`, `infomap_with_options`; map equation minimization via greedy Louvain-style vertex moves; computes PageRank flows with teleportation (τ=0.15) via power iteration on the transition matrix, then iteratively moves vertices between modules to minimize the two-level map equation codelength L(M) = q↷·H(Q) + Σᵢ(qᵢ↷ + pᵢ)·H(Pᵢ) where q↷ is inter-module teleportation flow, qᵢ↷ is exit flow from module i, and pᵢ is total PageRank flow in module i; multi-trial optimization with SplitMix64 PRNG for deterministic seeded Fisher-Yates shuffle; supports directed/undirected graphs and optional edge weights via non-uniform teleportation distribution; from-scratch implementation since igraph C merely wraps the external mapequation/infomap C++ library (~275-line wrapper); `InfomapResult { membership, codelength }` returns the best partition across all trials; karate club yields 3 communities with codelength ≈ 3.47; 10 unit tests covering basic two-communities, empty graph, single vertex, disconnected components, deterministic seeded, weighted edges, directed graph, invalid edge weights, invalid nb_trials, and karate club).

CO-019 (Spinglass community detection Reichardt & Bornholdt 2006 — `spinglass`, `spinglass_weighted`, `spinglass_with_options`; Potts model with simulated annealing, heat-bath spin updates at decreasing temperatures; two null models Simple (G(n,p)) and Config (degree-preserving configuration model); two update modes Sequential (random node per step) and Parallel (all nodes per sweep); `SpinglassResult { membership, modularity, temperature, nb_clusters, csize }`; `SpinglassOptions { spins, parallel_update, start_temp, stop_temp, cool_fact, update_rule, gamma, seed }`; `SpinglassUpdateRule { Simple, Config }`; requires connected graph; from-scratch implementation (~750 lines) since upstream is ~3300 lines C++; 14 unit tests + 27 integration tests + 5 proptests).

> Update the counters after every PR merge.
